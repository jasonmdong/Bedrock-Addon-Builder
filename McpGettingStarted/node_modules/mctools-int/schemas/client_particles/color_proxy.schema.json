{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/client_particles/color_proxy.form.json",
  "title": "Client Particles Color Proxy",
  "description": "Defines color gradients with smooth interpolation for particle effects. Use this to create particles that change color over their lifetime—from bright flames fading to smoke, magical auras shifting hues, or explosion flashes dimming over time. The gradient maps time points to colors, while the interpolant expression controls which color to sample.",
  "type": "object",
  "properties": {
    "gradient": {
      "title": "Gradient",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "interpolant": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        {
          "type": "string"
        },
        {
          "description": "\n\nProperties:\n• expression (string)\n• version (int)",
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "expression": {
                "title": "Expression",
                "type": "string"
              },
              "version": {
                "title": "Version",
                "type": "integer"
              }
            }
          }
        },
        {
          "type": "number"
        }
      ],
      "title": "Interpolant"
    }
  },
  "required": [
    "gradient",
    "interpolant"
  ]
}