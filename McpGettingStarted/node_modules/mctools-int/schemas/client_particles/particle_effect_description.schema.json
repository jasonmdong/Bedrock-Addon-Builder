{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/client_particles/particle_effect_description.form.json",
  "title": "Client Particles Particle Effect Description",
  "description": "The description MUST contain an identifier and basic_render_parameters.",
  "type": "object",
  "properties": {
    "basic_render_parameters": {
      "title": "Basic Render Parameters",
      "description": "The parameters MUST contain a material and texture.\n\nProperties:\n• material (string)\n• texture (string)",
      "$ref": "#/$defs/client_particles_particle_effect_basic_render_parameters",
      "defaultSnippets": [
        {
          "label": "Client Particles Particle Effect Basic Render Parameters",
          "description": "The parameters MUST contain a material and texture.",
          "body": {
            "material": "",
            "texture": ""
          }
        }
      ]
    },
    "identifier": {
      "title": "Identifier",
      "type": "string"
    }
  },
  "required": [
    "basic_render_parameters",
    "identifier"
  ],
  "$defs": {
    "client_particles_particle_effect_basic_render_parameters": {
      "type": "object",
      "title": "Client Particles Particle Effect Basic Render Parameters",
      "description": "The parameters MUST contain a material and texture.",
      "properties": {
        "material": {
          "title": "Material",
          "description": "The material used for rendering the particle effect.",
          "type": "string"
        },
        "texture": {
          "title": "Texture",
          "description": "The texture path used for the particle effect.",
          "type": "string"
        }
      },
      "required": [
        "material",
        "texture"
      ]
    }
  }
}