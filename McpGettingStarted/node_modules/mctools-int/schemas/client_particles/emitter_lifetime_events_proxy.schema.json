{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/client_particles/emitter_lifetime_events_proxy.form.json",
  "title": "Client Particles Emitter Lifetime Events Proxy",
  "description": "Controls emitter lifetime events like creation and expiration.",
  "type": "object",
  "properties": {
    "creation_event": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "type": "object"
          }
        },
        {
          "type": "string"
        }
      ],
      "title": "Creation Event"
    },
    "expiration_event": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "type": "object"
          }
        },
        {
          "type": "string"
        }
      ],
      "title": "Expiration Event"
    },
    "looping_travel_distance_events": {
      "title": "Looping Travel Distance Events",
      "description": "\n\nProperties:\n• distance (float)\n• effects",
      "$ref": "#/$defs/client_particles_emitter_lifetime_events_travel_distance_events",
      "defaultSnippets": [
        {
          "label": "Client Particles Emitter Lifetime Events Travel Distance Events",
          "description": "Triggers particle events when the emitter has traveled a specified distance. Useful for trail effects that change behavior—such as a projectile leaving a smoke trail that intensifies as it travels, or a mob's footsteps spawning dust particles at regular intervals as it walks.",
          "body": {
            "distance": 0,
            "effects": ""
          }
        }
      ]
    },
    "timeline": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "type": "object"
          }
        },
        {
          "type": "string"
        }
      ],
      "title": "Timeline"
    },
    "travel_distance_events": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "type": "object"
          }
        },
        {
          "type": "string"
        }
      ],
      "title": "Travel Distance Events"
    }
  },
  "$defs": {
    "client_particles_emitter_lifetime_events_travel_distance_events": {
      "type": "object",
      "title": "Client Particles Emitter Lifetime Events Travel Distance Events",
      "description": "Triggers particle events when the emitter has traveled a specified distance. Useful for trail effects that change behavior—such as a projectile leaving a smoke trail that intensifies as it travels, or a mob's footsteps spawning dust particles at regular intervals as it walks.",
      "properties": {
        "distance": {
          "title": "Distance",
          "type": "number"
        },
        "effects": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "object"
              }
            },
            {
              "type": "string"
            }
          ],
          "title": "Effects"
        }
      },
      "required": [
        "distance",
        "effects"
      ]
    }
  }
}