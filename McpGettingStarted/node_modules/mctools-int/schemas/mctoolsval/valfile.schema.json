{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/mctoolsval/valfile.form.json",
  "title": "File Validation",
  "description": "Validates that content files are properly formatted and parseable. Catches JSON syntax errors and encoding issues that would prevent content from loading.",
  "type": "object",
  "properties": {
    "102": {
      "title": "Invalid JSON",
      "description": "Error when a JSON file contains syntax errors and cannot be parsed. Check for missing commas, brackets, quotes, or invalid escape sequences.",
      "type": "string",
      "x-runtime-constraint-description": "Data contains the JSON parse error message"
    },
    "103": {
      "title": "Empty File",
      "description": "Warning when a JSON file is empty or contains only whitespace. The file may be incomplete or accidentally cleared.",
      "type": "string",
      "x-runtime-constraint-description": "Data contains the path to the empty file"
    },
    "104": {
      "title": "Binary Content",
      "description": "Error when a file expected to contain text/JSON data contains binary or non-string content. The file may be corrupted or the wrong type.",
      "type": "string",
      "x-runtime-constraint-description": "Data contains the path to the file with binary content"
    }
  }
}