{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/mctoolsval/rpdepends.form.json",
  "title": "Resource Pack Deps",
  "description": "Validates that resource pack dependencies declared in behavior pack manifests are properly resolved and available.",
  "type": "object",
  "properties": {
    "101": {
      "title": "Invalid Manifest JSON",
      "description": "Error when a pack manifest contains invalid JSON syntax. Check for missing commas, brackets, or malformed entries.",
      "type": "string",
      "x-runtime-constraint-description": "Data contains the path to the invalid manifest file"
    },
    "102": {
      "title": "Missing Resource Pack",
      "description": "Error when a behavior pack declares a dependency on a resource pack that cannot be found. Ensure the resource pack is included and the UUID matches.",
      "type": "string",
      "x-runtime-constraint-description": "Data contains the UUID of the missing resource pack dependency"
    },
    "103": {
      "title": "Processing Error",
      "description": "An internal error occurred while processing resource pack dependencies. Check logs for details.",
      "type": "string",
      "x-runtime-constraint-description": "Data contains details about the internal processing error"
    }
  }
}