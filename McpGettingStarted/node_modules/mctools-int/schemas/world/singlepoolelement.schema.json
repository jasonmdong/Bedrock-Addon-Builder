{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/world/singlepoolelement.form.json",
  "title": "World SinglePoolElement",
  "description": "Defines a single pool element with a structure location and processors.",
  "type": "object",
  "properties": {
    "element_type": {
      "title": "ElementType",
      "description": "The type of element.",
      "type": "string",
      "enum": [
        "minecraft:empty_pool_element",
        "minecraft:single_pool_element"
      ]
    },
    "location": {
      "title": "Location",
      "description": "The path of the structure file. This path is relative to the behavior pack's structures folder.",
      "type": "object"
    },
    "processors": {
      "title": "Processors",
      "description": "The identifier of the processor list to use when placing the structure.",
      "type": "object",
      "default": ""
    },
    "projection": {
      "title": "Projection",
      "description": "Specifies how structures should be placed relative to the terrain.",
      "type": "string",
      "enum": [
        "rigid",
        "terrain_matching"
      ],
      "default": "rigid"
    }
  },
  "required": [
    "element_type",
    "location"
  ]
}