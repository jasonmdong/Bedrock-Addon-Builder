{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/block_components/detailed_map_color.form.json",
  "title": "Block Components Detailed Map Color",
  "description": "Sets the color and optional tint of the block.",
  "type": "object",
  "properties": {
    "color": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "number"
          },
          "minLength": 3,
          "maxLength": 3
        }
      ],
      "title": "Color",
      "description": "Sets the color of the block when rendered to a map."
    },
    "tint_method": {
      "title": "Tint Method",
      "description": "Sets the tint method of the block when rendered to a map. Tint method logic varies, but often refers to the \"rain\" and \"temperature\" of the biome the block is placed in to compute the tint. Supported tint methods are \"none\", \"default_foliage\", \"birch_foliage\", \"evergreen_foliage\", \"dry_foliage\", \"grass\" and \"water\"",
      "type": "string",
      "enum": [
        "birch_foliage",
        "default_foliage",
        "dry_foliage",
        "evergreen_foliage",
        "grass",
        "none",
        "water"
      ],
      "default": "none"
    }
  },
  "required": [
    "color"
  ]
}