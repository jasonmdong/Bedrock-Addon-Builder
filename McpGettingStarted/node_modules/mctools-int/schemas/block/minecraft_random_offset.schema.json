{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/block/minecraft_random_offset.form.json",
  "title": "Random Offset",
  "description": "This component defines a random offset for the block, seeded based on the block's position and the specified range and steps. It affects the block's rendered position, outline, and collision.\nIf the random offset causes the collision box to extend beyond the bounds of a cube, the offset range will automatically adjust to keep the collision box within the cube.\nCulling for this block is performed without considering the random offset.",
  "type": "object",
  "properties": {
    "x": {
      "title": "X",
      "description": "X coordinate\n\nProperties:\n• range ({ min, max })\n• steps (int)",
      "$ref": "#/$defs/block_offset_range",
      "defaultSnippets": [
        {
          "label": "Range of values between two numbers",
          "description": "Specifies a range of values between two numbers, with a defined number of steps. This is used to control the variation in block properties such as offset, color, or other attributes.",
          "body": {
            "steps": 0
          }
        }
      ]
    },
    "y": {
      "title": "Y",
      "description": "Y coordinate\n\nProperties:\n• range ({ min, max })\n• steps (int)",
      "$ref": "#/$defs/block_offset_range",
      "defaultSnippets": [
        {
          "label": "Range of values between two numbers",
          "description": "Specifies a range of values between two numbers, with a defined number of steps. This is used to control the variation in block properties such as offset, color, or other attributes.",
          "body": {
            "steps": 0
          }
        }
      ]
    },
    "z": {
      "title": "Z",
      "description": "Z coordinate\n\nProperties:\n• range ({ min, max })\n• steps (int)",
      "$ref": "#/$defs/block_offset_range",
      "defaultSnippets": [
        {
          "label": "Range of values between two numbers",
          "description": "Specifies a range of values between two numbers, with a defined number of steps. This is used to control the variation in block properties such as offset, color, or other attributes.",
          "body": {
            "steps": 0
          }
        }
      ]
    }
  },
  "$defs": {
    "block_offset_range": {
      "type": "object",
      "title": "Range of values between two numbers",
      "description": "Specifies a range of values between two numbers, with a defined number of steps. This is used to control the variation in block properties such as offset, color, or other attributes.",
      "properties": {
        "range": {
          "title": "Range",
          "description": "Range of values\n\nProperties:\n• min (float)\n• max (float)",
          "type": "object",
          "properties": {
            "min": {
              "title": "Minimum value",
              "description": "Minimum value of the range",
              "type": "number"
            },
            "max": {
              "title": "Maximum value",
              "description": "Maximum value of the range",
              "type": "number"
            }
          },
          "defaultSnippets": [
            {
              "label": "Range of values",
              "description": "Range of values",
              "body": {
                "min": 0,
                "max": 0
              }
            }
          ]
        },
        "steps": {
          "title": "Steps",
          "description": "Number of steps between the range",
          "type": "integer"
        }
      }
    }
  }
}