{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/block/blockdescriptorproxy.form.json",
  "title": "BlockDescriptorProxy",
  "description": "Identifies a specific block by name, block states, and optional tags for matching. Used in block components and entity behaviors that need to reference other blocks—like specifying which blocks can support placement, what blocks drop when mined, or which blocks an entity can walk through.",
  "type": "object",
  "properties": {
    "name": {
      "title": "Name",
      "type": "string",
      "default": ""
    },
    "states": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "string"
        },
        {
          "type": "boolean"
        }
      ],
      "title": "States"
    },
    "tags": {
      "title": "Tags",
      "type": "string",
      "default": ""
    }
  }
}