{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/block/minecraft_embedded_visual.form.json",
  "title": "Embedded Visual",
  "description": "The description identifier of the geometry and material used to render this block when it it is embedded inside of another block (for example, a flower inside of a flower pot).",
  "type": "object",
  "properties": {
    "geometry": {
      "title": "Geometry",
      "description": "The \"minecraft:geometry\" component that will be used when rendered inside of another block.",
      "type": "array",
      "items": {
        "type": "object"
      }
    },
    "material_instances": {
      "title": "Material Instances",
      "description": "The \"minecraft:material_instances\" component that will be used when rendered inside of another block.",
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  },
  "required": [
    "geometry",
    "material_instances"
  ]
}