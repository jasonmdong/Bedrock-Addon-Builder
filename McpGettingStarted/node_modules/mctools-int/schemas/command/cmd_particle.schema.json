{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/command/cmd_particle.form.json",
  "title": "particle",
  "description": "Creates a particle emitter",
  "type": "object",
  "properties": {
    "effect": {
      "title": "effect",
      "description": "A `string` that specifies the particle to create.",
      "type": "string"
    },
    "position": {
      "title": "position",
      "description": "A `vector` with `[x y z]` coordinates that specifies where to make the particle. Default: the position of the executor.",
      "type": "string"
    }
  }
}