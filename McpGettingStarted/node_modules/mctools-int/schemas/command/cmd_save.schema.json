{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/command/cmd_save.form.json",
  "title": "save",
  "description": "Control or check how the game saves data to disk. This command is for use on a dedicated server only.",
  "type": "object",
  "properties": {
    "mode": {
      "title": "mode",
      "description": "Either `save-all` or `save-all flush` to save chunks to server immediately. This freezes the servers.",
      "type": "string"
    }
  }
}