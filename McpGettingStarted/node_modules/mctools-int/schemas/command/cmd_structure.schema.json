{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/command/cmd_structure.form.json",
  "title": "structure",
  "description": "Saves or loads a structure in the world. See examples of this command in use in the [Structure Blocks documentation](https://learn.microsoft.com/minecraft/creator/documents/structureblockstutorial#creating-structures-using-the-structure-command).",
  "type": "object",
  "properties": {
    "action": {
      "title": "action",
      "type": "string"
    },
    "name": {
      "title": "name",
      "description": "A `string` that specifies the name of the structure. May include a namespace.",
      "type": "string"
    },
    "from": {
      "title": "from",
      "description": "A `vector` used with `to` that specifies the opposing corners of a structure.",
      "type": "string"
    },
    "to": {
      "title": "to",
      "description": "A `vector` used with `from` that specifies the opposing corners of a structure.",
      "type": "string"
    },
    "includeentities": {
      "title": "includeEntities",
      "description": "A `Boolean` that specifies whether to save or load entities inside the structure. Default: `true`.",
      "type": "string"
    },
    "includeblocks": {
      "title": "includeBlocks",
      "description": "A `Boolean` that specifies whether to save or load blocks inside the structure. Default: `true`.",
      "type": "string"
    },
    "rotation": {
      "title": "rotation",
      "description": "An `enum` of the rotation when loading in a structure. Must be one of `0_degrees`, `90_degrees`, `180_degrees`, or `270_degrees`. Default: `none`.",
      "type": "string"
    },
    "mirror": {
      "title": "mirror",
      "description": "An `enum` that specifies the axis to mirror when loading a structure. Either `0_degrees`, `90_degrees`, `180_degrees`, or `270_degrees`. Default: `none`.",
      "type": "string"
    },
    "waterlogged": {
      "title": "waterlogged",
      "type": "string"
    },
    "integrity": {
      "title": "integrity",
      "description": "A `float` that specifies the percentage likelihood a block will load. 100 loads all blocks, 0 loads none. Default: 100.",
      "type": "string"
    },
    "seed": {
      "title": "seed",
      "description": "A `string` that specifies the seed when loading in a block with variable integrity.",
      "type": "string"
    },
    "animationmode": {
      "title": "animationMode",
      "description": "An `enum` of `block_by_block` to load blocks in one at a time or `layer_by_layer` to load them in in layers. Default: `none`.",
      "type": "string"
    },
    "animationseconds": {
      "title": "animationSeconds",
      "description": "A `float` of the duration of the time to animate the structure loading in. Default: `0.0`.",
      "type": "string"
    }
  }
}