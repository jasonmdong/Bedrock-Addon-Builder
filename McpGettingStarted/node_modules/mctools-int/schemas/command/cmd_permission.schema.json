{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/command/cmd_permission.form.json",
  "title": "permission",
  "description": "Reloads and applies permissions. This command is for use on a dedicated server only.",
  "type": "object",
  "properties": {
    "action": {
      "title": "action",
      "description": "Either `list` to list the permissions, `set` to set their permissions, or `reload` to reload a server's permissions file.",
      "type": "string"
    }
  }
}