{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/command/cmd_testforblocks.form.json",
  "title": "testforblocks",
  "description": "Tests whether the blocks in two regions match.",
  "type": "object",
  "properties": {
    "begin": {
      "title": "begin",
      "description": "A `vector` [x y z] that along with `end` of two opposite corners of a rectangular region to test for.",
      "type": "string"
    },
    "end": {
      "title": "end",
      "description": "Required. A `vector` [x y z] that along with `begin` of two opposite corners of a rectangular region to test for.",
      "type": "string"
    },
    "destination": {
      "title": "destination",
      "description": "Required. A `vector` [x y z] of the lower northwest corner to test for.",
      "type": "string"
    },
    "mode": {
      "title": "mode",
      "description": "Optional. Either `masked` where air blocks will match any destination, or default `all` which matches all blocks including air.",
      "type": "string"
    }
  }
}