"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var RideableEntityTrait_exports = {};
__export(RideableEntityTrait_exports, {
  RideableEntityTrait: () => RideableEntityTrait
});
module.exports = __toCommonJS(RideableEntityTrait_exports);
var import_ContentTraits = require("./ContentTraits");
class RideableEntityTrait extends import_ContentTraits.EntityContentTrait {
  get id() {
    return "rideable";
  }
  getData(config) {
    const seatCount = config?.seatCount ?? 1;
    const controllable = config?.controllable ?? true;
    const requiresSaddle = config?.requiresSaddle ?? true;
    const baseComponents = {
      "minecraft:rideable": {
        seat_count: seatCount,
        family_types: ["player"],
        interact_text: "action.interact.ride.horse",
        seats: [{ position: [0, 1.1, -0.2] }]
      }
    };
    if (controllable) {
      baseComponents["minecraft:input_ground_controlled"] = {};
    }
    const componentGroups = {};
    const events = {};
    if (requiresSaddle) {
      componentGroups["unsaddled"] = {};
      componentGroups["saddled"] = {
        "minecraft:is_saddled": {},
        ...baseComponents
      };
      events["on_saddle"] = {
        remove: { component_groups: ["unsaddled"] },
        add: { component_groups: ["saddled"] }
      };
      return {
        id: "rideable",
        displayName: "Rideable",
        description: "Can be ridden by the player",
        category: "interaction",
        components: {
          "minecraft:equippable": {
            slots: [
              {
                slot: 0,
                item: "saddle",
                accepted_items: ["saddle"],
                on_equip: { event: "on_saddle" }
              }
            ]
          }
        },
        componentGroups,
        events,
        spawnEvent: {
          add: { component_groups: ["unsaddled"] }
        }
      };
    }
    return {
      id: "rideable",
      displayName: "Rideable",
      description: "Can be ridden by the player",
      category: "interaction",
      components: baseComponents
    };
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  RideableEntityTrait
});
//# sourceMappingURL=RideableEntityTrait.js.map
