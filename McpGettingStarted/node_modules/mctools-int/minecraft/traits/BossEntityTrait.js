"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var BossEntityTrait_exports = {};
__export(BossEntityTrait_exports, {
  BossEntityTrait: () => BossEntityTrait
});
module.exports = __toCommonJS(BossEntityTrait_exports);
var import_ContentTraits = require("./ContentTraits");
class BossEntityTrait extends import_ContentTraits.EntityContentTrait {
  get id() {
    return "boss";
  }
  getData(config) {
    const bossName = config?.bossName ?? "Boss";
    const phases = config?.phases ?? 3;
    const componentGroups = {};
    const events = {};
    for (let i = 1; i <= phases; i++) {
      componentGroups[`phase_${i}`] = {
        "minecraft:scale": { value: 1 + (i - 1) * 0.1 },
        "minecraft:movement": { value: 0.25 + (i - 1) * 0.05 }
      };
      if (i < phases) {
        events[`enter_phase_${i + 1}`] = {
          remove: { component_groups: [`phase_${i}`] },
          add: { component_groups: [`phase_${i + 1}`] }
        };
      }
    }
    return {
      id: "boss",
      displayName: "Boss",
      description: "Boss mob with health bar and phases",
      category: "behavior",
      components: {
        "minecraft:boss": {
          should_darken_sky: true,
          hud_range: 55,
          name: bossName
        },
        "minecraft:knockback_resistance": { value: 1 }
      },
      componentGroups,
      events,
      spawnEvent: {
        add: { component_groups: ["phase_1"] }
      }
    };
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  BossEntityTrait
});
//# sourceMappingURL=BossEntityTrait.js.map
