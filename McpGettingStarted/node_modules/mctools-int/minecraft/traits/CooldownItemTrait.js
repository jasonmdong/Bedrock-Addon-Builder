"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CooldownItemTrait_exports = {};
__export(CooldownItemTrait_exports, {
  CooldownItemTrait: () => CooldownItemTrait
});
module.exports = __toCommonJS(CooldownItemTrait_exports);
var import_ContentTraits = require("./ContentTraits");
class CooldownItemTrait extends import_ContentTraits.ItemContentTrait {
  get id() {
    return "cooldown";
  }
  getData(config) {
    const cooldownDuration = config?.cooldownDuration ?? 1;
    const cooldownCategory = config?.cooldownCategory ?? "attack";
    return {
      id: "cooldown",
      displayName: "Cooldown",
      description: "Has a cooldown after use",
      category: "special",
      components: {
        "minecraft:cooldown": {
          category: cooldownCategory,
          duration: cooldownDuration
        }
      }
    };
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CooldownItemTrait
});
//# sourceMappingURL=CooldownItemTrait.js.map
