"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CropBlockTrait_exports = {};
__export(CropBlockTrait_exports, {
  CropBlockTrait: () => CropBlockTrait
});
module.exports = __toCommonJS(CropBlockTrait_exports);
var import_ContentTraits = require("./ContentTraits");
class CropBlockTrait extends import_ContentTraits.BlockContentTrait {
  get id() {
    return "crop";
  }
  getData(config) {
    const growthStages = config?.growthStages ?? 7;
    return {
      id: "crop",
      displayName: "Crop",
      description: "Can grow over time",
      category: "special",
      components: {
        "minecraft:collision_box": false,
        "minecraft:tick": {
          interval_range: [10, 20],
          looping: true
        },
        "minecraft:random_ticking": {
          on_tick: {
            event: "grow"
          }
        }
      },
      properties: {
        "custom:growth_stage": Array.from({ length: growthStages + 1 }, (_, i) => i)
      },
      permutations: Array.from({ length: growthStages + 1 }, (_, i) => ({
        condition: `q.block_state('custom:growth_stage') == ${i}`,
        components: {
          "minecraft:geometry": `geometry.crop_stage_${i}`
        }
      }))
    };
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CropBlockTrait
});
//# sourceMappingURL=CropBlockTrait.js.map
