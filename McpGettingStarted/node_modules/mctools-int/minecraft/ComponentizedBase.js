"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ComponentizedBase_exports = {};
__export(ComponentizedBase_exports, {
  default: () => ComponentizedBase
});
module.exports = __toCommonJS(ComponentizedBase_exports);
var import_ste_events = require("ste-events");
var import_Log = __toESM(require("../core/Log"));
var import_IntAttributeComponent = __toESM(require("./components/IntAttributeComponent"));
var import_FloatAttributeComponent = __toESM(require("./components/FloatAttributeComponent"));
var import_MinecraftUtilities = __toESM(require("./MinecraftUtilities"));
var import_ComponentProperty = __toESM(require("./ComponentProperty"));
var import_ManagedComponent = require("./ManagedComponent");
var import_Utilities = __toESM(require("../core/Utilities"));
class ComponentizedBase {
  _components;
  _managedComponents;
  _componentProperties;
  _onComponentAdded = new import_ste_events.EventDispatcher();
  _onComponentRemoved = new import_ste_events.EventDispatcher();
  _onComponentChanged = new import_ste_events.EventDispatcher();
  get onComponentAdded() {
    return this._onComponentAdded.asEvent();
  }
  get onComponentRemoved() {
    return this._onComponentRemoved.asEvent();
  }
  get onComponentChanged() {
    return this._onComponentChanged.asEvent();
  }
  id = "";
  constructor() {
    this._componentProperties = {};
    this._components = {};
    this._managedComponents = {};
  }
  getComponent(id) {
    if (this._components === void 0) {
      return void 0;
    }
    id = import_MinecraftUtilities.default.canonicalizeFullName(id);
    const component = this._components[id];
    if (component === void 0) {
      return void 0;
    }
    if (!this._managedComponents[id] && import_Utilities.default.isUsableAsObjectKey(id)) {
      this._managedComponents[id] = new import_ManagedComponent.ManagedComponent(this._components, id, component);
    }
    return this._managedComponents[id];
  }
  notifyComponentUpdated(id) {
    const component = this.getComponent(id);
    if (component === void 0) {
      import_Log.default.unexpectedUndefined("CBNCU");
    } else {
      this._onComponentChanged.dispatch(this, component);
    }
  }
  getAllComponents() {
    return this.getComponents();
  }
  getComponents() {
    const componentSet = [];
    if (this._components !== void 0) {
      for (const componentName in this._managedComponents) {
        const component = this.getComponent(componentName);
        if (component !== void 0) {
          componentSet.push(component);
        }
      }
    }
    return componentSet;
  }
  ensureDataComponent(id, component) {
    this._ensureComponentsInitialized();
    id = import_MinecraftUtilities.default.canonicalizeFullName(id);
    if (this._components === void 0 || this._managedComponents === void 0) {
      import_Log.default.unexpectedUndefined("CBEDC");
      throw new Error();
    }
    if (this._managedComponents[id] !== void 0) {
      return this._managedComponents[id].getData();
    }
    const mc = this.ensureComponent(id, new import_ManagedComponent.ManagedComponent(this._components, id, component));
    if (mc) {
      return mc.getData();
    }
    return void 0;
  }
  ensureComponent(id, component) {
    this._ensureComponentsInitialized();
    id = import_MinecraftUtilities.default.canonicalizeFullName(id);
    if (this._components === void 0 || this._managedComponents === void 0 || !import_Utilities.default.isUsableAsObjectKey(id)) {
      import_Log.default.unexpectedUndefined("CBEC");
      throw new Error();
    }
    if (this._managedComponents[id] !== void 0) {
      return this._managedComponents[id];
    }
    this._managedComponents[id] = component;
    this._components[id] = component.getData();
    this._onComponentAdded.dispatch(this, component);
    return component;
  }
  addComponent(id, component) {
    this._ensureComponentsInitialized();
    id = import_MinecraftUtilities.default.canonicalizeFullName(id);
    if (!import_Utilities.default.isUsableAsObjectKey(id)) {
      import_Log.default.unsupportedToken(id);
      throw new Error();
    }
    component.id = id;
    if (this._managedComponents !== void 0) {
      this._managedComponents[id] = component;
      this._onComponentAdded.dispatch(this, component);
    }
    if (this._components) {
      this._components[id] = component.getData();
    }
    return component;
  }
  removeComponent(id) {
    if (this._components === void 0) {
      return;
    }
    id = import_MinecraftUtilities.default.canonicalizeFullName(id);
    const newComponents = {};
    const newManagedComponents = {};
    for (const name in this._components) {
      const nameCanon = import_MinecraftUtilities.default.canonicalizeFullName(name);
      if (nameCanon !== id) {
        const component = this._components[name];
        newComponents[name] = component;
      }
    }
    for (const name in this._managedComponents) {
      const nameCanon = import_MinecraftUtilities.default.canonicalizeFullName(name);
      if (nameCanon !== id) {
        const component = this._managedComponents[name];
        newManagedComponents[name] = component;
      }
    }
    this._components = newComponents;
    this._managedComponents = newManagedComponents;
  }
  getComponentProperty(name) {
    return this._componentProperties[name];
  }
  ensureComponentProperty(name) {
    return this.addComponentProperty(name);
  }
  addComponentProperty(name) {
    let property = this._componentProperties[name];
    if (property == null) {
      property = new import_ComponentProperty.default(this, name);
      property.id = name;
      this._componentProperties[name] = property;
      this.notifyComponentPropertyChanged(property);
    }
    return property;
  }
  notifyComponentPropertyChanged(property) {
  }
  loadAttributeComponentsFromNbtTag(attributesTag) {
    const attributesChildren = attributesTag.getTagChildren();
    for (let i = 0; i < attributesChildren.length; i++) {
      const attributeChild = attributesChildren[i];
      const nameChild = attributeChild.find("Name");
      if (nameChild !== null) {
        const name = nameChild.valueAsString;
        let component = null;
        if (name.endsWith("luck") || name.endsWith("health") || name.endsWith("absorption") || name.endsWith("knockback_resistance") || name.endsWith("follow_range")) {
          component = new import_IntAttributeComponent.default();
        } else if (name.endsWith("movement") || name.endsWith("jump_strength") || name.endsWith("attack_damage")) {
          component = new import_FloatAttributeComponent.default();
        } else {
          import_Log.default.debugAlert("Unknown component '" + name + "' found");
          component = new import_FloatAttributeComponent.default();
        }
        const managedComponent = new import_ManagedComponent.ManagedComponent(this._components, name, component);
        this.ensureComponent(name, managedComponent);
      }
    }
  }
  _ensureComponentsInitialized() {
    if (this._components === null) {
      this._components = {};
    }
  }
}
//# sourceMappingURL=ComponentizedBase.js.map
