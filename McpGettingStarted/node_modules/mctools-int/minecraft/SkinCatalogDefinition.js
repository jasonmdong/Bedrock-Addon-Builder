"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var SkinCatalogDefinition_exports = {};
__export(SkinCatalogDefinition_exports, {
  default: () => SkinCatalogDefinition
});
module.exports = __toCommonJS(SkinCatalogDefinition_exports);
var import_Log = __toESM(require("../core/Log"));
var import_ste_events = require("ste-events");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_TextureDefinition = __toESM(require("./TextureDefinition"));
class SkinCatalogDefinition {
  _file;
  _isLoaded = false;
  _loadedWithComments = false;
  _data;
  _onLoaded = new import_ste_events.EventDispatcher();
  get isLoaded() {
    return this._isLoaded;
  }
  get data() {
    return this._data;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  get id() {
    if (!this._data) {
      return void 0;
    }
    return this._data.serialize_name;
  }
  get skins() {
    if (!this._data) {
      return void 0;
    }
    if (this._data.skins === void 0) {
      this._data.skins = [];
    }
    return this._data.skins;
  }
  getCanonicalizedTexturesList() {
    if (!this._data || !this._data.skins || !Array.isArray(this._data.skins)) {
      return void 0;
    }
    const textureList = [];
    for (const skin of this._data.skins) {
      if (skin.texture) {
        const texturePath = import_TextureDefinition.default.canonicalizeTexturePath(skin.texture);
        if (texturePath) {
          textureList.push(texturePath);
        }
      }
    }
    return textureList;
  }
  get skinNameList() {
    if (!this._data || !this._data.skins || !Array.isArray(this._data.skins)) {
      return void 0;
    }
    const skinNameList = [];
    for (const skin of this._data.skins) {
      if (skin.localization_name) {
        skinNameList.push(skin.localization_name);
      }
    }
    return skinNameList;
  }
  static async ensureOnFile(file, loadHandler) {
    let et;
    if (file.manager === void 0) {
      et = new SkinCatalogDefinition();
      et.file = file;
      file.manager = et;
    }
    if (file.manager !== void 0 && file.manager instanceof SkinCatalogDefinition) {
      et = file.manager;
      if (!et.isLoaded) {
        if (loadHandler) {
          et.onLoaded.subscribe(loadHandler);
        }
        await et.load();
      }
    }
    return et;
  }
  ensureData() {
    if (this._data) {
      return this._data;
    }
    this._data = {
      skins: []
    };
    return this._data;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    if (!this._data) {
      import_Log.default.unexpectedUndefined("SKCDP");
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._data);
  }
  /**
   * Loads the definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("SSDL");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._loadedWithComments = preserveComments;
      this._onLoaded.dispatch(this, this);
      return;
    }
    let data = {};
    let result = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      data = result;
    }
    this._data = data;
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
    this._onLoaded.dispatch(this, this);
  }
  async addChildItems(project, item) {
    const itemsCopy = project.getItemsCopy();
    const pack = await item.getPack();
    if (!pack) {
      return;
    }
    let packRootFolder = pack.folder;
    let textureList = this.getCanonicalizedTexturesList();
    for (const candItem of itemsCopy) {
      if (candItem.itemType === import_IProjectItemData.ProjectItemType.texture && packRootFolder && textureList) {
        if (!candItem.isContentLoaded) {
          await candItem.loadContent();
        }
        if (candItem.primaryFile) {
          let relativePath = import_TextureDefinition.default.canonicalizeTexturePath(
            import_StorageUtilities.default.getBaseRelativePath(candItem.primaryFile, packRootFolder)
          );
          if (relativePath) {
            if (textureList && textureList.includes(relativePath)) {
              item.addChildItem(candItem);
              textureList = import_Utilities.default.removeItemInArray(relativePath, textureList);
            }
          }
        }
      }
    }
    if (textureList) {
      for (const texturePath of textureList) {
        item.addUnfulfilledRelationship(texturePath, import_IProjectItemData.ProjectItemType.texture, false);
      }
    }
  }
}
//# sourceMappingURL=SkinCatalogDefinition.js.map
