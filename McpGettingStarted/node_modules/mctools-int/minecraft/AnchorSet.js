"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var AnchorSet_exports = {};
__export(AnchorSet_exports, {
  default: () => AnchorSet
});
module.exports = __toCommonJS(AnchorSet_exports);
var import_Utilities = __toESM(require("../core/Utilities"));
var import_Anchor = __toESM(require("./Anchor"));
class AnchorSet {
  data = {};
  anchors = {};
  clear(anchorName) {
    const exists = this.data[anchorName] !== void 0 || this.anchors[anchorName] !== void 0;
    if (import_Utilities.default.isUsableAsObjectKey(anchorName)) {
      this.data[anchorName] = void 0;
      this.anchors[anchorName] = void 0;
    }
    return exists;
  }
  clearAll() {
    this.data = {};
    this.anchors = {};
  }
  getLength() {
    let count = 0;
    for (const strKey in this.data) {
      count++;
    }
    return count;
  }
  getKeys() {
    const keyArr = [];
    for (const strKey in this.data) {
      keyArr.push(strKey);
    }
    return keyArr;
  }
  get(name) {
    if (!import_Utilities.default.isUsableAsObjectKey(name)) {
      throw new Error();
    }
    if (this.anchors[name]) {
      return this.anchors[name];
    }
    if (this.data[name]) {
      this.anchors[name] = new import_Anchor.default(this.data[name]);
      return this.anchors[name];
    }
    return void 0;
  }
  ensure(name, from, to) {
    let anchor;
    if (!import_Utilities.default.isUsableAsObjectKey(name)) {
      throw new Error();
    }
    if (this.anchors[name]) {
      anchor = this.anchors[name];
    }
    if (!anchor && this.data[name]) {
      this.anchors[name] = new import_Anchor.default(this.data[name]);
      anchor = this.anchors[name];
    }
    if (!anchor) {
      this.data[name] = {
        from,
        to,
        name
      };
      this.anchors[name] = new import_Anchor.default(this.data[name]);
      anchor = this.anchors[name];
    }
    if (anchor) {
      anchor.from = from;
      anchor.to = to;
      anchor.name = name;
    }
    return anchor;
  }
  getAsString() {
    return JSON.stringify(this.data);
  }
  fromString(incomingStr) {
    try {
      this.data = JSON.parse(incomingStr);
    } catch (e) {
    }
  }
}
//# sourceMappingURL=AnchorSet.js.map
