"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var Block_exports = {};
__export(Block_exports, {
  BlockDirection: () => BlockDirection,
  BlockFacingDirection: () => BlockFacingDirection,
  default: () => Block
});
module.exports = __toCommonJS(Block_exports);
var import_BlockProperty = __toESM(require("./BlockProperty"));
var import_Database = __toESM(require("./Database"));
var import_BlockRenderType = require("./BlockRenderType");
var import_ste_events = require("ste-events");
var import_Log = __toESM(require("../core/Log"));
var import_ComponentizedBase = __toESM(require("./ComponentizedBase"));
var import_Utilities = __toESM(require("../core/Utilities"));
var BlockFacingDirection = /* @__PURE__ */ ((BlockFacingDirection2) => {
  BlockFacingDirection2[BlockFacingDirection2["Down"] = 0] = "Down";
  BlockFacingDirection2[BlockFacingDirection2["up"] = 1] = "up";
  BlockFacingDirection2[BlockFacingDirection2["north"] = 2] = "north";
  BlockFacingDirection2[BlockFacingDirection2["south"] = 3] = "south";
  BlockFacingDirection2[BlockFacingDirection2["west"] = 4] = "west";
  BlockFacingDirection2[BlockFacingDirection2["east"] = 5] = "east";
  return BlockFacingDirection2;
})(BlockFacingDirection || {});
var BlockDirection = /* @__PURE__ */ ((BlockDirection2) => {
  BlockDirection2[BlockDirection2["South"] = 0] = "South";
  BlockDirection2[BlockDirection2["West"] = 1] = "West";
  BlockDirection2[BlockDirection2["North"] = 2] = "North";
  BlockDirection2[BlockDirection2["East"] = 3] = "East";
  return BlockDirection2;
})(BlockDirection || {});
class Block extends import_ComponentizedBase.default {
  static MAX_WATER_LEVEL = 16;
  _type;
  _blockType;
  _data = 0;
  _covered;
  _typeId;
  _z;
  _surroundings;
  line;
  extraLiquidDepth = -1;
  persistenceVersion = -1;
  properties = {};
  _onTypeChanged = new import_ste_events.EventDispatcher();
  _onPropertyChanged = new import_ste_events.EventDispatcher();
  get onTypeChanged() {
    return this._onTypeChanged.asEvent();
  }
  get onPropertyChanged() {
    return this._onPropertyChanged.asEvent();
  }
  get data() {
    return this._data;
  }
  set data(newData) {
    this._data = newData;
  }
  get surroundings() {
    if (this._surroundings !== void 0) {
      return this._surroundings;
    }
    this._ensureSurroundingsIfCube();
    return this._surroundings;
  }
  /**
   * Clears the cached surroundings so they will be recalculated on next access.
   * This should be called when a neighboring block changes.
   */
  clearSurroundings() {
    this._surroundings = void 0;
  }
  setType(blockType) {
    this._type = blockType;
    this._blockType = blockType;
    this._typeId = blockType.id;
  }
  get opaqueSideCount() {
    this._ensureSurroundingsIfCube();
    if (this._surroundings === void 0) {
      return 0;
    }
    let count = 0;
    const blockShortTypeName = this.shortTypeId;
    let adjacent = this._surroundings.left;
    if (adjacent === void 0 || adjacent.isEmpty || !adjacent.isOpaque && blockShortTypeName !== adjacent.shortTypeId) {
      count++;
    }
    adjacent = this._surroundings.right;
    if (adjacent === void 0 || adjacent.isEmpty || !adjacent.isOpaque && blockShortTypeName !== adjacent.shortTypeId) {
      count++;
    }
    adjacent = this._surroundings.up;
    if (adjacent === void 0 || adjacent.isEmpty || !adjacent.isOpaque && blockShortTypeName !== adjacent.shortTypeId) {
      count++;
    }
    adjacent = this._surroundings.down;
    if (adjacent === void 0 || adjacent.isEmpty || !adjacent.isOpaque && blockShortTypeName !== adjacent.shortTypeId) {
      count++;
    }
    adjacent = this._surroundings.forward;
    if (adjacent === void 0 || adjacent.isEmpty || !adjacent.isOpaque && blockShortTypeName !== adjacent.shortTypeId) {
      count++;
    }
    adjacent = this._surroundings.backward;
    if (adjacent === void 0 || adjacent.isEmpty || !adjacent.isOpaque && blockShortTypeName !== adjacent.shortTypeId) {
      count++;
    }
    return count;
  }
  get isOpaque() {
    if (this._blockType === void 0) {
      this._ensureTypes();
    }
    if (this._blockType !== void 0) {
      const baseType = this._blockType.baseType;
      return baseType.isOpaque;
    }
    return false;
  }
  _ensureTypes() {
    if (this._blockType !== void 0) {
      return;
    }
    if (this._typeId === void 0) {
      return;
    }
    this._type = import_Database.default.ensureBlockType(this._typeId);
    if (this._type.javaData !== null && this._type.javaData.name !== null && this._type.javaData.name !== this._typeId) {
      this._blockType = import_Database.default.ensureBlockType(this._type.javaData.name);
      const typeProp = this.getProperty("type");
      if (typeProp !== void 0) {
        if (this._blockType.shortId === "piston" && typeProp.asString("") === "sticky") {
          this._blockType = import_Database.default.ensureBlockType("sticky_piston");
        } else if (this._blockType.shortId === "sticky_piston" && typeProp.asString("") === "normal") {
          this._blockType = import_Database.default.ensureBlockType("piston");
        }
      }
    } else {
      this._blockType = this._type;
    }
  }
  _updateDataFromProperties() {
    this._ensureTypes();
    if (this._blockType === void 0) {
      return;
    }
    const baseType = this._blockType.baseType;
    let data = 0;
    if (baseType.data.properties !== void 0) {
      for (let i = 0; i < baseType.data.properties.length; i++) {
        const propData = baseType.data.properties[i];
        if (propData.values !== void 0) {
          const prop = this.getProperty(propData.name);
          if (prop !== null && prop !== void 0) {
            for (let j = 0; j < propData.values.length; j++) {
              if (prop.value === propData.values[j].id) {
                const dataAdd = propData.values[j].data;
                if (dataAdd !== void 0) {
                  data += dataAdd;
                }
              }
            }
          }
        }
      }
    }
    this._data = data;
  }
  _notifyTypeChanged() {
    this._onTypeChanged.dispatch(this, this);
  }
  _notifyPropertyChanged(blockProperty) {
    this._updateDataFromProperties();
    this._onPropertyChanged.dispatch(this, blockProperty);
    if (this.line !== void 0 && this.line.cube !== void 0) {
      this.line.cube._notifyBlockPropertyChanged(this);
    }
  }
  get blockType() {
    this._ensureTypes();
    if (this._blockType === void 0) {
      throw new Error("Block type is undefined for block: " + this.id);
    }
    return this._blockType;
  }
  get textureName() {
    this._ensureTypes();
    if (this._blockType === void 0) {
      return void 0;
    }
    if (this.shortTypeId === "water") {
      return void 0;
    }
    return this._blockType.baseType.name;
  }
  copyFrom(block) {
    this.typeName = block.typeName;
  }
  static fromLegacyId(byte) {
    if (byte === 0 || byte >= 256 || byte === void 0) {
      return new Block("minecraft:air");
    }
    const blockType = import_Database.default.getBlockTypeByLegacyId(byte);
    if (blockType) {
      return new Block(blockType.id);
    }
    return new Block("minecraft:dirt");
  }
  get effectiveWaterLevel() {
    let prop = this.getProperty("level");
    if (prop !== void 0) {
      return prop.asNumber(0) * 2;
    }
    const shortTypeName = this.shortTypeId;
    if (shortTypeName === "water") {
      return 15;
    }
    prop = this.getProperty("liquid_depth");
    if (prop !== void 0) {
      return 15 - prop.asNumber(0);
    }
    if (this.extraLiquidDepth < 0) {
      return 0;
    }
    return 15 - this.extraLiquidDepth;
  }
  get renderType() {
    this._ensureTypes();
    if (this._blockType === void 0) {
      return import_BlockRenderType.BlockRenderType.BlockOneTexture;
    }
    if (this.shortTypeId === "water") {
      return import_BlockRenderType.BlockRenderType.Water;
    }
    return this._blockType.renderType;
  }
  getProperty(name) {
    return this.properties[name];
  }
  ensureProperty(name) {
    return this.addProperty(name);
  }
  getPropertyBoolean(name, defaultValue) {
    const prop = this.properties[name];
    if (prop === void 0 || prop === null) {
      return defaultValue;
    }
    return prop.asBoolean(defaultValue);
  }
  getPropertyString(name, defaultValue) {
    const prop = this.properties[name];
    if (prop === void 0 || prop === null) {
      return defaultValue;
    }
    return prop.asString(defaultValue);
  }
  getPropertyNumber(name, defaultValue) {
    const prop = this.properties[name];
    if (prop === void 0 || prop === null) {
      return defaultValue;
    }
    return prop.asNumber(defaultValue);
  }
  addProperty(name) {
    if (!import_Utilities.default.isUsableAsObjectKey(name)) {
      import_Log.default.unsupportedToken(name);
      throw new Error();
    }
    let property = this.properties[name];
    if (property == null) {
      property = new import_BlockProperty.default(this);
      property.id = name;
      this.properties[name] = property;
    }
    return property;
  }
  get z() {
    return this._z;
  }
  set z(value) {
    this._z = value;
  }
  get x() {
    if (this.line === void 0) {
      return void 0;
    }
    return this.line.x;
  }
  get y() {
    if (this.line === void 0) {
      return void 0;
    }
    return this.line.y;
  }
  get coordinatesString() {
    if (this.line === void 0) {
      return "";
    }
    return this.x + "." + this.y + "." + this.z;
  }
  get typeName() {
    return this._typeId;
  }
  set typeName(val) {
    if (this._typeId !== val) {
      this._typeId = val;
      this._blockType = void 0;
      this._type = void 0;
      if (this.line !== void 0 && this.line.cube !== void 0) {
        this.line.cube._notifyBlockTypeChanged(this);
      }
    }
  }
  getBlockData() {
    let typeId = this.typeName;
    if (typeId === null || typeId === void 0) {
      typeId = "";
    }
    const props = {};
    for (const id in this.properties) {
      const blockProp = this.properties[id];
      props[id] = blockProp.value;
    }
    const blockData = {
      typeId,
      state: props
    };
    return blockData;
  }
  toString() {
    return JSON.stringify(this.getBlockData(), null, 2);
  }
  get isEmpty() {
    return this._typeId == null || this.shortTypeId === "air";
  }
  _ensureSurroundingsIfCube() {
    if (this.line === void 0 || this._z === void 0 || this._surroundings !== void 0) {
      return;
    }
    const cube = this.line.cube;
    const curX = this.line.x;
    const curY = this.line.y;
    const curZ = this._z;
    let down = void 0;
    let up = void 0;
    let left = void 0;
    let right = void 0;
    let forward = void 0;
    let backward = void 0;
    const plane = cube.x(curX);
    if (curY >= 1) {
      down = plane.y(curY - 1).z(curZ);
    }
    if (curY < cube.maxY - 1) {
      up = plane.y(curY + 1).z(curZ);
    }
    if (curX >= 1) {
      left = cube.x(curX - 1).y(curY).z(curZ);
    }
    if (curX < cube.maxX - 1) {
      right = cube.x(curX + 1).y(curY).z(curZ);
    }
    const line = cube.x(curX).y(curY);
    if (curZ >= 1) {
      backward = line.z(curZ - 1);
    }
    if (curZ < cube.maxZ - 1) {
      forward = line.z(curZ + 1);
    }
    this._surroundings = {
      down,
      up,
      left,
      right,
      backward,
      forward
    };
  }
  get isCovered() {
    if (this._covered !== void 0) {
      return this._covered;
    }
    this._ensureSurroundingsIfCube();
    if (this._surroundings === void 0 || this._z === void 0) {
      this._covered = false;
      return false;
    }
    const surround = this._surroundings;
    if (surround.down === void 0 || surround.down.isEmpty || !surround.down.isOpaque || !surround.down.isCovered || surround.up === void 0 || surround.up.isEmpty || !surround.up.isOpaque || !surround.up.isCovered || surround.left === void 0 || surround.left.isEmpty || !surround.left.isOpaque || !surround.left.isCovered || surround.right === void 0 || surround.right.isEmpty || !surround.right.isOpaque || !surround.right.isCovered || surround.forward === void 0 || surround.forward.isEmpty || !surround.forward.isOpaque || !surround.forward.isCovered || surround.backward === void 0 || surround.backward.isEmpty || !surround.backward.isOpaque || !surround.backward.isCovered) {
      this._covered = false;
      return false;
    }
    this._covered = true;
    return true;
  }
  get isTouchingOtherBlock() {
    this._ensureSurroundingsIfCube();
    if (this._surroundings === void 0) {
      return false;
    }
    const surr = this._surroundings;
    if (surr.down !== void 0 && !surr.down.isEmpty || surr.up !== void 0 && !surr.up.isEmpty || surr.left !== void 0 && !surr.left.isEmpty || surr.right !== void 0 && !surr.right.isEmpty || surr.forward !== void 0 && !surr.forward.isEmpty || surr.backward !== void 0 && !surr.backward.isEmpty) {
      return true;
    }
    return false;
  }
  get up() {
    this._ensureSurroundingsIfCube();
    if (this._surroundings === void 0) {
      return void 0;
    }
    return this._surroundings.up;
  }
  get down() {
    this._ensureSurroundingsIfCube();
    if (this._surroundings === void 0) {
      return void 0;
    }
    return this._surroundings.down;
  }
  get left() {
    this._ensureSurroundingsIfCube();
    if (this._surroundings === void 0) {
      return void 0;
    }
    return this._surroundings.left;
  }
  get right() {
    this._ensureSurroundingsIfCube();
    if (this._surroundings === void 0) {
      return void 0;
    }
    return this._surroundings.right;
  }
  get forward() {
    this._ensureSurroundingsIfCube();
    if (this._surroundings === void 0) {
      return void 0;
    }
    return this._surroundings.forward;
  }
  get backward() {
    this._ensureSurroundingsIfCube();
    if (this._surroundings === void 0) {
      return void 0;
    }
    return this._surroundings.backward;
  }
  get shortTypeId() {
    if (this._typeId !== void 0) {
      if (this._typeId.startsWith("minecraft:")) {
        return this._typeId.substring(10, this._typeId.length);
      }
      return this._typeId;
    }
    return void 0;
  }
  constructor(typeId) {
    super();
    this._typeId = typeId;
  }
  applyFrom(template) {
    import_Log.default.assert(template !== null && template !== void 0, "Undefined block template");
    this._typeId = template.typeName;
    for (const propName in template.properties) {
      const property = template.getProperty(propName);
      const prop = this.ensureProperty(propName);
      prop.nbtType = property.nbtType;
      prop.value = property.value;
    }
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  BlockDirection,
  BlockFacingDirection
});
//# sourceMappingURL=Block.js.map
