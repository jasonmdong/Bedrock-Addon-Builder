"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ManagedFilterClauseOrFilterClauseSet_exports = {};
__export(ManagedFilterClauseOrFilterClauseSet_exports, {
  default: () => ManagedFilterClauseOrFilterClauseSet
});
module.exports = __toCommonJS(ManagedFilterClauseOrFilterClauseSet_exports);
var import_Utilities = __toESM(require("../core/Utilities"));
class ManagedFilterClauseOrFilterClauseSet {
  _data;
  constructor(data) {
    this._data = data;
  }
  get test() {
    if (!this._data) {
      return void 0;
    }
    return this._data.test;
  }
  get operator() {
    if (!this._data) {
      return void 0;
    }
    return this._data.operator;
  }
  get subject() {
    if (!this._data) {
      return void 0;
    }
    return this._data.subject;
  }
  get value() {
    if (!this._data) {
      return void 0;
    }
    return this._data.value;
  }
  get anyOf() {
    if (!this._data) {
      return void 0;
    }
    const anyOfData = this._data.any_of;
    if (!anyOfData || !Array.isArray(anyOfData)) {
      return void 0;
    }
    let anyOfs = [];
    for (const dataNode of anyOfData) {
      anyOfs.push(new ManagedFilterClauseOrFilterClauseSet(dataNode));
    }
    return anyOfs;
  }
  get allOf() {
    if (!this._data) {
      return void 0;
    }
    const allOfData = this._data.all_of;
    if (!allOfData || !Array.isArray(allOfData)) {
      return void 0;
    }
    let allOfs = [];
    for (const dataNode of allOfData) {
      allOfs.push(new ManagedFilterClauseOrFilterClauseSet(dataNode));
    }
    return allOfs;
  }
  getHumanSummary() {
    let results = "";
    if (this.anyOf && Array.isArray(this.anyOf)) {
      results += "Any condition must match:";
      for (const anyOfNode of this.anyOf) {
        results += anyOfNode.getHumanSummary();
      }
    } else if (this.allOf && Array.isArray(this.allOf)) {
      results += "All conditions must match:";
      for (const allOfNode of this.allOf) {
        results += allOfNode.getHumanSummary();
      }
    } else if (this.test) {
      results += import_Utilities.default.humanifyMinecraftName(this.test);
      if (this.operator) {
        results += " " + this.operator;
      }
      if (this.value) {
        results += " " + this.value;
      }
    }
    return results;
  }
}
//# sourceMappingURL=ManagedFilterClauseOrFilterClauseSet.js.map
