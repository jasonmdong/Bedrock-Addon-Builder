"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var AudioDefinition_exports = {};
__export(AudioDefinition_exports, {
  default: () => AudioDefinition
});
module.exports = __toCommonJS(AudioDefinition_exports);
var import_ste_events = require("ste-events");
var import_ProjectItemCreateManager = __toESM(require("../app/ProjectItemCreateManager"));
var import_StorageUtilities = require("../storage/StorageUtilities");
class AudioDefinition {
  _file;
  _id;
  _isLoaded = false;
  _onLoaded = new import_ste_events.EventDispatcher();
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  get id() {
    if (this._file) {
      return this._file.name;
    }
    return this._id;
  }
  set id(newId) {
    this._id = newId;
  }
  static async ensureOnFile(file, loadHandler) {
    let afd;
    if (file.manager === void 0) {
      afd = new AudioDefinition();
      afd.file = file;
      file.manager = afd;
    }
    if (file.manager !== void 0 && file.manager instanceof AudioDefinition) {
      afd = file.manager;
      if (!afd.isLoaded) {
        if (loadHandler) {
          afd.onLoaded.subscribe(loadHandler);
        }
        await afd.load();
      }
    }
    return afd;
  }
  persist() {
    return false;
  }
  static canonicalizeAudioPath(projectPath) {
    if (projectPath === void 0) {
      return void 0;
    }
    projectPath = projectPath.toLowerCase();
    const lastPeriod = projectPath.lastIndexOf(".");
    if (lastPeriod >= 0) {
      const removedPart = projectPath.substring(lastPeriod + 1);
      if (import_StorageUtilities.AllowedExtensionsSet.has(removedPart)) {
        projectPath = projectPath.substring(0, lastPeriod);
      }
    }
    return projectPath;
  }
  async ensureSoundDefinitionsForFile(project) {
    if (!this._file) {
      return;
    }
    let soundDefinitionCat = await import_ProjectItemCreateManager.default.ensureSoundDefinitionCatalogDefinition(project);
    if (this._file && soundDefinitionCat) {
      let matches = soundDefinitionCat.getSoundDefinitionMatchesByPath(this._file);
      if (matches) {
        let keyCount = 0;
        for (const key in matches) {
          if (key) {
            keyCount++;
          }
        }
        if (keyCount === 0) {
          soundDefinitionCat.ensureDefintionForFile(project, this._file);
        }
      }
      soundDefinitionCat.persist();
    }
  }
  async load() {
    if (this._file === void 0 || this._isLoaded) {
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (this._file.content === null || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._onLoaded.dispatch(this, this);
      return;
    }
    this._isLoaded = true;
    this._onLoaded.dispatch(this, this);
  }
}
//# sourceMappingURL=AudioDefinition.js.map
