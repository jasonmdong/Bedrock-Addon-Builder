"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ComponentProperty_exports = {};
__export(ComponentProperty_exports, {
  default: () => ComponentProperty
});
module.exports = __toCommonJS(ComponentProperty_exports);
var import_Database = __toESM(require("./../minecraft/Database"));
var import_Log = __toESM(require("./../core/Log"));
var import_IField = require("../dataform/IField");
class ComponentProperty {
  id;
  _item;
  _form;
  _field;
  isCoreProp = false;
  isInstanceProp = false;
  constructor(item, id) {
    this._item = item;
    this.id = id;
  }
  load() {
    if (import_Database.default.uxCatalog === null || this._form !== void 0) {
      return;
    }
    const result = import_Database.default.getComponentFormField(this.id);
    if (result !== void 0) {
      this._form = result.form;
      if (this._form.id === "entityCore") {
        this.isCoreProp = true;
      } else if (this._form.id === "entityInstance") {
        this.isInstanceProp = true;
      }
      this._field = result.field;
    } else {
      import_Log.default.debugAlert("Form field " + this.id + " not found.");
    }
  }
  get type() {
    if (this._field !== void 0) {
      return this._field.dataType;
    }
    return import_IField.FieldDataType.string;
  }
  get defaultValue() {
    if (this._field === void 0) {
      import_Log.default.fail("Unbacked component property '" + this.id + "' found.");
      return void 0;
    }
    return this._field.defaultValue;
  }
  get value() {
    if (this._field === void 0) {
      import_Log.default.fail("Unbacked component property '" + this.id + "' found.");
      return void 0;
    }
    if (this.isCoreProp && this._field.groupId !== void 0) {
      const componentId = this._field.groupId;
      const component = this._item.getComponent(componentId);
      if (this._field.dataType === import_IField.FieldDataType.boolean) {
        if (component !== void 0) {
          return true;
        } else {
          return false;
        }
      } else if (this._field.dataType === import_IField.FieldDataType.number || this._field.dataType === import_IField.FieldDataType.int || this._field.dataType === import_IField.FieldDataType.intBoolean || this._field.dataType === import_IField.FieldDataType.intValueLookup) {
        if (component !== void 0) {
          const val = component.value;
          return val;
        }
      }
    } else if (!this.isCoreProp && this._form !== void 0 && this._field !== void 0 && this._form.id) {
      const componentId = this._form.id;
      const component = this._item.getComponent(componentId);
      if (component !== void 0 && typeof component !== "object") {
        return component[this._field.id];
      }
    }
    return this.defaultValue;
  }
  asBoolean(defaultVal) {
    const val = this.value;
    if (val === null || val === void 0) {
      return defaultVal;
    }
    if (typeof val === "boolean") {
      return val;
    } else if (typeof val === "number") {
      return val !== 0;
    } else if (typeof val === "string") {
      return val === "true" || val === "1";
    }
    return defaultVal;
  }
  asString(defaultVal) {
    const val = this.value;
    if (val === null || val === void 0) {
      return defaultVal;
    }
    return val.toString();
  }
  asNumber(defaultVal) {
    const val = this.value;
    if (val === null || val === void 0) {
      return defaultVal;
    }
    if (typeof val === "number") {
      return val;
    } else if (typeof val === "string") {
      return parseInt(val);
    } else if (typeof val === "boolean") {
      if (val) {
        return 1;
      } else {
        return 0;
      }
    }
    return defaultVal;
  }
  set value(newValue) {
    const val = this.value;
    if (val !== newValue) {
    }
  }
}
//# sourceMappingURL=ComponentProperty.js.map
