"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ItemTextureCatalogDefinition_exports = {};
__export(ItemTextureCatalogDefinition_exports, {
  default: () => ItemTextureCatalogDefinition
});
module.exports = __toCommonJS(ItemTextureCatalogDefinition_exports);
var import_Log = __toESM(require("../core/Log"));
var import_ste_events = require("ste-events");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_Database = __toESM(require("./Database"));
var import_TextureDefinition = __toESM(require("./TextureDefinition"));
class ItemTextureCatalogDefinition {
  _data;
  _file;
  _isLoaded = false;
  _loadedWithComments = false;
  _onLoaded = new import_ste_events.EventDispatcher();
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  get data() {
    return this._data;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  get textureData() {
    if (!this._data) {
      return void 0;
    }
    if (this._data.texture_data === void 0) {
      this._data.texture_data = {};
    }
    return this._data.texture_data;
  }
  getCanonicalizedTexturePathList() {
    if (!this._data || !this._data.texture_data) {
      return void 0;
    }
    const textureList = [];
    for (const key in this._data.texture_data) {
      const texturePathArr = this._data.texture_data[key];
      if (texturePathArr && texturePathArr.textures) {
        if (typeof texturePathArr.textures === "string") {
          const path = import_TextureDefinition.default.canonicalizeTexturePath(texturePathArr.textures);
          if (path) {
            textureList.push(path);
          }
        } else if (Array.isArray(texturePathArr.textures)) {
          for (const texturePath of texturePathArr.textures) {
            if (typeof texturePath === "string") {
              const path = import_TextureDefinition.default.canonicalizeTexturePath(texturePath);
              if (path) {
                textureList.push(path);
              }
            } else if (texturePath) {
              let tpath = texturePath.path;
              if (typeof tpath === "string") {
                tpath = import_TextureDefinition.default.canonicalizeTexturePath(tpath);
                if (tpath) {
                  textureList.push(tpath);
                }
              }
            }
          }
        }
      }
    }
    return textureList;
  }
  getTexturePathList() {
    if (!this._data || !this._data.texture_data) {
      return void 0;
    }
    const textureList = [];
    for (const key in this._data.texture_data) {
      const texturePathArr = this._data.texture_data[key];
      if (texturePathArr && texturePathArr.textures) {
        if (typeof texturePathArr.textures === "string") {
          textureList.push(texturePathArr.textures);
        } else if (Array.isArray(texturePathArr.textures)) {
          for (const texturePath of texturePathArr.textures) {
            if (typeof texturePath === "string") {
              const path = import_TextureDefinition.default.canonicalizeTexturePath(texturePath);
              if (path) {
                textureList.push(path);
              }
            } else if (texturePath) {
              let tpath = texturePath.path;
              if (typeof tpath === "string") {
                tpath = import_TextureDefinition.default.canonicalizeTexturePath(tpath);
                if (tpath) {
                  textureList.push(tpath);
                }
              }
            }
          }
        }
      }
    }
    return textureList;
  }
  get texturesIdList() {
    if (!this._data || !this._data.texture_data) {
      return void 0;
    }
    const textureIdList = [];
    for (const key in this._data.texture_data) {
      textureIdList.push(key);
    }
    return textureIdList;
  }
  static async ensureOnFile(file, loadHandler) {
    let et;
    if (file.manager === void 0) {
      et = new ItemTextureCatalogDefinition();
      et.file = file;
      file.manager = et;
    }
    if (file.manager !== void 0 && file.manager instanceof ItemTextureCatalogDefinition) {
      et = file.manager;
      if (!et.isLoaded) {
        if (loadHandler) {
          et.onLoaded.subscribe(loadHandler);
        }
        await et.load();
      }
    }
    return et;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    if (!this._data) {
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._data);
  }
  getPackRootFolder() {
    let packRootFolder = void 0;
    if (this.file && this.file.parentFolder) {
      let parentFolder = this.file.parentFolder;
      packRootFolder = import_StorageUtilities.default.getParentOfParentFolderNamed("textures", parentFolder);
    }
    return packRootFolder;
  }
  getTextureReferences() {
    const textureRefs = [];
    if (this.data?.texture_data) {
      for (const resourceId in this.data.texture_data) {
        const resource = this.data.texture_data[resourceId];
        if (resource && resource.textures) {
          if (!textureRefs.includes(resourceId)) {
            textureRefs.push(resourceId);
          }
        }
      }
    }
    return textureRefs;
  }
  async addChildItems(project, item) {
    const itemsCopy = project.getItemsCopy();
    let packRootFolder = this.getPackRootFolder();
    let texturePathList = this.getCanonicalizedTexturePathList();
    for (const candItem of itemsCopy) {
      if (candItem.itemType === import_IProjectItemData.ProjectItemType.texture && packRootFolder && texturePathList) {
        if (!candItem.isContentLoaded) {
          await candItem.loadContent();
        }
        if (candItem.primaryFile) {
          let relativePath = import_TextureDefinition.default.canonicalizeTexturePath(
            import_StorageUtilities.default.getBaseRelativePath(candItem.primaryFile, packRootFolder)
          );
          if (relativePath) {
            if (texturePathList && texturePathList.includes(relativePath)) {
              item.addChildItem(candItem);
              texturePathList = import_Utilities.default.removeItemInArray(relativePath, texturePathList);
            }
          }
        }
      }
    }
    if (texturePathList) {
      for (const texturePath of texturePathList) {
        item.addUnfulfilledRelationship(
          texturePath,
          import_IProjectItemData.ProjectItemType.texture,
          await import_Database.default.isVanillaToken(texturePath)
        );
      }
    }
  }
  /**
   * Loads the definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("ITCDF");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._loadedWithComments = preserveComments;
      this._onLoaded.dispatch(this, this);
      return;
    }
    let data = {};
    let result = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      data = result;
    }
    this._data = data;
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
    this._onLoaded.dispatch(this, this);
  }
}
//# sourceMappingURL=ItemTextureCatalogDefinition.js.map
