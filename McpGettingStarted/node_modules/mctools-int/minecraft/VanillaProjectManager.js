"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var VanillaProjectManager_exports = {};
__export(VanillaProjectManager_exports, {
  default: () => VanillaProjectManager
});
module.exports = __toCommonJS(VanillaProjectManager_exports);
var import_Database = __toESM(require("./Database"));
var import_ItemTextureCatalogDefinition = __toESM(require("./ItemTextureCatalogDefinition"));
var import_SoundDefinitionCatalogDefinition = __toESM(require("./SoundDefinitionCatalogDefinition"));
var import_BlocksCatalogDefinition = __toESM(require("./BlocksCatalogDefinition"));
var import_TerrainTextureCatalogDefinition = __toESM(require("./TerrainTextureCatalogDefinition"));
var import_EntityTypeResourceDefinition = __toESM(require("./EntityTypeResourceDefinition"));
var import_ModelGeometryDefinition = __toESM(require("./ModelGeometryDefinition"));
var import_Log = __toESM(require("../core/Log"));
var import_VanillaGeometryTransforms = require("./VanillaGeometryTransforms");
var import_CreatorToolsHost = __toESM(require("../app/CreatorToolsHost"));
class VanillaProjectManager {
  static blocksCatalog = null;
  static itemTextureCatalog = null;
  static terrainTextureCatalog = null;
  static soundDefinitionCatalog = null;
  // Cache for entity resource definitions
  static _entityResourceCache = /* @__PURE__ */ new Map();
  static _entityModelDataCache = /* @__PURE__ */ new Map();
  /**
   * Get a list of all vanilla entity type IDs
   */
  static async getVanillaEntityTypeIds() {
    const vanillaFolder = await import_Database.default.getPreviewVanillaFolder();
    if (!vanillaFolder) return [];
    const entityFolder = await vanillaFolder.getFolderFromRelativePath("/resource_pack/entity/");
    if (!entityFolder) return [];
    await entityFolder.load();
    const entityIds = [];
    for (const fileName in entityFolder.files) {
      if (fileName.endsWith(".entity.json") && !fileName.includes("v1.0") && !fileName.includes(".v2.") && !fileName.includes(".v3.") && !fileName.includes("_v1.") && !fileName.includes("_v2.") && !fileName.includes("_v3.")) {
        const entityId = fileName.replace(".entity.json", "");
        entityIds.push(entityId);
      }
    }
    entityIds.sort();
    return entityIds;
  }
  /**
   * Get entity resource definition for a vanilla entity by type ID
   */
  static async getVanillaEntityResource(typeId) {
    const shortId = typeId.replace("minecraft:", "");
    if (this._entityResourceCache.has(shortId)) {
      return this._entityResourceCache.get(shortId) || null;
    }
    const file = await import_Database.default.getPreviewVanillaFile(`/resource_pack/entity/${shortId}.entity.json`);
    if (!file) return null;
    const etrd = await import_EntityTypeResourceDefinition.default.ensureOnFile(file);
    if (etrd) {
      this._entityResourceCache.set(shortId, etrd);
      return etrd;
    }
    return null;
  }
  /**
   * Get complete model data for a vanilla entity, including geometry and texture.
   * Uses the "default" variant by default, ensuring geometry and texture are properly matched.
   * @param typeId - Entity type ID (e.g., "cow" or "minecraft:cow")
   * @param variantKey - Optional variant key (e.g., "default", "warm", "cold"). Defaults to "default".
   */
  static async getVanillaEntityModelData(typeId, variantKey = "default") {
    const shortId = typeId.replace("minecraft:", "");
    const cacheKey = `${shortId}_${variantKey}`;
    if (this._entityModelDataCache.has(cacheKey)) {
      const cached = this._entityModelDataCache.get(cacheKey) || null;
      if (cached && !cached.textureData && !cached.textureUrl) {
        this._entityModelDataCache.delete(cacheKey);
      } else {
        return cached;
      }
    }
    const entityResource = await this.getVanillaEntityResource(shortId);
    if (!entityResource) {
      return null;
    }
    const modelData = {
      entityTypeId: shortId
    };
    const matched = entityResource.getMatchedGeometryAndTexture(variantKey);
    if (matched.geometryId) {
      modelData.geometryId = matched.geometryId;
      const geometry = await this._loadVanillaGeometry(matched.geometryId);
      if (geometry) {
        modelData.geometry = (0, import_VanillaGeometryTransforms.applyGeometryTransforms)(geometry.geometry, matched.geometryId);
        modelData.modelDefinition = geometry.definition;
      } else {
        import_Log.default.debugAlert(`Failed to load geometry for ${shortId}: ${matched.geometryId}`);
      }
    }
    if (matched.texturePath) {
      let texturePath = matched.texturePath;
      if (texturePath.endsWith(".png") || texturePath.endsWith(".tga")) {
        texturePath = texturePath.substring(0, texturePath.lastIndexOf("."));
      }
      modelData.texturePath = texturePath;
      modelData.textureUrl = import_CreatorToolsHost.default.contentWebRoot + `res/latest/van/serve/resource_pack/${texturePath}.png`;
      const textureData = await this._loadVanillaTexture(texturePath);
      if (textureData) {
        modelData.textureData = textureData;
      }
    }
    this._entityModelDataCache.set(cacheKey, modelData);
    return modelData;
  }
  static async _loadVanillaGeometry(geometryId) {
    const parts = geometryId.split(".");
    if (parts.length < 2) return null;
    const baseName = parts[1];
    const baseNameWithUnderscore = this._addUnderscoreBeforeLastPart(baseName);
    const baseNameWithoutSuffix = baseName.replace(/_[a-z]$/, "");
    const baseNameWithUnderscoreNoSuffix = this._addUnderscoreBeforeLastPart(baseNameWithoutSuffix);
    const possibleBaseNames = [baseName, baseNameWithUnderscore, baseNameWithoutSuffix, baseNameWithUnderscoreNoSuffix];
    const uniqueBaseNames = [...new Set(possibleBaseNames)];
    const possibleFiles = [];
    for (const name of uniqueBaseNames) {
      possibleFiles.push(`${name}.geo.json`);
      possibleFiles.push(`${name}.v3.geo.json`);
      possibleFiles.push(`${name}.v2.geo.json`);
    }
    for (const fileName of possibleFiles) {
      const file = await import_Database.default.getPreviewVanillaFile(`/resource_pack/models/entity/${fileName}`);
      if (file) {
        const geoDef = await import_ModelGeometryDefinition.default.ensureOnFile(file);
        if (geoDef) {
          const exactGeometry = geoDef.getById(geometryId);
          if (exactGeometry) {
            return { geometry: exactGeometry, definition: geoDef };
          }
        }
      }
    }
    for (const fileName of possibleFiles) {
      const file = await import_Database.default.getPreviewVanillaFile(`/resource_pack/models/entity/${fileName}`);
      if (file) {
        const geoDef = await import_ModelGeometryDefinition.default.ensureOnFile(file);
        if (geoDef && geoDef.defaultGeometry) {
          import_Log.default.debugAlert(
            `VanillaProjectManager: No exact match for geometry "${geometryId}", falling back to default from ${fileName}`
          );
          return { geometry: geoDef.defaultGeometry, definition: geoDef };
        }
      }
    }
    return null;
  }
  /**
   * Adds an underscore before the last "word" in a name.
   * e.g., "tropicalfish" -> "tropical_fish", "tropicalfish_a" -> "tropical_fish_a"
   */
  static _addUnderscoreBeforeLastPart(name) {
    const commonWords = [
      "fish",
      "spawner",
      "golem",
      "spider",
      "skeleton",
      "zombie",
      "creeper",
      "slime",
      "cube",
      "guardian",
      "shulker",
      "villager",
      "illager",
      "pillager",
      "witch",
      "horse",
      "donkey",
      "mule",
      "llama",
      "wolf",
      "cat",
      "ocelot",
      "fox",
      "panda",
      "bee",
      "hoglin",
      "piglin",
      "strider",
      "axolotl",
      "goat",
      "frog",
      "warden",
      "sniffer",
      "camel",
      "breeze",
      "bogged",
      "armadillo"
    ];
    for (const word of commonWords) {
      const index = name.indexOf(word);
      if (index > 0 && name[index - 1] !== "_") {
        return name.substring(0, index) + "_" + name.substring(index);
      }
    }
    return name;
  }
  static async _loadVanillaTexture(texturePath) {
    const vanillaFolder = await import_Database.default.getServeVanillaFolder();
    if (!vanillaFolder) {
      import_Log.default.debugAlert("Could not get serve vanilla folder");
      return null;
    }
    const fullPath = `/resource_pack/${texturePath}.png`;
    const file = await vanillaFolder.getFileFromRelativePath(fullPath);
    if (!file) {
      import_Log.default.debugAlert(`Vanilla texture file not found: ${fullPath}`);
      const dirPath = `/resource_pack/${texturePath.substring(0, texturePath.lastIndexOf("/"))}`;
      return null;
    }
    await file.loadContent();
    if (file.content instanceof Uint8Array) {
      return file.content;
    }
    import_Log.default.debugAlert(`Vanilla texture file content is not Uint8Array: ${typeof file.content}`);
    return null;
  }
  static getBlocksCatalogDirect() {
    return this.blocksCatalog;
  }
  static async getBlocksCatalog() {
    if (!VanillaProjectManager.blocksCatalog) {
      const file = await import_Database.default.getPreviewVanillaFile("/resource_pack/blocks.json");
      if (file) {
        const blockCat = new import_BlocksCatalogDefinition.default();
        blockCat.file = file;
        await blockCat.load();
        this.blocksCatalog = blockCat;
      }
    }
    return this.blocksCatalog;
  }
  static getTerrainTexturesCatalogDirect() {
    return this.terrainTextureCatalog;
  }
  static async getTerrainTexturesCatalog() {
    if (!VanillaProjectManager.terrainTextureCatalog) {
      const file = await import_Database.default.getPreviewVanillaFile("/resource_pack/textures/terrain_texture.json");
      if (file) {
        const terrainCat = new import_TerrainTextureCatalogDefinition.default();
        terrainCat.file = file;
        await terrainCat.load();
        VanillaProjectManager.terrainTextureCatalog = terrainCat;
      }
    }
    return VanillaProjectManager.terrainTextureCatalog;
  }
  static async getItemTexturesCatalog() {
    if (!VanillaProjectManager.itemTextureCatalog) {
      const file = await import_Database.default.getPreviewVanillaFile("/resource_pack/textures/item_texture.json");
      if (file) {
        const itemCat = new import_ItemTextureCatalogDefinition.default();
        itemCat.file = file;
        await itemCat.load();
        VanillaProjectManager.itemTextureCatalog = itemCat;
      }
    }
    return VanillaProjectManager.itemTextureCatalog;
  }
  static async getSoundDefinitionCatalog() {
    if (!VanillaProjectManager.soundDefinitionCatalog) {
      const file = await import_Database.default.getPreviewVanillaFile("/resource_pack/sounds/sound_definitions.json");
      if (file) {
        const soundDefinitionCat = new import_SoundDefinitionCatalogDefinition.default();
        soundDefinitionCat.file = file;
        await soundDefinitionCat.load();
        VanillaProjectManager.soundDefinitionCatalog = soundDefinitionCat;
      }
    }
    return VanillaProjectManager.soundDefinitionCatalog;
  }
}
//# sourceMappingURL=VanillaProjectManager.js.map
