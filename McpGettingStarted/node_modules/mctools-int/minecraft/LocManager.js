"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var LocManager_exports = {};
__export(LocManager_exports, {
  default: () => LocManager
});
module.exports = __toCommonJS(LocManager_exports);
var import_IProjectItemData = require("../app/IProjectItemData");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_Log = __toESM(require("../core/Log"));
var import_Lang = __toESM(require("./Lang"));
class LocManager {
  _project;
  _isLoaded;
  _languages = {};
  tokens = {};
  constructor(project) {
    this._project = project;
    this._isLoaded = false;
  }
  async getTokenValue(tokenName) {
    if (!this._isLoaded) {
      await this.load();
    }
    return this.getTokenValueOrDefault(tokenName);
  }
  getAllTokenKeys() {
    return Object.keys(this.tokens);
  }
  getAllLanguages() {
    let langs = [];
    for (const langKey in this._languages) {
      langs.push(...this._languages[langKey]);
    }
    return langs;
  }
  static canonicalizeLanguageKey(locKey) {
    return locKey.toLowerCase().trim();
  }
  getEnUsLang() {
    const primary = this._languages["en_us"];
    import_Log.default.assert(!!primary, "No en-us language found.");
    return primary;
  }
  getNonEnUsLangs() {
    let langs = [];
    for (const langKey in this._languages) {
      if (LocManager.canonicalizeLanguageKey(langKey) !== "en_us") {
        langs.push(...this._languages[langKey]);
      }
    }
    return langs;
  }
  getExpandedValue(tokenName) {
    if (!this._isLoaded) {
      return tokenName;
    }
    const tok = this.getToken(tokenName);
    if (!tok) {
      return tokenName;
    }
    return tok.value + " - (" + tokenName + ")";
  }
  getTokenValueOrDefault(tokenName) {
    if (!this._isLoaded) {
      return tokenName;
    }
    const tok = this.getToken(tokenName);
    if (!tok) {
      return tokenName;
    }
    return tok.value;
  }
  getToken(tokenName, locale, packContainer) {
    const tokenSets = this.tokens[tokenName];
    if (tokenSets) {
      if (!locale) {
        locale = "en_US";
      }
      const tokenSetByLocale = tokenSets[LocManager.canonicalizeLanguageKey(locale)];
      if (tokenSetByLocale) {
        if (packContainer) {
          return tokenSetByLocale[packContainer];
        }
        for (const containerName in tokenSetByLocale) {
          return tokenSetByLocale[containerName];
        }
      }
    }
    return void 0;
  }
  async load(force) {
    if (this._isLoaded && !force) {
      return;
    }
    await this._project.ensureLoadedProjectFolder();
    this.tokens = {};
    const itemsCopy = this._project.getItemsCopy();
    for (const pi of itemsCopy) {
      if (pi.itemType === import_IProjectItemData.ProjectItemType.lang) {
        if (!pi.isContentLoaded) {
          await pi.loadContent();
        }
        if (pi.primaryFile) {
          const lang = await import_Lang.default.ensureOnFile(pi.primaryFile);
          if (lang) {
            await lang.load();
            if (lang.language && lang.containerName !== void 0 && import_Utilities.default.isUsableAsObjectKey(lang.language)) {
              const language = LocManager.canonicalizeLanguageKey(lang.language);
              if (!this._languages[language]) {
                this._languages[language] = [];
              }
              this._languages[language].push(lang);
              for (const tokenName in lang.tokens) {
                if (import_Utilities.default.isUsableAsObjectKey(tokenName)) {
                  if (this.tokens[tokenName] === void 0) {
                    this.tokens[tokenName] = {};
                  }
                  if (import_Utilities.default.isUsableAsObjectKey(lang.containerName)) {
                    if (this.tokens[tokenName][language] === void 0) {
                      this.tokens[tokenName][language] = {};
                    }
                    this.tokens[tokenName][language][lang.containerName] = lang.tokens[tokenName];
                  }
                }
              }
            }
          }
        }
      }
    }
    this._isLoaded = true;
  }
}
//# sourceMappingURL=LocManager.js.map
