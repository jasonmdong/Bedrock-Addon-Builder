"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TerrainTextureCatalogDefinition_exports = {};
__export(TerrainTextureCatalogDefinition_exports, {
  default: () => TerrainTextureCatalogDefinition
});
module.exports = __toCommonJS(TerrainTextureCatalogDefinition_exports);
var import_Log = __toESM(require("../core/Log"));
var import_ste_events = require("ste-events");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_Project = require("../app/Project");
var import_IProjectItemData = require("../app/IProjectItemData");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_Database = __toESM(require("./Database"));
class TerrainTextureCatalogDefinition {
  _data;
  _file;
  _isLoaded = false;
  _loadedWithComments = false;
  id;
  _onLoaded = new import_ste_events.EventDispatcher();
  get isLoaded() {
    return this._isLoaded;
  }
  get data() {
    return this._data;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  get textureData() {
    if (!this._data) {
      return void 0;
    }
    if (this._data.texture_data === void 0) {
      this._data.texture_data = {};
    }
    return this._data.texture_data;
  }
  getTexturePathList() {
    if (!this._data || !this._data.texture_data) {
      return void 0;
    }
    const textureList = [];
    for (const key in this._data.texture_data) {
      const texturePathArr = this._data.texture_data[key];
      if (texturePathArr && texturePathArr.textures) {
        if (typeof texturePathArr.textures === "string") {
          textureList.push(texturePathArr.textures.toLowerCase());
        } else if (Array.isArray(texturePathArr.textures)) {
          for (const texturePath of texturePathArr.textures) {
            if (typeof texturePath === "string") {
              textureList.push(texturePath.toLowerCase());
            } else if (texturePath.path) {
              textureList.push(texturePath.path.toLowerCase());
            } else if (typeof texturePath === "object" && texturePath.variations) {
              for (const variation of texturePath.variations) {
                if (typeof variation === "string") {
                  textureList.push(variation.toLowerCase());
                } else if (variation.path && typeof variation.path === "string") {
                  textureList.push(variation.path.toLowerCase());
                }
              }
            }
          }
        }
      }
    }
    return textureList;
  }
  get texturesIdList() {
    if (!this._data || !this._data.texture_data) {
      return void 0;
    }
    const textureIdList = [];
    for (const key in this._data.texture_data) {
      textureIdList.push(key);
    }
    return textureIdList;
  }
  getTerrainTextureDefinition(id) {
    if (!this._data || !this._data.texture_data) {
      return void 0;
    }
    return this._data.texture_data[id];
  }
  static async getTerrainTextureCatalog(project) {
    const terrainTextureItems = project.getItemsByType(import_IProjectItemData.ProjectItemType.terrainTextureCatalogResourceJson);
    if (terrainTextureItems && terrainTextureItems.length > 0) {
      if (!terrainTextureItems[0].isContentLoaded) {
        await terrainTextureItems[0].loadContent();
      }
      if (terrainTextureItems[0].primaryFile) {
        return await TerrainTextureCatalogDefinition.ensureOnFile(terrainTextureItems[0].primaryFile);
      }
    }
    return void 0;
  }
  static async ensureTerrainTextureCatalog(project) {
    let terrainTextureCatalog = await this.getTerrainTextureCatalog(project);
    if (terrainTextureCatalog) {
      return terrainTextureCatalog;
    }
    const defaultRp = await project.getDefaultResourcePackFolder();
    if (defaultRp) {
      const terrainTextureCatalogFile = await defaultRp.ensureFileFromRelativePath("/textures/terrain_texture.json");
      terrainTextureCatalogFile.setContent(
        JSON.stringify({
          texture_name: "atlas.terrain",
          padding: 8,
          num_mip_levels: 4,
          texture_data: {}
        })
      );
      project.ensureItemFromFile(
        terrainTextureCatalogFile,
        import_IProjectItemData.ProjectItemType.terrainTextureCatalogResourceJson,
        import_Project.FolderContext.resourcePack
      );
      return await TerrainTextureCatalogDefinition.ensureOnFile(terrainTextureCatalogFile);
    }
    return void 0;
  }
  static async ensureOnFile(file, loadHandler) {
    let et;
    if (file.manager === void 0) {
      et = new TerrainTextureCatalogDefinition();
      et.file = file;
      file.manager = et;
    }
    if (file.manager !== void 0 && file.manager instanceof TerrainTextureCatalogDefinition) {
      et = file.manager;
      if (!et.isLoaded) {
        if (loadHandler) {
          et.onLoaded.subscribe(loadHandler);
        }
        await et.load();
      }
    }
    return et;
  }
  getAllTexturePaths(textureId) {
    if (!this.data || !this.data.texture_data) {
      return void 0;
    }
    const elt = this.data.texture_data[textureId];
    if (!elt) {
      return void 0;
    }
    if (typeof elt.textures === "string") {
      return [elt.textures];
    } else if (Array.isArray(elt.textures) && elt.textures.length > 0) {
      const texturePaths = [];
      for (const tex of elt.textures) {
        if (typeof tex === "string") {
          texturePaths.push(tex);
        } else if (tex.path) {
          texturePaths.push(tex.path);
        }
      }
      return texturePaths;
    }
    return void 0;
  }
  getTexture(textureId) {
    if (!this.data || !this.data.texture_data) {
      return void 0;
    }
    return this.data.texture_data[textureId];
  }
  setTexture(textureId, textureData) {
    if (!this.data || !this.data.texture_data) {
      return;
    }
    this.data.texture_data[textureId] = textureData;
    this.persist();
  }
  getDefaultTexturePath(textureId) {
    if (!this.data || !this.data.texture_data) {
      return void 0;
    }
    const elt = this.data.texture_data[textureId];
    if (!elt) {
      return void 0;
    }
    if (typeof elt.textures === "string") {
      return elt.textures;
    } else if (Array.isArray(elt.textures) && elt.textures.length > 0) {
      if (typeof elt.textures[0] === "string") {
        return elt.textures[0];
      } else if (elt.textures[0].path) {
        return elt.textures[0].path;
      }
    }
    return void 0;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    if (!this._data) {
      import_Log.default.unexpectedUndefined("TTCDF");
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._data);
  }
  getTextureReferences() {
    const textureRefs = [];
    if (this.data?.texture_data) {
      for (const resourceId in this.data.texture_data) {
        const resource = this.data.texture_data[resourceId];
        if (resource && resource.textures) {
          if (!textureRefs.includes(resourceId)) {
            textureRefs.push(resourceId);
          }
        }
      }
    }
    return textureRefs;
  }
  getPackRootFolder() {
    let packRootFolder = void 0;
    if (this.file && this.file.parentFolder) {
      let parentFolder = this.file.parentFolder;
      packRootFolder = import_StorageUtilities.default.getParentOfParentFolderNamed("textures", parentFolder);
    }
    return packRootFolder;
  }
  async addChildItems(project, item) {
    const itemsCopy = project.getItemsCopy();
    let packRootFolder = this.getPackRootFolder();
    let texturePathList = this.getTexturePathList();
    for (const candItem of itemsCopy) {
      if (candItem.itemType === import_IProjectItemData.ProjectItemType.texture && packRootFolder && texturePathList) {
        if (!candItem.isContentLoaded) {
          await candItem.loadContent();
        }
        if (candItem.primaryFile) {
          let relativePath = import_StorageUtilities.default.getBaseRelativePath(candItem.primaryFile, packRootFolder);
          if (relativePath) {
            const relativePathLower = relativePath.toLowerCase();
            if (texturePathList && texturePathList.includes(relativePathLower)) {
              item.addChildItem(candItem);
              texturePathList = import_Utilities.default.removeItemInArray(relativePathLower, texturePathList);
            }
          }
        }
      }
    }
    if (texturePathList) {
      for (const texturePath of texturePathList) {
        item.addUnfulfilledRelationship(
          texturePath,
          import_IProjectItemData.ProjectItemType.texture,
          await import_Database.default.isVanillaToken(texturePath)
        );
      }
    }
  }
  /**
   * Loads the definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("TTCDF");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._loadedWithComments = preserveComments;
      this._onLoaded.dispatch(this, this);
      return;
    }
    let data = {};
    let result = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      data = result;
    }
    this._data = data;
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
    this._onLoaded.dispatch(this, this);
  }
}
//# sourceMappingURL=TerrainTextureCatalogDefinition.js.map
