"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var BlockLocation_exports = {};
__export(BlockLocation_exports, {
  default: () => BlockLocation
});
module.exports = __toCommonJS(BlockLocation_exports);
var import_Log = __toESM(require("../core/Log"));
var import_Location = __toESM(require("./Location"));
class BlockLocation {
  _x;
  _y;
  _z;
  get x() {
    return this._x;
  }
  set x(newX) {
    import_Log.default.assertIsInt(newX, "BLX");
    this._x = newX;
  }
  get y() {
    return this._y;
  }
  set y(newY) {
    import_Log.default.assertIsInt(newY, "BLY");
    this._y = newY;
  }
  get z() {
    return this._z;
  }
  set z(newZ) {
    import_Log.default.assertIsInt(newZ, "BLZ");
    this._z = newZ;
  }
  get title() {
    return this.x + "x" + this.y + "y" + this.z + "z";
  }
  toLocation() {
    return new import_Location.default(this.x, this.y, this.z);
  }
  toArray() {
    return [this.x, this.y, this.z];
  }
  toSummary() {
    return "(" + this.x + "," + this.y + "," + this.z + ");";
  }
  constructor(x, y, z) {
    this._x = x === void 0 ? 0 : x;
    this._y = y === void 0 ? 0 : y;
    this._z = z === void 0 ? 0 : z;
    import_Log.default.assertIsInt(this._x, "BLCX");
    import_Log.default.assertIsInt(this._y, "BLCY");
    import_Log.default.assertIsInt(this._z, "BLCZ");
  }
  static from(value) {
    if (value instanceof BlockLocation) {
      return value;
    }
    if (value instanceof import_Location.default) {
      return value.toRoundedBlockLocation();
    }
    if (value.length && value.length === 3) {
      return new BlockLocation(value[0], value[1], value[2]);
    }
    if (!value) {
      return new BlockLocation(0, 0, 0);
    }
    if (value.x || value.y || value.z) {
      return new BlockLocation(value.x, value.y, value.z);
    }
    return new BlockLocation(0, 0, 0);
  }
}
//# sourceMappingURL=BlockLocation.js.map
