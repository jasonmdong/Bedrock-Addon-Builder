"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ManagedPermutation_exports = {};
__export(ManagedPermutation_exports, {
  default: () => ManagedPermutation
});
module.exports = __toCommonJS(ManagedPermutation_exports);
var import_ste_events = require("ste-events");
var import_ManagedComponent = require("./ManagedComponent");
var import_Molang = __toESM(require("./Molang"));
var import_Utilities = __toESM(require("../core/Utilities"));
class ManagedPermutation {
  _data;
  _managed;
  _conditionMolang;
  _onComponentAdded = new import_ste_events.EventDispatcher();
  _onComponentRemoved = new import_ste_events.EventDispatcher();
  _onComponentChanged = new import_ste_events.EventDispatcher();
  constructor(data) {
    this._data = data;
    this._managed = {};
  }
  id = "";
  get onComponentAdded() {
    return this._onComponentAdded.asEvent();
  }
  get onComponentRemoved() {
    return this._onComponentRemoved.asEvent();
  }
  get onComponentChanged() {
    return this._onComponentChanged.asEvent();
  }
  get condition() {
    return this._data?.condition;
  }
  set condition(value) {
    if (value) {
      if (!this._data) {
        this._data = {
          condition: "",
          components: {}
        };
      }
      this._data.condition = value;
    }
  }
  get conditionExpression() {
    if (this._conditionMolang) {
      return this._conditionMolang;
    }
    const cond = this.condition;
    if (!cond) {
      return void 0;
    }
    this._conditionMolang = new import_Molang.default(cond);
    return this._conditionMolang;
  }
  addComponent(id, componentOrData) {
    if (!this._data) {
      this._data = {
        condition: "",
        components: {}
      };
    }
    if (!this._managed) {
      this._managed = {};
    }
    const mc = componentOrData instanceof import_ManagedComponent.ManagedComponent ? componentOrData : new import_ManagedComponent.ManagedComponent(this._data.components, id, componentOrData);
    if (import_Utilities.default.isUsableAsObjectKey(mc.id)) {
      this._data.components[mc.id] = mc.getData();
      this._managed[mc.id] = mc;
    }
    return mc;
  }
  removeComponent(id) {
    if (!this._data) {
      return;
    }
    if (!import_Utilities.default.isUsableAsObjectKey(id)) {
      throw new Error();
    }
    this._data.components[id] = void 0;
    if (this._managed) {
      this._managed[id] = void 0;
    }
  }
  getComponent(id) {
    if (!this._data) {
      return void 0;
    }
    if (!this._managed) {
      this._managed = {};
    }
    if (!import_Utilities.default.isUsableAsObjectKey(id)) {
      throw new Error();
    }
    if (!this._managed[id]) {
      const comps = this._data.components;
      if (!comps) {
        return void 0;
      }
      const data = comps[id];
      if (data) {
        this._managed[id] = new import_ManagedComponent.ManagedComponent(this._data.components, id, data);
      }
    }
    return this._managed[id];
  }
  getAllComponents() {
    return this.getComponents();
  }
  getComponents() {
    if (!this._data) {
      return [];
    }
    if (!this._managed) {
      this._managed = {};
    }
    const comparr = [];
    for (const c in this._data) {
      const comp = this._data.components[c];
      if (!this._managed[c] && comp) {
        this._managed[c] = new import_ManagedComponent.ManagedComponent(this._data.components, c, comp);
      }
      const mc = this._managed[c];
      if (mc) {
        comparr.push(mc);
      }
    }
    return comparr;
  }
  notifyComponentUpdated(id) {
    if (!this._managed) {
      return;
    }
    const comp = this._managed[id];
    if (comp) {
      this._onComponentChanged.dispatch(this, comp);
    }
  }
}
//# sourceMappingURL=ManagedPermutation.js.map
