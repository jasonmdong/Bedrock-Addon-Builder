"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TypeScriptDefinition_exports = {};
__export(TypeScriptDefinition_exports, {
  default: () => TypeScriptDefinition
});
module.exports = __toCommonJS(TypeScriptDefinition_exports);
var import_Log = __toESM(require("../core/Log"));
var import_ste_events = require("ste-events");
class TypeScriptDefinition {
  _data;
  _file;
  _isLoaded = false;
  _onLoaded = new import_ste_events.EventDispatcher();
  id;
  get data() {
    if (!this._file || !this._file.content || typeof this._file.content != "string") {
      return void 0;
    }
    return this._file.content;
  }
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  static async ensureOnFile(file, loadHandler) {
    let tsd;
    if (file.manager === void 0) {
      tsd = new TypeScriptDefinition();
      tsd.file = file;
      file.manager = tsd;
    }
    if (file.manager !== void 0 && file.manager instanceof TypeScriptDefinition) {
      tsd = file.manager;
      if (!tsd.isLoaded) {
        if (loadHandler) {
          tsd.onLoaded.subscribe(loadHandler);
        }
        await tsd.load();
      }
    }
    return tsd;
  }
  persist() {
    return false;
  }
  async load() {
    if (this._isLoaded) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("TSCDF");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._onLoaded.dispatch(this, this);
      return;
    }
    this._isLoaded = true;
    this._onLoaded.dispatch(this, this);
  }
}
//# sourceMappingURL=TypeScriptDefinition.js.map
