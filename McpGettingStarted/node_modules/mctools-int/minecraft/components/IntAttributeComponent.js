"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var IntAttributeComponent_exports = {};
__export(IntAttributeComponent_exports, {
  default: () => AttributeComponent
});
module.exports = __toCommonJS(IntAttributeComponent_exports);
class AttributeComponent {
  id;
  base;
  current;
  minimum;
  maximum;
  defaultMinimum;
  defaultMaximum;
  loadFromNbtTag(tag) {
    const minTag = tag.find("Min");
    if (minTag !== null) {
      this.minimum = minTag.valueAsInt;
    }
    let maxTag = tag.find("Max");
    if (maxTag !== null) {
      this.maximum = maxTag.valueAsInt;
    }
    const dminTag = tag.find("DefaultMin");
    if (dminTag !== null) {
      this.defaultMinimum = dminTag.valueAsInt;
    }
    const dmaxTag = tag.find("DefaultMax");
    if (dmaxTag !== null) {
      this.defaultMaximum = dmaxTag.valueAsInt;
    }
    const baseTag = tag.find("Base");
    if (baseTag !== null) {
      this.base = baseTag.valueAsInt;
    }
    const currentTag = tag.find("Current");
    if (currentTag !== null) {
      this.current = currentTag.valueAsInt;
    }
  }
}
//# sourceMappingURL=IntAttributeComponent.js.map
