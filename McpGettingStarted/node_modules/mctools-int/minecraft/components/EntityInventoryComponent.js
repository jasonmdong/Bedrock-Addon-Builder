"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var EntityInventoryComponent_exports = {};
__export(EntityInventoryComponent_exports, {
  default: () => EntityInventoryComponent
});
module.exports = __toCommonJS(EntityInventoryComponent_exports);
var import_InventorySlot = __toESM(require("../InventorySlot"));
var import_Log = __toESM(require("../../core/Log"));
var import_ComponentBase = __toESM(require("../ComponentBase"));
class EntityInventoryComponent extends import_ComponentBase.default {
  slots;
  constructor() {
    super();
    this.slots = {};
  }
  ensureSlot(slotIndex) {
    let slot = this.slots[slotIndex];
    if (slot === void 0) {
      slot = new import_InventorySlot.default(slotIndex);
      this.slots[slotIndex] = slot;
    }
    return slot;
  }
  loadFromNbtTag(tag) {
    const childSlotTags = tag.getTagChildren();
    for (let k = 0; k < childSlotTags.length; k++) {
      const childSlotTag = childSlotTags[k];
      const slotIndexTag = childSlotTag.find("Slot");
      const nameTag = childSlotTag.find("Name");
      const countTag = childSlotTag.find("Count");
      const wasPickedUpTag = childSlotTag.find("WasPickedUp");
      const damageTag = childSlotTag.find("Damage");
      if (nameTag !== null && nameTag.value !== null && countTag !== null && countTag !== null && damageTag !== null && damageTag.value !== null && wasPickedUpTag !== null && wasPickedUpTag.value !== null) {
        let slotIndex = k;
        if (slotIndexTag !== null) {
          slotIndex = slotIndexTag.valueAsInt;
        }
        const slot = this.ensureSlot(slotIndex);
        slot.name = nameTag.valueAsString;
        slot.count = countTag.valueAsInt;
        slot.wasPickedUp = wasPickedUpTag.valueAsBoolean;
        slot.damage = damageTag.valueAsInt;
      } else {
        import_Log.default.fail("Unexpected inventory item in structure.");
      }
    }
  }
}
//# sourceMappingURL=EntityInventoryComponent.js.map
