"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var EntityTypeComponentSetUtilities_exports = {};
__export(EntityTypeComponentSetUtilities_exports, {
  default: () => EntityTypeComponentSetUtilities
});
module.exports = __toCommonJS(EntityTypeComponentSetUtilities_exports);
var import_EntityTypeDefinition = __toESM(require("./EntityTypeDefinition"));
var import_Log = __toESM(require("../core/Log"));
var import_Database = __toESM(require("./Database"));
var import_IField = require("../dataform/IField");
var import_ManagedComponent = require("./ManagedComponent");
class EntityTypeComponentSetUtilities {
  static async getTriggers(componentSet, isBaseComponent) {
    const allTriggers = [];
    const comps = componentSet.getComponents();
    for (const comp of comps) {
      let formName = comp.id;
      if (formName.startsWith("minecraft:") && (isBaseComponent || formName !== "minecraft:breedable")) {
        formName = import_EntityTypeDefinition.default.getFormIdFromComponentId(formName);
        const form = await import_Database.default.ensureFormLoaded("entity", formName);
        import_Log.default.assert(form !== void 0);
        if (form) {
          await EntityTypeComponentSetUtilities.processFormForTriggers(allTriggers, form, comp, comp.id);
        }
      }
    }
    return allTriggers;
  }
  static async processFormForTriggers(triggers, form, component, propertyPath) {
    for (const field of form.fields) {
      if ((field.dataType === import_IField.FieldDataType.object || field.dataType === import_IField.FieldDataType.objectArray) && field.subForm) {
        let obj = void 0;
        if (component) {
          obj = component.getProperty(field.id);
        }
        const data = component?.getData();
        if (data && typeof data === "object") {
          if (field.dataType === import_IField.FieldDataType.objectArray && Array.isArray(obj)) {
            let index = 0;
            for (const arrObj of obj) {
              await this.processFormForTriggers(
                triggers,
                field.subForm,
                new import_ManagedComponent.ManagedComponent(data, field.id, arrObj),
                propertyPath + "." + field.id + "." + index
              );
              index++;
            }
          } else {
            await this.processFormForTriggers(
              triggers,
              field.subForm,
              new import_ManagedComponent.ManagedComponent(data, field.id, obj),
              propertyPath + "." + field.id
            );
          }
        }
      } else if (field.dataType === import_IField.FieldDataType.minecraftEventTrigger || field.dataType === import_IField.FieldDataType.minecraftEventReference) {
        let ref = void 0;
        if (component) {
          ref = component.getProperty(field.id);
        }
        const path = propertyPath + "." + field.id;
        if (!ref) {
          triggers.push({
            path
          });
        } else {
          triggers.push({
            path,
            reference: ref,
            referenceId: ref.event ? ref.event : ref
          });
        }
      }
    }
  }
}
//# sourceMappingURL=EntityTypeComponentSetUtilities.js.map
