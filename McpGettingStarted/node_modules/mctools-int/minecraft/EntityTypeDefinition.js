"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var EntityTypeDefinition_exports = {};
__export(EntityTypeDefinition_exports, {
  AttributeComponents: () => AttributeComponents,
  EntityPropertyType: () => EntityPropertyType,
  EntityTypeComponentCategory: () => EntityTypeComponentCategory,
  EntityTypeComponentExtendedCategory: () => EntityTypeComponentExtendedCategory,
  default: () => EntityTypeDefinition
});
module.exports = __toCommonJS(EntityTypeDefinition_exports);
var import_Log = __toESM(require("../core/Log"));
var import_ste_events = require("ste-events");
var import_ScriptGen = __toESM(require("../script/ScriptGen"));
var import_ManagedComponentGroup = __toESM(require("./ManagedComponentGroup"));
var import_ManagedComponent = require("./ManagedComponent");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_Database = __toESM(require("./Database"));
var import_MinecraftUtilities = __toESM(require("./MinecraftUtilities"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_EntityTypeResourceDefinition = __toESM(require("./EntityTypeResourceDefinition"));
var import_SpawnRulesBehaviorDefinition = __toESM(require("./SpawnRulesBehaviorDefinition"));
var import_Utilities = __toESM(require("../core/Utilities"));
var EntityTypeComponentCategory = /* @__PURE__ */ ((EntityTypeComponentCategory2) => {
  EntityTypeComponentCategory2[EntityTypeComponentCategory2["attribute"] = 0] = "attribute";
  EntityTypeComponentCategory2[EntityTypeComponentCategory2["complex"] = 1] = "complex";
  EntityTypeComponentCategory2[EntityTypeComponentCategory2["behavior"] = 2] = "behavior";
  EntityTypeComponentCategory2[EntityTypeComponentCategory2["trigger"] = 3] = "trigger";
  return EntityTypeComponentCategory2;
})(EntityTypeComponentCategory || {});
var EntityTypeComponentExtendedCategory = /* @__PURE__ */ ((EntityTypeComponentExtendedCategory2) => {
  EntityTypeComponentExtendedCategory2[EntityTypeComponentExtendedCategory2["attribute"] = 0] = "attribute";
  EntityTypeComponentExtendedCategory2[EntityTypeComponentExtendedCategory2["complex"] = 1] = "complex";
  EntityTypeComponentExtendedCategory2[EntityTypeComponentExtendedCategory2["movementComplex"] = 2] = "movementComplex";
  EntityTypeComponentExtendedCategory2[EntityTypeComponentExtendedCategory2["combatAndHealthComplex"] = 3] = "combatAndHealthComplex";
  EntityTypeComponentExtendedCategory2[EntityTypeComponentExtendedCategory2["sensorComponents"] = 4] = "sensorComponents";
  EntityTypeComponentExtendedCategory2[EntityTypeComponentExtendedCategory2["trigger"] = 5] = "trigger";
  EntityTypeComponentExtendedCategory2[EntityTypeComponentExtendedCategory2["behavior"] = 6] = "behavior";
  EntityTypeComponentExtendedCategory2[EntityTypeComponentExtendedCategory2["movementBehavior"] = 7] = "movementBehavior";
  EntityTypeComponentExtendedCategory2[EntityTypeComponentExtendedCategory2["mobSpecificBehavior"] = 8] = "mobSpecificBehavior";
  return EntityTypeComponentExtendedCategory2;
})(EntityTypeComponentExtendedCategory || {});
const AttributeComponents = {
  "minecraft:body_rotation_blocked": "b",
  "minecraft:can_climb": "b",
  "minecraft:can_fly": "b",
  "minecraft:can_power_jump": "b",
  "minecraft:cannot_be_attacked": "b",
  "minecraft:color": "s",
  "minecraft:color2": "s",
  "minecraft:default_look_angle": "f",
  "minecraft:fire_immune": "b",
  "minecraft:floats_in_liquid": "b",
  "minecraft:flying_speed": "f",
  "minecraft:friction_modifier": "f",
  "minecraft:ground_offset": "f",
  "minecraft:ignore_cannot_be_attacked": "b",
  "minecraft:input_ground_controlled": "b",
  "minecraft:is_baby": "b",
  "minecraft:is_charged": "b",
  "minecraft:is_chested": "b",
  "minecraft:is_dyeable": "b",
  "minecraft:is_hidden_when_invisible": "b",
  "minecraft:is_ignited": "b",
  "minecraft:is_illager_captain": "b",
  "minecraft:is_pregnant": "b",
  "minecraft:is_saddled": "b",
  "minecraft:is_sheared": "b",
  "minecraft:is_stackable": "b",
  "minecraft:is_stunned": "b",
  "minecraft:is_tamed": "b",
  "minecraft:mark_variant": "i",
  "minecraft:movement_sound_distance_offset": "f",
  "minecraft:push_through": "f",
  "minecraft:renders_when_invisible": "b",
  "minecraft:scale": "f",
  "minecraft:skin_id": "i",
  "minecraft:sound_volume": "f",
  "minecraft:type_family": "family",
  "minecraft:variant": "i",
  "minecraft:walk_animation_speed": "f",
  "minecraft:wants_jockey": "b"
};
var EntityPropertyType = /* @__PURE__ */ ((EntityPropertyType2) => {
  EntityPropertyType2[EntityPropertyType2["enum"] = 0] = "enum";
  EntityPropertyType2[EntityPropertyType2["boolean"] = 1] = "boolean";
  EntityPropertyType2[EntityPropertyType2["float"] = 2] = "float";
  EntityPropertyType2[EntityPropertyType2["int"] = 3] = "int";
  return EntityPropertyType2;
})(EntityPropertyType || {});
class EntityTypeDefinition {
  _wrapper;
  _file;
  _id;
  _isLoaded = false;
  _loadedWithComments = false;
  _managedComponents = {};
  _data;
  _componentGroups = {};
  _events = {};
  _onLoaded = new import_ste_events.EventDispatcher();
  _onComponentAdded = new import_ste_events.EventDispatcher();
  _onComponentRemoved = new import_ste_events.EventDispatcher();
  _onComponentChanged = new import_ste_events.EventDispatcher();
  get data() {
    return this._data;
  }
  get componentGroups() {
    return this._componentGroups;
  }
  static getFormIdFromComponentId(componentId) {
    return componentId.replace(/:/gi, "_").replace(/\./gi, "_");
  }
  static getComponentCategory(id) {
    if (id.startsWith("minecraft:behavior")) {
      return 2 /* behavior */;
    } else if (id.startsWith("minecraft:on_")) {
      return 3 /* trigger */;
    } else if (AttributeComponents[id] !== void 0) {
      return 0 /* attribute */;
    }
    return 1 /* complex */;
  }
  static getExtendedComponentCategory(id) {
    if (id.startsWith("minecraft:behavior.move") || id.startsWith("minecraft:behavior.jump") || id.startsWith("minecraft:behavior.go") || id.startsWith("minecraft:behavior.follow") || id.startsWith("minecraft:behavior.circle")) {
      return 7 /* movementBehavior */;
    } else if (id.startsWith("minecraft:behavior.dragon") || id.startsWith("minecraft:behavior.enderman") || id.startsWith("minecraft:behavior.guardian") || id.startsWith("minecraft:behavior.ocelot") || id.startsWith("minecraft:behavior.silverfish") || id.startsWith("minecraft:behavior.skeleton") || id.startsWith("minecraft:behavior.slime") || id.startsWith("minecraft:behavior.squid") || id.startsWith("minecraft:behavior.vex") || id.startsWith("minecraft:behavior.wither")) {
      return 8 /* mobSpecificBehavior */;
    } else if (id.startsWith("minecraft:behavior")) {
      return 6 /* behavior */;
    } else if (id.startsWith("minecraft:on_")) {
      return 5 /* trigger */;
    } else if (AttributeComponents[id] !== void 0) {
      return 0 /* attribute */;
    } else if (id.indexOf("sensor") >= 0) {
      return 4 /* sensorComponents */;
    } else if (id.indexOf("jump") >= 0 || id.indexOf("climb") >= 0 || id.indexOf("move") >= 0 || id.startsWith("minecraft:navigation") || id.startsWith("minecraft:flying") || id.startsWith("minecraft:friction") || id.startsWith("minecraft:walk_animation")) {
      return 2 /* movementComplex */;
    } else if (id.indexOf("attack") >= 0 || id.indexOf("combat") >= 0 || id.indexOf("damage") >= 0 || id.startsWith("minecraft:health") || id.startsWith("minecraft:healable") || id.startsWith("minecraft:hurt_on")) {
      return 3 /* combatAndHealthComplex */;
    }
    return 1 /* complex */;
  }
  get formatVersion() {
    return this._wrapper?.format_version;
  }
  static getComponentFromBaseFileName(name) {
    let canonName = name;
    if (canonName.startsWith("minecraft_")) {
      canonName = canonName.substring(10);
      if (canonName.startsWith("behavior_")) {
        canonName = "behavior." + canonName.substring(9);
      }
      if (canonName.startsWith("movement_")) {
        canonName = "movement." + canonName.substring(9);
      }
      if (canonName.startsWith("navigation_")) {
        canonName = "navigation." + canonName.substring(11);
      }
      if (canonName.startsWith("player_")) {
        canonName = "player." + canonName.substring(7);
      }
      if (canonName.startsWith("jump_")) {
        canonName = "jump." + canonName.substring(5);
      }
      if (canonName.startsWith("horse_")) {
        canonName = "horse." + canonName.substring(6);
      }
      if (canonName.startsWith("annotation_")) {
        canonName = "annotation." + canonName.substring(11);
      }
    }
    return canonName;
  }
  static getComponentCategoryDescription(category) {
    switch (category) {
      case 2 /* behavior */:
        return "Behavior";
      case 0 /* attribute */:
        return "Attribute";
      case 3 /* trigger */:
        return "Trigger";
      default:
        return "Component";
    }
  }
  static getPluralComponentCategoryDescription(category) {
    switch (category) {
      case 2 /* behavior */:
        return "Behaviors (AI)";
      case 0 /* attribute */:
        return "Attributes";
      case 3 /* trigger */:
        return "Triggers";
      default:
        return "Components";
    }
  }
  get onComponentAdded() {
    return this._onComponentAdded.asEvent();
  }
  get onComponentRemoved() {
    return this._onComponentRemoved.asEvent();
  }
  get onComponentChanged() {
    return this._onComponentChanged.asEvent();
  }
  get isLoaded() {
    return this._isLoaded;
  }
  get behaviorPackFile() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set behaviorPackFile(newFile) {
    if (this._file) {
      this._file.onFileContentUpdated.unsubscribe(this._handleFileUpdated);
    }
    this._file = newFile;
    if (this._file) {
      this._file.onFileContentUpdated.subscribe(this._handleFileUpdated);
    }
  }
  _handleFileUpdated(file, fileB) {
    this._data = void 0;
    this._isLoaded = false;
    this._wrapper = void 0;
    this._managedComponents = {};
  }
  get id() {
    if (this._id === void 0) {
      return "";
    }
    return this._id;
  }
  set id(newId) {
    this._id = newId;
  }
  get runtimeIdentifier() {
    if (this._data && this._data.description) {
      return this._data.description.runtime_identifier;
    }
    return void 0;
  }
  set runtimeIdentifier(newId) {
    if (this._data && this._data.description) {
      this._data.description.runtime_identifier = newId;
    }
  }
  get aliases() {
    if (!this._data || !this._data.description) {
      return void 0;
    }
    return this._data.description.aliases;
  }
  get properties() {
    if (!this._data || !this._data.description) {
      return void 0;
    }
    return this._data.description.properties;
  }
  constructor() {
    this._handleFileUpdated = this._handleFileUpdated.bind(this);
  }
  async getFormatVersionIsCurrent() {
    const fv = this.getFormatVersionAsNumberArray();
    if (fv === void 0 || fv.length !== 3) {
      return false;
    }
    return await import_Database.default.isRecentVersionFromVersionArray(fv);
  }
  getFormatVersionAsNumberArray() {
    if (!this._wrapper) {
      return void 0;
    }
    return import_MinecraftUtilities.default.getVersionArrayFrom(this._wrapper.format_version);
  }
  removeProperty(propertyName) {
    if (this._data?.description?.properties) {
      this._data.description.properties[propertyName] = void 0;
    }
  }
  get shortId() {
    if (this._id !== void 0) {
      let val = this._id;
      if (val.startsWith("minecraft:")) {
        return val.substring(10, this._id.length);
      }
      const firstColon = val.indexOf(":");
      if (firstColon >= 0) {
        val = val.substring(firstColon + 1);
      }
      return val;
    }
    return void 0;
  }
  getComponent(id) {
    if (this._data === void 0 || this._data.components === void 0) {
      return void 0;
    }
    if (!import_Utilities.default.isUsableAsObjectKey(id)) {
      import_Log.default.unsupportedToken(id);
      throw new Error();
    }
    if (!this._managedComponents[id]) {
      const comp = this._data.components[id];
      if (comp) {
        this._managedComponents[id] = new import_ManagedComponent.ManagedComponent(this._data.components, id, comp);
      }
    }
    return this._managedComponents[id];
  }
  getComponentsInBaseAndGroups(id) {
    if (this._data === void 0) {
      return [];
    }
    let results = [];
    let comp = this.getComponent(id);
    if (comp) {
      results.push(comp);
    }
    for (const componentGroupName in this._componentGroups) {
      const group = this._componentGroups[componentGroupName];
      if (group) {
        comp = group.getComponent(id);
        if (comp) {
          results.push(comp);
        }
      }
    }
    return results;
  }
  getCoreAndComponentGroupList() {
    const componentSets = [this];
    const cgs = this.getComponentGroups();
    componentSets.push(...cgs);
    return componentSets;
  }
  getAllComponents() {
    if (this._data === void 0) {
      return [];
    }
    let results = this.getComponents();
    for (const componentGroupName in this._componentGroups) {
      const group = this._componentGroups[componentGroupName];
      if (group) {
        for (const comp of group.getComponents()) {
          if (comp) {
            results.push(comp);
          }
        }
      }
    }
    return results;
  }
  get behaviorPackFormatVersion() {
    if (!this._wrapper || !this._wrapper.format_version) {
      return void 0;
    }
    return this._wrapper.format_version;
  }
  setBehaviorPackFormatVersion(versionStr) {
    this._ensureBehaviorPackDataInitialized();
    if (this._wrapper) {
      this._wrapper.format_version = versionStr;
    }
  }
  notifyComponentUpdated(id) {
    const component = this.getComponent(id);
    if (component === void 0) {
      import_Log.default.unexpectedUndefined("ETNCU");
    } else {
      this._onComponentChanged.dispatch(this, component);
    }
  }
  getComponents() {
    const componentSet = [];
    if (this._data !== void 0) {
      for (const componentName in this._data.components) {
        const component = this.getComponent(componentName);
        if (component !== void 0) {
          componentSet.push(component);
        }
      }
    }
    return componentSet;
  }
  getComponentGroupsComponentUsedIn(componentName) {
    const componentGroups = this.getComponentGroups();
    const cgsUsedIn = [];
    for (const cg of componentGroups) {
      if (cg && cg.getComponent(componentName)) {
        cgsUsedIn.push(cg);
      }
    }
    return cgsUsedIn;
  }
  getComponentGroup(componentGroupName) {
    if (this._data && this._data.component_groups) {
      if (!this._componentGroups[componentGroupName] && this._data.component_groups[componentGroupName]) {
        const componentGroupData = this._data.component_groups[componentGroupName];
        const cg = new import_ManagedComponentGroup.default(componentGroupData, componentGroupName);
        this._componentGroups[componentGroupName] = cg;
      }
      return this._componentGroups[componentGroupName];
    }
    return void 0;
  }
  getComponentGroups() {
    const componentGroups = [];
    if (this._data !== void 0) {
      for (const componentGroupName in this._data.component_groups) {
        if (!this._componentGroups[componentGroupName]) {
          const componentGroupData = this._data.component_groups[componentGroupName];
          const cg = new import_ManagedComponentGroup.default(componentGroupData, componentGroupName);
          this._componentGroups[componentGroupName] = cg;
          componentGroups.push(cg);
        } else {
          componentGroups.push(this._componentGroups[componentGroupName]);
        }
      }
    }
    return componentGroups;
  }
  getEvent(eventName) {
    try {
      if (this._data !== void 0 && import_Utilities.default.isUsableAsObjectKey(eventName)) {
        if (!this._events[eventName]) {
          const eventData = this._data.events[eventName];
          this._events[eventName] = eventData;
        }
        return this._events[eventName];
      }
    } catch (e) {
      import_Log.default.verbose("Error getting event " + eventName + ": " + e);
    }
    return void 0;
  }
  getEffectiveComponents(componentGroupAddRemoveList) {
    const componentState = JSON.parse(JSON.stringify(this._data?.components));
    for (let componentGroupId of componentGroupAddRemoveList) {
      let isAdd = true;
      if (componentGroupId.startsWith("-")) {
        componentGroupId = componentGroupId.substring(1);
        isAdd = false;
      } else if (componentGroupId.startsWith("+")) {
        componentGroupId = componentGroupId.substring(1);
      }
      const componentGroup = this.getComponentGroup(componentGroupId);
      if (componentGroup) {
        const cgComps = componentGroup.getComponents();
        if (isAdd) {
          for (const cgComp of cgComps) {
            componentState[cgComp.id] = cgComp.getData();
          }
        } else if (!isAdd) {
          for (const cgComp of cgComps) {
            componentState[cgComp.id] = void 0;
          }
        }
      }
    }
    return new import_ManagedComponentGroup.default(componentState, this.id);
  }
  getEvents() {
    const events = [];
    if (this._data !== void 0) {
      for (const eventName in this._data.events) {
        if (!this._events[eventName] && import_Utilities.default.isUsableAsObjectKey(eventName)) {
          const eventData = this._data.events[eventName];
          this._events[eventName] = eventData;
          events.push({ id: eventName, event: eventData });
        } else {
          events.push({ id: eventName, event: this._events[eventName] });
        }
      }
    }
    return events;
  }
  addComponent(id, componentOrData) {
    this._ensureBehaviorPackDataInitialized();
    const bpData = this._data;
    const mc = componentOrData instanceof import_ManagedComponent.ManagedComponent ? componentOrData : new import_ManagedComponent.ManagedComponent(bpData.components, id, componentOrData);
    bpData.components[id] = mc.getData();
    this._managedComponents[id] = mc;
    this._onComponentAdded.dispatch(this, mc);
    return mc;
  }
  addComponentGroup(id, componentOrData) {
    this._ensureBehaviorPackDataInitialized();
    if (id === void 0) {
      id = "group";
      let increment = 0;
      let cg = this.getComponentGroup(id);
      while (cg !== void 0 && increment < 100) {
        increment++;
        id = "group" + increment;
        cg = this.getComponentGroup(id);
      }
    }
    if (componentOrData === void 0) {
      componentOrData = {};
    }
    const bpData = this._data;
    const mcg = componentOrData instanceof import_ManagedComponentGroup.default ? componentOrData : new import_ManagedComponentGroup.default(componentOrData, id);
    const cgData = mcg.getData();
    if (cgData) {
      bpData.component_groups[id] = cgData;
    }
    this._componentGroups[id] = mcg;
    return mcg;
  }
  removeComponent(id) {
    if (this._data === void 0) {
      return;
    }
    const newBehaviorPacks = {};
    const newManagedComponents = {};
    for (const name in this._data.components) {
      if (name !== id && import_Utilities.default.isUsableAsObjectKey(name)) {
        const componentData = this._data.components[name];
        newBehaviorPacks[name] = componentData;
      }
    }
    for (const name in this._managedComponents) {
      if (name !== id && this._managedComponents[name] && import_Utilities.default.isUsableAsObjectKey(name)) {
        newManagedComponents[name] = this._managedComponents[name];
      }
    }
    this._data.components = newBehaviorPacks;
    this._managedComponents = newManagedComponents;
  }
  _ensureBehaviorPackDataInitialized() {
    if (!this._wrapper) {
      this._wrapper = {
        format_version: "1.20.0"
      };
    }
    if (this._data === void 0) {
      this._data = {
        description: {
          identifier: "unknown",
          is_experimental: false,
          is_spawnable: false,
          is_summonable: false
        },
        components: {},
        component_groups: {},
        events: {}
      };
      if (this._wrapper) {
        this._wrapper["minecraft:entity"] = this._data;
      }
    }
  }
  getProperties() {
    if (!this._data || !this._data?.description) {
      return void 0;
    }
    return this._data.description.properties;
  }
  getPropertyList() {
    const props = this.getProperties();
    if (!props) {
      return [];
    }
    const propertyList = [];
    for (const propName in props) {
      if (props[propName] !== void 0) {
        propertyList.push(propName);
      }
    }
    return propertyList;
  }
  static getPropertyTypeString(stateType) {
    switch (stateType) {
      case 1 /* boolean */:
        return "bool";
      case 2 /* float */:
        return "float";
      case 0 /* enum */:
        return "enum";
    }
    return "int";
  }
  addProperty(propertyName, propertyType) {
    if (!this._data || !this._data.description) {
      return;
    }
    let dataArr = void 0;
    let typeStr = EntityTypeDefinition.getPropertyTypeString(propertyType);
    let defaultValue = false;
    if (propertyType === 2 /* float */) {
      dataArr = [0, 1, 2];
      defaultValue = 0;
    } else if (propertyType === 0 /* enum */) {
      dataArr = ["value1", "value2"];
      defaultValue = "value1";
    }
    if (!this._data.description.properties) {
      this._data.description.properties = {};
    }
    this._data.description.properties[propertyName] = {
      type: typeStr,
      values: dataArr,
      default: defaultValue
    };
  }
  addEvent(eventName) {
    if (!this._data) {
      return;
    }
    if (!this._data.events) {
      this._data.events = {};
    }
    if (eventName === void 0) {
      eventName = "eventName";
      let increment = 0;
      let eve = this.getEvent(eventName);
      while (eve !== void 0 && increment < 100) {
        increment++;
        eventName = "eventName" + increment;
        eve = this.getEvent(eventName);
      }
    }
    this._data.events[eventName] = {};
  }
  async addChildItems(project, item) {
    let lootTablePaths = [];
    const comps = this.getComponentsInBaseAndGroups("minecraft:loot");
    for (const comp of comps) {
      let lootTablePath = comp.getProperty("table");
      if (lootTablePath) {
        lootTablePaths.push(lootTablePath);
      }
    }
    const entityResourceItems = project.getItemsByType(import_IProjectItemData.ProjectItemType.entityTypeResource);
    for (const candItem of entityResourceItems) {
      if (!candItem.isContentLoaded) {
        await candItem.loadContent();
      }
      if (candItem.primaryFile) {
        const etrd = await import_EntityTypeResourceDefinition.default.ensureOnFile(candItem.primaryFile);
        if (etrd) {
          const id = etrd.id;
          if (id === this.id) {
            item.addChildItem(candItem);
          }
        }
      }
    }
    const spawnRuleItems = project.getItemsByType(import_IProjectItemData.ProjectItemType.spawnRuleBehavior);
    for (const candItem of spawnRuleItems) {
      if (!candItem.isContentLoaded) {
        await candItem.loadContent();
      }
      if (candItem.primaryFile) {
        const srb = await import_SpawnRulesBehaviorDefinition.default.ensureOnFile(candItem.primaryFile);
        if (srb) {
          const id = srb.id;
          if (id === this.id) {
            item.addChildItem(candItem);
          }
        }
      }
    }
    if (lootTablePaths.length > 0) {
      const lootTableItems = project.getItemsByType(import_IProjectItemData.ProjectItemType.lootTableBehavior);
      for (const candItem of lootTableItems) {
        for (const lootTablePath of lootTablePaths) {
          if (candItem.projectPath?.endsWith(lootTablePath)) {
            item.addChildItem(candItem);
          }
        }
      }
    }
  }
  static async ensureOnFile(behaviorPackFile, loadHandler) {
    let et;
    if (behaviorPackFile.manager === void 0) {
      et = new EntityTypeDefinition();
      et.behaviorPackFile = behaviorPackFile;
      behaviorPackFile.manager = et;
    }
    if (behaviorPackFile.manager !== void 0 && behaviorPackFile.manager instanceof EntityTypeDefinition) {
      et = behaviorPackFile.manager;
      if (!et.isLoaded) {
        if (loadHandler) {
          et.onLoaded.subscribe(loadHandler);
        }
        await et.load();
      }
    }
    return et;
  }
  getScript(isTypeScript) {
    if (this.shortId === void 0) {
      return;
    }
    const className = import_ScriptGen.default.getClassName(this.shortId);
    const results = [];
    results.push("import * as mc from '@minecraft/server';");
    results.push("export default class " + className + "Base");
    results.push("{");
    if (isTypeScript) {
      results.push("  _entity : mc.Entity;");
      results.push("  constructor(entity : mc.Entity) {");
    } else {
      results.push("  _entity;\n");
      results.push("  constructor(entity) {");
    }
    results.push("    this._entity = entity;");
    results.push("  }\n\n");
    if (isTypeScript) {
      results.push("  static spawn(location : mc.Vector3) {");
    } else {
      results.push("  static spawn(location) {");
    }
    results.push('    const entity = world.getDimension("overworld").spawnEntity("' + this.id + '", location);');
    results.push("    const " + import_ScriptGen.default.getInstanceName(this.shortId) + " = new " + className + "(entity);");
    results.push("    return " + import_ScriptGen.default.getInstanceName(this.shortId) + ";");
    results.push("  }\n");
    if (this._data !== void 0) {
      const healthC = this._data.components["minecraft:health"];
      if (healthC !== void 0) {
        results.push("  fullyHeal() {");
        results.push('    this._entity.getComponent("minecraft:health").resetToMaxValue();');
        results.push("  }\n");
        if (isTypeScript) {
          results.push("  setHealth(newValue : number) {");
        } else {
          results.push("  setHealth(newValue) {");
        }
        results.push('    this._entity.getComponent("minecraft:health").setCurrent(newValue);');
        results.push("  }\n");
      }
      const rideableC = this._data.components["minecraft:rideable"];
      if (rideableC !== void 0) {
        if (isTypeScript) {
          results.push("  addRider(newRider : mc.Entity) {");
        } else {
          results.push("  addRider(newRider) {");
        }
        results.push('    this._entity.getComponent("minecraft:rideable").addRider(newRider);');
        results.push("  }\n");
        if (isTypeScript) {
          results.push("  ejectRider(rider : mc.Entity) {");
        } else {
          results.push("  ejectRider(rider) {");
        }
        results.push('    this._entity.getComponent("minecraft:rideable").addRider(rider);');
        results.push("  }\n");
      }
      const tameableC = this._data.components["minecraft:tameable"];
      if (tameableC !== void 0) {
        results.push("  tame() {");
        results.push('    return this._entity.getComponent("minecraft:tameable").tame();');
        results.push("  }\n");
      }
    }
    results.push("}");
    return results.join("\n");
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    import_Log.default.assert(!this._isLoaded || this._wrapper !== null, "ETDP");
    if (!this._wrapper) {
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._wrapper);
  }
  /**
   * Loads the entity type definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("ETBPF");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._onLoaded.dispatch(this, this);
      return;
    }
    let data = {};
    let result = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      data = result;
    }
    this._wrapper = data;
    const entity = data["minecraft:entity"];
    if (entity && entity.description) {
      this.id = entity.description.identifier;
    }
    this._data = entity;
    if (this._data) {
      if (this._data.components) {
        for (const compName in this._data.components) {
          const comp = this._data.components[compName];
          if (comp) {
            this._managedComponents[compName] = new import_ManagedComponent.ManagedComponent(this._data.components, compName, comp);
          }
        }
      }
      if (this._data.component_groups) {
        for (const compGroupName in this._data.component_groups) {
          const compGroup = this._data.component_groups[compGroupName];
          if (compGroup) {
            this._componentGroups[compGroupName] = new import_ManagedComponentGroup.default(compGroup, compGroupName);
          }
        }
      }
    }
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
    this._onLoaded.dispatch(this, this);
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  AttributeComponents,
  EntityPropertyType,
  EntityTypeComponentCategory,
  EntityTypeComponentExtendedCategory
});
//# sourceMappingURL=EntityTypeDefinition.js.map
