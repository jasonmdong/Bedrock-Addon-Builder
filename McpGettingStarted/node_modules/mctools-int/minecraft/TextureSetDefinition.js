"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TextureSetDefinition_exports = {};
__export(TextureSetDefinition_exports, {
  default: () => TextureSetDefinition
});
module.exports = __toCommonJS(TextureSetDefinition_exports);
var import_Log = __toESM(require("../core/Log"));
var import_ste_events = require("ste-events");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_Database = __toESM(require("./Database"));
class TextureSetDefinition {
  _data;
  _file;
  _isLoaded = false;
  _loadedWithComments = false;
  _onLoaded = new import_ste_events.EventDispatcher();
  id;
  get data() {
    return this._data;
  }
  get texturesList() {
    if (!this._data || !this._data["minecraft:texture_set"]) {
      return void 0;
    }
    const textureList = [];
    const textureSet = this._data["minecraft:texture_set"];
    if (textureSet.metalness_emissive_roughness && typeof textureSet.metalness_emissive_roughness === "string") {
      textureList.push(this.adaptTexturePath(textureSet.metalness_emissive_roughness));
    }
    if (textureSet.metalness_emissive_roughness_subsurface && typeof textureSet.metalness_emissive_roughness_subsurface === "string") {
      textureList.push(this.adaptTexturePath(textureSet.metalness_emissive_roughness_subsurface));
    }
    if (textureSet.heightmap && typeof textureSet.heightmap === "string") {
      textureList.push(this.adaptTexturePath(textureSet.heightmap));
    }
    if (textureSet.normal && typeof textureSet.normal === "string") {
      textureList.push(this.adaptTexturePath(textureSet.normal));
    }
    return textureList;
  }
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  adaptTexturePath(path) {
    path = path.toLowerCase();
    if (path.indexOf("/") >= 0) {
      return path;
    }
    if (this._file) {
      return this._file.parentFolder.fullPath + "/" + path;
    }
    return path;
  }
  static async ensureOnFile(file, loadHandler) {
    let tsd;
    if (file.manager === void 0) {
      tsd = new TextureSetDefinition();
      tsd.file = file;
      file.manager = tsd;
    }
    if (file.manager !== void 0 && file.manager instanceof TextureSetDefinition) {
      tsd = file.manager;
      if (!tsd.isLoaded) {
        if (loadHandler) {
          tsd.onLoaded.subscribe(loadHandler);
        }
        await tsd.load();
      }
    }
    return tsd;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    if (!this._data) {
      import_Log.default.unexpectedUndefined("TSTDF");
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._data);
  }
  /**
   * Loads the definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("TSTCDF");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._loadedWithComments = preserveComments;
      this._onLoaded.dispatch(this, this);
      return;
    }
    let data = [];
    let result = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      data = result;
    }
    this._data = data;
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
    this._onLoaded.dispatch(this, this);
  }
  getPackRootFolder() {
    let packRootFolder = void 0;
    if (this.file && this.file.parentFolder) {
      let parentFolder = this.file.parentFolder;
      packRootFolder = import_StorageUtilities.default.getParentOfParentFolderNamed("textures", parentFolder);
    }
    return packRootFolder;
  }
  async addChildItems(project, item) {
    const itemsCopy = project.getItemsCopy();
    let packRootFolder = this.getPackRootFolder();
    let textureListInitial = this.texturesList;
    let textureList = [];
    if (!packRootFolder || !textureListInitial) {
      return;
    }
    for (let texturePath of textureListInitial) {
      texturePath = import_StorageUtilities.default.canonicalizePath(texturePath).toLowerCase();
      const basePath = import_StorageUtilities.default.canonicalizePath(packRootFolder.fullPath).toLowerCase();
      if (texturePath.startsWith(basePath)) {
        textureList.push(texturePath.substring(basePath.length + 1).toLowerCase());
      } else {
        textureList.push(texturePath.toLowerCase());
      }
    }
    for (const candItem of itemsCopy) {
      if (candItem.itemType === import_IProjectItemData.ProjectItemType.texture && packRootFolder && textureList) {
        if (!candItem.isContentLoaded) {
          await candItem.loadContent();
        }
        if (candItem.primaryFile) {
          let relativePath = import_StorageUtilities.default.getBaseRelativePath(candItem.primaryFile, packRootFolder);
          if (relativePath) {
            if (textureList && textureList.includes(relativePath)) {
              item.addChildItem(candItem);
              textureList = import_Utilities.default.removeItemInArray(relativePath, textureList);
            }
          }
        }
      }
    }
    if (textureList) {
      for (const texturePath of textureList) {
        item.addUnfulfilledRelationship(
          texturePath,
          import_IProjectItemData.ProjectItemType.texture,
          await import_Database.default.matchesVanillaPath(texturePath)
        );
      }
    }
  }
}
//# sourceMappingURL=TextureSetDefinition.js.map
