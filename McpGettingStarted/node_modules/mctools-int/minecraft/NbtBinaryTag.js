"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var NbtBinaryTag_exports = {};
__export(NbtBinaryTag_exports, {
  NbtTagType: () => NbtTagType,
  default: () => NbtBinaryTag
});
module.exports = __toCommonJS(NbtBinaryTag_exports);
var import_Utilities = __toESM(require("./../core/Utilities"));
var import_Log = __toESM(require("./../core/Log"));
var import_INbtTag = __toESM(require("./INbtTag"));
var NbtTagType = /* @__PURE__ */ ((NbtTagType2) => {
  NbtTagType2[NbtTagType2["end"] = 0] = "end";
  NbtTagType2[NbtTagType2["byte"] = 1] = "byte";
  NbtTagType2[NbtTagType2["short"] = 2] = "short";
  NbtTagType2[NbtTagType2["int"] = 3] = "int";
  NbtTagType2[NbtTagType2["long"] = 4] = "long";
  NbtTagType2[NbtTagType2["float"] = 5] = "float";
  NbtTagType2[NbtTagType2["double"] = 6] = "double";
  NbtTagType2[NbtTagType2["byteArray"] = 7] = "byteArray";
  NbtTagType2[NbtTagType2["string"] = 8] = "string";
  NbtTagType2[NbtTagType2["list"] = 9] = "list";
  NbtTagType2[NbtTagType2["compound"] = 10] = "compound";
  NbtTagType2[NbtTagType2["intArray"] = 11] = "intArray";
  NbtTagType2[NbtTagType2["longArray"] = 12] = "longArray";
  NbtTagType2[NbtTagType2["unknown"] = 99] = "unknown";
  return NbtTagType2;
})(NbtTagType || {});
class NbtBinaryTag {
  type;
  childTagType = 99 /* unknown */;
  name;
  value = null;
  isListChild;
  _children = [];
  get childrenWithEnd() {
    return this._children;
  }
  getTagChildren() {
    if (this._children.length === 0) {
      return [];
    }
    if (this.lastTagIsEnd) {
      return this._children.slice(0, -1);
    }
    return this._children;
  }
  getTagLength() {
    if (this.lastTagIsEnd) {
      return this._children.length - 1;
    }
    return this._children.length;
  }
  get lastTagIsEnd() {
    if (this._children.length === 0) {
      return false;
    }
    const lastTag = this._children[this._children.length - 1];
    if (lastTag.type === 0 /* end */) {
      return true;
    } else if (this.childTagType === 0 /* end */ && lastTag._children.length === 0) {
      return true;
    }
    return false;
  }
  get valueAsString() {
    if (typeof this.value === "number") {
      return this.value.toString();
    } else if (typeof this.value === "string") {
      return this.value;
    }
    return "";
  }
  get valueAsJSONObject() {
    let obj = void 0;
    const str = this.valueAsString;
    if (str) {
      try {
        obj = JSON.parse(str);
      } catch (e) {
        obj = void 0;
      }
    }
    return obj;
  }
  get valueAsNumericArray() {
    const childTags = this.getTagChildren();
    const numarr = [];
    for (const childTag of childTags) {
      numarr.push(childTag.valueAsInt);
    }
    return numarr;
  }
  get valueAsStringArray() {
    const childTags = this.getTagChildren();
    const numarr = [];
    for (const childTag of childTags) {
      numarr.push(childTag.valueAsString);
    }
    return numarr;
  }
  get valueAsBoolean() {
    if (typeof this.value === "boolean") {
      return this.value;
    } else if (typeof this.value === "string") {
      if (this.value === "true" || this.value === "TRUE" || this.value === "1") {
        return true;
      }
      return false;
    } else if (typeof this.value === "number") {
      if (this.value <= 0) {
        return false;
      }
      return true;
    }
    return false;
  }
  get valueAsInt() {
    if (typeof this.value === "number") {
      return this.value;
    } else if (typeof this.value === "string") {
      return parseInt(this.value);
    } else if (typeof this.value === "boolean" && this.value === true) {
      return 1;
    }
    return 0;
  }
  get valueAsFloat() {
    if (typeof this.value === "number") {
      return this.value;
    } else if (typeof this.value === "string") {
      return parseFloat(this.value);
    }
    return 0;
  }
  get valueAsBigInt() {
    if (typeof this.value === "bigint") {
      return this.value;
    }
    const type = typeof this.value;
    if (this.value !== null && (type === "number" || type === "string" || type === "boolean" || type === "bigint")) {
      return BigInt(this.value);
    }
    return BigInt(0);
  }
  constructor(type, name, isListChild) {
    this.type = type;
    this.name = name;
    this.isListChild = isListChild;
  }
  setListFromArray(arr) {
    let children = this.getTagChildren();
    if (children.length > arr.length) {
      while (children.length > arr.length) {
        this.removeTagByIndex(children.length - 1);
        children = this.getTagChildren();
      }
    } else {
      while (children.length < arr.length) {
        const newTag = new NbtBinaryTag(3 /* int */, "", this.type === 9 /* list */);
        this.pushTag(newTag);
        children = this.getTagChildren();
      }
    }
    for (let i = 0; i < children.length; i++) {
      children[i].value = arr[i];
    }
  }
  pushTag(tag) {
    if (this.type === 9 /* list */ && this.lastTagIsEnd && this._children.length === 1 && tag.type !== 0 /* end */) {
      this._children[0] = tag;
      if ((this.childTagType === 99 /* unknown */ || this.childTagType === 0 /* end */) && tag.childTagType !== 0 /* end */) {
        this.childTagType = tag.type;
      }
    } else if (this.type === 9 /* list */) {
      if ((this.childTagType === 99 /* unknown */ || this.childTagType === 0 /* end */) && tag.childTagType !== 0 /* end */) {
        this.childTagType = tag.type;
      }
      this._children.push(tag);
    } else if (this.type === 10 /* compound */ && this.lastTagIsEnd && tag.type !== 0 /* end */) {
      this._children.push(this._children[this._children.length - 1]);
      this._children[this._children.length - 2] = tag;
    } else {
      this._children.push(tag);
    }
  }
  prepareForSave(includingChildren) {
    if (this.type === 9 /* list */) {
      if (this._children.length === 0) {
        this.childTagType = 0 /* end */;
        this.addTag(0 /* end */);
      } else if (this._children.length > 1 && this.lastTagIsEnd) {
        this._children = this._children.slice(0, -1);
      }
    } else if (this.type === 10 /* compound */ && !this.lastTagIsEnd) {
      this.addTag(0 /* end */);
    }
    if (includingChildren) {
      for (let i = 0; i < this._children.length; i++) {
        this._children[i].prepareForSave(includingChildren);
      }
    }
  }
  getByteSize() {
    this.prepareForSave(false);
    let byteSize = 0;
    if (!this.isListChild) {
      byteSize += 1;
      if (this.type !== 0 /* end */) {
        byteSize += 2 + this.name.length;
      }
    }
    switch (this.type) {
      case 1 /* byte */:
        byteSize += 1;
        break;
      case 2 /* short */:
        byteSize += 2;
        break;
      case 3 /* int */:
        byteSize += 4;
        break;
      case 4 /* long */:
        byteSize += 8;
        break;
      case 5 /* float */:
        byteSize += 4;
        break;
      case 6 /* double */:
        byteSize += 8;
        break;
      case 8 /* string */:
        byteSize += 2;
        if (typeof this.value === "string") {
          const bytes = import_Utilities.default.convertStringToBytes(this.value, "UTF-8");
          if (bytes === void 0) {
            throw new Error("Unexpected NBT conversion error in writing string.");
          }
          byteSize += bytes.length;
        }
        break;
      case 9 /* list */:
        byteSize += 5;
        break;
      case 11 /* intArray */:
        byteSize += 4;
        byteSize += 4 * this.value.length;
        break;
      case 12 /* longArray */:
        byteSize += 4;
        byteSize += 8 * this.value.length;
        break;
      default:
        break;
    }
    for (let i = 0; i < this._children.length; i++) {
      byteSize += this._children[i].getByteSize();
    }
    return byteSize;
  }
  getJsonString() {
    return JSON.stringify(this.getJson(), null, 2);
  }
  getJson() {
    this.prepareForSave(false);
    const tag = new import_INbtTag.default();
    if (this.name !== void 0 && this.name !== "") {
      tag.name = this.name;
    }
    if (this.type !== 99 /* unknown */) {
      tag.type = this.type;
    }
    if (this.value !== void 0) {
      tag.value = this.value;
    }
    if (this.childTagType !== 99 /* unknown */) {
      tag.childTagType = this.childTagType;
    }
    if (this._children.length > 0) {
      tag.children = [];
      for (let i = 0; i < this._children.length; i++) {
        tag.children.push(this._children[i].getJson());
      }
    }
    return tag;
  }
  writeBytes(bytes, index, littleEndian) {
    this.prepareForSave(false);
    const byteLength = bytes.buffer.byteLength;
    const dv = new DataView(bytes.buffer, 0, byteLength);
    if (!this.isListChild) {
      dv.setInt8(index, this.type);
      index += 1;
      if (this.type !== 0 /* end */) {
        dv.setInt16(index, this.name.length, littleEndian);
        index += 2;
        for (let j = 0; j < this.name.length; j++) {
          bytes[index++] = this.name.charCodeAt(j);
        }
      }
    }
    switch (this.type) {
      case 1 /* byte */:
        dv.setInt8(index, this.valueAsInt);
        index += 1;
        break;
      case 2 /* short */:
        dv.setInt16(index, this.valueAsInt, littleEndian);
        index += 2;
        break;
      case 3 /* int */:
        dv.setInt32(index, this.valueAsInt, littleEndian);
        index += 4;
        break;
      case 4 /* long */:
        dv.setBigInt64(index, this.valueAsBigInt, littleEndian);
        index += 8;
        break;
      case 5 /* float */:
        dv.setFloat32(index, this.valueAsFloat, littleEndian);
        index += 4;
        break;
      case 6 /* double */:
        dv.setFloat64(index, this.valueAsFloat, littleEndian);
        index += 8;
        break;
      case 8 /* string */:
        if (typeof this.value === "string") {
          const bytes2 = import_Utilities.default.convertStringToBytes(this.value, "UTF-8");
          if (bytes2 === void 0) {
            throw new Error("Unexpected NBT conversion error in writing string.");
          }
          dv.setInt16(index, bytes2.length, littleEndian);
          index += 2;
          index = import_Utilities.default.writeString(dv, index, this.value, "UTF-8");
          if (index < 0 || index >= byteLength) {
            throw new Error("Unexpected error writing string.");
          }
        } else {
          dv.setInt16(index, 0, littleEndian);
          index += 2;
        }
        break;
      case 9 /* list */:
        dv.setInt8(index, this.childTagType);
        index += 1;
        dv.setInt32(index, this.getTagLength(), littleEndian);
        index += 4;
        break;
      case 11 /* intArray */:
        dv.setInt32(index, this._children.length, littleEndian);
        index += 4;
        const nums = this.value;
        for (let i = 0; i < nums.length; i++) {
          dv.setInt32(index, nums[i], littleEndian);
          index += 4;
        }
        break;
      case 12 /* longArray */:
        dv.setInt32(index, this._children.length, littleEndian);
        index += 4;
        const bigints = this.value;
        for (let i = 0; i < bigints.length; i++) {
          dv.setBigInt64(index, bigints[i], littleEndian);
          index += 8;
        }
        break;
    }
    for (let i = 0; i < this._children.length; i++) {
      index = this._children[i].writeBytes(bytes, index, littleEndian);
    }
    return index;
  }
  removeTagByIndex(index) {
    const newChildren = [];
    let removedItem = false;
    for (let i = 0; i < this._children.length; i++) {
      if (i !== index) {
        newChildren.push(this._children[i]);
      } else {
        removedItem = true;
      }
    }
    this._children = newChildren;
    return removedItem;
  }
  removeTag(tagName) {
    const newChildren = [];
    let removedItem = false;
    for (let i = 0; i < this._children.length; i++) {
      if (this._children[i].name !== tagName) {
        newChildren.push(this._children[i]);
      } else {
        removedItem = true;
      }
    }
    this._children = newChildren;
    return removedItem;
  }
  ensureTag(tagName, tagType) {
    const tag = this.child(tagName);
    if (tag !== null) {
      import_Log.default.assert(tag.type === tagType, "Unexpected data type '" + tag.type + "' for an nbt tag '" + tagName + "'");
      return tag;
    }
    return this.addTag(tagType, tagName);
  }
  addTag(tagType, tagName) {
    if (this.type !== 10 /* compound */ && this.type !== 9 /* list */) {
      throw new Error("Unsupported type for adding a tag");
    }
    if (tagName === void 0) {
      tagName = "";
    }
    const newTag = new NbtBinaryTag(tagType, tagName, this.type === 9 /* list */);
    this.pushTag(newTag);
    return newTag;
  }
  getProperty(name) {
    for (let i = 0; i < this._children.length; i++) {
      if (this._children[i].name === name) {
        return this._children[i];
      }
    }
    return null;
  }
  child(name) {
    if (this.name === name) {
      return this;
    }
    for (let i = 0; i < this._children.length; i++) {
      if (this._children[i].name === name) {
        return this._children[i];
      }
    }
    return null;
  }
  find(name) {
    if (this.name === name) {
      return this;
    }
    for (let i = 0; i < this._children.length; i++) {
      if (this._children[i].name === name) {
        return this._children[i];
      }
    }
    for (let i = 0; i < this._children.length; i++) {
      const tagChild = this._children[i];
      if (tagChild.type === 10 /* compound */ || tagChild.type === 9 /* list */) {
        const result = tagChild.find(name);
        if (result != null) {
          return result;
        }
      }
    }
    return null;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  NbtTagType
});
//# sourceMappingURL=NbtBinaryTag.js.map
