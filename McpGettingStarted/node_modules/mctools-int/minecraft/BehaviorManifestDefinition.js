"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var BehaviorManifestDefinition_exports = {};
__export(BehaviorManifestDefinition_exports, {
  default: () => BehaviorManifestDefinition
});
module.exports = __toCommonJS(BehaviorManifestDefinition_exports);
var import_ste_events = require("ste-events");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_Project = require("../app/Project");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_IProjectData = require("../app/IProjectData");
var import_ResourceManifestDefinition = __toESM(require("./ResourceManifestDefinition"));
var import_MinecraftUtilities = __toESM(require("./MinecraftUtilities"));
var import_Log = __toESM(require("../core/Log"));
class BehaviorManifestDefinition {
  _file;
  _id;
  _isLoaded = false;
  _loadedWithComments = false;
  definition;
  _onLoaded = new import_ste_events.EventDispatcher();
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  set file(newFile) {
    this._file = newFile;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  get productType() {
    if (!this.definition || !this.definition.metadata) {
      return void 0;
    }
    return this.definition.metadata.product_type;
  }
  set productType(value) {
    this.ensureMetadata();
    if (!this.definition || !this.definition.metadata) {
      return;
    }
    this.definition.metadata.product_type = value;
  }
  get description() {
    if (!this.definition || !this.definition.header) {
      return void 0;
    }
    return this.definition.header.description;
  }
  set description(newDescription) {
    if (this.definition && this.definition.header && newDescription) {
      this.definition.header.description = newDescription;
    }
  }
  get name() {
    if (this.definition && this.definition.header) {
      return this.definition.header.name;
    }
    return void 0;
  }
  set name(newName) {
    if (this.definition && this.definition.header && newName) {
      this.definition.header.name = newName;
    }
  }
  get capabilities() {
    if (!this.definition || !this.definition.capabilities) {
      return void 0;
    }
    return this.definition.capabilities;
  }
  get id() {
    if (this.definition && this.definition.header) {
      return this.definition.header.uuid;
    }
    return this._id;
  }
  set id(newId) {
    if (this.definition && this.definition.header && newId) {
      this.definition.header.uuid = newId;
    }
    this._id = newId;
  }
  async setUuid(newId, project) {
    const oldId = this.id;
    this.id = newId;
    if (newId && oldId && project) {
      await BehaviorManifestDefinition.setNewBehaviorPackId(project, newId, oldId);
    }
  }
  static async setNewBehaviorPackId(project, newBehaviorPackId, oldBehaviorPackId) {
    const itemsCopy = project.getItemsCopy();
    let setBehaviorPack = false;
    for (let i = 0; i < itemsCopy.length; i++) {
      const pi = itemsCopy[i];
      if (pi.primaryFile) {
        if (pi.itemType === import_IProjectItemData.ProjectItemType.behaviorPackManifestJson && !setBehaviorPack) {
          const bpManifestJson = await BehaviorManifestDefinition.ensureOnFile(pi.primaryFile);
          if (bpManifestJson) {
            if (bpManifestJson.id && import_Utilities.default.uuidEqual(bpManifestJson.id, oldBehaviorPackId)) {
              bpManifestJson.id = newBehaviorPackId;
              setBehaviorPack = true;
              await bpManifestJson.save();
            } else if (bpManifestJson.definition && bpManifestJson.definition.dependencies) {
              const deps = bpManifestJson.definition?.dependencies;
              if (deps && Array.isArray(deps)) {
                for (const dep of deps) {
                  if (dep.uuid === oldBehaviorPackId) {
                    dep.uuid = newBehaviorPackId;
                  }
                }
              }
              await bpManifestJson.save();
            }
          }
        } else if (pi.itemType === import_IProjectItemData.ProjectItemType.resourcePackManifestJson) {
          const rpManifestJson = await import_ResourceManifestDefinition.default.ensureOnFile(pi.primaryFile);
          if (rpManifestJson) {
            if (rpManifestJson.definition && rpManifestJson.definition.dependencies) {
              const deps = rpManifestJson.definition?.dependencies;
              if (deps && Array.isArray(deps)) {
                for (const dep of deps) {
                  if (dep.uuid === oldBehaviorPackId) {
                    dep.uuid = newBehaviorPackId;
                  }
                }
              }
              await rpManifestJson.save();
            }
          }
        }
      }
    }
  }
  hasAddonProperties() {
    return this.productType === "addon";
  }
  async setAddonProperties() {
    this.productType = "addon";
    await this.save();
  }
  randomizeModuleUuids(newScriptModuleId, oldScriptModuleId) {
    if (!this.definition) {
      return;
    }
    for (let i = 0; i < this.definition.modules.length; i++) {
      const mod = this.definition.modules[i];
      if (mod.uuid) {
        if (oldScriptModuleId && newScriptModuleId && (mod.uuid === oldScriptModuleId || mod.uuid === newScriptModuleId)) {
          mod.uuid = newScriptModuleId;
        } else {
          mod.uuid = import_Utilities.default.createUuid();
        }
      }
    }
  }
  getNonScriptModuleDependencyCount() {
    if (!this.definition || !this.definition.dependencies || !Array.isArray(this.definition.dependencies)) {
      return 0;
    }
    let count = 0;
    for (let dependency of this.definition.dependencies) {
      if (dependency.uuid) {
        count++;
      }
    }
    return count;
  }
  getFirstNonScriptModuleDependency() {
    if (!this.definition || !this.definition.dependencies || !Array.isArray(this.definition.dependencies)) {
      return void 0;
    }
    for (let dependency of this.definition.dependencies) {
      if (dependency.uuid) {
        return dependency;
      }
    }
    return void 0;
  }
  static async ensureOnFile(file, loadHandler) {
    let bmj;
    if (file.manager === void 0) {
      bmj = new BehaviorManifestDefinition();
      bmj.file = file;
      file.manager = bmj;
    }
    if (file.manager !== void 0 && file.manager instanceof BehaviorManifestDefinition) {
      bmj = file.manager;
      if (!bmj.isLoaded) {
        if (loadHandler) {
          bmj.onLoaded.subscribe(loadHandler);
        }
        await bmj.load();
      }
    }
    return bmj;
  }
  get version() {
    if (!this.definition || !this.definition.header || !this.definition.header.version) {
      return void 0;
    }
    return this.definition.header.version;
  }
  get minEngineVersion() {
    if (!this.definition || !this.definition.header || !this.definition.header.min_engine_version) {
      return void 0;
    }
    return this.definition.header.min_engine_version;
  }
  setMinEngineVersion(versionArray, project) {
    const header = this.ensureHeaderForProject(project);
    header.min_engine_version = versionArray;
  }
  setModuleVersion(moduleName, version) {
    if (!this.definition || !this.definition.dependencies) {
      return false;
    }
    let verActual = version;
    let changedVals = false;
    if (version.indexOf("-") < 1) {
      const verArr = version.split(".");
      if (verArr && verArr.length === 3) {
        verActual = [];
        for (let i = 0; i < verArr.length; i++) {
          try {
            verActual.push(parseInt(verArr[i]));
          } catch (e) {
            throw e;
          }
          if (Number.isNaN(verActual[i])) {
            throw new Error("Could not parse " + version);
          }
        }
      }
    }
    for (let i = 0; i < this.definition.dependencies.length; i++) {
      const dep = this.definition.dependencies[i];
      if (dep.module_name === moduleName) {
        if (dep.version !== verActual) {
          dep.version = verActual;
          changedVals = true;
        }
      }
    }
    return changedVals;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    import_Log.default.assert(this.definition !== null, "BMDP");
    if (!this.definition) {
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this.definition);
  }
  ensureDefinition(name, description) {
    if (!this.definition) {
      this.definition = {
        format_version: 2,
        header: {
          name,
          description,
          version: [0, 0, 1],
          min_engine_version: [1, 20, 10],
          uuid: import_Utilities.default.createUuid()
        },
        modules: [],
        dependencies: []
      };
    }
  }
  ensureHeaderForProject(project) {
    return this.ensureHeader(project.title, project.description);
  }
  ensureMetadata() {
    if (!this.definition) {
      return void 0;
    }
    if (!this.definition.metadata) {
      this.definition.metadata = {};
    }
    return this.definition.metadata;
  }
  ensureGeneratedWith(toolName, versionString) {
    const metadata = this.ensureMetadata();
    if (!metadata) {
      return void 0;
    }
    if (!metadata.generated_with) {
      metadata.generated_with = {};
    }
    if (!metadata.generated_with[toolName]) {
      metadata.generated_with[toolName] = [];
    }
    if (!metadata.generated_with[toolName].includes(versionString)) {
      metadata.generated_with[toolName].push(versionString);
    }
  }
  ensureHeader(name, description) {
    this.ensureDefinition(name, description);
    if (!this.definition) {
      throw new Error();
    }
    if (!this.definition.header) {
      this.definition.header = this.getDefaultHeader(name, description);
    }
    return this.definition.header;
  }
  getDefaultHeader(name, description) {
    return {
      name,
      description,
      version: [0, 0, 1],
      min_engine_version: [1, 20, 10],
      uuid: import_Utilities.default.createUuid()
    };
  }
  getScriptModule() {
    if (!this.definition) {
      return void 0;
    }
    for (let i = 0; i < this.definition.modules.length; i++) {
      const mod = this.definition.modules[i];
      if (mod.type && mod.type.toLowerCase() === "script") {
        return mod;
      }
    }
    return void 0;
  }
  ensureScriptModule(name, description) {
    this.ensureDefinition(name, description);
    if (!this.definition) {
      throw new Error();
    }
    for (let i = 0; i < this.definition.modules.length; i++) {
      const mod2 = this.definition.modules[i];
      if (mod2.type && mod2.type.toLowerCase() === "script") {
        return mod2;
      }
    }
    const mod = {
      type: "script",
      description: name,
      version: [0, 0, 1],
      uuid: import_Utilities.default.createUuid(),
      language: "javascript"
    };
    this.definition.modules.push(mod);
    return mod;
  }
  async save() {
    if (this._file === void 0) {
      return;
    }
    if (this.persist()) {
      await this._file.saveContent(false);
    }
  }
  /**
   * Loads the definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (this._file.content === null || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._loadedWithComments = preserveComments;
      this._onLoaded.dispatch(this, this);
      return;
    }
    this.definition = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
    this._onLoaded.dispatch(this, this);
  }
  static async getContent(project, contentOnlyUpdate) {
    let versionMajor = 0;
    let versionMinor = 0;
    let versionPatch = 1;
    if (project.versionMajor !== void 0) {
      versionMajor = project.versionMajor;
    }
    if (project.versionMinor !== void 0) {
      versionMinor = project.versionMinor;
    }
    if (project.versionPatch !== void 0) {
      versionPatch = project.versionPatch;
    }
    const modulesList = [];
    const dependenciesList = [];
    let scriptFile = void 0;
    let pi = project.getFirstItemByType(import_IProjectItemData.ProjectItemType.catalogIndexJs);
    if (pi !== void 0) {
      scriptFile = pi.primaryFile;
    }
    const behaviorPackRootFolder = await project.getDefaultBehaviorPackFolder(false, contentOnlyUpdate);
    if (scriptFile === void 0) {
      pi = project.getFirstItemByType(import_IProjectItemData.ProjectItemType.js);
      if (pi !== void 0) {
        scriptFile = pi.primaryFile;
      }
      if (scriptFile === void 0) {
        pi = project.getFirstItemByType(import_IProjectItemData.ProjectItemType.testJs);
        if (pi !== void 0) {
          scriptFile = pi.primaryFile;
        }
      }
    }
    const scriptState = await project.getScriptState();
    if (scriptState.hasScript && scriptFile !== null && scriptFile !== void 0 && behaviorPackRootFolder !== null) {
      let path = scriptFile.getFolderRelativePath(behaviorPackRootFolder);
      if (path !== void 0) {
        if (path.startsWith("/")) {
          path = path.substring(1, path.length);
        }
        modulesList.push({
          description: project.title + " script",
          language: "javascript",
          type: "script",
          uuid: project.defaultScriptModuleUniqueId,
          version: [versionMajor, versionMinor, versionPatch],
          entry: path
        });
        for (let i = 0; i < import_Project.minecraftScriptModules.length; i++) {
          const mod = import_Project.minecraftScriptModules[i];
          if (scriptState.hasModule[mod.id]) {
            dependenciesList.push({
              uuid: mod.uuid,
              module_name: mod.module_name,
              version: mod.preferredVersion
            });
          }
        }
      }
    }
    const manifest = {
      format_version: 2,
      __comment__: import_Project.AUTOGENERATED_WHOLEFILE_GENERAL_SEPARATOR,
      header: {
        description: project.description,
        name: project.title,
        uuid: project.defaultBehaviorPackUniqueId,
        version: [versionMajor, versionMinor, versionPatch],
        min_engine_version: [1, 20, 10]
      },
      modules: modulesList,
      dependencies: dependenciesList
    };
    if (project.focus === import_IProjectData.ProjectFocus.editorExtension) {
      manifest.capabilities = ["editorExtension"];
    }
    return JSON.stringify(manifest, null, 2);
  }
  static validatePackReferenceVersion(version) {
    if (!version) {
      return {
        isValid: false,
        errorMessage: "Missing version"
      };
    }
    const versionArray = import_MinecraftUtilities.default.getVersionArrayFrom(version);
    if (!versionArray || versionArray.length !== 3 || !versionArray.every((v) => typeof v === "number" && v >= 0)) {
      return {
        isValid: false,
        errorMessage: "Invalid version format. Expected valid version string or array of 3 non-negative numbers"
      };
    }
    return {
      isValid: true,
      versionArray
    };
  }
  static validatePackId(packId) {
    if (!packId || typeof packId !== "string") {
      return {
        isValid: false,
        errorMessage: "Missing or invalid pack_id"
      };
    }
    if (!import_MinecraftUtilities.default.isValidUuid(packId)) {
      return {
        isValid: false,
        errorMessage: `Invalid UUID format for pack_id [${packId}]`
      };
    }
    return { isValid: true };
  }
}
//# sourceMappingURL=BehaviorManifestDefinition.js.map
