"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var JsonUIResourceDefinition_exports = {};
__export(JsonUIResourceDefinition_exports, {
  default: () => JsonUIResourceDefinition
});
module.exports = __toCommonJS(JsonUIResourceDefinition_exports);
var import_Log = __toESM(require("../core/Log"));
var import_ste_events = require("ste-events");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_Database = __toESM(require("./Database"));
var import_TextureDefinition = __toESM(require("./TextureDefinition"));
class JsonUIResourceDefinition {
  jsonUIScreen;
  _file;
  _isLoaded = false;
  _loadedWithComments = false;
  _onLoaded = new import_ste_events.EventDispatcher();
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  get namespaceId() {
    if (!this.jsonUIScreen || !this.jsonUIScreen["namespace"]) {
      return void 0;
    }
    return this.jsonUIScreen["namespace"];
  }
  static async ensureOnFile(file, loadHandler) {
    let et;
    if (file.manager === void 0) {
      et = new JsonUIResourceDefinition();
      et.file = file;
      file.manager = et;
    }
    if (file.manager !== void 0 && file.manager instanceof JsonUIResourceDefinition) {
      et = file.manager;
      if (!et.isLoaded) {
        if (loadHandler) {
          et.onLoaded.subscribe(loadHandler);
        }
        await et.load();
      }
    }
    return et;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    if (!this.jsonUIScreen) {
      import_Log.default.unexpectedUndefined("ITRDP");
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this.jsonUIScreen);
  }
  getControlRefs() {
    const namespaceId = this.namespaceId;
    const jsonUITextureRefs = [];
    if (namespaceId && this.jsonUIScreen) {
      for (const jsonControlId in this.jsonUIScreen) {
        const jsonControl = this.jsonUIScreen[jsonControlId];
        if (jsonControlId !== "namespace" && jsonControl && jsonControl.texture) {
          if (!jsonUITextureRefs.includes(jsonControlId)) {
            jsonUITextureRefs.push(jsonControlId);
          }
        }
      }
    }
    return jsonUITextureRefs;
  }
  getTexturePaths() {
    const texturePaths = [];
    if (this.jsonUIScreen) {
      this.addTexturesFromArrayOfKeyPaths([this.jsonUIScreen], texturePaths);
    }
    return texturePaths;
  }
  addTexturesFromArrayOfKeyPaths(controlSets, texturePaths) {
    if (!Array.isArray(controlSets)) {
      return;
    }
    for (const controlSet of controlSets) {
      for (const key in controlSet) {
        const control = controlSet[key];
        if (key !== "namespace" && key !== "controls") {
          if (typeof control === "string") {
            texturePaths.push(control);
          } else {
            if (control.texture) {
              const tex = import_TextureDefinition.default.canonicalizeTexturePath(control.texture);
              if (tex) {
                texturePaths.push(tex);
              }
            }
            if (control.controls) {
              this.addTexturesFromArrayOfKeyPaths(control.controls, texturePaths);
            }
          }
        }
      }
    }
  }
  /**
   * Loads the definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("PERPF");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._loadedWithComments = preserveComments;
      this._onLoaded.dispatch(this, this);
      return;
    }
    let data = {};
    let result = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      data = result;
    }
    this.jsonUIScreen = data;
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
    this._onLoaded.dispatch(this, this);
  }
  getPackRootFolder() {
    let packRootFolder = void 0;
    if (this.file && this.file.parentFolder) {
      let parentFolder = this.file.parentFolder;
      packRootFolder = import_StorageUtilities.default.getParentOfParentFolderNamed("ui", parentFolder);
    }
    return packRootFolder;
  }
  async addChildItems(project, item) {
    const itemsCopy = project.getItemsCopy();
    let packRootFolder = this.getPackRootFolder();
    let textureList = this.getTexturePaths();
    if (textureList.length > 0) {
      for (const candItem of itemsCopy) {
        if ((candItem.itemType === import_IProjectItemData.ProjectItemType.texture || candItem.itemType === import_IProjectItemData.ProjectItemType.uiTexture) && packRootFolder && textureList) {
          if (!candItem.isContentLoaded) {
            await candItem.loadContent();
          }
          if (candItem.primaryFile) {
            let relativePath = import_TextureDefinition.default.canonicalizeTexturePath(
              import_StorageUtilities.default.getBaseRelativePath(candItem.primaryFile, packRootFolder)
            );
            if (relativePath) {
              if (textureList && textureList.includes(relativePath)) {
                item.addChildItem(candItem);
                textureList = import_Utilities.default.removeItemInArray(relativePath, textureList);
              }
            }
          }
        }
      }
      if (textureList) {
        for (const texturePath of textureList) {
          if (!texturePath.startsWith("$")) {
            const isVanilla = await import_Database.default.isVanillaToken(texturePath);
            item.addUnfulfilledRelationship(texturePath, import_IProjectItemData.ProjectItemType.texture, isVanilla);
          }
        }
      }
    }
  }
}
//# sourceMappingURL=JsonUIResourceDefinition.js.map
