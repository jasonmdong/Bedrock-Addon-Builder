"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var Command_exports = {};
__export(Command_exports, {
  default: () => Command
});
module.exports = __toCommonJS(Command_exports);
var import_CommandArgument = __toESM(require("./CommandArgument"));
class Command {
  _commandName;
  _arguments;
  constructor(commandText) {
    const firstSpace = commandText.indexOf(" ");
    this._arguments = [];
    let _coordinatePos = -1;
    if (firstSpace < 0) {
      this._commandName = commandText;
    } else {
      this._commandName = commandText.substring(0, firstSpace);
      const args = commandText.substring(firstSpace + 1, commandText.length).split(" ");
      for (let i = 0; i < args.length; i++) {
        const cmdArg = new import_CommandArgument.default(args[i]);
        if (cmdArg.type === import_CommandArgument.CommandArgumentType.coordinate) {
          _coordinatePos++;
          if (_coordinatePos >= 3) {
            _coordinatePos = 0;
          }
          if (_coordinatePos === 0) {
            cmdArg.location = import_CommandArgument.CoordinateLocation.x;
          } else if (_coordinatePos === 1) {
            cmdArg.location = import_CommandArgument.CoordinateLocation.y;
          } else if (_coordinatePos === 2) {
            cmdArg.location = import_CommandArgument.CoordinateLocation.z;
          }
        } else {
          _coordinatePos = -1;
        }
        this._arguments.push(cmdArg);
      }
    }
    if (this._commandName.startsWith("/")) {
      this._commandName = this._commandName.substring(1, this._commandName.length);
    }
  }
  absolutizeCoordinates(absoluteX, absoluteY, absoluteZ) {
    for (let i = 0; i < this._arguments.length; i++) {
      const arg = this._arguments[i];
      if (arg.type === import_CommandArgument.CommandArgumentType.coordinate) {
        if (arg.coordinateType === import_CommandArgument.CoordinateType.relativeTilde) {
          if (arg.location === import_CommandArgument.CoordinateLocation.x) {
            arg.coordinateType = import_CommandArgument.CoordinateType.absolute;
            arg.position = absoluteX + arg.position;
          } else if (arg.location === import_CommandArgument.CoordinateLocation.y) {
            arg.coordinateType = import_CommandArgument.CoordinateType.absolute;
            arg.position = absoluteY + arg.position;
          } else if (arg.location === import_CommandArgument.CoordinateLocation.z) {
            arg.coordinateType = import_CommandArgument.CoordinateType.absolute;
            arg.position = absoluteZ + arg.position;
          }
        }
      }
    }
  }
  get hasRelativeOrLocalCoordinates() {
    for (let i = 0; i < this._arguments.length; i++) {
      const arg = this._arguments[i];
      if (arg.type === import_CommandArgument.CommandArgumentType.coordinate && arg.coordinateType !== import_CommandArgument.CoordinateType.absolute) {
        return true;
      }
    }
    return false;
  }
  get firstX() {
    for (let i = 0; i < this._arguments.length; i++) {
      const arg = this._arguments[i];
      if (arg.type === import_CommandArgument.CommandArgumentType.coordinate && arg.location === import_CommandArgument.CoordinateLocation.x) {
        return arg;
      }
    }
    return void 0;
  }
  get firstY() {
    for (let i = 0; i < this._arguments.length; i++) {
      const arg = this._arguments[i];
      if (arg.type === import_CommandArgument.CommandArgumentType.coordinate && arg.location === import_CommandArgument.CoordinateLocation.y) {
        return arg;
      }
    }
    return void 0;
  }
  get firstZ() {
    for (let i = 0; i < this._arguments.length; i++) {
      const arg = this._arguments[i];
      if (arg.type === import_CommandArgument.CommandArgumentType.coordinate && arg.location === import_CommandArgument.CoordinateLocation.z) {
        return arg;
      }
    }
    return void 0;
  }
  toString() {
    let commandResult = this._commandName;
    for (let i = 0; i < this._arguments.length; i++) {
      commandResult += " ";
      commandResult += this._arguments[i].toString();
    }
    return commandResult;
  }
}
//# sourceMappingURL=Command.js.map
