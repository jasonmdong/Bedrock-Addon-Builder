"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var JigsawTemplatePoolDefinition_exports = {};
__export(JigsawTemplatePoolDefinition_exports, {
  default: () => JigsawTemplatePoolDefinition
});
module.exports = __toCommonJS(JigsawTemplatePoolDefinition_exports);
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_JigsawProcessorListDefinition = __toESM(require("./JigsawProcessorListDefinition"));
var import_Log = __toESM(require("../core/Log"));
class JigsawTemplatePoolDefinition {
  _file;
  _data;
  _isLoaded = false;
  _loadedWithComments = false;
  get data() {
    return this._data;
  }
  get file() {
    return this._file;
  }
  set file(newFile) {
    this._file = newFile;
  }
  get isLoaded() {
    return this._isLoaded;
  }
  get id() {
    if (this._data?.["minecraft:template_pool"]?.description?.identifier) {
      return this._data["minecraft:template_pool"].description.identifier;
    }
    return "";
  }
  get elements() {
    if (this._data?.["minecraft:template_pool"]?.elements) {
      return this._data["minecraft:template_pool"].elements;
    }
    return [];
  }
  async getFormatVersionIsCurrent() {
    return true;
  }
  async addChildItems(project, item) {
    const elements = this.elements;
    if (!elements || elements.length === 0) {
      return;
    }
    const jigsawProcessorListItems = project.getItemsByType(import_IProjectItemData.ProjectItemType.jigsawProcessorList);
    for (const element of elements) {
      if (element.element.location) {
        const structurePath = element.element.location + ".mcstructure";
        for (const candItem of jigsawProcessorListItems) {
          if (candItem.itemType === import_IProjectItemData.ProjectItemType.structure) {
            if (candItem.projectPath?.endsWith(structurePath) || candItem.projectPath?.includes(element.element.location)) {
              item.addChildItem(candItem);
            }
          }
        }
      }
      if (element.element.processors) {
        for (const candItem of jigsawProcessorListItems) {
          if (!candItem.isContentLoaded) {
            await candItem.loadContent();
          }
          if (candItem.primaryFile) {
            const processorList = await import_JigsawProcessorListDefinition.default.ensureOnFile(candItem.primaryFile);
            if (processorList && processorList.id === element.element.processors) {
              item.addChildItem(candItem);
            }
          }
        }
      }
    }
  }
  static async ensureOnFile(file) {
    let jigsawTemplatePool;
    if (file.manager === void 0) {
      jigsawTemplatePool = new JigsawTemplatePoolDefinition();
      jigsawTemplatePool.file = file;
      file.manager = jigsawTemplatePool;
    }
    if (file.manager !== void 0 && file.manager instanceof JigsawTemplatePoolDefinition) {
      jigsawTemplatePool = file.manager;
      if (!jigsawTemplatePool.isLoaded) {
        await jigsawTemplatePool.load();
      }
    }
    return jigsawTemplatePool;
  }
  /**
   * Loads the definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._loadedWithComments = preserveComments;
      return;
    }
    const result = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      this._data = result;
    }
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    if (!this._data) {
      import_Log.default.unexpectedUndefined("ITRDP");
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._data);
  }
}
//# sourceMappingURL=JigsawTemplatePoolDefinition.js.map
