"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var Pack_exports = {};
__export(Pack_exports, {
  PackType: () => PackType,
  default: () => Pack
});
module.exports = __toCommonJS(Pack_exports);
var import_BehaviorManifestDefinition = __toESM(require("./BehaviorManifestDefinition"));
var import_ResourceManifestDefinition = __toESM(require("./ResourceManifestDefinition"));
var import_SkinManifestDefinition = __toESM(require("./SkinManifestDefinition"));
var import_PersonaManifestDefinition = __toESM(require("./PersonaManifestDefinition"));
var import_DesignManifestDefinition = __toESM(require("./DesignManifestDefinition"));
var import_Log = __toESM(require("../core/Log"));
var PackType = /* @__PURE__ */ ((PackType2) => {
  PackType2[PackType2["resource"] = 0] = "resource";
  PackType2[PackType2["behavior"] = 1] = "behavior";
  PackType2[PackType2["skin"] = 2] = "skin";
  PackType2[PackType2["persona"] = 3] = "persona";
  PackType2[PackType2["design"] = 4] = "design";
  return PackType2;
})(PackType || {});
class Pack {
  packType;
  manifestFile;
  folder;
  project;
  projectItem;
  isInWorld = false;
  manifest;
  _items;
  //stubbing in for use later
  get isEDUOffer() {
    return false;
  }
  constructor(folderIn, packTypeIn, project, projectItem) {
    this.project = project;
    this.projectItem = projectItem;
    this.folder = folderIn;
    this.packType = packTypeIn;
  }
  ensureManifestFile() {
    if (this.manifestFile === void 0) {
      this.manifestFile = this.folder.ensureFile("manifest.json");
    }
    return this.manifestFile;
  }
  async ensureManifest() {
    if (this.manifest) {
      return this.manifest;
    }
    this.manifestFile = this.ensureManifestFile();
    if (this.packType === 1 /* behavior */) {
      this.manifest = await import_BehaviorManifestDefinition.default.ensureOnFile(this.manifestFile);
    } else if (this.packType === 2 /* skin */) {
      this.manifest = await import_SkinManifestDefinition.default.ensureOnFile(this.manifestFile);
    } else if (this.packType === 3 /* persona */) {
      this.manifest = await import_PersonaManifestDefinition.default.ensureOnFile(this.manifestFile);
    } else if (this.packType === 4 /* design */) {
      this.manifest = await import_DesignManifestDefinition.default.ensureOnFile(this.manifestFile);
    } else {
      this.manifest = await import_ResourceManifestDefinition.default.ensureOnFile(this.manifestFile);
    }
    if (this.manifest && !this.manifest.isLoaded) {
      this.manifest.load();
    }
    if (this.manifest && !this.manifest.definition) {
      this.manifest.ensureHeaderForProject(this.project);
      this.manifest.persist();
    }
    return this.manifest;
  }
  getManifest() {
    const manifest = this.getPackItems().find((item) => item.name === "manifest.json");
    import_Log.default.assert(!!manifest, "Pack should always have a manifest item");
    return manifest;
  }
  getPackItems() {
    if (!!this._items) {
      return this._items;
    }
    const folderPath = this.projectItem.projectPath;
    if (!folderPath) {
      throw new Error("Pack.getPackItems called without a project path");
    }
    this._items = this.project.items.filter((item) => item.projectPath?.startsWith(folderPath));
    return this._items;
  }
  static ensureOnFolder(folder, packType, project, projectItem) {
    if (folder.manager === void 0) {
      const pack = new Pack(folder, packType, project, projectItem);
      if (projectItem.isInWorld) {
        pack.isInWorld = true;
      }
      pack.project = project;
      pack.packType = packType;
      return pack;
    } else {
      return folder.manager;
    }
  }
  get name() {
    return this.projectItem?.name || "Unnamed pack";
  }
  async getFiles(predicate) {
    const result = [];
    for await (const file of this.folder.allFiles) {
      if (!file.isContentLoaded) {
        await file.loadContent();
      }
      if (file.content && (!predicate || predicate(file))) {
        result.push(file);
      }
    }
    return result;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  PackType
});
//# sourceMappingURL=Pack.js.map
