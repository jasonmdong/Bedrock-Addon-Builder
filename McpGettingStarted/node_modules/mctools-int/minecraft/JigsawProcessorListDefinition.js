"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var JigsawProcessorListDefinition_exports = {};
__export(JigsawProcessorListDefinition_exports, {
  default: () => JigsawProcessorListDefinition
});
module.exports = __toCommonJS(JigsawProcessorListDefinition_exports);
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_Log = __toESM(require("../core/Log"));
class JigsawProcessorListDefinition {
  _file;
  _data;
  _isLoaded = false;
  _loadedWithComments = false;
  get data() {
    return this._data;
  }
  get file() {
    return this._file;
  }
  set file(newFile) {
    this._file = newFile;
  }
  get isLoaded() {
    return this._isLoaded;
  }
  get id() {
    if (this._data?.["minecraft:processor_list"]?.description?.identifier) {
      return this._data["minecraft:processor_list"].description.identifier;
    }
    return "";
  }
  get processors() {
    if (this._data?.["minecraft:processor_list"]?.processors) {
      return this._data["minecraft:processor_list"].processors;
    }
    return [];
  }
  async getFormatVersionIsCurrent() {
    return true;
  }
  async addChildItems(project, item) {
  }
  static async ensureOnFile(file) {
    let jigsawProcessorList;
    if (file.manager === void 0) {
      jigsawProcessorList = new JigsawProcessorListDefinition();
      jigsawProcessorList.file = file;
      file.manager = jigsawProcessorList;
    }
    if (file.manager !== void 0 && file.manager instanceof JigsawProcessorListDefinition) {
      jigsawProcessorList = file.manager;
      if (!jigsawProcessorList.isLoaded) {
        await jigsawProcessorList.load();
      }
    }
    return jigsawProcessorList;
  }
  /**
   * Loads the definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._loadedWithComments = preserveComments;
      return;
    }
    const result = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      this._data = result;
    }
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    if (!this._data) {
      import_Log.default.unexpectedUndefined("JPLDP");
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._data);
  }
}
//# sourceMappingURL=JigsawProcessorListDefinition.js.map
