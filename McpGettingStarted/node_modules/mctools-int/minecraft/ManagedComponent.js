"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ManagedComponent_exports = {};
__export(ManagedComponent_exports, {
  ManagedComponent: () => ManagedComponent
});
module.exports = __toCommonJS(ManagedComponent_exports);
var import_ste_events = require("ste-events");
var import_Utilities = __toESM(require("../core/Utilities"));
class ManagedComponent {
  _data;
  _parent;
  _uniqueId;
  id;
  _onPropertyChanged = new import_ste_events.EventDispatcher();
  get onPropertyChanged() {
    return this._onPropertyChanged.asEvent();
  }
  constructor(parent, id, data) {
    this._parent = parent;
    this._data = data;
    this.id = id;
    this._uniqueId = import_Utilities.default.createRandomLowerId(8);
    this.handlePropertyChanged = this.handlePropertyChanged.bind(this);
  }
  getData() {
    return this._data;
  }
  getBaseValue() {
    return this._data;
  }
  setData(newData) {
    if (import_Utilities.default.isUsableAsObjectKey(this.id)) {
      this._parent[this.id] = newData;
    }
    this._data = newData;
  }
  setBaseValue(value) {
    this._parent[this.id] = value;
    this._data = value;
  }
  handlePropertyChanged(props, property, newValue, updatedObject) {
    if (updatedObject) {
      this.setData(updatedObject);
    }
  }
  getProperty(id) {
    if (!this._data) {
      return void 0;
    }
    return this._data[id];
  }
  setProperty(propertyId, value) {
    if (typeof this._data === "string" || typeof this._data === "number" || typeof this._data === "boolean" || Array.isArray(this._data)) {
      if (typeof this._data === typeof value || typeof value === "object") {
        this.setData(value);
        return;
      } else {
        this.setData({
          value: this._data
        });
      }
    }
    if (this._data && import_Utilities.default.isUsableAsObjectKey(propertyId)) {
      this._data[propertyId] = value;
    }
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ManagedComponent
});
//# sourceMappingURL=ManagedComponent.js.map
