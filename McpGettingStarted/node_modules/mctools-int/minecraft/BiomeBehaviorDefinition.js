"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var BiomeBehaviorDefinition_exports = {};
__export(BiomeBehaviorDefinition_exports, {
  default: () => BiomeBehaviorDefinition
});
module.exports = __toCommonJS(BiomeBehaviorDefinition_exports);
var import_Utilities = __toESM(require("../core/Utilities"));
var import_ste_events = require("ste-events");
var import_Log = __toESM(require("../core/Log"));
var import_ManagedComponent = require("./ManagedComponent");
var import_IProjectItemData = require("../app/IProjectItemData");
var import_MinecraftDefinitions = __toESM(require("./MinecraftDefinitions"));
class BiomeBehaviorDefinition {
  _typeId = "";
  _file;
  _id;
  _isLoaded = false;
  _data;
  _managed = {};
  _onLoaded = new import_ste_events.EventDispatcher();
  _onComponentAdded = new import_ste_events.EventDispatcher();
  _onComponentRemoved = new import_ste_events.EventDispatcher();
  _onComponentChanged = new import_ste_events.EventDispatcher();
  get data() {
    return this._data;
  }
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  get onComponentAdded() {
    return this._onComponentAdded.asEvent();
  }
  get onComponentRemoved() {
    return this._onComponentRemoved.asEvent();
  }
  get onComponentChanged() {
    return this._onComponentChanged.asEvent();
  }
  constructor(typeId) {
    if (typeId) {
      this._typeId = typeId;
      this._id = typeId;
    }
  }
  get typeId() {
    return this._typeId;
  }
  set typeId(newId) {
    this._typeId = newId;
    this._id = newId;
  }
  get id() {
    return this._id || this._typeId || "";
  }
  set id(newId) {
    this._id = newId;
    this._typeId = newId;
  }
  _ensureBiomeDataInitialized() {
    if (!this._data) {
      this._data = {
        format_version: "1.16.0",
        "minecraft:biome": {
          description: {
            identifier: this._typeId
          },
          components: {}
        }
      };
    }
    if (!this._data["minecraft:biome"]) {
      this._data["minecraft:biome"] = {
        description: {
          identifier: this._typeId
        },
        components: {}
      };
    }
    if (!this._data["minecraft:biome"].description) {
      this._data["minecraft:biome"].description = {
        identifier: this._typeId
      };
    }
    if (!this._data["minecraft:biome"].components) {
      this._data["minecraft:biome"].components = {};
    }
  }
  ensureComponent(id, defaultData) {
    const comp = this.getComponent(id);
    if (comp) {
      return comp;
    }
    return this.addComponent(id, defaultData);
  }
  getComponent(id) {
    if (this._data === void 0) {
      return void 0;
    }
    if (!this._managed[id]) {
      if (this._data["minecraft:biome"]?.components) {
        const comp = this._data["minecraft:biome"].components[id];
        if (comp) {
          this._managed[id] = new import_ManagedComponent.ManagedComponent(this._data["minecraft:biome"].components, id, comp);
        }
      }
    }
    return this._managed[id];
  }
  notifyComponentUpdated(id) {
    const component = this.getComponent(id);
    if (component === void 0) {
      import_Log.default.unexpectedUndefined("BDNCU");
    } else {
      this._onComponentChanged.dispatch(this, component);
    }
  }
  getAllComponents() {
    return this.getComponents();
  }
  getComponents() {
    const componentSet = [];
    if (this._data !== void 0 && this._data["minecraft:biome"]?.components) {
      for (const componentName in this._data["minecraft:biome"].components) {
        const component = this.getComponent(componentName);
        if (component !== void 0) {
          componentSet.push(component);
        }
      }
    }
    return componentSet;
  }
  addComponent(id, componentOrData) {
    this._ensureBiomeDataInitialized();
    const biomeData = this._data;
    const mc = componentOrData instanceof import_ManagedComponent.ManagedComponent ? componentOrData : new import_ManagedComponent.ManagedComponent(biomeData["minecraft:biome"].components, id, componentOrData);
    biomeData["minecraft:biome"].components[id] = mc.getData();
    this._managed[id] = mc;
    this._onComponentAdded.dispatch(this, mc);
    return mc;
  }
  removeComponent(id) {
    if (this._data === void 0 || !this._data["minecraft:biome"]?.components) {
      return;
    }
    const newComponents = {};
    const newManagedComponents = {};
    for (const name in this._data["minecraft:biome"].components) {
      if (name !== id) {
        if (import_Utilities.default.isUsableAsObjectKey(name)) {
          const component = this._data["minecraft:biome"].components[name];
          newComponents[name] = component;
        }
      }
    }
    for (const name in this._managed) {
      if (name !== id) {
        newManagedComponents[name] = this._managed[name];
      }
    }
    this._data["minecraft:biome"].components = newComponents;
    this._managed = newManagedComponents;
    this._onComponentRemoved.dispatch(this, id);
  }
  static async ensureOnFile(file) {
    if (file.manager === void 0) {
      const bd = new BiomeBehaviorDefinition();
      bd._file = file;
      if (!bd.isLoaded) {
        await bd.load();
      }
      file.manager = bd;
    }
    if (file.manager instanceof BiomeBehaviorDefinition) {
      return file.manager;
    }
    return void 0;
  }
  async persist() {
    if (this._file === void 0) {
      return false;
    }
    import_Log.default.assert(this._data !== null, "BBDP");
    if (!this._data) {
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._data);
  }
  async load() {
    if (this._file === void 0) {
      return;
    }
    if (this._isLoaded) {
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    const fileContent = this._file.content;
    if (fileContent === null || fileContent instanceof Uint8Array) {
      this._isLoaded = true;
      this._onLoaded.dispatch(this, this);
      return;
    }
    try {
      this._data = JSON.parse(fileContent);
      if (this._data && this._data["minecraft:biome"]?.description?.identifier) {
        this._typeId = this._data["minecraft:biome"].description.identifier;
        this._id = this._typeId;
      }
    } catch (e) {
      import_Log.default.error("Could not load biome definition: " + e);
    }
    this._isLoaded = true;
    this._onLoaded.dispatch(this, this);
  }
  async addChildItems(project, item) {
    const itemsCopy = project.getItemsCopy();
    for (const candItem of itemsCopy) {
      if (candItem.itemType === import_IProjectItemData.ProjectItemType.biomeResource) {
        const biomeResourceDef = await import_MinecraftDefinitions.default.get(candItem);
        if (biomeResourceDef && biomeResourceDef.id === this.id) {
          item.addChildItem(candItem);
        }
      }
    }
  }
  static getComponentFromBaseFileName(name) {
    let canonName = name;
    if (canonName.startsWith("minecraft_")) {
      canonName = canonName.substring(10);
    }
    return canonName;
  }
  get shortId() {
    if (this._typeId && this._typeId.indexOf(":") >= 0) {
      return this._typeId.substring(this._typeId.indexOf(":") + 1);
    }
    return this._typeId;
  }
  get namespace() {
    if (this._typeId && this._typeId.indexOf(":") >= 0) {
      return this._typeId.substring(0, this._typeId.indexOf(":"));
    }
    return void 0;
  }
  ensureDefinition() {
    this._ensureBiomeDataInitialized();
    if (this._data && this._data["minecraft:biome"]?.description) {
      this._data["minecraft:biome"].description.identifier = this._typeId;
    }
  }
}
//# sourceMappingURL=BiomeBehaviorDefinition.js.map
