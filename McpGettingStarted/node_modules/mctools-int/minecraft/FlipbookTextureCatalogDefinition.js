"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var FlipbookTextureCatalogDefinition_exports = {};
__export(FlipbookTextureCatalogDefinition_exports, {
  default: () => FlipbookTextureCatalogDefinition
});
module.exports = __toCommonJS(FlipbookTextureCatalogDefinition_exports);
var import_Log = __toESM(require("../core/Log"));
var import_ste_events = require("ste-events");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_Database = __toESM(require("./Database"));
class FlipbookTextureCatalogDefinition {
  _data;
  _file;
  _isLoaded = false;
  _loadedWithComments = false;
  _onLoaded = new import_ste_events.EventDispatcher();
  id;
  get data() {
    return this._data;
  }
  get texturesList() {
    if (!this._data || !Array.isArray(this._data)) {
      return void 0;
    }
    const textureList = [];
    for (const flipbookTexture of this._data) {
      const texturePath = flipbookTexture.flipbook_texture;
      if (texturePath) {
        textureList.push(texturePath.toLowerCase());
      }
    }
    return textureList;
  }
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  static async ensureOnFile(file, loadHandler) {
    let et;
    if (file.manager === void 0) {
      et = new FlipbookTextureCatalogDefinition();
      et.file = file;
      file.manager = et;
    }
    if (file.manager !== void 0 && file.manager instanceof FlipbookTextureCatalogDefinition) {
      et = file.manager;
      if (!et.isLoaded) {
        if (loadHandler) {
          et.onLoaded.subscribe(loadHandler);
        }
        await et.load();
      }
    }
    return et;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._data);
  }
  /**
   * Loads the definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("FBTCDF");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._loadedWithComments = preserveComments;
      this._onLoaded.dispatch(this, this);
      return;
    }
    let data = [];
    let result = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      data = result;
    }
    this._data = data;
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
    this._onLoaded.dispatch(this, this);
  }
  async deleteLinkToChild(rel) {
    let packRootFolder = this.getPackRootFolder();
    if (this._data === void 0) {
      if (!this.isLoaded) {
        await this.load();
      }
    }
    if (!this._data || !Array.isArray(this._data)) {
      return;
    }
    if (rel.childItem.itemType === import_IProjectItemData.ProjectItemType.texture) {
      if (!rel.childItem.isContentLoaded) {
        await rel.childItem.loadContent();
      }
      if (rel.childItem.primaryFile && packRootFolder) {
        let relativePath = import_StorageUtilities.default.getBaseRelativePath(rel.childItem.primaryFile, packRootFolder);
        if (relativePath) {
          let newFlipbookTextures = [];
          for (const flipbookTexture of this._data) {
            if (flipbookTexture.flipbook_texture !== relativePath) {
              newFlipbookTextures.push(flipbookTexture);
            }
          }
          this._data = newFlipbookTextures;
        }
      }
    }
    this.persist();
  }
  getTexturePaths() {
    const texturePaths = [];
    if (this.data && Array.isArray(this.data)) {
      for (const flipbookResource of this.data) {
        const resource = flipbookResource.flipbook_texture;
        if (!texturePaths.includes(resource)) {
          texturePaths.push(resource);
        }
      }
    }
    return texturePaths;
  }
  getPackRootFolder() {
    let packRootFolder = void 0;
    if (this.file && this.file.parentFolder) {
      let parentFolder = this.file.parentFolder;
      packRootFolder = import_StorageUtilities.default.getParentOfParentFolderNamed("textures", parentFolder);
    }
    return packRootFolder;
  }
  async addChildItems(project, item) {
    const itemsCopy = project.getItemsCopy();
    let packRootFolder = this.getPackRootFolder();
    let textureList = this.texturesList;
    for (const candItem of itemsCopy) {
      if (candItem.itemType === import_IProjectItemData.ProjectItemType.texture && packRootFolder && textureList) {
        if (!candItem.isContentLoaded) {
          await candItem.loadContent();
        }
        if (candItem.primaryFile) {
          let relativePath = import_StorageUtilities.default.getBaseRelativePath(candItem.primaryFile, packRootFolder);
          if (relativePath) {
            if (textureList && textureList.includes(relativePath)) {
              item.addChildItem(candItem);
              textureList = import_Utilities.default.removeItemInArray(relativePath, textureList);
            }
          }
        }
      }
    }
    if (textureList) {
      for (const texturePath of textureList) {
        item.addUnfulfilledRelationship(
          texturePath,
          import_IProjectItemData.ProjectItemType.texture,
          await import_Database.default.matchesVanillaPath(texturePath)
        );
      }
    }
  }
}
//# sourceMappingURL=FlipbookTextureCatalogDefinition.js.map
