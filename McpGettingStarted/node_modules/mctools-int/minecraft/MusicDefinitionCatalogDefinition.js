"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var MusicDefinitionCatalogDefinition_exports = {};
__export(MusicDefinitionCatalogDefinition_exports, {
  default: () => MusicDefinitionCatalogDefinition
});
module.exports = __toCommonJS(MusicDefinitionCatalogDefinition_exports);
var import_Log = __toESM(require("../core/Log"));
var import_ste_events = require("ste-events");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_SoundDefinitionCatalogDefinition = __toESM(require("./SoundDefinitionCatalogDefinition"));
var import_Database = __toESM(require("./Database"));
class MusicDefinitionCatalogDefinition {
  _data;
  _file;
  _isLoaded = false;
  _loadedWithComments = false;
  _onLoaded = new import_ste_events.EventDispatcher();
  id;
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  get musicDefinitionNameList() {
    if (!this._data) {
      return void 0;
    }
    const musicDefNameList = [];
    for (const key in this._data) {
      musicDefNameList.push(key);
    }
    return musicDefNameList;
  }
  get musicDefinitionEventNameList() {
    if (!this._data) {
      return void 0;
    }
    const musicDefNameList = [];
    for (const key in this._data) {
      const def = this._data[key];
      if (def && def.event_name) {
        if (!musicDefNameList.includes(def.event_name)) {
          musicDefNameList.push(def.event_name);
        }
      }
    }
    return musicDefNameList;
  }
  static async ensureOnFile(file, loadHandler) {
    let et;
    if (file.manager === void 0) {
      et = new MusicDefinitionCatalogDefinition();
      et.file = file;
      file.manager = et;
    }
    if (file.manager !== void 0 && file.manager instanceof MusicDefinitionCatalogDefinition) {
      et = file.manager;
      if (!et.isLoaded && loadHandler) {
        et.onLoaded.subscribe(loadHandler);
      }
      await et.load();
    }
    return et;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    if (!this._data) {
      import_Log.default.unexpectedUndefined("MUSP");
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._data);
  }
  /**
   * Loads the definition from the file.
   * @param preserveComments If true, uses comment-preserving JSON parsing for edit/save cycles.
   *                         If false (default), uses efficient standard JSON parsing.
   *                         Can be called again with true to "upgrade" a read-only load to read/write.
   */
  async load(preserveComments = false) {
    if (this._isLoaded && this._loadedWithComments) {
      return;
    }
    if (this._isLoaded && !preserveComments) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("TTCDF");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._loadedWithComments = preserveComments;
      this._onLoaded.dispatch(this, this);
      return;
    }
    let data = {};
    let result = preserveComments ? import_StorageUtilities.default.getJsonObjectWithComments(this._file) : import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      data = result;
    }
    this._data = data;
    this._isLoaded = true;
    this._loadedWithComments = preserveComments;
    this._onLoaded.dispatch(this, this);
  }
  async addChildItems(project, item) {
    const itemsCopy = project.getItemsCopy();
    let musicDefList = this.musicDefinitionEventNameList;
    for (const candItem of itemsCopy) {
      if (candItem.itemType === import_IProjectItemData.ProjectItemType.soundDefinitionCatalog && musicDefList) {
        if (!candItem.isContentLoaded) {
          await candItem.loadContent();
        }
        if (candItem.primaryFile) {
          const soundDef = await import_SoundDefinitionCatalogDefinition.default.ensureOnFile(candItem.primaryFile);
          const soundSetNames = soundDef?.getSoundDefinitionSetNameList();
          if (soundSetNames) {
            for (const musicDefName of musicDefList) {
              if (soundSetNames.includes(musicDefName)) {
                item.addChildItem(candItem);
                const nextMusicDefNames = [];
                for (const texturePath of musicDefList) {
                  if (texturePath !== musicDefName) {
                    nextMusicDefNames.push(texturePath);
                  }
                }
                musicDefList = nextMusicDefNames;
              }
            }
          }
        }
      }
    }
    if (musicDefList) {
      for (const musicDef of musicDefList) {
        item.addUnfulfilledRelationship(
          musicDef,
          import_IProjectItemData.ProjectItemType.soundDefinitionCatalog,
          await import_Database.default.isVanillaToken(musicDef)
        );
      }
    }
  }
}
//# sourceMappingURL=MusicDefinitionCatalogDefinition.js.map
