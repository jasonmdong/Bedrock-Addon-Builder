"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var PngEncoder_exports = {};
__export(PngEncoder_exports, {
  default: () => PngEncoder
});
module.exports = __toCommonJS(PngEncoder_exports);
var import_ImageCodec = __toESM(require("../core/ImageCodec"));
var import_Utilities = __toESM(require("../core/Utilities"));
const PLACEHOLDER_TEXTURES = {
  // 64x64 gray solid for entities
  entity: "iVBORw0KGgoAAAANSUhEUgAAAEAAAABACAYAAACqaXHeAAAAeElEQVR4AeXBAQEAMAyDMI7yOt+FkLxtR5jESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzESZzEfbNUAv/+T/uYAAAAAElFTkSuQmCC",
  // 16x16 gray solid for blocks
  block: "iVBORw0KGgoAAAANSUhEUgAAABAAAAAQCAYAAAAf8/9hAAAALElEQVR4AaXBAQEAMAyDMI7yOt9FkLxtRyCRRBJJJJFEEkkkkUQSSSSRRBJ9YpoCnyiF6V0AAAAASUVORK5CYII=",
  // 16x16 light gray solid for items
  item: "iVBORw0KGgoAAAANSUhEUgAAABAAAAAQCAYAAAAf8/9hAAAALUlEQVR4AaXBAQEAMAyDMI5/EXW6iyB5245AIokkkkgiiSSSSCKJJJJIIokk+ufpAv+MBfj8AAAAAElFTkSuQmCC"
};
class PngEncoder {
  /**
   * Encode RGBA pixel data to PNG format.
   * Delegates to ImageCodec.encodeToPngSync.
   *
   * @param pixels RGBA pixel data (4 bytes per pixel)
   * @param width Image width in pixels
   * @param height Image height in pixels
   * @returns PNG data as Uint8Array, or undefined on error
   * @deprecated Use ImageCodec.encodeToPngSync() directly.
   */
  static encodeRgbaToPng(pixels, width, height) {
    return import_ImageCodec.default.encodeToPngSync(pixels, width, height);
  }
  /**
   * Create a simple solid-color texture.
   *
   * @param width Texture width
   * @param height Texture height
   * @param hexColor Hex color (e.g., "#FF0000")
   * @returns PNG data as Uint8Array
   */
  static createSolidColorPng(width, height, hexColor) {
    const h = hexColor.startsWith("#") ? hexColor.slice(1) : hexColor;
    const r = parseInt(h.slice(0, 2), 16) || 128;
    const g = parseInt(h.slice(2, 4), 16) || 128;
    const b = parseInt(h.slice(4, 6), 16) || 128;
    const decoded = import_ImageCodec.default.createSolidColor(width, height, r, g, b, 255);
    return import_ImageCodec.default.encodeToPngSync(decoded.pixels, decoded.width, decoded.height);
  }
  /**
   * Create a checkerboard pattern texture with two colors.
   *
   * @param width Texture width
   * @param height Texture height
   * @param primaryHex Primary color hex
   * @param secondaryHex Secondary color hex
   * @param cellSize Size of each checkerboard cell
   * @returns PNG data as Uint8Array
   */
  static createCheckerboardPng(width, height, primaryHex, secondaryHex, cellSize = 8) {
    const pixels = new Uint8Array(width * height * 4);
    const parseHex = (hex) => {
      const h = hex.startsWith("#") ? hex.slice(1) : hex;
      return {
        r: parseInt(h.slice(0, 2), 16) || 128,
        g: parseInt(h.slice(2, 4), 16) || 128,
        b: parseInt(h.slice(4, 6), 16) || 128
      };
    };
    const primary = parseHex(primaryHex);
    const secondary = parseHex(secondaryHex);
    for (let y = 0; y < height; y++) {
      for (let x = 0; x < width; x++) {
        const idx = (y * width + x) * 4;
        const isSecondary = Math.floor(x / cellSize) % 2 === Math.floor(y / cellSize) % 2;
        const color = isSecondary ? secondary : primary;
        pixels[idx] = color.r;
        pixels[idx + 1] = color.g;
        pixels[idx + 2] = color.b;
        pixels[idx + 3] = 255;
      }
    }
    const encoded = import_ImageCodec.default.encodeToPngSync(pixels, width, height);
    if (encoded) {
      return encoded;
    }
    if (width === 64 && height === 64) {
      return PngEncoder.getPlaceholderTexture("entity");
    } else if (width === 16 && height === 16) {
      return PngEncoder.getPlaceholderTexture("block");
    }
    return PngEncoder.getPlaceholderTexture("entity");
  }
  /**
   * Get a pre-encoded placeholder texture.
   * These work in all environments without requiring runtime PNG encoding.
   *
   * @param type Type of placeholder: "entity" (64x64), "block" (16x16), or "item" (16x16)
   * @returns PNG data as Uint8Array
   */
  static getPlaceholderTexture(type) {
    let base64;
    switch (type) {
      case "entity":
        base64 = PLACEHOLDER_TEXTURES.entity;
        break;
      case "block":
        base64 = PLACEHOLDER_TEXTURES.block;
        break;
      case "item":
        base64 = PLACEHOLDER_TEXTURES.item;
        break;
      default:
        base64 = PLACEHOLDER_TEXTURES.entity;
    }
    return import_Utilities.default.base64ToUint8Array(base64);
  }
}
//# sourceMappingURL=PngEncoder.js.map
