"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var MinecraftUtilities_exports = {};
__export(MinecraftUtilities_exports, {
  MINECRAFT_BASE_PORT: () => MINECRAFT_BASE_PORT,
  MINECRAFT_DEBUG_PORT_OFFSET: () => MINECRAFT_DEBUG_PORT_OFFSET,
  MINECRAFT_PORT_INCREMENT: () => MINECRAFT_PORT_INCREMENT,
  default: () => MinecraftUtilities
});
module.exports = __toCommonJS(MinecraftUtilities_exports);
var import_Utilities = __toESM(require("../core/Utilities"));
var import_Log = __toESM(require("../core/Log"));
var import_VanillaProjectManager = __toESM(require("./VanillaProjectManager"));
var import_CreatorTools = require("../app/CreatorTools");
const MINECRAFT_BASE_PORT = 19132;
const MINECRAFT_PORT_INCREMENT = 32;
const MINECRAFT_DEBUG_PORT_OFFSET = 12;
class MinecraftUtilities {
  /**
   * Get the Minecraft UDP port for a given slot number.
   * Slot 0 = 19132, Slot 1 = 19164, Slot 2 = 19196, etc.
   * @param slotNumber The slot number (0-based)
   * @returns The UDP port for Minecraft connections
   */
  static getPortForSlot(slotNumber) {
    return MINECRAFT_BASE_PORT + slotNumber * MINECRAFT_PORT_INCREMENT;
  }
  /**
   * Get the slot number from a Minecraft UDP port.
   * @param port The UDP port
   * @returns The slot number (0-based)
   */
  static getSlotFromPort(port) {
    return Math.floor((port - MINECRAFT_BASE_PORT) / MINECRAFT_PORT_INCREMENT);
  }
  /**
   * Get the script debugger port for a given slot number.
   * The debug port is the base port + 12 for each slot.
   * Slot 0 = 19144, Slot 1 = 19176, Slot 2 = 19208, etc.
   * @param slotNumber The slot number (0-based)
   * @returns The debug port for the script debugger
   */
  static getDebugPortForSlot(slotNumber) {
    return MinecraftUtilities.getPortForSlot(slotNumber) + MINECRAFT_DEBUG_PORT_OFFSET;
  }
  static isReloadableSetOfChanges(differenceSet) {
    return differenceSet.hasFileOnlyOfExtension("js", "ts", "map") && !differenceSet.getHasDeletions();
  }
  static removeSubTypeExtensionFromName(name) {
    let canonId = name.trim();
    if (name.endsWith(".json")) {
      canonId = canonId.substring(0, canonId.length - 5);
    }
    const lastPeriod = name.lastIndexOf(".");
    if (lastPeriod >= 0) {
      canonId = canonId.substring(0, lastPeriod);
    }
    return canonId;
  }
  static getServerStatusFromState(state) {
    switch (state) {
      case import_CreatorTools.CreatorToolsMinecraftState.stopped:
        return "stopped";
      case import_CreatorTools.CreatorToolsMinecraftState.error:
        return "error";
      case import_CreatorTools.CreatorToolsMinecraftState.starting:
        return "starting";
      case import_CreatorTools.CreatorToolsMinecraftState.preparing:
        return "preparing";
      case import_CreatorTools.CreatorToolsMinecraftState.started:
        return "running";
      case import_CreatorTools.CreatorToolsMinecraftState.stopping:
        return "stopping";
      case import_CreatorTools.CreatorToolsMinecraftState.disconnected:
        return "disconnected";
      case import_CreatorTools.CreatorToolsMinecraftState.initializing:
        return "initializing";
      case import_CreatorTools.CreatorToolsMinecraftState.initialized:
        return "initialized";
      case import_CreatorTools.CreatorToolsMinecraftState.prepared:
        return "prepared";
      case import_CreatorTools.CreatorToolsMinecraftState.newMinecraft:
        return "new minecraft";
      case import_CreatorTools.CreatorToolsMinecraftState.none:
        return "none";
      default:
        return "unknown";
    }
  }
  static getIsBuiltIn(eventId) {
    return eventId === "minecraft:entity_spawned" || eventId === "minecraft:entity_born" || eventId === "minecraft:transformed" || eventId === "minecraft:on_prime";
  }
  static getAfterPackPath(path) {
    const hashIndex = path.indexOf("#");
    if (hashIndex >= 0) {
      path = path.substring(hashIndex + 1);
    }
    return path;
  }
  static getNamespacedIdNamspace(name) {
    let canonId = name.toLowerCase().trim();
    let colonIndex = canonId.indexOf(":");
    if (colonIndex >= 0) {
      canonId = canonId.substring(0, colonIndex);
    }
    return canonId;
  }
  static getNamespacedIdName(name) {
    let canonId = name.toLowerCase().trim();
    let colonIndex = canonId.indexOf(":");
    if (colonIndex >= 0) {
      canonId = canonId.substring(colonIndex + 1);
    }
    return canonId;
  }
  static pathLooksLikeSkinPackContainerName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("skin_packs") >= 0 || pathCanon.indexOf("_sps") >= 0 || pathCanon.indexOf("/sps_") >= 0 || pathCanon.endsWith("sps/") || pathCanon === "/sps/" || pathCanon === "/sp/") {
      return true;
    }
    return false;
  }
  static pathLooksLikeBehaviorPackContainerName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("behavior_packs") >= 0 || pathCanon.indexOf("_bps") >= 0 || pathCanon.indexOf("/bps_") >= 0 || pathCanon.endsWith("bps/") || pathCanon === "/bps/" || pathCanon === "/bp/") {
      return true;
    }
    return false;
  }
  static pathLooksLikeDesignPackContainerName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("design_packs") >= 0 || pathCanon.indexOf("_dps") >= 0 || pathCanon.indexOf("/dps_") >= 0 || pathCanon.endsWith("dps/") || pathCanon === "/dps/" || pathCanon === "/dp/") {
      return true;
    }
    return false;
  }
  static pathLooksLikeResourcePackContainerName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("resource_packs") >= 0 || pathCanon.indexOf("_rps") >= 0 || pathCanon.indexOf("/rps_") >= 0 || pathCanon.endsWith("rps/") || pathCanon === "/rps/" || pathCanon === "/rp/") {
      return true;
    }
    return false;
  }
  static pathLooksLikePackContainerName(path) {
    return this.pathLooksLikeBehaviorPackContainerName(path) || this.pathLooksLikeResourcePackContainerName(path) || this.pathLooksLikeResourcePackContainerName(path) || this.pathLooksLikeSkinPackContainerName(path);
  }
  static pathLooksLikeDesignPackName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("/design") >= 0 || pathCanon.indexOf("design_pack") >= 0 || pathCanon.indexOf("_dp") >= 0 || // bp is uncommon as the start of a word
    pathCanon.indexOf("/dp_") >= 0 || pathCanon.indexOf("/dp/") >= 0) {
      return true;
    }
    return false;
  }
  static pathLooksLikeBehaviorPackName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("/behavior") >= 0 || pathCanon.indexOf("behavior_pack") >= 0 || pathCanon.indexOf("_bp") >= 0 || // bp is uncommon as the start of a word
    pathCanon.indexOf("/bp_") >= 0 || pathCanon.indexOf("/bp/") >= 0) {
      return true;
    }
    return false;
  }
  static isBedrockItem(itemId) {
    if (itemId.indexOf("element") >= 0) {
      return false;
    }
    return true;
  }
  static getBlockDefaultTexturePath(blockId) {
    let blockCat = import_VanillaProjectManager.default.getBlocksCatalogDirect();
    let textureCat = import_VanillaProjectManager.default.getTerrainTexturesCatalogDirect();
    if (!blockCat || !textureCat) {
      return void 0;
    }
    const blockTextureId = blockCat.getDefaultTextureId(blockId);
    if (!blockTextureId) {
      return void 0;
    }
    return textureCat.getDefaultTexturePath(blockTextureId);
  }
  static shortenFilterDescription(descript) {
    descript = descript.replace("Returns true if ", "");
    descript = descript.replace("True if ", "");
    descript = descript.replace("Returns true when ", "");
    descript = descript.replace("Tests the ", "");
    descript = descript.replace("Tests if ", "");
    descript = descript.replace("Tests whether ", "");
    descript = descript.replace("Tests for ", "");
    descript = descript.replace("Returns ", "");
    if (descript.startsWith("the ")) {
      descript = descript.substring(4);
    }
    descript = import_Utilities.default.ensureLooksLikeSentence(descript);
    return descript;
  }
  static clearCommonTerms(path) {
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/resource_packs/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/behavior_packs/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/design_packs/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/skin_packs/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/resource_pack/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/behavior_pack/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/design_pack/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/skin_pack/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/behavior/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/resource/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/design/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/static-assets/", "/");
    path = import_Utilities.default.replaceAllCaseInsensitive(path, "/zips/", "/");
    path = path.replace(/\/\//gi, "/");
    return path;
  }
  static pathLooksLikeResourcePackName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("/resource") >= 0 || pathCanon.indexOf("resource_pack") >= 0 || pathCanon.indexOf("_rp") >= 0 || // rp is uncommon as the start of a word
    pathCanon.indexOf("/rp_") >= 0 || pathCanon.indexOf("/rp/") >= 0) {
      return true;
    }
    return false;
  }
  static pathLooksLikeSkinPackName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("/skin") >= 0 || pathCanon.indexOf("skin_pack") >= 0) {
      return true;
    }
    return false;
  }
  static pathLooksLikePersonaPackName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("/persona/") >= 0) {
      return true;
    }
    return false;
  }
  static pathLooksLikeWorldFolderName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("/world") >= 0) {
      return true;
    }
    return false;
  }
  static pathLooksLikeSubPacksFolderName(path) {
    let pathCanon = path.toLowerCase();
    pathCanon = pathCanon.replace(/\\/gi, "/");
    pathCanon = pathCanon.replace(/ /gi, "_");
    pathCanon = import_Utilities.default.ensureEndsWithSlash(import_Utilities.default.ensureStartsWithSlash(pathCanon));
    if (pathCanon.indexOf("/subpacks") >= 0) {
      return true;
    }
    return false;
  }
  static pathLooksLikePackName(path) {
    return this.pathLooksLikeBehaviorPackName(path) || this.pathLooksLikeResourcePackName(path) || this.pathLooksLikeSkinPackName(path);
  }
  static replaceMinecraftPathTokens(tokenizePath) {
    if (tokenizePath.startsWith("<RPME>")) {
      tokenizePath += ".geo.json";
    } else if (tokenizePath.startsWith("<BPE>") || tokenizePath.startsWith("<BPLE>") || tokenizePath.startsWith("<BPSR>")) {
      tokenizePath += ".json";
    } else if (tokenizePath.startsWith("<RPE>")) {
      tokenizePath += ".entity.json";
    } else if (tokenizePath.startsWith("<RPAC>")) {
      tokenizePath += ".animation_controllers.json";
    } else if (tokenizePath.startsWith("<RPRC>")) {
      tokenizePath += ".render_controllers.json";
    } else if (tokenizePath.startsWith("<RPA>")) {
      tokenizePath += ".animation.json";
    } else if (tokenizePath.startsWith("<RPTE>")) {
      if (tokenizePath.indexOf(".") < 0) {
        tokenizePath += ".png";
      }
    }
    tokenizePath = tokenizePath.replace(/<RPTE>/gi, "/resource_pack/textures/entity/");
    tokenizePath = tokenizePath.replace(/<RPME>/gi, "/resource_pack/models/entity/");
    tokenizePath = tokenizePath.replace(/<RPAC>/gi, "/resource_pack/animation_controllers/");
    tokenizePath = tokenizePath.replace(/<RPRC>/gi, "/resource_pack/render_controllers/");
    tokenizePath = tokenizePath.replace(/<RPA>/gi, "/resource_pack/animations/");
    tokenizePath = tokenizePath.replace(/<RPE>/gi, "/resource_pack/entity/");
    tokenizePath = tokenizePath.replace(/<BPE>/gi, "/behavior_pack/entities/");
    tokenizePath = tokenizePath.replace(/<BPLE>/gi, "/behavior_pack/loot_tables/entities/");
    tokenizePath = tokenizePath.replace(/<BPSR>/gi, "/behavior_pack/spawn_rules/");
    return tokenizePath;
  }
  static makeNameScriptSafe(tokenName) {
    tokenName = tokenName.replace(/[^a-z0-9_]/gi, "-").replace(/\s+/g, "-");
    while (tokenName.length > 1 && !import_Utilities.default.isAlpha(tokenName.substring(0, 1))) {
      tokenName = tokenName.substring(1);
    }
    if (!import_Utilities.default.isAlpha(tokenName.substring(0, 1))) {
      tokenName = "a" + tokenName;
    }
    return tokenName;
  }
  static makeNameFolderSafe(tokenName) {
    tokenName = import_Utilities.default.replaceAll(tokenName, " ", "-").toLowerCase();
    if (tokenName.length > 16) {
      tokenName = tokenName.substring(0, 16);
    }
    return tokenName;
  }
  static getVersionArrayFrom(ver) {
    if (typeof ver === "number") {
      if (Math.floor(ver) !== ver) {
        return [Math.floor(ver), Math.floor((ver - Math.floor(ver)) * 10), 0];
      } else {
        return [ver, 0, 0];
      }
    }
    if (ver === void 0) {
      return [0, 0, 0];
    }
    if (typeof ver === "string") {
      let fvArr = ver.split(".");
      let fvArrInt = [];
      for (let i = 0; i < fvArr.length; i++) {
        try {
          const num = parseInt(fvArr[i]);
          if (isNaN(num)) {
            return [0, 0, 0];
          }
          fvArrInt.push(num);
        } catch (e) {
        }
      }
      while (fvArrInt.length < 3) {
        fvArrInt.push(0);
      }
      return fvArrInt;
    }
    return ver;
  }
  static getVersionNumber(ver) {
    if (ver === void 0) {
      return 0;
    }
    let fvArr = ver.split(".");
    let fvArrInt = [];
    for (let i = 0; i < fvArr.length; i++) {
      try {
        const num = parseInt(fvArr[i]);
        if (isNaN(num)) {
          return 0;
        }
        fvArrInt.push(num);
      } catch (e) {
      }
    }
    while (fvArrInt.length < 3) {
      fvArrInt.push(0);
    }
    if (fvArrInt.length < 3) {
      return 0;
    }
    let verNum = fvArrInt[0] * 1e6 + 1e7;
    verNum += fvArrInt[1] * 1e3 + fvArrInt[2];
    return verNum;
  }
  static cleanUpScriptDescription(scriptDescription) {
    scriptDescription = import_Utilities.default.replaceAll(scriptDescription, "/*", "");
    scriptDescription = import_Utilities.default.replaceAll(scriptDescription, "*/\n", "");
    scriptDescription = import_Utilities.default.replaceAll(scriptDescription, "*\n", "");
    scriptDescription = import_Utilities.default.replaceAll(scriptDescription, "*/", "");
    scriptDescription = import_Utilities.default.replaceAll(scriptDescription, "*", "");
    scriptDescription = import_Utilities.default.replaceAll(scriptDescription, "@beta", "");
    scriptDescription = scriptDescription.trim();
    return scriptDescription;
  }
  static getIdsAndVersions(packStr) {
    const refs = [];
    const packRefStrs = packStr.split(",");
    for (const packRefStr of packRefStrs) {
      const packRefStrParts = packRefStr.split("@");
      let wasAdded = false;
      if (packRefStrParts.length === 2) {
        const versionNumberParts = packRefStrParts[1].split(".");
        if (versionNumberParts.length === 3) {
          const verNumber = [];
          for (let i = 0; i < versionNumberParts.length; i++) {
            let num = void 0;
            try {
              num = parseInt(versionNumberParts[i]);
            } catch (e) {
            }
            if (num !== void 0) {
              verNumber.push(num);
            }
          }
          if (verNumber.length === 3 && this.isValidUuid(packRefStrParts[0])) {
            refs.push({
              uuid: packRefStrParts[0],
              version: verNumber
            });
            wasAdded = true;
          }
        }
      }
      if (!wasAdded) {
        import_Log.default.fail("UUID/version is not in a good form (uuid@x.y.z) - is '" + packRefStr + "'");
      }
    }
    return refs;
  }
  static isValidUuid(uuid) {
    if (uuid.length === 36) {
      const parts = uuid.split("-");
      if (parts.length === 5 && parts[0].length === 8 && parts[1].length === 4 && parts[2].length === 4 && parts[3].length === 4 && parts[4].length === 12) {
        for (let i = 0; i < parts.length; i++) {
          for (let j = 0; j < parts[i].length; j++) {
            if (!import_Utilities.default.isAlphaNumeric(parts[i][j])) {
              return false;
            }
          }
        }
      }
    }
    return false;
  }
  static fixupJson(json) {
    json = json.replace(/([, ;}\]])0b([, ;}\]])/gi, "$1false$2");
    json = json.replace(/([, ;}\]])1b([, ;}\]])/gi, "$1true$2");
    json = json.replace(/(\d)b([, ;\]])/gi, "$1$2");
    json = json.replace(/(\d)d([, ;\]])/gi, "$1$2");
    json = json.replace(/(\d)f([, ;\]])/gi, "$1$2");
    json = json.replace(/(\d)s([, ;\]])/gi, "$1$2");
    json = json.replace(/\[I;/gi, "[");
    json = json.replace(/…/gi, "");
    return json;
  }
  static canonicalizeName(id) {
    if (id === void 0 || id === null) {
      import_Log.default.debug("canonicalizeName called with undefined/null id");
      return "";
    }
    let canonId = id.toLowerCase().trim();
    if (canonId.startsWith("minecraft:")) {
      canonId = canonId.substring(10, canonId.length);
    }
    return canonId;
  }
  static canonicalizeFullName(id) {
    if (id === void 0 || id === null) {
      import_Log.default.debug("canonicalizeFullName called with undefined/null id");
      return "";
    }
    let canonId = id.toLowerCase().trim();
    if (canonId.indexOf(":") < 0) {
      canonId = "minecraft:" + canonId;
    }
    return canonId;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  MINECRAFT_BASE_PORT,
  MINECRAFT_DEBUG_PORT_OFFSET,
  MINECRAFT_PORT_INCREMENT
});
//# sourceMappingURL=MinecraftUtilities.js.map
