"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var DocumentedCommandSet_exports = {};
__export(DocumentedCommandSet_exports, {
  default: () => DocumentedCommandSet
});
module.exports = __toCommonJS(DocumentedCommandSet_exports);
var import_ste_events = require("ste-events");
var import_DocumentedCommand = __toESM(require("./DocumentedCommand"));
var import_Project = require("../../app/Project");
var import_StorageUtilities = __toESM(require("../../storage/StorageUtilities"));
class DocumentedCommandSet {
  _file;
  _id;
  _name;
  _version;
  _isLoaded = false;
  _docFolder;
  _docCommands = {};
  commandSetDefinition;
  _onLoaded = new import_ste_events.EventDispatcher();
  get isLoaded() {
    return this._isLoaded;
  }
  get docFolder() {
    return this._docFolder;
  }
  get commands() {
    return this._docCommands;
  }
  get file() {
    return this._file;
  }
  set file(newFile) {
    this._file = newFile;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  get name() {
    if (this.commandSetDefinition) {
      return this.commandSetDefinition.name;
    }
    return this._name;
  }
  get id() {
    return this._id;
  }
  set id(newId) {
    this._id = newId;
    if (newId) {
      const underscore = newId.lastIndexOf("_");
      if (underscore >= 0 && underscore < newId.length - 2) {
        this._name = newId.substring(0, underscore);
        this._version = newId.substring(underscore + 1);
      } else {
        this._name = newId;
        this._version = "";
      }
    }
  }
  constructor(docFolder) {
    this._docFolder = docFolder;
  }
  getEnum(name) {
    const enums = this.commandSetDefinition?.command_enums;
    if (enums) {
      name = name.toLowerCase();
      for (const enumItem of enums) {
        if (enumItem.name.toLowerCase() === name) {
          return enumItem;
        }
      }
    }
    return void 0;
  }
  static async ensureOnFile(file, docFolder, loadHandler) {
    let dt;
    if (file.manager === void 0) {
      dt = new DocumentedCommandSet(docFolder);
      dt.file = file;
      file.manager = dt;
    }
    if (file.manager !== void 0 && file.manager instanceof DocumentedCommandSet) {
      dt = file.manager;
      if (!dt.isLoaded) {
        if (loadHandler) {
          dt.onLoaded.subscribe(loadHandler);
        }
        await dt.load();
      }
    }
    return dt;
  }
  ensureDocFolder(docsFolder) {
    if (!this.name) {
      return;
    }
    let folderName = this.name;
    folderName = folderName.toLowerCase();
    if (import_Project.remappedMinecraftScriptModules[folderName]) {
      folderName = import_Project.remappedMinecraftScriptModules[folderName];
    }
    const folders = folderName.split("/");
    let curFolder = docsFolder;
    for (let i = 0; i < folders.length; i++) {
      curFolder = curFolder.ensureFolder(folders[i]);
    }
    return curFolder;
  }
  isEnumUsedInMultipleCommands(enumInstance) {
    let commandCount = 0;
    for (const commandName in this.commands) {
      const command = this.commands[commandName];
      if (command) {
        let isInCommand = false;
        const overloads = command.getOverloads();
        for (const overload of overloads) {
          for (const param of overload.params) {
            if (param.type && param.type.name.toUpperCase() === enumInstance.name.toUpperCase()) {
              isInCommand = true;
            }
          }
        }
        if (isInCommand) {
          commandCount++;
          if (commandCount > 1) {
            return true;
          }
        }
      }
    }
    return false;
  }
  async persist() {
    if (this._file === void 0) {
      return false;
    }
    let didPersist = false;
    for (const docCommandName in this._docCommands) {
      const docCommand = this._docCommands[docCommandName];
      if (await docCommand.persist()) {
        didPersist = true;
      }
    }
    return didPersist;
  }
  async load() {
    if (this._file === void 0 || this._isLoaded) {
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (this._file.content === null || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._onLoaded.dispatch(this, this);
      return;
    }
    this.id = this._file.name;
    this.commandSetDefinition = import_StorageUtilities.default.getJsonObject(this._file);
    this._docCommands = {};
    if (this.commandSetDefinition) {
      for (const docClass of this.commandSetDefinition?.commands) {
        this._docCommands[docClass.name] = new import_DocumentedCommand.default(this, docClass);
      }
    }
    this._isLoaded = true;
  }
}
//# sourceMappingURL=DocumentedCommandSet.js.map
