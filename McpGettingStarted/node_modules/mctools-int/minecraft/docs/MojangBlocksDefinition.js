"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var MojangBlocksDefinition_exports = {};
__export(MojangBlocksDefinition_exports, {
  default: () => MojangBlocksDefinition
});
module.exports = __toCommonJS(MojangBlocksDefinition_exports);
var import_Log = __toESM(require("../../core/Log"));
var import_ste_events = require("ste-events");
var import_StorageUtilities = __toESM(require("../../storage/StorageUtilities"));
var import_Database = __toESM(require("../Database"));
class MojangBlocksDefinition {
  _data;
  _file;
  _isLoaded = false;
  _onLoaded = new import_ste_events.EventDispatcher();
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  static async load(isPreview) {
    let metadataFolder = void 0;
    if (isPreview) {
      metadataFolder = await import_Database.default.loadPreviewMetadataFolder();
    } else {
      metadataFolder = await import_Database.default.loadReleaseMetadataFolder();
    }
    if (!metadataFolder) {
      return void 0;
    }
    const blocksFile = await metadataFolder.ensureFileFromRelativePath("/vanilladata_modules/mojang_blocks.json");
    if (!blocksFile) {
      return void 0;
    }
    if (!await blocksFile.exists()) {
      return void 0;
    }
    const mojangBlocksDef = await MojangBlocksDefinition.ensureOnFile(blocksFile);
    return mojangBlocksDef;
  }
  static async ensureOnFile(file, loadHandler) {
    let mbd;
    if (file.manager === void 0) {
      mbd = new MojangBlocksDefinition();
      mbd.file = file;
      file.manager = mbd;
    }
    if (file.manager !== void 0 && file.manager instanceof MojangBlocksDefinition) {
      mbd = file.manager;
      if (!mbd.isLoaded) {
        if (loadHandler) {
          mbd.onLoaded.subscribe(loadHandler);
        }
        await mbd.loadBlocks();
      }
    }
    return mbd;
  }
  async loadBlocks() {
    if (this._isLoaded) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("LDDF");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._onLoaded.dispatch(this, this);
      return;
    }
    let data = {};
    let result = import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      data = result;
    }
    this._data = data;
    this._isLoaded = true;
    this._onLoaded.dispatch(this, this);
  }
}
//# sourceMappingURL=MojangBlocksDefinition.js.map
