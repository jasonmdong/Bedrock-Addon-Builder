"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var LegacyDocumentationDefinition_exports = {};
__export(LegacyDocumentationDefinition_exports, {
  default: () => LegacyDocumentationDefinition
});
module.exports = __toCommonJS(LegacyDocumentationDefinition_exports);
var import_Log = __toESM(require("../../core/Log"));
var import_ste_events = require("ste-events");
var import_StorageUtilities = __toESM(require("../../storage/StorageUtilities"));
var import_Database = __toESM(require("../Database"));
class LegacyDocumentationDefinition {
  _data;
  _file;
  _isLoaded = false;
  _onLoaded = new import_ste_events.EventDispatcher();
  get isLoaded() {
    return this._isLoaded;
  }
  get file() {
    return this._file;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  set file(newFile) {
    this._file = newFile;
  }
  static async loadNode(catalog, nodePath, isPreview) {
    let metadataFolder = void 0;
    if (isPreview) {
      metadataFolder = await import_Database.default.loadPreviewMetadataFolder();
    } else {
      metadataFolder = await import_Database.default.loadReleaseMetadataFolder();
    }
    if (!metadataFolder) {
      return void 0;
    }
    const docFile = await metadataFolder.ensureFileFromRelativePath("/doc_modules/" + catalog + ".json");
    if (!docFile) {
      return void 0;
    }
    if (!await docFile.exists()) {
      return void 0;
    }
    const legacyDocDef = await LegacyDocumentationDefinition.ensureOnFile(docFile);
    if (!legacyDocDef) {
      return void 0;
    }
    return legacyDocDef.getNode(nodePath);
  }
  getNode(nodePath) {
    if (!this._data) {
      return;
    }
    const nodeEntries = nodePath.split("/");
    let curNode = this._data;
    for (let nodeEntry of nodeEntries) {
      if (nodeEntry.length > 0) {
        nodeEntry = nodeEntry.toLowerCase();
        if (!curNode.nodes || curNode.nodes.length === 0) {
          return void 0;
        }
        let foundNextNode = false;
        for (const node of curNode.nodes) {
          if (!foundNextNode && (node.name && node.name.toLowerCase() === nodeEntry || node.examples_title && node.examples_title.toLowerCase() === nodeEntry)) {
            curNode = node;
            foundNextNode = true;
          }
        }
        if (!foundNextNode) {
          return void 0;
        }
      }
    }
    return curNode;
  }
  static async ensureOnFile(file, loadHandler) {
    let ldd;
    if (file.manager === void 0) {
      ldd = new LegacyDocumentationDefinition();
      ldd.file = file;
      file.manager = ldd;
    }
    if (file.manager !== void 0 && file.manager instanceof LegacyDocumentationDefinition) {
      ldd = file.manager;
      if (!ldd.isLoaded) {
        if (loadHandler) {
          ldd.onLoaded.subscribe(loadHandler);
        }
        await ldd.load();
      }
    }
    return ldd;
  }
  persist() {
    if (this._file === void 0) {
      return false;
    }
    if (!this._data) {
      return false;
    }
    return this._file.setObjectContentIfSemanticallyDifferent(this._data);
  }
  async load() {
    if (this._isLoaded) {
      return;
    }
    if (this._file === void 0) {
      import_Log.default.unexpectedUndefined("LDDF");
      return;
    }
    if (!this._file.isContentLoaded) {
      await this._file.loadContent();
    }
    if (!this._file.content || this._file.content instanceof Uint8Array) {
      this._isLoaded = true;
      this._onLoaded.dispatch(this, this);
      return;
    }
    let data = {};
    let result = import_StorageUtilities.default.getJsonObject(this._file);
    if (result) {
      data = result;
    }
    this._data = data;
    this._isLoaded = true;
    this._onLoaded.dispatch(this, this);
  }
}
//# sourceMappingURL=LegacyDocumentationDefinition.js.map
