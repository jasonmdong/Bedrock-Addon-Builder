"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var JsonUtilities_exports = {};
__export(JsonUtilities_exports, {
  CommentArrayClass: () => CommentArrayClass,
  assignJsonPreservingComments: () => assignJsonPreservingComments,
  cloneJsonWithComments: () => cloneJsonWithComments,
  default: () => JsonUtilities_default,
  hasCommentMetadata: () => hasCommentMetadata,
  jsonContentsSemanticallyEqual: () => jsonContentsSemanticallyEqual,
  jsonObjectsSemanticallyEqual: () => jsonObjectsSemanticallyEqual,
  mergeJsonPreservingComments: () => mergeJsonPreservingComments,
  parseJsonWithComments: () => parseJsonWithComments,
  stringifyJsonWithComments: () => stringifyJsonWithComments
});
module.exports = __toCommonJS(JsonUtilities_exports);
var commentJson = __toESM(require("comment-json"));
var import_Utilities = __toESM(require("./Utilities"));
const CommentArrayClass = commentJson.CommentArray;
function parseJsonWithComments(jsonString, fixContent = true) {
  if (fixContent) {
    jsonString = import_Utilities.default.fixJsonContentForCommentJson(jsonString);
  }
  return commentJson.parse(jsonString);
}
function stringifyJsonWithComments(value, space = 2) {
  return commentJson.stringify(value, null, space);
}
function assignJsonPreservingComments(target, source, keys) {
  return commentJson.assign(target, source, keys);
}
function jsonContentsSemanticallyEqual(contentA, contentB) {
  try {
    const objA = commentJson.parse(contentA, null, true);
    const objB = commentJson.parse(contentB, null, true);
    return import_Utilities.default.consistentStringify(objA) === import_Utilities.default.consistentStringify(objB);
  } catch (e) {
    return contentA === contentB;
  }
}
function jsonObjectsSemanticallyEqual(objA, objB) {
  try {
    return import_Utilities.default.consistentStringify(objA) === import_Utilities.default.consistentStringify(objB);
  } catch (e) {
    return false;
  }
}
function mergeJsonPreservingComments(original, updates) {
  for (const key of Object.keys(updates)) {
    if (key === "__proto__" || key === "constructor" || key === "prototype") {
      continue;
    }
    const value = updates[key];
    if (value !== void 0) {
      if (typeof value === "object" && value !== null && !Array.isArray(value) && typeof original[key] === "object" && original[key] !== null && !Array.isArray(original[key])) {
        mergeJsonPreservingComments(original[key], value);
      } else {
        original[key] = value;
      }
    }
  }
  return original;
}
function cloneJsonWithComments(obj) {
  const jsonString = commentJson.stringify(obj, null, 2);
  return commentJson.parse(jsonString);
}
function hasCommentMetadata(obj) {
  if (obj === null || typeof obj !== "object") {
    return false;
  }
  const symbols = Object.getOwnPropertySymbols(obj);
  return symbols.some((sym) => sym.description?.startsWith("before") || sym.description?.startsWith("after"));
}
const JsonUtilities = {
  parseJsonWithComments,
  stringifyJsonWithComments,
  assignJsonPreservingComments,
  mergeJsonPreservingComments,
  jsonContentsSemanticallyEqual,
  jsonObjectsSemanticallyEqual,
  cloneJsonWithComments,
  hasCommentMetadata,
  CommentArrayClass
};
var JsonUtilities_default = JsonUtilities;
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CommentArrayClass,
  assignJsonPreservingComments,
  cloneJsonWithComments,
  hasCommentMetadata,
  jsonContentsSemanticallyEqual,
  jsonObjectsSemanticallyEqual,
  mergeJsonPreservingComments,
  parseJsonWithComments,
  stringifyJsonWithComments
});
//# sourceMappingURL=JsonUtilities.js.map
