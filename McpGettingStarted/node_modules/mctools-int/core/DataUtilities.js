"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var DataUtilities_exports = {};
__export(DataUtilities_exports, {
  default: () => DataUtilities
});
module.exports = __toCommonJS(DataUtilities_exports);
class DataUtilities {
  static getUnsignedInteger(num1, num2, num3, num4, littleEndian) {
    let result;
    if (littleEndian) {
      result = (num4 & 255) << 24 | (num3 & 255) << 16 | (num2 & 255) << 8 | num1 & 255;
    } else {
      result = (num1 & 255) << 24 | (num2 & 255) << 16 | (num3 & 255) << 8 | num4 & 255;
    }
    return result >>> 0;
  }
  static writeUnsignedInteger(data, index, value, littleEndian) {
    const view = new DataView(data.buffer, index, 4);
    view.setUint32(0, value, littleEndian);
  }
  static getFloat(num1, num2, num3, num4, littleEndian) {
    let intValue;
    if (littleEndian) {
      intValue = (num4 & 255) << 24 | (num3 & 255) << 16 | (num2 & 255) << 8 | num1 & 255;
    } else {
      intValue = (num1 & 255) << 24 | (num2 & 255) << 16 | (num3 & 255) << 8 | num4 & 255;
    }
    const floatArray = new Float32Array(1);
    const intArray = new Uint32Array(floatArray.buffer);
    intArray[0] = intValue >>> 0;
    return floatArray[0];
  }
  static getSignedLong(num1, num2, num3, num4, num5, num6, num7, num8, littleEndian) {
    const bytes = [num1, num2, num3, num4, num5, num6, num7, num8];
    let result = 0n;
    if (littleEndian) {
      for (let i = 7; i >= 0; i--) {
        result = result << 8n | BigInt(bytes[i] & 255);
      }
    } else {
      for (let i = 0; i < 8; i++) {
        result = result << 8n | BigInt(bytes[i] & 255);
      }
    }
    if (result >= 0x8000000000000000n) {
      result = result - 0x10000000000000000n;
    }
    return result;
  }
  static getVarInt(data, index) {
    let bytesRead = 0;
    let result = 0;
    let currentByte = 0;
    do {
      currentByte = data[index];
      const currentByteVal = currentByte & 127;
      result |= currentByteVal << 7 * bytesRead;
      bytesRead++;
      if (bytesRead > 5) {
        throw new Error("VarInt is unexpectedly large");
      }
    } while ((currentByte & 128) !== 0);
    return {
      value: result,
      bytesRead
    };
  }
  static getSignedShort(num1, num2, littleEndian) {
    let value;
    if (littleEndian) {
      value = (num2 & 255) << 8 | num1 & 255;
    } else {
      value = (num1 & 255) << 8 | num2 & 255;
    }
    return value & 32768 ? value - 65536 : value;
  }
  static getUnsignedShort(num1, num2, littleEndian) {
    if (littleEndian) {
      return (num2 & 255) << 8 | num1 & 255;
    } else {
      return (num1 & 255) << 8 | num2 & 255;
    }
  }
  static getSignedInteger(num1, num2, num3, num4, littleEndian) {
    let value;
    if (littleEndian) {
      value = (num4 & 255) << 24 | (num3 & 255) << 16 | (num2 & 255) << 8 | num1 & 255;
    } else {
      value = (num1 & 255) << 24 | (num2 & 255) << 16 | (num3 & 255) << 8 | num4 & 255;
    }
    return value | 0;
  }
  static getSignedByte(value) {
    return value & 128 ? value - 256 : value;
  }
  static getSignedDouble(num1, num2, num3, num4, num5, num6, num7, num8, littleEndian) {
    const buffer = new ArrayBuffer(8);
    const bytes = new Uint8Array(buffer);
    bytes[0] = num1;
    bytes[1] = num2;
    bytes[2] = num3;
    bytes[3] = num4;
    bytes[4] = num5;
    bytes[5] = num6;
    bytes[6] = num7;
    bytes[7] = num8;
    const view = new DataView(buffer);
    return view.getFloat64(0, littleEndian);
  }
}
//# sourceMappingURL=DataUtilities.js.map
