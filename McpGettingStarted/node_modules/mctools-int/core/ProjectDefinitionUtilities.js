"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ProjectDefinitionUtilities_exports = {};
__export(ProjectDefinitionUtilities_exports, {
  ProjectDefinitionUtilities: () => ProjectDefinitionUtilities
});
module.exports = __toCommonJS(ProjectDefinitionUtilities_exports);
var import_Database = __toESM(require("../minecraft/Database"));
class ProjectDefinitionUtilities {
  static getVanillaBlockTextureById(blockId, side) {
    let blockType = import_Database.default.ensureBlockType(blockId);
    if (!blockType) {
      return void 0;
    }
    return ProjectDefinitionUtilities.getVanillaBlockTexture(blockType, side);
  }
  static getVanillaBlockTexture(blockType, side) {
    if (!import_Database.default.blocksCatalog || !import_Database.default.terrainTextureCatalog || !import_Database.default.vanillaCatalog) {
      return void 0;
    }
    const blockDef = blockType.catalogResource;
    if (!blockDef) {
      return void 0;
    }
    let textureSource = blockDef.carried_textures || blockDef.textures;
    if (!textureSource) {
      return void 0;
    }
    let textureOrId = textureSource;
    if (typeof textureOrId === "object") {
      textureOrId = textureOrId[side];
    }
    if (!textureOrId || typeof textureOrId !== "string") {
      return void 0;
    }
    const texture = import_Database.default.terrainTextureCatalog.getTerrainTextureDefinition(textureOrId);
    if (!texture || !texture.textures) {
      return void 0;
    }
    if (typeof texture.textures === "string") {
      return texture.textures;
    }
    if (Array.isArray(texture.textures) && texture.textures.length > 0) {
      let tex = texture.textures[0];
      if (typeof tex === "string") {
        return tex;
      }
      if (tex && typeof tex === "object" && tex.path) {
        return tex.path;
      }
    }
    if (typeof texture.textures === "object" && !Array.isArray(texture.textures)) {
      if (texture.textures.path) {
        return texture.textures.path;
      }
    }
    return void 0;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ProjectDefinitionUtilities
});
//# sourceMappingURL=ProjectDefinitionUtilities.js.map
