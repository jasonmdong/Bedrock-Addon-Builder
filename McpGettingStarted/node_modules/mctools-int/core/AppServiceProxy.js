"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var AppServiceProxy_exports = {};
__export(AppServiceProxy_exports, {
  AppServiceProxyCommands: () => AppServiceProxyCommands,
  default: () => AppServiceProxy
});
module.exports = __toCommonJS(AppServiceProxy_exports);
var import_Utilities = __toESM(require("./../core/Utilities"));
var import_Log = __toESM(require("./../core/Log"));
var import_ste_events = require("ste-events");
var import_CreatorToolsHost = __toESM(require("../app/CreatorToolsHost"));
var AppServiceProxyCommands = /* @__PURE__ */ ((AppServiceProxyCommands2) => {
  AppServiceProxyCommands2["fsExists"] = "fsExists";
  AppServiceProxyCommands2["fsFolderExists"] = "fsFolderExists";
  AppServiceProxyCommands2["fsRenameFolder"] = "fsRenameFolder";
  AppServiceProxyCommands2["fsRootStorageExists"] = "fsRootStorageExists";
  AppServiceProxyCommands2["fsMkdir"] = "fsMkdir";
  AppServiceProxyCommands2["fsReadUtf8File"] = "fsReadUtf8File";
  AppServiceProxyCommands2["fsReadFile"] = "fsReadFile";
  AppServiceProxyCommands2["fsWriteUtf8File"] = "fsWriteUtf8File";
  AppServiceProxyCommands2["fsWriteFile"] = "fsWriteFile";
  AppServiceProxyCommands2["fsReaddir"] = "fsReaddir";
  AppServiceProxyCommands2["fsStat"] = "fsStat";
  AppServiceProxyCommands2["getDirname"] = "getDirname";
  AppServiceProxyCommands2["getDedicatedServerStatus"] = "getDedicatedServerStatus";
  AppServiceProxyCommands2["getDedicatedServerProjectDeployDir"] = "getDedicatedServerProjectDir";
  AppServiceProxyCommands2["getDedicatedServerWorldDeployDir"] = "getDedicatedServerWorldDir";
  AppServiceProxyCommands2["getMinecraftGameProjectDeployDir"] = "getMinecraftGameProjectDeployDir";
  AppServiceProxyCommands2["getMinecraftGameWorldDeployDir"] = "getMinecraftGameWorldDeployDir";
  AppServiceProxyCommands2["getIsDev"] = "getIsDev";
  AppServiceProxyCommands2["openFolder"] = "openFolder";
  AppServiceProxyCommands2["shellOpenPath"] = "shellOpenPath";
  AppServiceProxyCommands2["shellOpenFolderInExplorer"] = "shellOpenFolderInExplorer";
  AppServiceProxyCommands2["webSocketCommand"] = "webSocketCommand";
  AppServiceProxyCommands2["dedicatedServerCommand"] = "dedicatedServerCommand";
  AppServiceProxyCommands2["startWebSocketServer"] = "startWebSocketServer";
  AppServiceProxyCommands2["stopWebSocketServer"] = "stopWebSocketServer";
  AppServiceProxyCommands2["startDedicatedServer"] = "startDedicatedServer";
  AppServiceProxyCommands2["stopDedicatedServer"] = "stopDedicatedServer";
  AppServiceProxyCommands2["shellRecycleItem"] = "shellRecycleItem";
  AppServiceProxyCommands2["reloadMct"] = "reloadMct";
  AppServiceProxyCommands2["getContentSources"] = "getContentSources";
  AppServiceProxyCommands2["minecraftShell"] = "minecraftShell";
  AppServiceProxyCommands2["windowClose"] = "windowClose";
  AppServiceProxyCommands2["windowRestore"] = "windowRestore";
  AppServiceProxyCommands2["windowMove"] = "windowMove";
  AppServiceProxyCommands2["logToConsole"] = "logToConsole";
  AppServiceProxyCommands2["windowMinimize"] = "windowMinimize";
  AppServiceProxyCommands2["windowMaximize"] = "windowMaximize";
  AppServiceProxyCommands2["windowUpdate"] = "windowUpdate";
  AppServiceProxyCommands2["windowLeftSide"] = "windowLeftSide";
  AppServiceProxyCommands2["windowRightSide"] = "windowRightSide";
  AppServiceProxyCommands2["getWindowState"] = "getWindowState";
  AppServiceProxyCommands2["getPlatform"] = "getPlatform";
  AppServiceProxyCommands2["appGetPath"] = "appGetPath";
  AppServiceProxyCommands2["updateIAgree"] = "updateIAgree";
  AppServiceProxyCommands2["convertFile"] = "convertFile";
  return AppServiceProxyCommands2;
})(AppServiceProxyCommands || {});
class AppServiceProxy {
  static _api;
  static _pendingStringPromiseResolvers = [];
  static _pendingArrayBufferPromiseResolvers = [];
  static _pendingStringPromiseRejecters = [];
  static _pendingArrayPromiseRejecters = [];
  static _onMessage = new import_ste_events.EventDispatcher();
  static get onMessage() {
    return AppServiceProxy._onMessage.asEvent();
  }
  static get hasAppService() {
    return AppServiceProxy._api !== void 0;
  }
  static get hasAppServiceOrDebug() {
    return AppServiceProxy._api !== void 0 || import_Utilities.default.isDebug;
  }
  static get hasAppServiceOrSim() {
    return AppServiceProxy._api !== void 0 || import_Utilities.default.isAppSim;
  }
  static init() {
    if (typeof window !== "undefined") {
      AppServiceProxy._api = window.api;
      if (AppServiceProxy._api && import_CreatorToolsHost.default.hostType !== import_CreatorToolsHost.HostType.vsCodeMainWeb && import_CreatorToolsHost.default.hostType !== import_CreatorToolsHost.HostType.vsCodeWebWeb) {
        import_CreatorToolsHost.default.hostType = import_CreatorToolsHost.HostType.electronWeb;
        import_CreatorToolsHost.default.fullLocalStorage = true;
      }
    }
    if (AppServiceProxy._api !== void 0) {
      AppServiceProxy._api.receive("appsvc", AppServiceProxy._handleNewMessage);
      import_Log.default.onItemAdded.subscribe(AppServiceProxy._handleLog);
    }
  }
  static _handleLog(log, item) {
    AppServiceProxy.logToConsole(item.message + " " + import_Log.default.getStack().replace("Error\n", ""));
  }
  static async logToConsole(message) {
    await AppServiceProxy.sendAsync("logToConsole" /* logToConsole */, message);
  }
  static send(commandName, data) {
    if (AppServiceProxy._api === void 0) {
      if (import_Utilities.default.isAppSim) {
        import_Log.default.debugAlert("Command: " + commandName + " Data: " + data);
        return;
      }
      throw new Error("Not an Electron API");
    }
    if (commandName === "fsWriteFile" /* fsWriteFile */) {
      let content = data.content;
      if (content instanceof Uint8Array) {
        content = import_Utilities.default.uint8ArrayToBase64(content);
      } else if (data instanceof ArrayBuffer) {
        content = import_Utilities.default.arrayBufferToBase64(content);
      } else if (data instanceof Uint16Array || data instanceof Uint32Array || data instanceof Uint8ClampedArray || data instanceof BigUint64Array) {
        import_Log.default.fail("Unsupported binary type encountered." + data);
      }
      data.content = content;
    }
    let result = void 0;
    try {
      result = AppServiceProxy._api.send("appweb", commandName, data);
    } catch (e) {
      let dataStr = data;
      if (typeof data === "object") {
        dataStr = JSON.stringify(data);
      }
      throw new Error("Error running command " + commandName + ", data: " + dataStr + ", error: " + e);
    }
    if (commandName === "fsReadFile" /* fsReadFile */) {
      if (result === void 0 || result.length === 0) {
        return void 0;
      }
      return import_Utilities.default.base64ToArrayBuffer(result);
    }
    return result;
  }
  static sendAsyncBinary(commandName, data) {
    const promise = new Promise(AppServiceProxy._arrayBufferPromiseHandler);
    if (AppServiceProxy._api === void 0) {
      if (import_Utilities.default.isAppSim) {
        import_Log.default.debugAlert("Command: " + commandName + " Data: " + data);
        return promise;
      }
      throw new Error("Not an Electron API");
    }
    const position = AppServiceProxy._pendingArrayBufferPromiseResolvers.length - 1;
    const commandStr = "bsync" + commandName + "|" + position;
    AppServiceProxy._api.send("appweb", commandStr, data);
    return promise;
  }
  static sendBinaryAsync(commandName, data) {
    const promise = new Promise(AppServiceProxy._stringPromiseHandler);
    if (AppServiceProxy._api === void 0) {
      if (import_Utilities.default.isAppSim) {
        import_Log.default.debugAlert("Command: " + commandName + " Data: " + data);
        return promise;
      }
      throw new Error("Not an Electron API");
    }
    let content = data.content;
    if (content instanceof Uint8Array) {
      content = import_Utilities.default.uint8ArrayToBase64(content);
    } else if (data instanceof ArrayBuffer) {
      content = import_Utilities.default.arrayBufferToBase64(content);
    } else if (data instanceof Uint16Array || data instanceof Uint32Array || data instanceof Uint8ClampedArray || data instanceof BigUint64Array) {
      import_Log.default.fail("Unsupported binary type encountered." + data);
    }
    data.content = content;
    const position = AppServiceProxy._pendingStringPromiseResolvers.length - 1;
    const commandStr = "async" + commandName + "|" + position;
    AppServiceProxy._api.send("appweb", commandStr, data);
    return promise;
  }
  static sendAsync(commandName, data, ignoreInSim) {
    const promise = new Promise(AppServiceProxy._stringPromiseHandler);
    if (AppServiceProxy._api === void 0) {
      if (import_Utilities.default.isAppSim) {
        if (!ignoreInSim) {
          import_Log.default.debugAlert("Command: " + commandName + " Data: " + data);
        }
        return promise;
      }
      throw new Error("Not an Electron API");
    }
    const position = AppServiceProxy._pendingStringPromiseResolvers.length - 1;
    const commandStr = "async" + commandName + "|" + position;
    AppServiceProxy._api.send("appweb", commandStr, data);
    return promise;
  }
  static sendHost(serviceName, commandName, data) {
    if (AppServiceProxy._api === void 0) {
      throw new Error("Not an Electron API");
    }
    if (typeof data === "object") {
      data = JSON.stringify(data);
    } else if (typeof data !== "string") {
      data = data.toString();
    }
    AppServiceProxy._api.send(serviceName, commandName, data);
  }
  static openLinkExternal(url) {
    try {
      if (typeof window !== "undefined") {
        window.open(url, "_blank");
      }
    } catch (e) {
    }
  }
  static _arrayBufferPromiseHandler(resolve, reject) {
    const position = AppServiceProxy._pendingArrayBufferPromiseResolvers.length;
    AppServiceProxy._pendingArrayBufferPromiseResolvers[position] = resolve;
    AppServiceProxy._pendingStringPromiseRejecters[position] = reject;
  }
  static _stringPromiseHandler(resolve, reject) {
    const position = AppServiceProxy._pendingStringPromiseResolvers.length;
    AppServiceProxy._pendingStringPromiseResolvers[position] = resolve;
    AppServiceProxy._pendingStringPromiseRejecters[position] = reject;
  }
  static _handleNewMessage(args) {
    if (args !== null && args.length > 0) {
      if (args.startsWith("async")) {
        const argSplit = import_Utilities.default.splitUntil(args, "|", 2);
        if (argSplit.length > 2) {
          const index = parseInt(argSplit[1]);
          if (index >= 0) {
            const promiseResolver = AppServiceProxy._pendingStringPromiseResolvers[index];
            let val = argSplit[2];
            if (val === "<undefined>") {
              val = void 0;
            }
            promiseResolver(val);
          }
        }
      } else if (args.startsWith("bsync")) {
        const argSplit = import_Utilities.default.splitUntil(args, "|", 2);
        if (argSplit.length > 2) {
          const index = parseInt(argSplit[1]);
          if (index >= 0) {
            const promiseResolver = AppServiceProxy._pendingArrayBufferPromiseResolvers[index];
            let val = void 0;
            if (argSplit[2] !== "<undefined>") {
              val = import_Utilities.default.base64ToArrayBuffer(argSplit[2]);
            }
            promiseResolver(val);
          }
        }
      } else {
        const firstPipe = args.indexOf("|");
        if (firstPipe >= 0) {
          AppServiceProxy._onMessage.dispatch(args.substring(0, firstPipe), args.substring(firstPipe + 1, args.length));
        }
      }
    }
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  AppServiceProxyCommands
});
//# sourceMappingURL=AppServiceProxy.js.map
