"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var HashUtilities_exports = {};
__export(HashUtilities_exports, {
  default: () => HashUtilities
});
module.exports = __toCommonJS(HashUtilities_exports);
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_Log = __toESM(require("./Log"));
var md5 = __toESM(require("js-md5"));
var import_Utilities = __toESM(require("./Utilities"));
class HashUtilities {
  /**
   * Generates hash catalog entries for a file (both complete file hash and property hashes for JSON files)
   * @param file The file to generate hashes for
   * @param filePath The file path to store in the catalog entry
   * @param hashCatalog The hash catalog to populate
   * @param onError Optional error callback for handling errors
   */
  static async addHashesForFile(hashCatalog, file, filePath) {
    try {
      await file.loadContent();
      if (file.content !== null && file.content !== void 0) {
        const fileHash = await file.getHash();
        if (fileHash) {
          hashCatalog[fileHash] = {
            fileName: file.name,
            propertyName: "",
            filePath
          };
        }
      }
      if (file.name.endsWith(".json")) {
        await file.loadContent();
        if (file.isString && typeof file.content === "string") {
          try {
            const bytes = import_StorageUtilities.default.getContentsAsBinary(file);
            const hasBOM = import_StorageUtilities.default.hasUTF8ByteOrderMark(bytes);
            let cleanContent = file.content;
            if (hasBOM && bytes) {
              const withoutBOM = bytes.slice(3);
              cleanContent = new TextDecoder("utf-8").decode(withoutBOM);
            }
            const fixedContent = import_Utilities.default.fixJsonContent(cleanContent);
            const jsonContent = JSON.parse(fixedContent);
            for (const key in jsonContent) {
              const value = jsonContent[key];
              if (value !== null && !key.includes(":") && typeof value === "object") {
                const stringifiedObj = JSON.stringify(value);
                const stringifiedObjAndKey = stringifiedObj + key;
                const propertyHash = md5.md5(stringifiedObjAndKey).toLowerCase();
                hashCatalog[propertyHash] = {
                  fileName: file.name,
                  propertyName: key,
                  filePath
                };
              }
            }
          } catch (error) {
            import_Log.default.verbose(`Skipping property hash generation for ${file.fullPath}: ${error}`);
          }
        }
      }
    } catch (error) {
      import_Log.default.verbose(`Skipping property hash generation for ${file.fullPath}: ${error}`);
    }
  }
}
//# sourceMappingURL=HashUtilities.js.map
