"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var DistributedContentIndex_exports = {};
__export(DistributedContentIndex_exports, {
  default: () => DistributedContentIndex
});
module.exports = __toCommonJS(DistributedContentIndex_exports);
var import_ContentIndex = __toESM(require("../core/ContentIndex"));
var import_Utilities = __toESM(require("../core/Utilities"));
var import_StorageUtilities = __toESM(require("./StorageUtilities"));
class DistributedContentIndex {
  #fileIndexes = {};
  #rootFolder;
  #isLoaded = false;
  #isError = false;
  #items = [];
  get items() {
    return this.#items;
  }
  get isError() {
    return this.#isError;
  }
  get startLength() {
    return 6;
  }
  constructor(rootFolder) {
    this.#rootFolder = rootFolder;
  }
  getValuesFromIndexArray(indices) {
    const results = [];
    if (!this.#items) {
      return results;
    }
    for (const index of indices) {
      if (typeof index === "object") {
        const indexN = index.n;
        if (indexN >= 0 && indexN < this.#items.length) {
          results.push({ value: this.#items[indexN], annotation: index.a });
        }
      } else if (index >= 0 && index < this.#items.length) {
        results.push({ value: this.#items[index], annotation: void 0 });
      }
    }
    return results;
  }
  async ensureLoaded() {
    if (this.#isLoaded === true) {
      return;
    }
    const cItemsFile = this.#rootFolder.ensureFile("citems.json");
    const itemsExists = await cItemsFile.exists();
    if (!itemsExists) {
      this.#isError = true;
      this.#isLoaded = true;
      return;
    }
    if (!cItemsFile.isContentLoaded) {
      await cItemsFile.loadContent();
    }
    const jsonO = import_StorageUtilities.default.getJsonObject(cItemsFile);
    if (jsonO === void 0) {
      this.#isError = true;
      this.#isLoaded = true;
      return;
    }
    this.#items = jsonO;
    this.#isLoaded = true;
  }
  async getMatches(searchString, wholeTermSearch, withAnyAnnotation) {
    if (typeof searchString !== "string") {
      return void 0;
    }
    searchString = searchString.trim().toLowerCase();
    let terms = [searchString];
    if (!wholeTermSearch) {
      terms = searchString.split(" ");
    }
    let termWasSearched = false;
    let andResults;
    for (const term of terms) {
      if (term.length > 3) {
        const results = await this.getTermMatch(term);
        termWasSearched = true;
        if (results && results.length) {
          if (andResults === void 0) {
            andResults = results;
          } else {
            const newArr = [];
            for (let num of results) {
              if (andResults.includes(num)) {
                newArr.push(num);
              }
            }
          }
        }
      }
    }
    if (andResults === void 0 || andResults.length === 0) {
      if (termWasSearched) {
        return [];
      }
      return void 0;
    }
    return import_ContentIndex.default.processResultValues(this.getValuesFromIndexArray(andResults), withAnyAnnotation);
  }
  async getTermMatch(term) {
    await this.ensureLoaded();
    if (term.length >= 7) {
      const indexAddress = term.substring(0, 6);
      let res = this.#fileIndexes[indexAddress];
      if (res === false || res === true) {
        return void 0;
      } else if (res === void 0) {
        const firstFolder = this.#rootFolder.ensureFolder(import_StorageUtilities.default.sanitizePath(term.substring(0, 2)));
        const secondFolder = firstFolder.ensureFolder(import_StorageUtilities.default.sanitizePath(term.substring(2, 4)));
        const file = secondFolder.ensureFile(import_StorageUtilities.default.sanitizePath(term.substring(4, 6)) + ".json");
        const fileExists = await file.exists();
        if (!fileExists) {
          this.#fileIndexes[indexAddress] = false;
          return void 0;
        }
        if (!file.isContentLoaded) {
          await file.loadContent();
        }
        const jsonO = import_StorageUtilities.default.getJsonObject(file);
        if (jsonO === void 0) {
          this.#fileIndexes[indexAddress] = false;
          return void 0;
        }
        const contentIndex = new import_ContentIndex.default();
        contentIndex.setTrie(jsonO);
        if (this.#items) {
          contentIndex.setItems(this.#items);
        }
        res = {
          file,
          index: contentIndex
        };
        this.#fileIndexes[indexAddress] = res;
      }
      return res.index.getTermMatch(term.substring(6));
    } else if (term.length >= 3) {
      const indexAddress = term.substring(0, 3);
      let res = this.#fileIndexes[indexAddress];
      if (res === false || res === true) {
        return void 0;
      } else if (res === void 0) {
        const firstFolder = this.#rootFolder.ensureFolder(import_StorageUtilities.default.sanitizePath(term.substring(0, 2)));
        const file = firstFolder.ensureFile(import_StorageUtilities.default.sanitizePath(term.substring(2, 3)) + ".json");
        const fileExists = await file.exists();
        if (!fileExists) {
          this.#fileIndexes[indexAddress] = false;
          return void 0;
        }
        if (!file.isContentLoaded) {
          await file.loadContent();
        }
        const jsonO = import_StorageUtilities.default.getJsonObject(file);
        if (jsonO === void 0) {
          this.#fileIndexes[indexAddress] = false;
          return void 0;
        }
        const contentIndex = new import_ContentIndex.default();
        contentIndex.setTrie(jsonO);
        if (this.#items) {
          contentIndex.setItems(this.#items);
        }
        res = {
          file,
          index: contentIndex
        };
        this.#fileIndexes[indexAddress] = res;
      }
      return res.index.getTermMatch(term.substring(3));
    }
  }
  async getDescendentStrings(term) {
    await this.ensureLoaded();
    if (term.length >= 7) {
      const indexAddress = term.substring(0, 6);
      const newResults = {};
      if (import_Utilities.default.isUsableAsObjectKey(indexAddress)) {
        let res = this.#fileIndexes[indexAddress];
        if (res === false || res === true) {
          return void 0;
        } else if (res === void 0) {
          const firstFolder = this.#rootFolder.ensureFolder(import_StorageUtilities.default.sanitizePath(term.substring(0, 2)));
          const secondFolder = firstFolder.ensureFolder(import_StorageUtilities.default.sanitizePath(term.substring(2, 4)));
          const file = secondFolder.ensureFile(import_StorageUtilities.default.sanitizePath(term.substring(4, 6)) + ".json");
          const fileExists = await file.exists();
          if (!fileExists) {
            this.#fileIndexes[indexAddress] = false;
            return void 0;
          }
          if (!file.isContentLoaded) {
            await file.loadContent();
          }
          const jsonO = import_StorageUtilities.default.getJsonObject(file);
          if (jsonO === void 0) {
            this.#fileIndexes[indexAddress] = false;
            return void 0;
          }
          const contentIndex = new import_ContentIndex.default();
          contentIndex.setTrie(jsonO);
          if (this.#items) {
            contentIndex.setItems(this.#items);
          }
          res = {
            file,
            index: contentIndex
          };
          this.#fileIndexes[indexAddress] = res;
        }
        const results = await res.index.getDescendentStrings(term.substring(6));
        if (!results) {
          return void 0;
        }
        for (const key in results) {
          if (results[key] !== void 0) {
            if (import_Utilities.default.isUsableAsObjectKey(indexAddress + key)) {
              newResults[indexAddress + key] = results[key];
            }
          }
        }
      }
      return newResults;
    }
    return void 0;
  }
  static async saveContentIndexToFolder(contentIndex, folder) {
    const all = contentIndex.getAll();
    const fileIndexes = {};
    const cItemsFile = folder.ensureFile("citems.json");
    import_Utilities.default.encodeObjectWithSequentialRunLengthEncodeUsingNegative(contentIndex.data.trie);
    cItemsFile.setContent(JSON.stringify(contentIndex.data.items));
    await cItemsFile.saveContent();
    for (const key in all) {
      if (key.length >= 3) {
        const firstFolder = folder.ensureFolder(import_StorageUtilities.default.sanitizePath(key.substring(0, 2)));
        if (key.length >= 7) {
          const secondFolder = firstFolder.ensureFolder(import_StorageUtilities.default.sanitizePath(key.substring(2, 4)));
          const res = fileIndexes[key.substring(0, 6)];
          if (!res) {
            const file = secondFolder.ensureFile(import_StorageUtilities.default.sanitizePath(key.substring(4, 6)) + ".json");
            const newContentIndex = new import_ContentIndex.default();
            newContentIndex.setItems(contentIndex.data.items);
            newContentIndex.insertArray(key.substring(6), all[key]);
            fileIndexes[key.substring(0, 6)] = { file, index: newContentIndex };
          } else {
            res.index.insertArray(key.substring(6), all[key]);
          }
        } else {
          const res = fileIndexes[key.substring(0, 3)];
          if (!res) {
            const file = firstFolder.ensureFile(import_StorageUtilities.default.sanitizePath(key.substring(2, 3)) + ".json");
            const newContentIndex = new import_ContentIndex.default();
            newContentIndex.setItems(newContentIndex.data.items);
            newContentIndex.insertArray(key.substring(3), all[key]);
            fileIndexes[key.substring(0, 3)] = { file, index: newContentIndex };
          } else {
            res.index.insertArray(key.substring(3), all[key]);
          }
        }
      }
    }
    for (const fileIndexKey in fileIndexes) {
      const fileIndex = fileIndexes[fileIndexKey];
      import_Utilities.default.encodeObjectWithSequentialRunLengthEncodeUsingNegative(fileIndex.index.data.trie);
      fileIndex.file.setContent(JSON.stringify(fileIndex.index.data.trie));
      await fileIndex.file.saveContent();
    }
  }
}
//# sourceMappingURL=DistributedContentIndex.js.map
