"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var BrowserFolder_exports = {};
__export(BrowserFolder_exports, {
  default: () => BrowserFolder
});
module.exports = __toCommonJS(BrowserFolder_exports);
var import_BrowserFile = __toESM(require("./BrowserFile"));
var import_BrowserStorage = __toESM(require("./BrowserStorage"));
var import_StorageUtilities = __toESM(require("./StorageUtilities"));
var import_Utilities = __toESM(require("./../core/Utilities"));
var import_Log = __toESM(require("./../core/Log"));
var import_localforage = __toESM(require("localforage"));
var import_FolderBase = __toESM(require("./FolderBase"));
class BrowserFolder extends import_FolderBase.default {
  _name;
  _parentPath;
  _lastSavedContent;
  _storage;
  _parentFolder;
  _lastLoadedPath;
  get storage() {
    return this._storage;
  }
  get parentFolder() {
    return this._parentFolder;
  }
  folders;
  files;
  lastSavedFileCount;
  modifiedAtLoad;
  get name() {
    return this._name;
  }
  get fullPath() {
    return this._parentPath + import_BrowserStorage.default.slashFolderDelimiter + import_StorageUtilities.default.canonicalizeName(this.name);
  }
  constructor(storage, parentFolder, parentPath, folderName) {
    super();
    this._storage = storage;
    this._parentFolder = parentFolder;
    this._parentPath = parentPath;
    this._name = folderName;
    this._lastSavedContent = "";
    this.lastSavedFileCount = 0;
    this.folders = {};
    this.files = {};
  }
  ensureFile(name) {
    const nameCanon = import_StorageUtilities.default.canonicalizeName(name);
    let candFile = this.files[nameCanon];
    if (candFile == null) {
      candFile = new import_BrowserFile.default(this, name);
      this.files[nameCanon] = candFile;
      this.storage.notifyFileAdded(candFile);
    }
    return candFile;
  }
  async exists() {
    return true;
  }
  async ensureExists() {
    return true;
  }
  async deleteThisFolder() {
    if (this.storage.readOnly) {
      throw new Error("Can't save read-only file.");
    }
    let result = await this.recursiveDeleteThisFolder();
    await import_localforage.default.removeItem(this.fullPath + import_BrowserStorage.default.slashFolderDelimiter);
    return result;
  }
  async deleteAllFolderContents() {
    throw new Error("Deletion of all folder contents at " + this.fullPath + " is not supported.");
  }
  ensureFolder(name) {
    const nameCanon = import_StorageUtilities.default.canonicalizeName(name);
    let candFolder = this.folders[nameCanon];
    if (!candFolder) {
      candFolder = new BrowserFolder(this._storage, this, this.fullPath, name);
      this.folders[nameCanon] = candFolder;
    }
    return candFolder;
  }
  async scanForChanges() {
  }
  _removeFile(file) {
    const nameCanon = import_StorageUtilities.default.canonicalizeName(file.name);
    const candFile = this.files[nameCanon];
    import_Log.default.assert(candFile === file, "Files don't match.");
    this.files[nameCanon] = void 0;
    this.storage.notifyFileRemoved(this.storageRelativePath + file.name);
  }
  _addExistingFile(file) {
    const nameCanon = import_StorageUtilities.default.canonicalizeName(file.name);
    if (import_Utilities.default.isUsableAsObjectKey(nameCanon)) {
      this.files[nameCanon] = file;
    }
  }
  async moveTo(newStorageRelativePath, ignoreParentSave) {
    const newFolderPath = import_StorageUtilities.default.getFolderPath(newStorageRelativePath);
    const newFolderName = import_StorageUtilities.default.getLeafName(newStorageRelativePath);
    if (newFolderName.length < 2) {
      throw new Error("New path is not correct.");
    }
    if (this.isSameFolder(newStorageRelativePath)) {
      return false;
    }
    if (this.isLoaded) {
      await this.load();
    }
    const oldPath = this._lastLoadedPath;
    if (this._parentFolder !== null) {
      const newParentFolder = await this._parentFolder.storage.ensureFolderFromStorageRelativePath(newFolderPath);
      if (newParentFolder.folders[newFolderName] !== void 0) {
        throw new Error("Folder exists at specified path.");
      }
      this._parentFolder._removeExistingFolderFromParent(this);
      this._parentFolder = newParentFolder;
      this._name = newFolderName;
      newParentFolder._addExistingFolderToParent(this);
    }
    this._name = newFolderName;
    for (const fileName in this.files) {
      const file = this.files[fileName];
      if (file) {
        await file.resaveAfterMove();
      }
    }
    for (const folderName in this.folders) {
      const folder = this.folders[folderName];
      if (folder) {
        await folder.resaveAfterMove(this.fullPath);
      }
    }
    await this.save(true);
    if (oldPath) {
      await import_localforage.default.removeItem(oldPath);
    }
    if (this._parentFolder !== null && !ignoreParentSave) {
      await this._parentFolder.save(true);
    }
    if (!ignoreParentSave) {
      this.notifyFolderMoved({ previousStoragePath: oldPath, newStoragePath: this.fullPath, folder: this });
    }
    return true;
  }
  async createFile(name) {
    const file = this.ensureFile(name);
    this.storage.notifyFileAdded(file);
    return file;
  }
  async load(force) {
    if (this.lastLoadedOrSaved != null && !force) {
      return this.lastLoadedOrSaved;
    }
    this._lastLoadedPath = this.fullPath + import_BrowserStorage.default.slashFolderDelimiter;
    const listingContent = await import_localforage.default.getItem(this.fullPath + import_BrowserStorage.default.slashFolderDelimiter);
    if (listingContent != null) {
      this._lastSavedContent = listingContent;
      let folderState;
      try {
        folderState = JSON.parse(listingContent);
      } catch (e) {
        import_Log.default.debugAlert("Failure to parse browser folder content JSON:" + e.toString());
      }
      if (folderState) {
        for (var i = 0; i < folderState.files.length; i++) {
          const fileMeta = folderState.files[i];
          if (fileMeta.modified != null && !(fileMeta.modified instanceof Date)) {
            fileMeta.modified = new Date(fileMeta.modified);
          }
          const newFile = this.ensureFile(fileMeta.name);
          newFile.modifiedAtLoad = fileMeta.modified == null ? null : fileMeta.modified;
          if (newFile.sizeAtLoad === void 0) {
            newFile.sizeAtLoad = fileMeta.size;
          }
        }
        for (var j = 0; j < folderState.folders.length; j++) {
          const folderMeta = folderState.folders[j];
          if (folderMeta.modified != null && !(folderMeta.modified instanceof Date)) {
            folderMeta.modified = new Date(folderMeta.modified);
          }
          const newFolder = this.ensureFolder(folderMeta.name);
          newFolder.modifiedAtLoad = folderMeta.modified;
          newFolder.lastSavedFileCount = folderMeta.fileCount;
        }
      }
    }
    this.updateLastLoadedOrSaved();
    return this.lastLoadedOrSaved;
  }
  async resaveAfterMove(newParentPath) {
    this._parentPath = newParentPath;
    for (const fileName in this.files) {
      const file = this.files[fileName];
      if (file) {
        await file.resaveAfterMove();
      }
    }
    for (const folderName in this.folders) {
      const folder = this.folders[folderName];
      if (folder) {
        await folder.resaveAfterMove(this.fullPath);
      }
    }
    if (this._lastLoadedPath === void 0) {
      return;
    }
    if (this._lastLoadedPath !== import_StorageUtilities.default.ensureEndsWithDelimiter(this.fullPath)) {
      const oldPath = this._lastLoadedPath;
      await this.save(true);
      await import_localforage.default.removeItem(oldPath);
    }
  }
  async save(force) {
    if (!this.isLoaded) {
      await this.load(false);
    }
    this.updateLastLoadedOrSaved();
    const folderState = {
      updated: this.lastLoadedOrSaved,
      files: [],
      folders: []
    };
    for (const fileName in this.files) {
      const file = this.files[fileName];
      if (file !== void 0) {
        const fileState = {
          name: file.name,
          size: file.size,
          modified: file.latestModified == null ? /* @__PURE__ */ new Date() : file.latestModified
        };
        folderState.files.push(fileState);
      }
    }
    for (const folderName in this.folders) {
      const childFolder = this.folders[folderName];
      if (childFolder !== void 0 && !childFolder.errorStatus) {
        const childFolderState = {
          name: childFolder.name,
          fileCount: import_Utilities.default.lengthOfDictionary(childFolder.files),
          modified: childFolder.modifiedAtLoad == null ? /* @__PURE__ */ new Date() : childFolder.modifiedAtLoad
        };
        folderState.folders.push(childFolderState);
      }
    }
    const saveContent = JSON.stringify(folderState);
    if (this._lastSavedContent !== saveContent || force) {
      this._lastSavedContent = saveContent;
      this._lastLoadedPath = this.fullPath + import_BrowserStorage.default.slashFolderDelimiter;
      await import_localforage.default.setItem(this.fullPath + import_BrowserStorage.default.slashFolderDelimiter, saveContent);
    }
    return this.lastLoadedOrSaved;
  }
  async saveAll(force) {
    if (this.isDisposed) {
      import_Log.default.throwIsDisposed();
    }
    const initialDateTime = (/* @__PURE__ */ new Date()).getTime();
    let needsContentSave = false;
    for (const fileName in this.files) {
      const file = this.files[fileName];
      if (file !== void 0 && (file.needsSave || force)) {
        let newContentSaveDate = await file.saveContent(force, true);
        if (newContentSaveDate.getTime() > initialDateTime) {
          needsContentSave = true;
        }
      }
    }
    for (const folderName in this.folders) {
      const folder = this.folders[folderName];
      if (folder !== void 0 && !folder.errorStatus) {
        needsContentSave = true;
        await folder.saveAll(force);
      }
    }
    if (needsContentSave || this._lastSavedContent === void 0 || force === true) {
      await this.save(force);
    }
    return true;
  }
}
//# sourceMappingURL=BrowserFolder.js.map
