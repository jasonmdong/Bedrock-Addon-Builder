"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var FileBase_exports = {};
__export(FileBase_exports, {
  default: () => FileBase
});
module.exports = __toCommonJS(FileBase_exports);
var import_IFile = require("./IFile");
var import_StorageUtilities = __toESM(require("./StorageUtilities"));
var md5 = __toESM(require("js-md5"));
var import_Log = __toESM(require("../core/Log"));
var import_ste_events = require("ste-events");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_JsonUtilities = __toESM(require("../core/JsonUtilities"));
class FileBase {
  priorVersions = [];
  isDisposed = false;
  isInErrorState;
  errorStateMessage;
  /**
   * Cache for comment-json parsed object.
   * When set, contains the parsed JSON with comment metadata preserved as Symbol properties.
   */
  commentJsonCache;
  _content;
  #fileContainerStorage = null;
  #onFileContentUpdated = new import_ste_events.EventDispatcher();
  get fileContainerStorage() {
    return this.#fileContainerStorage;
  }
  get onFileContentUpdated() {
    return this.#onFileContentUpdated.asEvent();
  }
  set fileContainerStorage(newStorage) {
    this.#fileContainerStorage = newStorage;
  }
  get isString() {
    return this._content !== null && typeof this._content === "string";
  }
  get canIgnore() {
    return import_StorageUtilities.default.canIgnoreFileName(this.name) || import_StorageUtilities.default.canIgnoreFileExtension(this.type);
  }
  get isBinary() {
    if (this._content === void 0) {
      return false;
    }
    if (this._content instanceof Uint8Array) {
      return true;
    }
    return false;
  }
  get content() {
    return this._content;
  }
  get latestModified() {
    if (this.modified == null) {
      return this.modifiedAtLoad;
    }
    if (this.modifiedAtLoad == null) {
      return null;
    }
    if (this.modified.getTime() > this.modifiedAtLoad.getTime()) {
      return this.modified;
    }
    return this.modifiedAtLoad;
  }
  modified;
  modifiedAtLoad;
  lastLoadedOrSaved;
  manager;
  tag;
  get type() {
    return import_StorageUtilities.default.getTypeFromName(this.name);
  }
  get needsSave() {
    if (this.isDisposed) {
      import_Log.default.throwIsDisposed();
    }
    return this.modified != null && (this.lastLoadedOrSaved == null || this.modified >= this.lastLoadedOrSaved);
  }
  get fullPath() {
    return this.storageRelativePath;
  }
  get extendedPath() {
    let start = "";
    if (this.parentFolder.storage.storagePath) {
      start = this.parentFolder.storage.storagePath;
    }
    return start + this.fullPath;
  }
  get storageRelativePath() {
    return this.parentFolder.storageRelativePath + this.name;
  }
  getRootRelativePath() {
    if (this.isDisposed) {
      import_Log.default.throwIsDisposed();
    }
    return this.getFolderRelativePath(this.parentFolder.storage.rootFolder);
  }
  getFolderRelativePath(toFolder) {
    if (this.isDisposed) {
      import_Log.default.throwIsDisposed();
    }
    let result = this.parentFolder.getFolderRelativePath(toFolder);
    if (result === void 0) {
      return void 0;
    }
    return result + this.name;
  }
  get coreContentLength() {
    if (!this.content) {
      return 0;
    }
    if (this.isBinary || typeof this.content !== "string") {
      return this.content.length;
    }
    return this.content.replace(/\s/g, "").length;
  }
  constructor() {
    this.modified = null;
    this.modifiedAtLoad = null;
    this.lastLoadedOrSaved = null;
    this._content = null;
  }
  contentWasModified(oldContent, updateType, sourceId) {
    if (this.isDisposed) {
      import_Log.default.throwIsDisposed();
    }
    let oldVersionContent = void 0;
    if (updateType !== import_IFile.FileUpdateType.versionRestoration && updateType !== import_IFile.FileUpdateType.versionlessEdit) {
      let oldModified = this.modified;
      if (oldModified === null) {
        oldModified = this.lastLoadedOrSaved;
      }
      oldVersionContent = {
        id: import_Utilities.default.createRandomId(10),
        content: oldContent,
        file: this,
        versionTime: oldModified
      };
      this.parentFolder.storage.addVersion(oldVersionContent, updateType ? updateType : import_IFile.FileUpdateType.regularEdit);
    }
    this.modified = /* @__PURE__ */ new Date();
    this.notifyFileContentUpdated();
    if (this.parentFolder.storage) {
      this.parentFolder.storage.notifyFileContentsUpdated({
        file: this,
        updateType: updateType ? updateType : import_IFile.FileUpdateType.regularEdit,
        sourceId,
        priorVersion: oldVersionContent
      });
    }
  }
  notifyFileContentUpdated() {
    this.#onFileContentUpdated.dispatch(this, this);
  }
  async getHash() {
    if (!this.isContentLoaded) {
      await this.loadContent(false);
    }
    if (this._content === void 0 || this._content === null) {
      return void 0;
    }
    return md5.md5(this._content);
  }
  unload() {
    this._content = null;
    this.lastLoadedOrSaved = null;
    this.manager = void 0;
    this.commentJsonCache = void 0;
  }
  dispose() {
    this.manager = void 0;
    this._content = null;
    this.lastLoadedOrSaved = null;
    this.commentJsonCache = void 0;
    this.isDisposed = true;
  }
  async exists() {
    if (this.isDisposed) {
      import_Log.default.throwIsDisposed();
    }
    await this.loadContent(false);
    return this._content !== null;
  }
  getRelativePathFor(file) {
    if (file.parentFolder.storage !== this.parentFolder.storage) {
      return void 0;
    }
    const foldersByPath = {};
    let targetParentFolder = file.parentFolder;
    while (targetParentFolder) {
      foldersByPath[targetParentFolder.storageRelativePath] = targetParentFolder;
      targetParentFolder = targetParentFolder.parentFolder;
    }
    let myParentFolder = this.parentFolder;
    let relativePath = "." + myParentFolder.storage.folderDelimiter;
    while (myParentFolder && foldersByPath[myParentFolder.storageRelativePath] === void 0) {
      relativePath += ".." + myParentFolder.storage.folderDelimiter;
      myParentFolder = myParentFolder.parentFolder;
    }
    if (!myParentFolder) {
      return void 0;
    }
    const folderRelativePath = file.getFolderRelativePath(myParentFolder);
    if (!folderRelativePath) {
      return void 0;
    }
    return relativePath + import_StorageUtilities.default.ensureNotStartsWithDelimiter(folderRelativePath);
  }
  setObjectContentIfSemanticallyDifferent(value, updateType, sourceId) {
    if (value === null || value === void 0) {
      if (this._content !== null) {
        this.setContent(null, updateType, sourceId);
        this.commentJsonCache = void 0;
        return true;
      }
      return false;
    }
    if (!(typeof this._content === "string")) {
      if (import_JsonUtilities.default.hasCommentMetadata(value)) {
        this.setContent(import_JsonUtilities.default.stringifyJsonWithComments(value), updateType, sourceId);
        this.commentJsonCache = value;
      } else {
        this.setContent(JSON.stringify(value, null, 2), updateType, sourceId);
      }
      return true;
    }
    try {
      if (import_JsonUtilities.default.hasCommentMetadata(value)) {
        const currentObj2 = this.commentJsonCache ?? import_JsonUtilities.default.parseJsonWithComments(this._content);
        if (!import_JsonUtilities.default.jsonObjectsSemanticallyEqual(currentObj2, value)) {
          this.setContent(import_JsonUtilities.default.stringifyJsonWithComments(value), updateType, sourceId);
          this.commentJsonCache = value;
          return true;
        }
        return false;
      }
      const currentObj = JSON.parse(this._content);
      if (import_Utilities.default.consistentStringify(currentObj) !== import_Utilities.default.consistentStringify(value)) {
        this.setContent(JSON.stringify(value, null, 2), updateType, sourceId);
        return true;
      }
    } catch (e) {
      if (import_JsonUtilities.default.hasCommentMetadata(value)) {
        this.setContent(import_JsonUtilities.default.stringifyJsonWithComments(value), updateType, sourceId);
        this.commentJsonCache = value;
      } else {
        this.setContent(JSON.stringify(value, null, 2), updateType, sourceId);
      }
      return true;
    }
    return false;
  }
  async reloadAfterExternalUpdate() {
    let existingContent = this._content;
    await this.loadContent(true);
    if (this._content !== existingContent) {
      this.contentWasModified(existingContent, import_IFile.FileUpdateType.externalChange);
    }
  }
  setContentIfSemanticallyDifferent(value, updateType, sourceId) {
    if (value === null) {
      if (this._content !== null) {
        this.setContent(null, updateType, sourceId);
        return true;
      }
      return false;
    }
    if (value instanceof Uint8Array) {
      if (!(this._content instanceof Uint8Array) || this._content.length !== value.length) {
        this.setContent(value, updateType, sourceId);
        return true;
      } else {
        for (let i = 0; i < value.length; i++) {
          if (this._content[i] !== value[i]) {
            this.setContent(value, updateType, sourceId);
            return true;
          }
        }
      }
    } else if (typeof value === "string") {
      if (!(typeof this._content === "string")) {
        this.setContent(value, updateType, sourceId);
        return true;
      } else {
        if (this.type === "json") {
          try {
            const currentObj = JSON.parse(this._content);
            const newObj = JSON.parse(value);
            if (import_Utilities.default.consistentStringify(currentObj) !== import_Utilities.default.consistentStringify(newObj)) {
              return this.setContent(value, updateType, sourceId);
            }
          } catch (e) {
            return this.setContent(value);
          }
        } else {
          if (this._content.length !== value.length) {
            return this.setContent(value, updateType, sourceId);
          }
          for (let i = 0; i < value.length; i++) {
            if (this._content[i] !== value[i]) {
              return this.setContent(value, updateType, sourceId);
            }
          }
        }
      }
    }
    return false;
  }
}
//# sourceMappingURL=FileBase.js.map
