"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var Thumbprint_exports = {};
__export(Thumbprint_exports, {
  default: () => Thumbprint
});
module.exports = __toCommonJS(Thumbprint_exports);
var import_DifferenceSet = __toESM(require("./DifferenceSet"));
var import_IFileDifference = require("./IFileDifference");
var import_StorageUtilities = __toESM(require("./StorageUtilities"));
class Thumbprint {
  fileInfos = {};
  compare(secondThumbprint, compareOnlyLeafs) {
    const tempFileInfos = {};
    for (const fileInfoPath in this.fileInfos) {
      let comparePath = fileInfoPath;
      if (compareOnlyLeafs) {
        comparePath = import_StorageUtilities.default.canonicalizeName(import_StorageUtilities.default.getLeafName(fileInfoPath));
      }
      tempFileInfos[comparePath] = this.fileInfos[fileInfoPath];
    }
    for (const fileInfoPath in secondThumbprint.fileInfos) {
      const compareFile = secondThumbprint.fileInfos[fileInfoPath];
      if (compareFile) {
        let comparePath = fileInfoPath;
        if (compareOnlyLeafs) {
          comparePath = import_StorageUtilities.default.canonicalizeName(import_StorageUtilities.default.getLeafName(fileInfoPath));
        }
        const sourceFileInfo = tempFileInfos[comparePath];
        if (sourceFileInfo !== void 0) {
          if (sourceFileInfo.size === compareFile.size) {
            tempFileInfos[comparePath] = void 0;
          }
        }
      }
    }
    const diffSet = new import_DifferenceSet.default();
    for (const fileInfoPath in tempFileInfos) {
      const fi = tempFileInfos[fileInfoPath];
      if (fi) {
        diffSet.fileDifferences.push({
          type: import_IFileDifference.FileDifferenceType.contentsDifferent,
          path: fileInfoPath
        });
      }
    }
    return diffSet;
  }
  async create(folder) {
    const tempFileInfos = {};
    await this._createInternal(folder, tempFileInfos);
    this.fileInfos = tempFileInfos;
  }
  async _createInternal(folder, fileInfos) {
    if (!folder.isLoaded) {
      await folder.load();
    }
    for (const fileName in folder.files) {
      const file = folder.files[fileName];
      if (file) {
        fileInfos[file.storageRelativePath] = { size: file.content ? file.content.length : -1 };
      }
    }
    for (const folderName in folder.folders) {
      const childFolder = folder.folders[folderName];
      if (childFolder && !childFolder.errorStatus) {
        await this._createInternal(childFolder, fileInfos);
      }
    }
  }
}
//# sourceMappingURL=Thumbprint.js.map
