"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var LocalTools_exports = {};
__export(LocalTools_exports, {
  default: () => LocalTools
});
module.exports = __toCommonJS(LocalTools_exports);
var import_ProjectExporter = __toESM(require("../app/ProjectExporter"));
var fs = __toESM(require("fs"));
var import_open = __toESM(require("open"));
class LocalTools {
  static async exportWorld(creatorTools2, project, path) {
    creatorTools2.notifyStatusUpdate("Starting export");
    const name = project.name + " World";
    const fileName = project.name + ".mcworld";
    creatorTools2.notifyStatusUpdate("Packing " + fileName);
    const newBytes = await import_ProjectExporter.default.generateFlatBetaApisWorldWithPacksZipBytes(creatorTools2, project, name);
    creatorTools2.notifyStatusUpdate("Now saving " + fileName);
    if (newBytes !== void 0) {
      fs.writeFileSync(path, newBytes);
    }
  }
  static async convertFromJavaWorld(creatorTools2, javaWorldFile) {
  }
  static async launchWorld(creatorTools2, worldFolderName) {
    const commandLine = "minecraft://mode/?load=" + worldFolderName;
    console.log("Running " + commandLine);
    await (0, import_open.default)(commandLine);
  }
  static async ensureFlatPackRefWorldTo(creatorTools2, project, rootFolder, name) {
    const childFolder = rootFolder.ensureFolder(name);
    await childFolder.ensureExists();
    await import_ProjectExporter.default.syncFlatPackRefWorldTo(creatorTools2, project, childFolder, name);
    await childFolder.saveAll();
  }
  static async deploy(creatorTools2, project, storage, rootFolder, name) {
    await import_ProjectExporter.default.deployProject(creatorTools2, project, rootFolder);
    await rootFolder.saveAll();
  }
}
//# sourceMappingURL=LocalTools.js.map
