"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var LocalUtilities_exports = {};
__export(LocalUtilities_exports, {
  UNSAFE_PORTS: () => UNSAFE_PORTS,
  default: () => LocalUtilities
});
module.exports = __toCommonJS(LocalUtilities_exports);
var os = __toESM(require("os"));
var path = __toESM(require("path"));
var crypto = __toESM(require("crypto"));
var import_uuid = require("uuid");
var import_NodeStorage = __toESM(require("./NodeStorage"));
var import_ILocalUtilities = require("./ILocalUtilities");
var import_Log = __toESM(require("../core/Log"));
var import_ImageCodecNode = __toESM(require("./ImageCodecNode"));
const UNSAFE_PORTS = /* @__PURE__ */ new Set([
  // === Browser-blocked ports (Chrome/Firefox/Safari) ===
  1,
  // tcpmux
  7,
  // echo
  9,
  // discard
  11,
  // systat
  13,
  // daytime
  15,
  // netstat
  17,
  // qotd
  19,
  // chargen
  20,
  // ftp data
  21,
  // ftp access
  22,
  // ssh
  23,
  // telnet
  25,
  // smtp
  37,
  // time
  42,
  // name
  43,
  // nicname
  53,
  // domain
  69,
  // tftp
  77,
  // priv-rjs
  79,
  // finger
  87,
  // ttylink
  95,
  // supdup
  101,
  // hostriame
  102,
  // iso-tsap
  103,
  // gppitnp
  104,
  // acr-nema
  109,
  // pop2
  110,
  // pop3
  111,
  // sunrpc
  113,
  // auth
  115,
  // sftp
  117,
  // uucp-path
  119,
  // nntp
  123,
  // NTP
  135,
  // loc-srv / epmap
  137,
  // netbios
  139,
  // netbios
  143,
  // imap2
  161,
  // snmp
  179,
  // BGP
  389,
  // ldap
  427,
  // SLP (Also used by determine.exe)
  465,
  // smtp+ssl
  512,
  // print / exec
  513,
  // login
  514,
  // shell
  515,
  // printer
  526,
  // tempo
  530,
  // courier
  531,
  // chat
  532,
  // netnews
  540,
  // uucp
  548,
  // AFP (Apple Filing Protocol)
  554,
  // rtsp
  556,
  // remotefs
  563,
  // nntp+ssl
  587,
  // smtp (submission)
  601,
  // syslog-conn
  636,
  // ldap+ssl
  989,
  // ftps-data
  990,
  // ftps
  993,
  // ldap+ssl
  995,
  // pop3+ssl
  1719,
  // h323gatestat
  1720,
  // h323hostcall
  1723,
  // pptp
  2049,
  // nfs
  3659,
  // apple-sasl / PasswordServer
  4045,
  // lockd
  5060,
  // sip
  5061,
  // sips
  6e3,
  // X11
  6566,
  // sane-port
  6665,
  // Alternate IRC (browser-blocked + botnet target)
  6666,
  // Alternate IRC (browser-blocked + botnet target)
  6667,
  // Standard IRC (browser-blocked + botnet target)
  6668,
  // Alternate IRC (browser-blocked + botnet target)
  6669,
  // Alternate IRC (browser-blocked + botnet target)
  6697,
  // IRC + TLS (browser-blocked + botnet target)
  10080,
  // Amanda
  // === Security-sensitive service ports (not browser-blocked but should be avoided) ===
  6379
  // Redis
]);
class LocalUtilities {
  #productNameSeed = "mctools";
  #basePathAdjust = void 0;
  /**
   * Get the data directory override from MCTOOLS_DATA_DIR environment variable.
   * This is primarily used for Docker containers where data should be stored
   * in a volume-mounted directory like /data.
   */
  static get dataDirectoryOverride() {
    return process.env.MCTOOLS_DATA_DIR;
  }
  /**
   * Check if MCTOOLS_I_ACCEPT_EULA_AT_MINECRAFTDOTNETSLASHEULA environment variable is set to "true".
   * Used for non-interactive EULA acceptance in Docker containers.
   * The long name is intentional to ensure users explicitly acknowledge the EULA.
   */
  static get eulaAcceptedViaEnvironment() {
    const value = process.env.MCTOOLS_I_ACCEPT_EULA_AT_MINECRAFTDOTNETSLASHEULA;
    return value?.toLowerCase() === "true";
  }
  get basePathAdjust() {
    return this.#basePathAdjust;
  }
  set basePathAdjust(pathAdjust) {
    this.#basePathAdjust = pathAdjust;
  }
  get platform() {
    switch (os.platform()) {
      case "win32":
        return import_ILocalUtilities.Platform.windows;
      case "darwin":
        return import_ILocalUtilities.Platform.macOS;
      case "linux":
        return import_ILocalUtilities.Platform.linux;
      default:
        return import_ILocalUtilities.Platform.unsupported;
    }
  }
  get productNameSeed() {
    return this.#productNameSeed;
  }
  setProductNameSeed(newSeed) {
    this.#productNameSeed = newSeed;
  }
  get userDataPath() {
    return os.homedir();
  }
  get localAppDataPath() {
    if (this.platform === import_ILocalUtilities.Platform.windows) {
      return this.userDataPath + import_NodeStorage.default.platformFolderDelimiter + "AppData" + import_NodeStorage.default.platformFolderDelimiter + "Local" + import_NodeStorage.default.platformFolderDelimiter;
    } else {
      return this.userDataPath;
    }
  }
  get roamingAppDataPath() {
    if (this.platform === import_ILocalUtilities.Platform.windows) {
      return this.userDataPath + import_NodeStorage.default.platformFolderDelimiter + "AppData" + import_NodeStorage.default.platformFolderDelimiter + "Roaming" + import_NodeStorage.default.platformFolderDelimiter;
    } else {
      return this.userDataPath;
    }
  }
  get localReleaseServerLogPath() {
    if (this.platform === import_ILocalUtilities.Platform.windows) {
      return this.userDataPath + import_NodeStorage.default.platformFolderDelimiter + "AppData" + import_NodeStorage.default.platformFolderDelimiter + "Roaming" + import_NodeStorage.default.platformFolderDelimiter + "Minecraft Bedrock" + import_NodeStorage.default.platformFolderDelimiter + "logs" + import_NodeStorage.default.platformFolderDelimiter;
    } else {
      return "." + import_NodeStorage.default.platformFolderDelimiter;
    }
  }
  get localPreviewServerLogPath() {
    if (this.platform === import_ILocalUtilities.Platform.windows) {
      return this.userDataPath + import_NodeStorage.default.platformFolderDelimiter + "AppData" + import_NodeStorage.default.platformFolderDelimiter + "Roaming" + import_NodeStorage.default.platformFolderDelimiter + "Minecraft Bedrock Preview" + import_NodeStorage.default.platformFolderDelimiter + "logs" + import_NodeStorage.default.platformFolderDelimiter;
    } else {
      return "." + import_NodeStorage.default.platformFolderDelimiter;
    }
  }
  get minecraftPath() {
    return this.userDataPath + import_NodeStorage.default.platformFolderDelimiter + "AppData" + import_NodeStorage.default.platformFolderDelimiter + "Roaming" + import_NodeStorage.default.platformFolderDelimiter + "Microsoft Bedrock" + import_NodeStorage.default.platformFolderDelimiter + "Users" + import_NodeStorage.default.platformFolderDelimiter + "Shared" + import_NodeStorage.default.platformFolderDelimiter + "games" + import_NodeStorage.default.platformFolderDelimiter + "com.mojang" + import_NodeStorage.default.platformFolderDelimiter;
  }
  get minecraftPreviewPath() {
    return this.userDataPath + import_NodeStorage.default.platformFolderDelimiter + "AppData" + import_NodeStorage.default.platformFolderDelimiter + "Roaming" + import_NodeStorage.default.platformFolderDelimiter + "Microsoft Bedrock Preview" + import_NodeStorage.default.platformFolderDelimiter + "Users" + import_NodeStorage.default.platformFolderDelimiter + "Shared" + import_NodeStorage.default.platformFolderDelimiter + "games" + import_NodeStorage.default.platformFolderDelimiter + "com.mojang" + import_NodeStorage.default.platformFolderDelimiter;
  }
  get minecraftUwpPath() {
    return this.localAppDataPath + "Packages" + import_NodeStorage.default.platformFolderDelimiter + "Microsoft.MinecraftUWP_8wekyb3d8bbwe" + import_NodeStorage.default.platformFolderDelimiter + "LocalState" + import_NodeStorage.default.platformFolderDelimiter + "games" + import_NodeStorage.default.platformFolderDelimiter + "com.mojang" + import_NodeStorage.default.platformFolderDelimiter;
  }
  get minecraftPreviewUwpPath() {
    return this.localAppDataPath + "Packages" + import_NodeStorage.default.platformFolderDelimiter + "Microsoft.MinecraftWindowsBeta_8wekyb3d8bbwe" + import_NodeStorage.default.platformFolderDelimiter + "LocalState" + import_NodeStorage.default.platformFolderDelimiter + "games" + import_NodeStorage.default.platformFolderDelimiter + "com.mojang" + import_NodeStorage.default.platformFolderDelimiter;
  }
  get testWorkingPath() {
    const dataDir = LocalUtilities.dataDirectoryOverride;
    if (dataDir) {
      return import_NodeStorage.default.ensureEndsWithDelimiter(dataDir) + "test" + import_NodeStorage.default.platformFolderDelimiter;
    }
    let path2 = this.localAppDataPath;
    if (this.platform === import_ILocalUtilities.Platform.windows) {
      path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + this.#productNameSeed + "_test" + import_NodeStorage.default.platformFolderDelimiter;
    } else {
      path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + "." + this.#productNameSeed + import_NodeStorage.default.platformFolderDelimiter + "test" + import_NodeStorage.default.platformFolderDelimiter;
    }
    return path2;
  }
  get cliWorkingPath() {
    const dataDir = LocalUtilities.dataDirectoryOverride;
    if (dataDir) {
      return import_NodeStorage.default.ensureEndsWithDelimiter(dataDir) + "cli" + import_NodeStorage.default.platformFolderDelimiter;
    }
    let path2 = this.localAppDataPath;
    if (this.platform === import_ILocalUtilities.Platform.windows) {
      path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + this.#productNameSeed + "_cli" + import_NodeStorage.default.platformFolderDelimiter;
    } else {
      path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + "." + this.#productNameSeed + import_NodeStorage.default.platformFolderDelimiter + "cli" + import_NodeStorage.default.platformFolderDelimiter;
    }
    return path2;
  }
  get serverWorkingPath() {
    const dataDir = LocalUtilities.dataDirectoryOverride;
    if (dataDir) {
      return import_NodeStorage.default.ensureEndsWithDelimiter(dataDir) + "server" + import_NodeStorage.default.platformFolderDelimiter;
    }
    let path2 = this.localAppDataPath;
    if (this.platform === import_ILocalUtilities.Platform.windows) {
      path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + this.#productNameSeed + "_server" + import_NodeStorage.default.platformFolderDelimiter;
    } else {
      path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + "." + this.#productNameSeed + import_NodeStorage.default.platformFolderDelimiter + "server" + import_NodeStorage.default.platformFolderDelimiter;
    }
    return path2;
  }
  get worldsWorkingPath() {
    const dataDir = LocalUtilities.dataDirectoryOverride;
    if (dataDir) {
      return import_NodeStorage.default.ensureEndsWithDelimiter(dataDir) + "worlds" + import_NodeStorage.default.platformFolderDelimiter;
    }
    let path2 = this.localAppDataPath;
    if (this.platform === import_ILocalUtilities.Platform.windows) {
      path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + this.#productNameSeed + "_worlds" + import_NodeStorage.default.platformFolderDelimiter;
    } else {
      path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + "." + this.#productNameSeed + import_NodeStorage.default.platformFolderDelimiter + "worlds" + import_NodeStorage.default.platformFolderDelimiter;
    }
    return path2;
  }
  get serversPath() {
    let path2 = this.serverWorkingPath;
    path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + "servers" + import_NodeStorage.default.platformFolderDelimiter;
    return path2;
  }
  get sourceServersPath() {
    let path2 = this.serverWorkingPath;
    path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + "serverSources" + import_NodeStorage.default.platformFolderDelimiter;
    return path2;
  }
  get packCachePath() {
    let path2 = this.serverWorkingPath;
    path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + "packCache" + import_NodeStorage.default.platformFolderDelimiter;
    return path2;
  }
  get envPrefsPath() {
    let path2 = this.serverWorkingPath;
    path2 = import_NodeStorage.default.ensureEndsWithDelimiter(path2) + "envprefs" + import_NodeStorage.default.platformFolderDelimiter;
    return path2;
  }
  generateCryptoRandomNumber(toVal) {
    const maxUint32 = 4294967295;
    const limit = maxUint32 - maxUint32 % toVal;
    let randomValue;
    do {
      randomValue = new Uint32Array(crypto.randomBytes(4).buffer)[0];
    } while (randomValue >= limit);
    return randomValue % toVal;
  }
  generateUuid() {
    return (0, import_uuid.v4)();
  }
  validateFolderPath(path2) {
    if (path2.indexOf("..") >= 0 || path2.indexOf("\\\\") >= 0 || path2.indexOf("//") >= 0) {
      throw new Error("Unsupported path combinations: " + path2);
    }
    if (path2.lastIndexOf(":") >= 3) {
      throw new Error("Unsupported drive location: " + path2);
    }
    const count = this.countChar(path2, "\\") + this.countChar(path2, "/");
    if (count < 3) {
      throw new Error("Unsupported base path: " + path2);
    }
  }
  countChar(source, find) {
    let count = 0;
    let index = source.indexOf(find);
    while (index >= 0) {
      count++;
      index = source.indexOf(find, index + find.length);
    }
    return count;
  }
  ensureStartsWithSlash(pathSegment) {
    if (!pathSegment.startsWith("/")) {
      pathSegment = "/" + pathSegment;
    }
    return pathSegment;
  }
  ensureEndsWithSlash(pathSegment) {
    if (!pathSegment.endsWith("/")) {
      pathSegment += "/";
    }
    return pathSegment;
  }
  ensureStartsWithBackSlash(pathSegment) {
    if (!pathSegment.startsWith("\\")) {
      pathSegment = "\\" + pathSegment;
    }
    return pathSegment;
  }
  ensureEndsWithBackSlash(pathSegment) {
    if (!pathSegment.endsWith("\\")) {
      pathSegment += "\\";
    }
    return pathSegment;
  }
  getFullPath(relativePath) {
    let fullPath;
    if (this.#basePathAdjust) {
      fullPath = path.resolve(process.cwd(), this.#basePathAdjust);
      fullPath = import_NodeStorage.default.ensureEndsWithDelimiter(fullPath);
    } else {
      fullPath = __dirname;
      const lastSlash = Math.max(
        fullPath.lastIndexOf("\\", fullPath.length - 2),
        fullPath.lastIndexOf("/", fullPath.length - 2)
      );
      if (lastSlash >= 0) {
        fullPath = fullPath.substring(0, lastSlash + 1);
      }
    }
    if (this.platform === import_ILocalUtilities.Platform.windows) {
      fullPath += relativePath.replace(/\//g, "\\");
    } else {
      fullPath += relativePath.replace(/\\/g, import_NodeStorage.default.platformFolderDelimiter);
    }
    return fullPath;
  }
  createStorage(path2) {
    const fullPath = this.getFullPath(path2);
    return new import_NodeStorage.default(fullPath, "");
  }
  async readJsonFile(path2) {
    const fs = require("fs");
    const fullPath = this.getFullPath(path2);
    try {
      if (!fs.existsSync(fullPath)) {
        return null;
      }
      const rawData = fs.readFileSync(fullPath);
      if (!rawData) {
        return null;
      }
      const jsonData = JSON.parse(rawData);
      return jsonData;
    } catch (e) {
      return null;
    }
  }
  async processConversion(conversionSettings) {
    return true;
  }
  /**
   * Check if a port is available for use.
   * @param port The port number to check
   * @param host The host to check (default: localhost)
   * @returns Promise that resolves to true if the port is available, false otherwise
   */
  static isPortAvailable(port, host = "localhost") {
    const net = require("net");
    return new Promise((resolve) => {
      const server = net.createServer();
      server.once("error", (err) => {
        if (err.code === "EADDRINUSE") {
          resolve(false);
        } else {
          resolve(false);
        }
      });
      server.once("listening", () => {
        server.close();
        resolve(true);
      });
      server.listen(port, host);
    });
  }
  /**
   * Find an available port within a given range.
   * @param startPort The starting port of the range (inclusive)
   * @param endPort The ending port of the range (inclusive)
   * @param host The host to check (default: localhost)
   * @returns Promise that resolves to an available port, or undefined if none found
   */
  static async findAvailablePort(startPort, endPort, host = "localhost") {
    const ports = [];
    for (let port = startPort; port <= endPort; port++) {
      if (!UNSAFE_PORTS.has(port)) {
        ports.push(port);
      }
    }
    for (let i = ports.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [ports[i], ports[j]] = [ports[j], ports[i]];
    }
    for (const port of ports) {
      const available = await LocalUtilities.isPortAvailable(port, host);
      if (available) {
        return port;
      }
    }
    return void 0;
  }
  /**
   * Get a random port within a range, excluding browser-unsafe ports.
   * This is a synchronous function that doesn't check if the port is available.
   * Use findAvailablePort() if you need to verify the port is not in use.
   *
   * @param startPort The starting port of the range (inclusive)
   * @param endPort The ending port of the range (inclusive)
   * @returns A random port in the range that is not in the UNSAFE_PORTS list
   */
  static getRandomSafePort(startPort, endPort) {
    const safePorts = [];
    for (let port = startPort; port <= endPort; port++) {
      if (!UNSAFE_PORTS.has(port)) {
        safePorts.push(port);
      }
    }
    if (safePorts.length === 0) {
      import_Log.default.debugAlert(`No safe ports available in range ${startPort}-${endPort}, using ${startPort}`);
      return startPort;
    }
    return safePorts[Math.floor(Math.random() * safePorts.length)];
  }
  /**
   * Verifies the Authenticode digital signature of a Windows executable.
   * Uses the native WinVerifyTrust API via win-verify-signature package.
   *
   * @param exePath Absolute path to the executable to verify
   * @returns Promise with verification result including validity, signer, and Microsoft check
   */
  static async verifyAuthenticodeSignature(exePath) {
    if (os.platform() !== "win32") {
      return {
        isValid: false,
        status: "UnsupportedPlatform",
        error: "Authenticode signature verification is only supported on Windows"
      };
    }
    const fs = await import("fs");
    if (!fs.existsSync(exePath)) {
      return {
        isValid: false,
        status: "FileNotFound",
        error: `File not found: ${exePath}`
      };
    }
    try {
      const winVerify = require("win-verify-signature");
      const { verifySignatureByPublishNameAsync } = winVerify;
      const allowedPublishers = ["Microsoft Corporation", "Mojang"];
      const result = await verifySignatureByPublishNameAsync(exePath, allowedPublishers);
      const signerSubject = result.subject;
      let isMicrosoftSigned = result.signed;
      if (signerSubject) {
        isMicrosoftSigned = signerSubject.includes("Microsoft Corporation") || signerSubject.includes("Mojang") || isMicrosoftSigned;
      }
      return {
        isValid: result.signed,
        status: result.signed ? "Valid" : "Invalid",
        signer: signerSubject ?? void 0,
        isMicrosoftSigned,
        error: result.signed ? void 0 : result.message
      };
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : String(err);
      return {
        isValid: false,
        status: "VerificationError",
        error: `Failed to verify signature: ${errorMessage}`
      };
    }
  }
  // ============================================================================
  // IMAGE CODEC METHODS (Node.js specific)
  // ============================================================================
  /**
   * Decode PNG image data using pngjs.
   * This is a Node.js-specific implementation that uses native modules.
   */
  decodePng(data) {
    return import_ImageCodecNode.default.decodePng(data);
  }
  /**
   * Encode RGBA pixel data to PNG format using pngjs.
   * This is a Node.js-specific implementation that uses native modules.
   */
  encodeToPng(pixels, width, height) {
    return import_ImageCodecNode.default.encodeToPng(pixels, width, height);
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  UNSAFE_PORTS
});
//# sourceMappingURL=LocalUtilities.js.map
