"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var DedicatedServer_exports = {};
__export(DedicatedServer_exports, {
  DedicatedServerBackupStatus: () => DedicatedServerBackupStatus,
  DedicatedServerStatus: () => DedicatedServerStatus,
  MaxTimeToWaitForServerToStart: () => MaxTimeToWaitForServerToStart,
  default: () => DedicatedServer
});
module.exports = __toCommonJS(DedicatedServer_exports);
var import_stringio = require("@rauschma/stringio");
var import_child_process = require("child_process");
var import_ste_events = require("ste-events");
var import_Player = __toESM(require("./../minecraft/Player"));
var import_ServerConfigManager = __toESM(require("./ServerConfigManager"));
var import_SecurityUtilities = __toESM(require("../core/SecurityUtilities"));
var import_ServerMessage = __toESM(require("./ServerMessage"));
var import_ServerPropertiesManager = __toESM(require("../minecraft/ServerPropertiesManager"));
var import_NodeStorage = __toESM(require("./NodeStorage"));
var import_Log = __toESM(require("../core/Log"));
var fs = __toESM(require("fs"));
var os = __toESM(require("os"));
var import_Project = __toESM(require("../app/Project"));
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_LocalUtilities = __toESM(require("./LocalUtilities"));
var import_MCWorld = __toESM(require("../minecraft/MCWorld"));
var import_MinecraftUtilities = __toESM(require("../minecraft/MinecraftUtilities"));
var import_Thumbprint = __toESM(require("../storage/Thumbprint"));
var import_ICreatorToolsData = require("../app/ICreatorToolsData");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_timers = require("timers");
var import_IWorldSettings = require("../minecraft/IWorldSettings");
var import_NodeFile = __toESM(require("./NodeFile"));
var import_ZipStorage = __toESM(require("../storage/ZipStorage"));
var import_MinecraftDebugClient = __toESM(require("../debugger/MinecraftDebugClient"));
var import_IWorldBackupData = require("./IWorldBackupData");
var DedicatedServerStatus = /* @__PURE__ */ ((DedicatedServerStatus2) => {
  DedicatedServerStatus2[DedicatedServerStatus2["stopped"] = 1] = "stopped";
  DedicatedServerStatus2[DedicatedServerStatus2["deploying"] = 2] = "deploying";
  DedicatedServerStatus2[DedicatedServerStatus2["launching"] = 3] = "launching";
  DedicatedServerStatus2[DedicatedServerStatus2["starting"] = 4] = "starting";
  DedicatedServerStatus2[DedicatedServerStatus2["started"] = 5] = "started";
  return DedicatedServerStatus2;
})(DedicatedServerStatus || {});
var DedicatedServerBackupStatus = /* @__PURE__ */ ((DedicatedServerBackupStatus2) => {
  DedicatedServerBackupStatus2[DedicatedServerBackupStatus2["none"] = 0] = "none";
  DedicatedServerBackupStatus2[DedicatedServerBackupStatus2["suspendingSaveCommandIssued"] = 1] = "suspendingSaveCommandIssued";
  DedicatedServerBackupStatus2[DedicatedServerBackupStatus2["suspendingQueryCommandIssued"] = 2] = "suspendingQueryCommandIssued";
  DedicatedServerBackupStatus2[DedicatedServerBackupStatus2["suspendingQueryResultsPending"] = 3] = "suspendingQueryResultsPending";
  DedicatedServerBackupStatus2[DedicatedServerBackupStatus2["saveSuspended"] = 4] = "saveSuspended";
  DedicatedServerBackupStatus2[DedicatedServerBackupStatus2["copyingFiles"] = 5] = "copyingFiles";
  DedicatedServerBackupStatus2[DedicatedServerBackupStatus2["resumingSave"] = 6] = "resumingSave";
  DedicatedServerBackupStatus2[DedicatedServerBackupStatus2["saveResumed"] = 7] = "saveResumed";
  return DedicatedServerBackupStatus2;
})(DedicatedServerBackupStatus || {});
const MaxTimeToWaitForServerToStart = 5e3;
const PLAYER_POSITION_POLL_INTERVAL = 5e3;
const PLAYER_MOVE_THRESHOLD = 2;
class DedicatedServer {
  #pendingCommands = [];
  #pendingRequestIds = [];
  #pendingCommandsInternal = [];
  // Track which commands are internal (don't log)
  #worldBackupContainerFolder;
  serverPath;
  name;
  version;
  #backupInterval;
  #backupTimeoutTimer;
  // Timeout to prevent backup from getting stuck
  #playerPositionPollInterval;
  #lastPlayerPositions = /* @__PURE__ */ new Map();
  updates = [];
  #unexpectedStopLog = [];
  #backupStatus = 0 /* none */;
  #behaviorPacksStorage;
  #defaultWorldStorage;
  #resourcePacksStorage;
  #activeStdIn = null;
  #env;
  #currentCommandId = 0;
  #dsm;
  #starts = 0;
  #lastResult;
  startConfigurationHash = void 0;
  #port;
  #activeProcess = null;
  #status = 1 /* stopped */;
  // Debug client for connecting to the Minecraft script debugger
  #debugClient;
  // enableDebugger: Whether BDS enables script debugger listening
  // The debug port is calculated dynamically based on the slot (base port + 12)
  #enableDebugger = true;
  // enableDebuggerStreaming: Whether we connect to the debug port and stream stats to web console
  // Enabled by default for serve command. Set worldSettings.enableDebuggerStreaming=true to enable.
  #enableDebuggerStreaming = false;
  // Flag to track when we're waiting for the debug listener to be ready
  // Set to true after sending 'script debugger listen', cleared when we receive 'Debugger listening' message
  #awaitingDebuggerListening = false;
  // Flag to track if the debug listener is ready but we haven't connected yet
  // Used when delaying debug client connection until a player joins
  #debugListenerReady = false;
  // Whether to delay debug client connection until a player joins
  // Note: This was used during debugging but the real fix was sending 'resume' after
  // protocol handshake (see MinecraftDebugClient._handleProtocolEvent)
  #delayDebugClientUntilPlayerJoins = false;
  // Track if at least one player has joined since server start
  // Used to know when it's safe to start debug streaming
  #hasPlayerJoined = false;
  // Associated managed world ID for the new backup system.
  // If set, backups will be stored in the WorldBackupManager structure.
  // If not set, backups use the legacy per-slot backup folder structure.
  #managedWorldId;
  // Last backup result for tracking what was backed up
  #lastBackupResult;
  outputLines = [];
  #opList;
  #gameTest;
  get opList() {
    return this.#opList;
  }
  set opList(newOps) {
    this.#opList = newOps;
  }
  get port() {
    return this.#port;
  }
  /**
   * Get the script debugger port for this server instance.
   * The debug port is the base port + 12, ensuring each slot has a unique debug port.
   * Slot 0: port 19132 -> debug port 19144
   * Slot 1: port 19164 -> debug port 19176
   * etc.
   */
  get debugPort() {
    const basePort = this.#port ?? 19132;
    return basePort + 12;
  }
  get lastResult() {
    return this.#lastResult;
  }
  set port(newPort) {
    this.#port = newPort;
  }
  get defaultWorldFolder() {
    if (!this.#defaultWorldStorage) {
      return void 0;
    }
    return this.#defaultWorldStorage.rootFolder;
  }
  get behaviorPacksFolder() {
    if (!this.#behaviorPacksStorage) {
      return void 0;
    }
    return this.#behaviorPacksStorage.rootFolder;
  }
  get resourcePacksFolder() {
    if (!this.#resourcePacksStorage) {
      return void 0;
    }
    return this.#resourcePacksStorage.rootFolder;
  }
  /**
   * Get the behavior packs storage (NodeStorage) for file watching purposes.
   */
  get behaviorPacksStorage() {
    return this.#behaviorPacksStorage;
  }
  /**
   * Get the default world storage (NodeStorage) for file watching purposes.
   */
  get defaultWorldStorage() {
    return this.#defaultWorldStorage;
  }
  /**
   * Get the resource packs storage (NodeStorage) for file watching purposes.
   */
  get resourcePacksStorage() {
    return this.#resourcePacksStorage;
  }
  config;
  properties;
  deployCount = 0;
  #onServerOutput = new import_ste_events.EventDispatcher();
  #onServerStarted = new import_ste_events.EventDispatcher();
  #onServerRefreshed = new import_ste_events.EventDispatcher();
  #onServerError = new import_ste_events.EventDispatcher();
  #onServerStarting = new import_ste_events.EventDispatcher();
  #onServerStopping = new import_ste_events.EventDispatcher();
  #onServerStopped = new import_ste_events.EventDispatcher();
  #onServerGameEvent = new import_ste_events.EventDispatcher();
  #onPlayerConnected = new import_ste_events.EventDispatcher();
  #onPlayerDisconnected = new import_ste_events.EventDispatcher();
  #onTestStarted = new import_ste_events.EventDispatcher();
  #onTestFailed = new import_ste_events.EventDispatcher();
  #onTestSucceeded = new import_ste_events.EventDispatcher();
  // Debug client events
  #onDebugConnected = new import_ste_events.EventDispatcher();
  #onDebugDisconnected = new import_ste_events.EventDispatcher();
  #onDebugStats = new import_ste_events.EventDispatcher();
  #onDebugPaused = new import_ste_events.EventDispatcher();
  #onDebugResumed = new import_ste_events.EventDispatcher();
  #onProfilerCapture = new import_ste_events.EventDispatcher();
  #updateIds = {};
  constructor(name, dsm, env, serverPath, worldBackupContainerFolder) {
    this.name = name;
    this.serverPath = serverPath;
    this.#worldBackupContainerFolder = worldBackupContainerFolder;
    this.#dsm = dsm;
    this.#env = env;
    this.config = new import_ServerConfigManager.default();
    this.config.ensureDefaultConfig();
    this.config.addCartoConfig();
    this.properties = new import_ServerPropertiesManager.default();
    this.handleClose = this.handleClose.bind(this);
    this.doRunningBackup = this.doRunningBackup.bind(this);
    this.startServer = this.startServer.bind(this);
    this.stopServer = this.stopServer.bind(this);
    this.directOutput = this.directOutput.bind(this);
    this.handleCommandRequest = this.handleCommandRequest.bind(this);
  }
  get worldStoragePath() {
    return import_NodeStorage.default.ensureEndsWithDelimiter(this.serverPath) + "worlds" + import_NodeStorage.default.platformFolderDelimiter + "defaultWorld" + import_NodeStorage.default.platformFolderDelimiter;
  }
  pushUpdates(additionalUpdates) {
    for (let i = 0; i < additionalUpdates.length; i++) {
      const update = additionalUpdates[i];
      const updateId = update.eventId;
      if (!updateId || !this.#updateIds[updateId]) {
        if (updateId) {
          this.#updateIds[updateId] = true;
        }
        this.#onServerGameEvent.dispatch(this, update);
        this.updates.push(update);
      }
    }
  }
  get gameTest() {
    return this.#gameTest;
  }
  set gameTest(newGameTest) {
    this.#gameTest = newGameTest;
  }
  get status() {
    return this.#status;
  }
  get onServerStarting() {
    return this.#onServerStarting.asEvent();
  }
  get onServerStopping() {
    return this.#onServerStopping.asEvent();
  }
  get onServerStopped() {
    return this.#onServerStopped.asEvent();
  }
  get onServerRefreshed() {
    return this.#onServerRefreshed.asEvent();
  }
  get onServerOutput() {
    return this.#onServerOutput.asEvent();
  }
  get onServerGameEvent() {
    return this.#onServerGameEvent.asEvent();
  }
  get onServerError() {
    return this.#onServerError.asEvent();
  }
  get onServerStarted() {
    return this.#onServerStarted.asEvent();
  }
  get onTestStarted() {
    return this.#onTestStarted.asEvent();
  }
  get onTestFailed() {
    return this.#onTestFailed.asEvent();
  }
  get onTestSucceeded() {
    return this.#onTestSucceeded.asEvent();
  }
  get onPlayerConnected() {
    return this.#onPlayerConnected.asEvent();
  }
  get onPlayerDisconnected() {
    return this.#onPlayerDisconnected.asEvent();
  }
  get onDebugConnected() {
    return this.#onDebugConnected.asEvent();
  }
  get onDebugDisconnected() {
    return this.#onDebugDisconnected.asEvent();
  }
  get onDebugStats() {
    return this.#onDebugStats.asEvent();
  }
  get onDebugPaused() {
    return this.#onDebugPaused.asEvent();
  }
  get onDebugResumed() {
    return this.#onDebugResumed.asEvent();
  }
  get onProfilerCapture() {
    return this.#onProfilerCapture.asEvent();
  }
  get debugClient() {
    return this.#debugClient;
  }
  get debuggerEnabled() {
    return this.#enableDebugger;
  }
  set debuggerEnabled(value) {
    this.#enableDebugger = value;
  }
  get debuggerStreamingEnabled() {
    return this.#enableDebuggerStreaming;
  }
  set debuggerStreamingEnabled(value) {
    this.#enableDebuggerStreaming = value;
  }
  /**
   * Get the managed world ID for this server.
   * If set, backups will use the WorldBackupManager system.
   */
  get managedWorldId() {
    return this.#managedWorldId;
  }
  /**
   * Set the managed world ID for this server.
   * @param worldId The world ID from WorldBackupManager, or undefined to use legacy backups
   */
  set managedWorldId(worldId) {
    this.#managedWorldId = worldId;
  }
  /**
   * Get the last backup result.
   */
  get lastBackupResult() {
    return this.#lastBackupResult;
  }
  handleCommandRequest(event, data) {
    const slargs = import_Utilities.default.splitUntil(data, "|", 1);
    this.runCommand(slargs[1], slargs[0]);
  }
  async runActionSet(actionSet, requestId) {
    if (!requestId) {
      requestId = "";
    }
    const actionData = JSON.stringify(actionSet);
    await this.runCommand("mct:runactions " + actionData, requestId);
  }
  async waitUntilStarted() {
    let waitTicks = 0;
    while (this.status !== 5 /* started */ && this.status !== 1 /* stopped */ && waitTicks < MaxTimeToWaitForServerToStart) {
      await import_Utilities.default.sleep(5);
      waitTicks++;
    }
    if (waitTicks >= MaxTimeToWaitForServerToStart) {
      import_Log.default.message("Timed out waiting for server to start.");
    }
  }
  async runCommandImmediate(command, tokenId) {
    import_Log.default.message("Running command: " + command);
    let targetResultLine = this.outputLines.length;
    await this.writeToServer(command);
    let waitTicks = 0;
    if (tokenId) {
      let foundLineIndex = -1;
      while (foundLineIndex < 0 && waitTicks < 500) {
        await import_Utilities.default.sleep(5);
        for (let i = targetResultLine; i < this.outputLines.length; i++) {
          if (this.outputLines[i].message.indexOf(tokenId) >= 0) {
            foundLineIndex = i;
          }
        }
        waitTicks++;
      }
      if (foundLineIndex >= 0) {
        let result = this.outputLines[foundLineIndex].message;
        import_Log.default.message("Command run complete: " + command + "| Result: " + result);
        return result;
      }
    } else {
      while (targetResultLine >= this.outputLines.length && waitTicks < 500) {
        await import_Utilities.default.sleep(5);
        waitTicks++;
      }
      if (this.outputLines.length > targetResultLine) {
        let result = this.outputLines[targetResultLine].message;
        import_Log.default.message("Command run complete: " + command + "| Result: " + result);
        return result;
      }
    }
    return void 0;
  }
  async runCommand(command, requestId, isInternal) {
    if (!requestId) {
      requestId = "";
    }
    const newCommand = this.#pendingCommands.length;
    this.#pendingCommands[newCommand] = command;
    this.#pendingRequestIds[newCommand] = requestId;
    this.#pendingCommandsInternal[newCommand] = isInternal === true;
    if (newCommand === this.#currentCommandId) {
      await this.executeNextCommand();
    }
  }
  /**
   * Run an internal command that doesn't show in logs.
   * Used for implementation details like querytarget polling.
   */
  async runInternalCommand(command, requestId) {
    return this.runCommand(command, requestId, true);
  }
  async writeToServer(commandLine) {
    if (this.#activeStdIn === null) {
      import_Log.default.message("Could not find active stdin to run command '" + commandLine + "'.");
      return;
    }
    commandLine = import_SecurityUtilities.default.sanitizeCommand(commandLine);
    if (!import_SecurityUtilities.default.isCommandSafe(commandLine)) {
      import_Log.default.message("Command rejected as unsafe: " + commandLine);
      return;
    }
    await (0, import_stringio.streamWrite)(this.#activeStdIn, commandLine + "\n");
  }
  async ensureServerFolders() {
    if (!this.#behaviorPacksStorage) {
      this.#behaviorPacksStorage = new import_NodeStorage.default(
        import_NodeStorage.default.ensureEndsWithDelimiter(this.serverPath) + "development_behavior_packs",
        ""
      );
      await this.#behaviorPacksStorage.rootFolder.ensureExists();
    }
    if (!this.#resourcePacksStorage) {
      this.#resourcePacksStorage = new import_NodeStorage.default(
        import_NodeStorage.default.ensureEndsWithDelimiter(this.serverPath) + "development_resource_packs",
        ""
      );
      await this.#resourcePacksStorage.rootFolder.ensureExists();
    }
    if (!this.#defaultWorldStorage) {
      this.#defaultWorldStorage = new import_NodeStorage.default(this.worldStoragePath, "");
      await this.#defaultWorldStorage.rootFolder.ensureExists();
    }
  }
  async restoreLatestBackupWorld() {
    if (this.#managedWorldId && this.#dsm.worldBackupManager && this.#defaultWorldStorage) {
      return await this.restoreManagedWorld();
    }
    const worldBackupContainerFolderExists = fs.existsSync(this.#worldBackupContainerFolder.fullPath);
    if (!worldBackupContainerFolderExists) {
      return false;
    }
    const folders = fs.readdirSync(this.#worldBackupContainerFolder.fullPath);
    let latestWorldName;
    let latestWorldDate = new Date(0, 0, 0);
    const operId = await this.#dsm.creatorTools.notifyOperationStarted(
      "Restoring world from '" + this.#worldBackupContainerFolder.fullPath + "'"
    );
    for (const folder of folders) {
      if (folder.startsWith("world") && folder.length === 19) {
        const dateStr = folder.substring(5);
        if (import_Utilities.default.isNumeric(dateStr)) {
          const fullPath = import_NodeStorage.default.ensureEndsWithDelimiter(this.#worldBackupContainerFolder.fullPath) + folder + import_NodeStorage.default.platformFolderDelimiter;
          const filesJsonPath = fullPath + "files.json";
          const filesJsonExists = fs.existsSync(filesJsonPath);
          const worldDate = import_Utilities.default.getDateFromStr(dateStr);
          if (filesJsonExists && worldDate.getTime() > latestWorldDate.getTime()) {
            latestWorldName = folder;
            latestWorldDate = worldDate;
          }
        }
      }
    }
    if (latestWorldName) {
      const lastBackupWorldFolder = this.#worldBackupContainerFolder.folders[latestWorldName];
      if (lastBackupWorldFolder && this.#defaultWorldStorage) {
        await this.#dsm.creatorTools.notifyStatusUpdate("Restoring world '" + lastBackupWorldFolder.name + "'");
        await lastBackupWorldFolder.copyContentsOut(this.#defaultWorldStorage.rootFolder);
        await this.#dsm.creatorTools.notifyOperationEnded(
          operId,
          "Completed restoring world '" + lastBackupWorldFolder.name + "'"
        );
        return true;
      }
    }
    await this.#dsm.creatorTools.notifyOperationEnded(operId, "Was not able to restore world.");
    return false;
  }
  /**
   * Restore the latest backup using the new WorldBackupManager system.
   */
  async restoreManagedWorld() {
    if (!this.#managedWorldId || !this.#dsm.worldBackupManager || !this.#defaultWorldStorage) {
      throw new Error("Managed restore requires managedWorldId and WorldBackupManager");
    }
    const world = this.#dsm.worldBackupManager.getWorld(this.#managedWorldId);
    if (!world) {
      import_Log.default.message(`No managed world found with ID ${this.#managedWorldId}`);
      return false;
    }
    await world.loadBackups();
    if (world.backups.length === 0) {
      import_Log.default.message(`No backups found for world ${this.#managedWorldId}`);
      return false;
    }
    const latestBackup = world.backups[world.backups.length - 1];
    const operId = await this.#dsm.creatorTools.notifyOperationStarted(
      `Restoring managed world '${world.friendlyName}' (${this.#managedWorldId})`
    );
    try {
      await this.#dsm.worldBackupManager.restoreBackup(
        this.#managedWorldId,
        latestBackup.id,
        this.#defaultWorldStorage.rootFolder.fullPath
      );
      await this.#dsm.creatorTools.notifyOperationEnded(operId, `Completed restoring world '${world.friendlyName}'`);
      return true;
    } catch (error) {
      import_Log.default.error(`Failed to restore managed world: ${error.message}`);
      await this.#dsm.creatorTools.notifyOperationEnded(operId, `Failed to restore world: ${error.message}`);
      return false;
    }
  }
  async applyWorldSettings(mcworld, startInfo) {
    if (startInfo?.worldSettings && startInfo.worldSettings.packageReferences) {
      for (let i = 0; i < startInfo.worldSettings.packageReferences.length; i++) {
        const packRefSet = startInfo.worldSettings.packageReferences[i];
        mcworld.ensurePackReferenceSet(packRefSet);
        if (packRefSet.resourcePackReferences.length > 0) {
          mcworld.deferredTechnicalPreviewExperiment = true;
        }
        if (packRefSet.behaviorPackReferences.length > 0) {
          mcworld.betaApisExperiment = true;
        }
      }
    }
    if (startInfo && startInfo.worldSettings) {
      if (startInfo.worldSettings.worldTemplateReferences === void 0 || startInfo.worldSettings.worldTemplateReferences.length <= 0) {
        mcworld.applyWorldSettings(startInfo?.worldSettings);
      }
    }
    await mcworld.save();
  }
  async getStorageFromPath(path) {
    if (!fs.existsSync(path)) {
      return void 0;
    }
    const content = await import_NodeStorage.default.createFromPath(path);
    if (content instanceof import_NodeFile.default && (path.endsWith(".mcpack") || path.endsWith(".mcaddon") || path.endsWith(".mcworld") || path.endsWith(".zip") || path.endsWith(".mcproject"))) {
      const zs = await import_ZipStorage.default.loadFromFile(content);
      return zs;
    }
    return void 0;
  }
  async ensureWorld(startInfo) {
    const worldServerStorage = new import_NodeStorage.default(this.worldStoragePath, "");
    const worldSourcePath = startInfo?.worldSettings?.worldContentPath;
    if (worldSourcePath) {
      let folder = await import_NodeStorage.default.createFromPathIncludingZip(worldSourcePath);
      if (folder) {
        await import_StorageUtilities.default.syncFolderTo(folder, worldServerStorage.rootFolder, false, false, false);
      }
    }
    const mcworld = new import_MCWorld.default();
    mcworld.folder = worldServerStorage.rootFolder;
    await mcworld.loadMetaFiles(false);
    await this.applyWorldSettings(mcworld, startInfo);
  }
  async ensureContentDeployed(startInfo) {
    if (startInfo?.additionalContentPath) {
      let folder = await import_NodeStorage.default.createFromPathIncludingZip(startInfo.additionalContentPath);
      if (folder) {
        await this.deploy(folder, false, false);
      }
    }
  }
  async startServer(restartIfAlreadyRunning, start) {
    if (start === void 0) {
      start = {
        worldSettings: this.#dsm.creatorTools.worldSettings,
        mode: import_ICreatorToolsData.DedicatedServerMode.auto,
        iagree: this.#env.iAgreeToTheMinecraftEndUserLicenseAgreementAndPrivacyStatementAtMinecraftDotNetSlashEula
      };
    }
    if (this.#status === 3 /* launching */ || this.#status === 5 /* started */ || this.#status === 4 /* starting */) {
      if (restartIfAlreadyRunning) {
        await this.stopServer();
      } else {
        return;
      }
    }
    let rootPath = this.serverPath;
    this.#onServerStarting.dispatch(this, "");
    this.#status = 3 /* launching */;
    const ns = new import_NodeStorage.default(rootPath, "");
    if (start.worldSettings?.backupType === import_IWorldSettings.BackupType.every2Minutes) {
      this.#backupInterval = (0, import_timers.setInterval)(this.doRunningBackup, 12e4);
    } else if (start.worldSettings?.backupType === import_IWorldSettings.BackupType.every5Minutes) {
      this.#backupInterval = (0, import_timers.setInterval)(this.doRunningBackup, 3e5);
    }
    if (this.#starts === 0) {
      await this.ensureServerFolders();
      this.properties.serverFolder = ns.rootFolder;
      this.properties.levelName = "defaultWorld";
      this.properties.contentLogFileEnabled = true;
      if (this.#port) {
        this.properties.serverPort = this.#port;
      }
      if (start && start.worldSettings) {
        this.properties.applyFromWorldSettings(start.worldSettings);
        if (start.worldSettings.enableDebugger !== void 0) {
          this.#enableDebugger = start.worldSettings.enableDebugger;
        }
        if (start.worldSettings.enableDebuggerStreaming !== void 0) {
          this.#enableDebuggerStreaming = start.worldSettings.enableDebuggerStreaming;
        }
      }
      await this.properties.writeFile();
      const configFolder = ns.rootFolder.ensureFolder("config");
      await configFolder.ensureExists();
      this.config.serverConfigFolder = configFolder;
      this.config.writeFiles();
    }
    rootPath = import_NodeStorage.default.ensureEndsWithDelimiter(rootPath);
    this.#env.utilities.validateFolderPath(rootPath);
    const executableName = os.platform() === "win32" ? "bedrock_server.exe" : "bedrock_server";
    const fullPath = rootPath + executableName;
    if (!fs.existsSync(fullPath)) {
      const errorMsg = `Server executable not found at ${fullPath}`;
      import_Log.default.fail(errorMsg);
      this.#status = 1 /* stopped */;
      this.#onServerError.dispatch(this, errorMsg);
      return;
    }
    if (os.platform() === "win32" && !start?.unsafeSkipSignatureValidation) {
      import_Log.default.message("Verifying digital signature of " + fullPath + "...");
      const sigResult = await import_LocalUtilities.default.verifyAuthenticodeSignature(fullPath);
      if (!sigResult.isValid) {
        const errorMsg = `Digital signature verification failed for ${fullPath}. Status: ${sigResult.status}. ${sigResult.error || ""}
This could indicate the file has been tampered with or corrupted.
If you trust this file, you can skip signature verification with --unsafe-skip-signature-validation.`;
        import_Log.default.fail(errorMsg);
        this.#status = 1 /* stopped */;
        this.#onServerError.dispatch(this, errorMsg);
        return;
      }
      if (!sigResult.isMicrosoftSigned) {
        const errorMsg = `Digital signature verification: ${fullPath} is signed, but not by Microsoft/Mojang. Signer: ${sigResult.signer || "unknown"}
This could indicate the file is not an official Minecraft Dedicated Server.
If you trust this file, you can skip signature verification with --unsafe-skip-signature-validation.`;
        import_Log.default.fail(errorMsg);
        this.#status = 1 /* stopped */;
        this.#onServerError.dispatch(this, errorMsg);
        return;
      }
      import_Log.default.message(`Signature verified: ${sigResult.signer}`);
    } else if (start?.unsafeSkipSignatureValidation) {
      import_Log.default.message("WARNING: Skipping digital signature verification as requested. This is unsafe.");
    }
    import_Log.default.message("Starting server from " + fullPath);
    const spawnOptions = {
      cwd: rootPath
    };
    if (os.platform() !== "win32") {
      spawnOptions.env = {
        ...process.env,
        LD_LIBRARY_PATH: rootPath
      };
    }
    const childProcess = (0, import_child_process.spawn)(fullPath, [], spawnOptions);
    this.#status = 4 /* starting */;
    childProcess.on("close", this.handleClose);
    this.#activeStdIn = childProcess.stdin;
    this.#activeProcess = childProcess;
    this.directOutput(childProcess.stdout);
    this.directErrors(childProcess.stderr);
    import_Log.default.verbose("Server '" + this.name + "' at '" + fullPath + "' launched (starts: " + this.#starts + ").");
  }
  async executeNextCommand() {
    if (this.#currentCommandId < this.#pendingCommands.length) {
      this.#currentCommandId++;
      const nextCommand = this.#currentCommandId - 1;
      const commandLine = this.#pendingCommands[nextCommand];
      const isInternal = this.#pendingCommandsInternal[nextCommand];
      if (!isInternal) {
        import_Log.default.message("Command " + this.#currentCommandId + " sent:" + commandLine);
      }
      await this.writeToServer(commandLine);
      await this.executeNextCommand();
    }
  }
  async doRunningBackup() {
    if (this.#backupStatus === 0 /* none */ || this.#backupStatus === 7 /* saveResumed */) {
      this.#backupStatus = 1 /* suspendingSaveCommandIssued */;
      if (this.#backupTimeoutTimer) {
        (0, import_timers.clearTimeout)(this.#backupTimeoutTimer);
      }
      this.#backupTimeoutTimer = setTimeout(() => {
        if (this.#backupStatus !== 0 /* none */ && this.#backupStatus !== 7 /* saveResumed */) {
          import_Log.default.error("Backup timed out after 60 seconds - forcing save resume");
          this.#backupStatus = 0 /* none */;
          this.runCommand("save resume");
        }
      }, 6e4);
      this.runCommand("save hold");
    }
  }
  async doBackup(backupFileLine) {
    if (this.#managedWorldId && this.#dsm.worldBackupManager && this.#defaultWorldStorage) {
      await this.doManagedBackup(backupFileLine);
      return;
    }
    const worldPath = "world" + import_Utilities.default.getDateStr(/* @__PURE__ */ new Date());
    const backupFolder = this.#worldBackupContainerFolder.ensureFolder(worldPath);
    await backupFolder.ensureExists();
    const inclusionList = [];
    if (backupFileLine) {
      const items = backupFileLine.split(", ");
      for (let i = 0; i < items.length; i++) {
        const fileItem = items[i].split(":");
        if (fileItem.length === 2) {
          let size = void 0;
          let path = fileItem[0];
          const firstSlash = path.indexOf("/");
          if (firstSlash > 0) {
            path = path.substring(firstSlash + 1);
          }
          try {
            size = parseInt(fileItem[1]);
          } catch {
          }
          if (size !== void 0 && firstSlash > 0) {
            inclusionList.push({ path, size });
          }
        }
      }
    }
    if (this.#defaultWorldStorage) {
      import_Log.default.message(
        "Backing world up to '" + this.#defaultWorldStorage.rootFolder.fullPath + "' to '" + backupFolder.fullPath + "'"
      );
      await this.#defaultWorldStorage.rootFolder.copyContentsTo(
        backupFolder.fullPath,
        inclusionList,
        backupFileLine === void 0,
        this.#dsm.backupWorldFileListings,
        import_StorageUtilities.default.ensureStartsWithDelimiter(
          import_StorageUtilities.default.ensureEndsWithDelimiter(
            this.#worldBackupContainerFolder.name + import_StorageUtilities.default.standardFolderDelimiter + worldPath
          )
        )
      );
    }
    if (inclusionList) {
      await backupFolder.saveFilesList(worldPath, inclusionList);
    }
  }
  /**
   * Create a backup using the new WorldBackupManager system.
   * This provides better organization, deduplication, and export capabilities.
   */
  async doManagedBackup(backupFileLine) {
    if (!this.#managedWorldId || !this.#dsm.worldBackupManager || !this.#defaultWorldStorage) {
      throw new Error("Managed backup requires managedWorldId and WorldBackupManager");
    }
    const inclusionList = [];
    if (backupFileLine) {
      const items = backupFileLine.split(", ");
      for (let i = 0; i < items.length; i++) {
        const fileItem = items[i].split(":");
        if (fileItem.length === 2) {
          let size = void 0;
          let path = fileItem[0];
          const firstSlash = path.indexOf("/");
          if (firstSlash > 0) {
            path = path.substring(firstSlash + 1);
          }
          try {
            size = parseInt(fileItem[1]);
          } catch {
          }
          if (size !== void 0 && firstSlash > 0) {
            inclusionList.push({ path, size });
          }
        }
      }
    }
    import_Log.default.message(`Creating managed backup for world ${this.#managedWorldId}`);
    const backupType = backupFileLine !== void 0 ? import_IWorldBackupData.WorldBackupType.runtime : import_IWorldBackupData.WorldBackupType.shutdown;
    const result = await this.#dsm.worldBackupManager.createBackup(
      this.#managedWorldId,
      this.#defaultWorldStorage.rootFolder.fullPath,
      {
        backupType,
        incrementalFileList: inclusionList.length > 0 ? inclusionList : void 0,
        notes: backupType === import_IWorldBackupData.WorldBackupType.shutdown ? "Server shutdown backup" : "Runtime incremental backup"
      }
    );
    this.#lastBackupResult = result;
    if (result.stats) {
      import_Log.default.message(
        `Managed backup complete: ${result.stats.newFiles} files written, ${result.stats.deduplicatedFiles} files deduped, ${result.stats.totalBytes} bytes written, ${result.stats.savedBytes} bytes saved`
      );
    } else {
      import_Log.default.message(`Managed backup complete: ${result.success ? "success" : "failed"}`);
    }
  }
  async stopServer() {
    if (this.#activeProcess !== null) {
      import_Log.default.message("Stopping server '" + this.name + "'...");
      this.#onServerStopping.dispatch(this, "stop");
      await this.writeToServer("stop");
    }
  }
  async handleClose() {
    if (this.#activeProcess) {
      this.#dsm.creatorTools.notifyStatusUpdate("Server was closed unexpectedly.");
      this.#unexpectedStopLog.push(/* @__PURE__ */ new Date());
      const statusBefore = this.#status;
      this.#activeProcess = null;
      await this.finalizeStopServer();
      const recentStarts = this.getRecentStarts(6e4);
      if (recentStarts < 4) {
        const backoffMs = Math.min(1e3 * Math.pow(2, recentStarts), 16e3);
        this.#dsm.creatorTools.notifyStatusUpdate(
          `Restarting server in ${backoffMs / 1e3}s (recent stops: ${recentStarts})`
        );
        if (statusBefore === 5 /* started */) {
          await import_Utilities.default.sleep(backoffMs);
          await this.startServer(true, void 0);
        }
      } else {
        this.#dsm.creatorTools.notifyStatusUpdate(
          "Restarted too many times in a 60 second window; not auto-restarting."
        );
      }
    }
  }
  getRecentStarts(timeWindowMs) {
    const now = /* @__PURE__ */ new Date();
    let recents = 0;
    for (let i = 0; i < this.#unexpectedStopLog.length; i++) {
      if (now.getTime() - this.#unexpectedStopLog[i].getTime() < timeWindowMs) {
        recents++;
      }
    }
    return recents;
  }
  async continueStopServer() {
    if (this.#activeProcess !== null) {
      const proc = this.#activeProcess;
      this.#activeProcess = null;
      await (0, import_stringio.onExit)(proc);
      this.finalizeStopServer();
    }
  }
  async finalizeStopServer() {
    if (this.#backupInterval) {
      (0, import_timers.clearTimeout)(this.#backupInterval);
      this.#backupInterval = void 0;
    }
    if (this.#backupTimeoutTimer) {
      (0, import_timers.clearTimeout)(this.#backupTimeoutTimer);
      this.#backupTimeoutTimer = void 0;
    }
    this.stopPlayerPositionPolling();
    this.disconnectDebugClient();
    await this.doBackup();
    this.#onServerStopped.dispatch(this, "stop");
    this.#status = 1 /* stopped */;
    import_Log.default.message("Server '" + this.name + "' stopped.");
  }
  async deploy(fromFolder, isPatch, isReloadable) {
    const originalStatus = this.#status;
    let filesConsidered = 0;
    let filesUpdated = 0;
    let wroteWorld = false;
    import_Log.default.important("Starting deployment from '" + fromFolder.fullPath + "'...");
    await this.#dsm.creatorTools?.notifyStatusUpdate("Starting deployment...");
    await fromFolder.load(true);
    if (fromFolder.folders["build"]) {
      fromFolder = fromFolder.folders["build"];
      await fromFolder.load(true);
    } else if (fromFolder.folders["out"]) {
      fromFolder = fromFolder.folders["out"];
      await fromFolder.load(true);
    } else if (fromFolder.folders["dist"]) {
      fromFolder = fromFolder.folders["dist"];
      await fromFolder.load(true);
    }
    if (!this.#dsm.creatorTools) {
      import_Log.default.fail("Could not find associated context in dedicated server::deploy.");
      return;
    }
    let doReload = false;
    if (originalStatus !== 5 /* started */) {
      doReload = false;
    }
    import_Log.default.message("Deploying from '" + fromFolder.fullPath + "'");
    let deployProj = new import_Project.default(this.#dsm.creatorTools, "deploy", null);
    deployProj.setProjectFolder(fromFolder);
    await deployProj.inferProjectItemsFromFiles();
    if (deployProj.distBuildFolder) {
      fromFolder = deployProj.distBuildFolder;
      deployProj = new import_Project.default(this.#dsm.creatorTools, "deploy", null);
      deployProj.setProjectFolder(fromFolder);
      await deployProj.inferProjectItemsFromFiles();
    }
    let originalBehaviorPackTargetThumbprint = void 0;
    let originalResourcePackTargetThumbprint = void 0;
    if (doReload) {
      if (this.#behaviorPacksStorage) {
        originalBehaviorPackTargetThumbprint = new import_Thumbprint.default();
        await originalBehaviorPackTargetThumbprint.create(this.#behaviorPacksStorage.rootFolder);
      }
      if (this.#resourcePacksStorage) {
        originalResourcePackTargetThumbprint = new import_Thumbprint.default();
        await originalResourcePackTargetThumbprint.create(this.#resourcePacksStorage.rootFolder);
      }
    }
    this.#status = 2 /* deploying */;
    await this.ensureServerFolders();
    if (this.#behaviorPacksStorage && this.#defaultWorldStorage) {
      let folderName = void 0;
      const bpFolder = await deployProj.getDefaultBehaviorPackFolder();
      if (bpFolder !== null) {
        folderName = import_StorageUtilities.default.getAvailableFolderName(bpFolder);
        import_Log.default.message("Deploying default behavior pack from '" + folderName + "'");
        const defaultBehaviorPackFolder = this.#behaviorPacksStorage.rootFolder.ensureFolder(folderName);
        await defaultBehaviorPackFolder.ensureExists();
        filesConsidered += await import_StorageUtilities.default.syncFolderTo(
          bpFolder,
          defaultBehaviorPackFolder,
          false,
          false,
          false
        );
      }
      if (fromFolder.folders["development_behavior_packs"]) {
        const dbpSourceFolder = fromFolder.folders["development_behavior_packs"];
        import_Log.default.message(
          "Synchronizing all dev BP folders from '" + dbpSourceFolder.fullPath + "' to '" + this.#behaviorPacksStorage.rootFolder.fullPath + "'"
        );
        filesConsidered += await import_StorageUtilities.default.syncFolderTo(
          dbpSourceFolder,
          this.#behaviorPacksStorage.rootFolder,
          false,
          false,
          false,
          folderName ? [folderName] : void 0
        );
      }
      if (fromFolder.folders["behavior_packs"]) {
        const dbpSourceFolder = fromFolder.folders["behavior_packs"];
        filesConsidered += await import_StorageUtilities.default.syncFolderTo(
          dbpSourceFolder,
          this.#behaviorPacksStorage.rootFolder,
          false,
          false,
          false,
          folderName ? [folderName] : void 0
        );
      }
      await this.#behaviorPacksStorage.rootFolder.saveAll();
    }
    if (this.#resourcePacksStorage && this.#defaultWorldStorage) {
      let folderName = void 0;
      const rpFolder = await deployProj.getDefaultResourcePackFolder();
      if (rpFolder !== null) {
        folderName = import_StorageUtilities.default.getAvailableFolderName(rpFolder);
        const defaultResourcePackFolder = this.#resourcePacksStorage.rootFolder.ensureFolder(folderName);
        await defaultResourcePackFolder.ensureExists();
        filesConsidered += await import_StorageUtilities.default.syncFolderTo(
          rpFolder,
          defaultResourcePackFolder,
          false,
          false,
          false
        );
      }
      if (fromFolder.folders["development_resource_packs"]) {
        const drpSourceFolder = fromFolder.folders["development_resource_packs"];
        import_Log.default.message(
          "Synchronizing all dev RP folders from '" + drpSourceFolder.fullPath + "' to '" + this.#resourcePacksStorage.rootFolder.fullPath + "'"
        );
        filesConsidered += await import_StorageUtilities.default.syncFolderTo(
          drpSourceFolder,
          this.#resourcePacksStorage.rootFolder,
          false,
          false,
          false,
          folderName ? [folderName] : void 0
        );
      }
      if (fromFolder.folders["resource_packs"]) {
        const drpSourceFolder = fromFolder.folders["resource_packs"];
        filesConsidered += await import_StorageUtilities.default.syncFolderTo(
          drpSourceFolder,
          this.#resourcePacksStorage.rootFolder,
          false,
          false,
          false,
          folderName ? [folderName] : void 0
        );
      }
      await this.#resourcePacksStorage.rootFolder.saveAll();
    }
    if (this.#defaultWorldStorage) {
      const worldFolder = await deployProj.getDefaultWorldFolder();
      if (worldFolder !== null) {
        if (!worldFolder.isLoaded) {
          await worldFolder.load();
        }
        if (worldFolder.fileCount > 0) {
          filesConsidered += await import_StorageUtilities.default.syncFolderTo(
            worldFolder,
            this.#defaultWorldStorage.rootFolder,
            false,
            false,
            false
          );
          wroteWorld = true;
          this.#defaultWorldStorage.rootFolder.saveAll();
        }
      }
    }
    let nextResourcePackThumbprint = void 0;
    if (doReload && this.#resourcePacksStorage && originalResourcePackTargetThumbprint && this.deployCount > 0) {
      nextResourcePackThumbprint = new import_Thumbprint.default();
      await nextResourcePackThumbprint.create(this.#resourcePacksStorage.rootFolder);
      if (nextResourcePackThumbprint) {
        const diffSet = originalResourcePackTargetThumbprint.compare(nextResourcePackThumbprint, true);
        filesUpdated += diffSet.fileDifferences.length;
        if (diffSet.fileDifferences.length > 0 || diffSet.folderDifferences.length > 0) {
          doReload = false;
        }
      }
    }
    let nextBehaviorPackThumbprint = void 0;
    if (doReload && this.#behaviorPacksStorage && originalBehaviorPackTargetThumbprint && this.deployCount > 0) {
      nextBehaviorPackThumbprint = new import_Thumbprint.default();
      await nextBehaviorPackThumbprint.create(this.#behaviorPacksStorage.rootFolder);
      if (nextBehaviorPackThumbprint) {
        const diffSet = originalBehaviorPackTargetThumbprint.compare(nextBehaviorPackThumbprint, true);
        filesUpdated += diffSet.fileDifferences.length;
        if (!import_MinecraftUtilities.default.isReloadableSetOfChanges(diffSet)) {
          doReload = false;
        }
      }
    }
    if (this.#defaultWorldStorage && !wroteWorld) {
      const hasScript = await deployProj.hasScript();
      const mcworld = new import_MCWorld.default();
      mcworld.folder = this.#defaultWorldStorage.rootFolder;
      await mcworld.loadMetaFiles(false);
      let needsSave = mcworld.ensureBehaviorPack(
        deployProj.defaultBehaviorPackUniqueId,
        deployProj.defaultBehaviorPackVersion,
        deployProj.name
      ) || mcworld.ensureResourcePack(
        deployProj.defaultResourcePackUniqueId,
        deployProj.defaultResourcePackVersion,
        deployProj.name
      );
      if (hasScript) {
        if (!mcworld.betaApisExperiment) {
          mcworld.betaApisExperiment = true;
          needsSave = true;
        }
      }
      if (needsSave) {
        import_Log.default.message("Updating world pack references for deployed packs.");
        await mcworld.save();
      }
    }
    if (!doReload || originalStatus !== 5 /* started */) {
      if (originalStatus === 1 /* stopped */) {
        import_Log.default.message("Starting world after deployment.");
        await this.#dsm.creatorTools?.notifyStatusUpdate("Starting server after deployment...");
      } else if (doReload) {
        import_Log.default.message("Server needs restarting due to significant file change.");
        await this.#dsm.creatorTools?.notifyStatusUpdate("Restarting server due to file changes...");
      } else {
        import_Log.default.message("Ensuring world is started after deployment (" + originalStatus + ")");
        await this.#dsm.creatorTools?.notifyStatusUpdate("Restarting server for deployment...");
      }
      if (originalStatus !== 1 /* stopped */) {
        this.#status = 1 /* stopped */;
        await this.stopServer();
      }
      await this.startServer(true, void 0);
      this.deployCount++;
      await this.waitUntilStarted();
      if (this.status === 5 /* started */) {
        import_Log.default.important("Deployment complete. Server restarted and ready.");
        await this.#dsm.creatorTools?.notifyStatusUpdate("Deployment complete. Server restarted.");
      } else {
        import_Log.default.message("Deployment complete but server may still be starting (status: " + this.status + ")");
        await this.#dsm.creatorTools?.notifyStatusUpdate("Deployment complete. Server starting...");
      }
    } else if (filesUpdated === 0) {
      this.#status = 5 /* started */;
      import_Log.default.important("No new files deployed.");
      await this.#dsm.creatorTools?.notifyStatusUpdate("Deployment complete. No new files.");
      await this.runCommand("say World has been reloaded.");
      this.#onServerRefreshed.dispatch(this, "reload");
    } else {
      this.#status = 5 /* started */;
      import_Log.default.important(
        filesUpdated + " files updated; hot-reloading world '" + this.name + "' at " + (/* @__PURE__ */ new Date()).toString()
      );
      await this.#dsm.creatorTools?.notifyStatusUpdate("Hot-reloading " + filesUpdated + " files...");
      await this.runCommand("reload");
      await this.runCommand("say World has been reloaded.");
      import_Log.default.important("Deployment complete. World hot-reloaded.");
      await this.#dsm.creatorTools?.notifyStatusUpdate("Deployment complete. World hot-reloaded.");
      this.#onServerRefreshed.dispatch(this, "reload");
    }
  }
  async directOutput(readable) {
    let time = (/* @__PURE__ */ new Date()).getTime();
    for await (const line of (0, import_stringio.chunksToLinesAsync)(readable)) {
      if (line !== void 0 && line.length >= 0) {
        let lineUp = line.replace(/\\n/g, "");
        lineUp = lineUp.replace(/\\r/g, "").trim();
        let port = this.port;
        if (!port) {
          port = 19132;
        }
        const sm = new import_ServerMessage.default(lineUp);
        if (sm.category !== import_ServerMessage.ServerMessageCategory.internalSystemMessage) {
          import_Log.default.message(this.name + "@" + port + ": " + lineUp);
        }
        if (sm.category === import_ServerMessage.ServerMessageCategory.serverStarted) {
          this.#starts++;
          if (this.#enableDebugger && this.#enableDebuggerStreaming) {
            if (this.#delayDebugClientUntilPlayerJoins) {
              import_Log.default.debug(`[Debug] Debugger enabled but DELAYING 'script debugger listen' until player joins`);
              this.#debugListenerReady = false;
            } else {
              const me = this;
              const debugPort = this.debugPort;
              setTimeout(async function() {
                import_Log.default.debug(`[Debug] Sending 'script debugger listen ${debugPort}' command...`);
                me.#awaitingDebuggerListening = true;
                await me.runCommand(`script debugger listen ${debugPort}`);
                import_Log.default.debug(`[Debug] Command sent, waiting for 'Debugger listening' response...`);
                setTimeout(function() {
                  if (me.#awaitingDebuggerListening && me.#enableDebuggerStreaming) {
                    me.#awaitingDebuggerListening = false;
                    me.#debugListenerReady = true;
                    if (me.#delayDebugClientUntilPlayerJoins) {
                      import_Log.default.debug(`[Debug] Timeout waiting for 'Debugger listening', will connect when player joins...`);
                    } else {
                      import_Log.default.debug(`[Debug] Timeout waiting for 'Debugger listening', attempting connection anyway...`);
                      me.connectDebugClient();
                    }
                  }
                }, 1e4);
              }, 3e3);
            }
          }
          this.#status = 5 /* started */;
          this.handleServerStarted(lineUp);
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.debuggerListening) {
          if (this.#awaitingDebuggerListening) {
            this.#awaitingDebuggerListening = false;
            this.#debugListenerReady = true;
            if (this.#enableDebuggerStreaming) {
              if (this.#delayDebugClientUntilPlayerJoins && !this.#hasPlayerJoined) {
                import_Log.default.debug(`[Debug] Debugger listening - waiting for player to join before connecting client`);
              } else {
                import_Log.default.debug(`[Debug] Debugger listening, connecting debug client...`);
                this.connectDebugClient();
              }
            } else {
              import_Log.default.debug(`[Debug] Debugger listening, but streaming not enabled, skipping client connection`);
            }
          }
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.serverStopped) {
          await this.continueStopServer();
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.playerConnected) {
          const playerName = this.getPlayerIdFromLine(lineUp);
          const xuid = this.getPlayerXuidFromLine(lineUp);
          import_Log.default.message("Player '" + playerName + "' connected.");
          this.#hasPlayerJoined = true;
          if (this.#enableDebugger && this.#enableDebuggerStreaming && !this.#debugClient && this.#delayDebugClientUntilPlayerJoins) {
            import_Log.default.debug(`[Debug] Player connected - NOW starting debugger setup (was delayed until player join)...`);
            const me = this;
            const debugPort = this.debugPort;
            setTimeout(async function() {
              import_Log.default.debug(`[Debug] Sending 'script debugger listen ${debugPort}' command (post-player-join)...`);
              me.#awaitingDebuggerListening = true;
              await me.runCommand(`script debugger listen ${debugPort}`);
              import_Log.default.debug(`[Debug] Command sent, waiting for 'Debugger listening' response...`);
              setTimeout(function() {
                if (me.#awaitingDebuggerListening && me.#enableDebuggerStreaming) {
                  me.#awaitingDebuggerListening = false;
                  me.#debugListenerReady = true;
                  import_Log.default.debug(`[Debug] Timeout waiting for 'Debugger listening', connecting client anyway...`);
                  me.connectDebugClient();
                }
              }, 1e4);
            }, 1e3);
          }
          if (playerName && xuid) {
            const p = new import_Player.default();
            p.id = import_SecurityUtilities.default.sanitizePlayerName(playerName);
            p.xuid = xuid;
            this.handlePlayerConnected(p);
          }
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.playerDisconnected) {
          const playerName = this.getPlayerIdFromLine(lineUp);
          const xuid = this.getPlayerXuidFromLine(lineUp);
          import_Log.default.message("Player '" + playerName + "' disconnected.");
          if (playerName && xuid) {
            const p = new import_Player.default();
            p.id = import_SecurityUtilities.default.sanitizePlayerName(playerName);
            p.xuid = xuid;
            this.handlePlayerDisconnected(p);
          }
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.backupSaving && this.#backupStatus === 1 /* suspendingSaveCommandIssued */) {
          this.#backupStatus = 2 /* suspendingQueryCommandIssued */;
          this.runCommand("save query");
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.backupSaved && this.#backupStatus === 2 /* suspendingQueryCommandIssued */) {
          this.#backupStatus = 3 /* suspendingQueryResultsPending */;
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.levelDatUpdate && this.#backupStatus === 3 /* suspendingQueryResultsPending */) {
          this.#backupStatus = 5 /* copyingFiles */;
          await this.doBackup(lineUp);
          this.#backupStatus = 6 /* resumingSave */;
          this.runCommand("save resume");
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.backupComplete && this.#backupStatus === 6 /* resumingSave */) {
          this.#backupStatus = 0 /* none */;
          if (this.#backupTimeoutTimer) {
            (0, import_timers.clearTimeout)(this.#backupTimeoutTimer);
            this.#backupTimeoutTimer = void 0;
          }
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.gameTestLoaded) {
          let testName = this.getTestIdFromLine(lineUp);
          if (testName === void 0) {
            testName = "(unknown test id)";
          }
          this.#onTestStarted.dispatch(this, testName);
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.gameTestFailed) {
          let testName = this.getTestIdFromLine(lineUp);
          if (testName === void 0) {
            testName = "(unknown test id)";
          }
          this.#onTestFailed.dispatch(this, testName);
        } else if (sm.category === import_ServerMessage.ServerMessageCategory.gameTestPassed) {
          let testName = this.getTestIdFromLine(lineUp);
          if (testName === void 0) {
            testName = "(unknown test id)";
          }
          this.#onTestSucceeded.dispatch(this, testName);
        }
        this.#lastResult = lineUp;
        this.outputLines.push({ message: lineUp, received: time });
        time++;
        this.#onServerOutput.dispatch(this, sm);
      }
    }
  }
  handleServerStarted(line) {
    import_Log.default.message("handleServerStarted called: " + line);
    if (this.gameTest) {
      const me = this;
      setTimeout(function() {
        import_Log.default.message("Running gametest " + me.gameTest);
        me.writeToServer("gametest run " + me.gameTest);
      }, 5e3);
    }
    this.startPlayerPositionPolling();
    this.#onServerStarted.dispatch(this, line);
  }
  /**
   * Connect to the Minecraft script debugger.
   * The server should already be listening on the debug port after
   * receiving the "script debugger listen <port>" command.
   */
  async connectDebugClient() {
    if (this.#debugClient) {
      import_Log.default.debug(`[Debug] connectDebugClient: Already have a debug client, skipping`);
      return;
    }
    const port = this.debugPort;
    import_Log.default.debug(`[Debug] connectDebugClient: Creating MinecraftDebugClient for localhost:${port}...`);
    this.#debugClient = new import_MinecraftDebugClient.default();
    this.#debugClient.onConnected.subscribe((client, sessionInfo) => {
      import_Log.default.debug(`[Debug] Debug client connected: protocol v${sessionInfo.protocolVersion}`);
      this.#onDebugConnected.dispatch(this, sessionInfo);
    });
    this.#debugClient.onDisconnected.subscribe((client, reason) => {
      import_Log.default.debug(`[Debug] Debug client disconnected: ${reason}`);
      this.#onDebugDisconnected.dispatch(this, reason);
    });
    this.#debugClient.onStats.subscribe((client, statsData) => {
      import_Log.default.verbose(`[Debug] DedicatedServer: Received stats tick=${statsData.tick}, stats=${statsData.stats.length}`);
      this.#onDebugStats.dispatch(this, statsData);
    });
    this.#debugClient.onStopped.subscribe((client, stoppedEvent) => {
      import_Log.default.verbose(`[Debug] Script execution paused: ${stoppedEvent.reason}`);
      this.#onDebugPaused.dispatch(this, stoppedEvent.reason);
    });
    this.#debugClient.onProfilerCapture.subscribe((client, captureEvent) => {
      import_Log.default.message(`[Debug] Profiler capture received: ${captureEvent.capture_base_path}`);
      this.#onProfilerCapture.dispatch(this, captureEvent);
    });
    this.#debugClient.onError.subscribe((client, error) => {
      import_Log.default.debug(`Debug client error: ${error}`);
    });
    try {
      import_Log.default.debug(`[Debug] connectDebugClient: Calling connect(localhost, ${this.debugPort})...`);
      await this.#debugClient.connect("localhost", this.debugPort);
      import_Log.default.debug(`[Debug] connectDebugClient: connect() returned successfully`);
    } catch (e) {
      import_Log.default.message(`[Debug] connectDebugClient: connect() FAILED: ${e}`);
      this.#debugClient = void 0;
    }
  }
  /**
   * Disconnect the debug client if connected.
   */
  disconnectDebugClient() {
    if (this.#debugClient) {
      this.#debugClient.disconnect();
      this.#debugClient = void 0;
    }
  }
  /**
   * Start polling for player positions.
   * Uses /querytarget @a to get all player positions periodically.
   */
  startPlayerPositionPolling() {
    if (this.#playerPositionPollInterval) {
      return;
    }
    import_Log.default.message("Starting player position polling");
    const pollFn = async () => {
      if (this.#status !== 5 /* started */) {
        return;
      }
      try {
        const startLineIndex = this.outputLines.length;
        await this.runInternalCommand("querytarget @a");
        await import_Utilities.default.sleep(500);
        let jsonLines = [];
        let collecting = false;
        let bracketCount = 0;
        for (let i = startLineIndex; i < this.outputLines.length; i++) {
          const line = this.outputLines[i].message;
          if (line.includes("Target data:")) {
            collecting = true;
            const dataStart = line.indexOf("Target data:");
            const afterData = line.substring(dataStart + "Target data:".length).trim();
            if (afterData) {
              jsonLines.push(afterData);
              bracketCount += (afterData.match(/\[/g) || []).length;
              bracketCount -= (afterData.match(/\]/g) || []).length;
            }
            continue;
          }
          if (collecting) {
            let cleanLine = line;
            if (cleanLine.match(/^\[\d{4}-\d{2}-\d{2}/)) {
              break;
            }
            jsonLines.push(cleanLine);
            bracketCount += (cleanLine.match(/\[/g) || []).length;
            bracketCount -= (cleanLine.match(/\]/g) || []).length;
            if (bracketCount === 0 && jsonLines.length > 0) {
              break;
            }
          }
        }
        if (jsonLines.length > 0) {
          const jsonStr = jsonLines.join("");
          import_Log.default.verbose("Collected JSON: " + jsonStr.substring(0, 100) + "...");
          if (jsonStr.startsWith("[")) {
            const players = JSON.parse(jsonStr);
            import_Log.default.verbose("Parsed " + players.length + " players from querytarget");
            for (const player of players) {
              if (player.uniqueId && player.position) {
                const playerId = player.uniqueId;
                const lastPos = this.#lastPlayerPositions.get(playerId);
                const newPos = player.position;
                const dimension = player.dimension ?? 0;
                let shouldDispatch = !lastPos;
                if (lastPos) {
                  const dx = newPos.x - lastPos.x;
                  const dy = newPos.y - lastPos.y;
                  const dz = newPos.z - lastPos.z;
                  const distance = Math.sqrt(dx * dx + dy * dy + dz * dz);
                  shouldDispatch = distance >= PLAYER_MOVE_THRESHOLD || dimension !== lastPos.dimension;
                }
                if (shouldDispatch) {
                  this.#lastPlayerPositions.set(playerId, { ...newPos, dimension });
                  const playerName = this.getPlayerNameFromId(playerId) ?? playerId;
                  import_Log.default.message("Dispatching player position update for " + playerName + " at " + JSON.stringify(newPos));
                  const travelEvent = {
                    eventId: `poll_${Date.now()}_${playerId}`,
                    header: {
                      eventName: "PlayerTravelled",
                      purpose: "event",
                      version: 1
                    },
                    body: {
                      isUnderwater: false,
                      metersTravelled: lastPos ? Math.sqrt(
                        (newPos.x - lastPos.x) ** 2 + (newPos.y - lastPos.y) ** 2 + (newPos.z - lastPos.z) ** 2
                      ) : 0,
                      newBiome: 0,
                      player: {
                        color: "",
                        dimension,
                        id: player.id ?? 0,
                        name: playerName,
                        position: newPos,
                        type: "player",
                        variant: 0,
                        yRot: player.yRot ?? 0
                      },
                      travelMethod: 0
                    }
                  };
                  this.#onServerGameEvent.dispatch(this, travelEvent);
                }
              }
            }
          }
        }
      } catch (e) {
        import_Log.default.verbose("Player position poll error: " + e);
      }
    };
    setTimeout(pollFn, 2e3);
    this.#playerPositionPollInterval = (0, import_timers.setInterval)(pollFn, PLAYER_POSITION_POLL_INTERVAL);
  }
  /**
   * Stop player position polling.
   */
  stopPlayerPositionPolling() {
    if (this.#playerPositionPollInterval) {
      clearInterval(this.#playerPositionPollInterval);
      this.#playerPositionPollInterval = void 0;
    }
    this.#lastPlayerPositions.clear();
  }
  /**
   * Get player name from uniqueId by looking up connected players.
   */
  getPlayerNameFromId(uniqueId) {
    return void 0;
  }
  handlePlayerConnected(player) {
    if (this.#opList) {
      for (let i = 0; i < this.#opList.length; i++) {
        const op = this.#opList[i];
        const me = this;
        if (op === player.id) {
          setTimeout(function() {
            me.writeToServer("op " + player.id);
          }, 5e3);
        }
      }
    }
    this.#onPlayerConnected.dispatch(this, player);
  }
  handlePlayerDisconnected(player) {
    this.#onPlayerDisconnected.dispatch(this, player);
  }
  getTestIdFromLine(line) {
    const firstColon = line.indexOf(":");
    if (firstColon < 0) {
      return void 0;
    }
    let nextSpace = line.indexOf(" ", firstColon + 1);
    if (nextSpace < 0) {
      nextSpace = line.length;
    }
    return line.substring(firstColon + 1, nextSpace);
  }
  getPlayerIdFromLine(line) {
    const playerIndex = line.indexOf("Player ");
    if (playerIndex < 0) {
      return void 0;
    }
    const nextColon = line.indexOf(":", playerIndex);
    if (nextColon < 0) {
      return void 0;
    }
    const nextComma = line.indexOf(",", nextColon);
    if (nextComma < 0) {
      return void 0;
    }
    return line.substring(nextColon + 2, nextComma);
  }
  getPlayerXuidFromLine(line) {
    const xuidIndex = line.indexOf("xuid");
    if (xuidIndex < 0) {
      return void 0;
    }
    const nextColon = line.indexOf(":", xuidIndex);
    if (nextColon < 0) {
      return void 0;
    }
    let nextComma = line.indexOf(",", nextColon);
    if (nextComma < 0) {
      nextComma = line.length;
    }
    return line.substring(nextColon + 2, nextComma);
  }
  async directErrors(readable) {
    for await (const line of (0, import_stringio.chunksToLinesAsync)(readable)) {
      if (line !== void 0 && line.length >= 0) {
        let lineUp = line.replace(/\\n/g, "");
        lineUp = lineUp.replace(/\\r/g, "");
        this.#onServerError.dispatch(this, lineUp);
        import_Log.default.message("Server error: " + lineUp);
      }
    }
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  DedicatedServerBackupStatus,
  DedicatedServerStatus,
  MaxTimeToWaitForServerToStart
});
//# sourceMappingURL=DedicatedServer.js.map
