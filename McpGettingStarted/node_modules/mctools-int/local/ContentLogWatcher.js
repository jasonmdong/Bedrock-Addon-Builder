"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ContentLogWatcher_exports = {};
__export(ContentLogWatcher_exports, {
  default: () => ContentLogWatcher
});
module.exports = __toCommonJS(ContentLogWatcher_exports);
var fs = __toESM(require("fs"));
var import_MinecraftLogItem = __toESM(require("./MinecraftLogItem"));
var import_ste_events = require("ste-events");
class ContentLogWatcher {
  _onLogItem = new import_ste_events.EventDispatcher();
  _env;
  logItems = [];
  constructor(env) {
    this._env = env;
  }
  get onLogItem() {
    return this._onLogItem.asEvent();
  }
  watchServerFolder() {
    const mcLogPath = this._env.utilities.localReleaseServerLogPath;
    if (!mcLogPath || !fs.existsSync(mcLogPath)) {
      return;
    }
    this.watchLogFolder(mcLogPath);
  }
  watchMinecraftReleaseFolder() {
    let mcLogPath = this._env.utilities.localReleaseServerLogPath;
    if (!mcLogPath) {
      return;
    }
    this.watchLogFolder(mcLogPath);
  }
  watchLogFolder(mcLogPath) {
    const exists = fs.existsSync(mcLogPath);
    if (exists) {
      const now = /* @__PURE__ */ new Date();
      this.processFolder(mcLogPath, new Date(now.getTime() - 30 * 60 * 1e3));
    }
  }
  processFolder(mcLogPath, sinceDate) {
    const sinceDateMs = sinceDate.getTime();
    const exists = fs.existsSync(mcLogPath);
    if (exists) {
      const files = fs.readdirSync(mcLogPath);
      for (let i = 0; i < files.length; i++) {
        const file = files[i];
        const stat = fs.statSync(mcLogPath + file);
        if (stat.isFile() && stat.mtimeMs > sinceDateMs) {
          this.processFile(mcLogPath, file);
        }
      }
    }
  }
  processFile(logPath, fileName) {
    if (fileName !== null) {
      fileName = fileName.toLowerCase();
      if (fileName.startsWith("contentlog__")) {
        const content = fs.readFileSync(logPath + fileName, { encoding: "utf8", flag: "r" });
        const logItem = new import_MinecraftLogItem.default();
        logItem.fileName = fileName;
        logItem.message = content;
        this.logItems.push(logItem);
        this._onLogItem.dispatch(this, logItem);
      }
    }
  }
}
//# sourceMappingURL=ContentLogWatcher.js.map
