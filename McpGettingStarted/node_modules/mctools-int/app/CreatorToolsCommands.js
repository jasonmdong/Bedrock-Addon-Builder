"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CreatorToolsCommands_exports = {};
__export(CreatorToolsCommands_exports, {
  default: () => CreatorToolsCommands
});
module.exports = __toCommonJS(CreatorToolsCommands_exports);
var import_Log = __toESM(require("../core/Log"));
var import_CommandRegistry = __toESM(require("./CommandRegistry"));
var import_ICommand = require("./ICommand");
class CreatorToolsCommands {
  static async debugProjectInformation(context, name, args) {
    if (!context.project) {
      return { status: import_ICommand.CommandStatus.invalidArguments };
    }
    let message = "N: " + context.project.name;
    if (context.project.projectFolder) {
      message += " PF: " + context.project.projectFolder.fullPath;
    }
    import_Log.default.message(message);
    return { status: import_ICommand.CommandStatus.completed };
  }
  static async debugMinecraft(context, name, args) {
    let message = "LAM: " + context.creatorTools.lastActiveMinecraftFlavor;
    if (context.creatorTools.remoteServerUrl) {
      message += " RSURL: " + context.creatorTools.remoteServerUrl;
    }
    if (context.creatorTools.remoteServerPort) {
      message += " RSP: " + context.creatorTools.remoteServerPort;
    }
    if (context.creatorTools.dedicatedServerMode) {
      message += " DSM: " + context.creatorTools.dedicatedServerMode;
    }
    if (context.creatorTools.dedicatedServerPath) {
      message += " DSP: " + context.creatorTools.dedicatedServerPath;
    }
    if (context.creatorTools.dedicatedServerSlotCount) {
      message += " DSS: " + context.creatorTools.dedicatedServerSlotCount;
    }
    import_Log.default.message(message);
    return { status: import_ICommand.CommandStatus.completed };
  }
  static async startMinecraft(context, name, args) {
    if (!context.minecraft && !context.creatorTools.lastActiveMinecraftFlavor) {
      return { status: import_ICommand.CommandStatus.invalidEnvironment };
    }
    let mc = context.minecraft;
    if (!mc && context.creatorTools.lastActiveMinecraftFlavor) {
      mc = context.creatorTools.ensureMinecraft(context.creatorTools.lastActiveMinecraftFlavor);
    }
    if (!mc) {
      return { status: import_ICommand.CommandStatus.invalidEnvironment };
    }
    import_Log.default.message("Starting server.");
    await mc.prepareAndStart({
      project: context.project
    });
    return { status: import_ICommand.CommandStatus.completed };
  }
  static async stopMinecraft(context, name, args) {
    if (!context.minecraft) {
      return { status: import_ICommand.CommandStatus.invalidEnvironment };
    }
    import_Log.default.message("Stopping server.");
    await context.minecraft.stop();
    return { status: import_ICommand.CommandStatus.completed };
  }
  static async updateMinecraft(context, name, args) {
    if (context.minecraft) {
      context.minecraft.prepare(true);
    }
    return { status: import_ICommand.CommandStatus.completed };
  }
  static registerCommonCommands() {
    import_CommandRegistry.default.main.registerCommand("dbgproj", import_CommandRegistry.CommandScope.debug, CreatorToolsCommands.debugProjectInformation);
    import_CommandRegistry.default.main.registerCommand("dbgmc", import_CommandRegistry.CommandScope.debug, CreatorToolsCommands.debugMinecraft);
    import_CommandRegistry.default.main.registerCommand("startmc", import_CommandRegistry.CommandScope.any, CreatorToolsCommands.startMinecraft);
    import_CommandRegistry.default.main.registerCommand("stopmc", import_CommandRegistry.CommandScope.minecraft, CreatorToolsCommands.stopMinecraft);
    import_CommandRegistry.default.main.registerCommand("updatemc", import_CommandRegistry.CommandScope.minecraft, CreatorToolsCommands.updateMinecraft);
  }
}
//# sourceMappingURL=CreatorToolsCommands.js.map
