"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ProjectTools_exports = {};
__export(ProjectTools_exports, {
  default: () => ProjectTools
});
module.exports = __toCommonJS(ProjectTools_exports);
var import_ITool = require("./ITool");
var import_IProjectItemData = require("./IProjectItemData");
var import_CommandRunner = __toESM(require("./CommandRunner"));
class ProjectTools {
  static addGlobalTools(tools) {
    tools.push({
      title: "Reload",
      type: import_ITool.ToolType.reload,
      scope: import_ITool.ToolScope.global
    });
    tools.push({
      title: "Hello World",
      type: import_ITool.ToolType.say,
      parameter1: "Hello from Minecraft Creator Tools.",
      scope: import_ITool.ToolScope.global
    });
  }
  static generateTools(creatorTools, project) {
    const tools = [];
    ProjectTools.addGlobalTools(tools);
    for (let i = 0; i < 10; i++) {
      const ctool = creatorTools.getCustomTool(i);
      if (ctool.text && ctool.text.length > 3) {
        let title = ctool.name;
        if (!title || title.length < 1) {
          title = "Tool " + (i + 1).toString();
        }
        tools.push({
          title,
          type: import_ITool.ToolType.customTool,
          scope: import_ITool.ToolScope.global,
          parameter1: i.toString()
        });
      }
    }
    if (project) {
      for (let i = 0; i < project.items.length; i++) {
        const pi = project.items[i];
        if (pi.itemType === import_IProjectItemData.ProjectItemType.structure && pi.projectPath !== null) {
          let structureName = pi.name;
          structureName = structureName.replace(".mcstructure", "");
          const tool = {
            title: "Push " + structureName + " to Minecraft",
            type: import_ITool.ToolType.pushStructure,
            scope: import_ITool.ToolScope.project,
            parameter1: pi.projectPath
          };
          tools.push(tool);
        }
      }
    }
    return tools;
  }
  static async executeTool(tool, creatorTools, project) {
    switch (tool.type) {
      case import_ITool.ToolType.customTool:
        if (tool.parameter1) {
          import_CommandRunner.default.runCustomTool(creatorTools, parseInt(tool.parameter1) + 1);
        }
        break;
      case import_ITool.ToolType.reload:
        ProjectTools.reload(creatorTools);
        break;
      case import_ITool.ToolType.say:
        if (tool.parameter1 && tool.parameter1.length > 0) {
          ProjectTools.say(creatorTools, tool.parameter1);
        }
        break;
    }
  }
  static async reload(creatorTools) {
    await creatorTools.runMinecraftCommand("reload");
  }
  static async say(creatorTools, message) {
    await creatorTools.runMinecraftCommand("say " + message);
  }
}
//# sourceMappingURL=ProjectTools.js.map
