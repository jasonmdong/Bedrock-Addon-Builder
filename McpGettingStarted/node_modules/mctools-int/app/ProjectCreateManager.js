"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ProjectCreateManager_exports = {};
__export(ProjectCreateManager_exports, {
  MATERIAL_NAMES_TO_FIXUP: () => MATERIAL_NAMES_TO_FIXUP,
  STANDARD_NAME_TOKEN: () => STANDARD_NAME_TOKEN,
  default: () => ProjectCreateManager
});
module.exports = __toCommonJS(ProjectCreateManager_exports);
var import_Log = __toESM(require("../core/Log"));
var import_Utilities = __toESM(require("../core/Utilities"));
var import_BlockTypeDefinition = __toESM(require("../minecraft/BlockTypeDefinition"));
var import_Database = __toESM(require("../minecraft/Database"));
var import_MinecraftDefinitions = __toESM(require("../minecraft/MinecraftDefinitions"));
var import_ModelDesignUtilities = __toESM(require("../minecraft/ModelDesignUtilities"));
var import_HttpStorage = __toESM(require("../storage/HttpStorage"));
var import_CreatorToolsHost = __toESM(require("./CreatorToolsHost"));
var import_IProjectItemData = require("./IProjectItemData");
var import_ProjectItemUtilities = __toESM(require("./ProjectItemUtilities"));
const STANDARD_NAME_TOKEN = "_name_";
const MATERIAL_NAMES_TO_FIXUP = [
  "cold",
  "warm",
  "body",
  "head",
  "legs",
  "eyes",
  "flower",
  "charging",
  "masked",
  "bioluminescent_layer",
  "armor",
  "charged",
  "ghost",
  "cape",
  "body_layer",
  "outer",
  "animated",
  "spectator",
  "overlay",
  "limbs",
  "breeze_eyes",
  "breeze_wind",
  "invisible"
];
class ProjectCreateManager {
  static async addEntityTypeFromGallery(project, entityTypeProject, entityTypeName, addMode, messageUpdater, dontOverwriteExistingFiles) {
    await ProjectCreateManager.copyGalleryPackFilesAndFixupIds(
      project,
      entityTypeProject,
      entityTypeName,
      messageUpdater,
      dontOverwriteExistingFiles
    );
    await project.inferProjectItemsFromFiles(true);
    const items = project.getItemsCopy();
    for (const item of items) {
      if (item.itemType === import_IProjectItemData.ProjectItemType.entityTypeBehavior) {
        let minecraftEntityType = await import_MinecraftDefinitions.default.get(item);
        if (minecraftEntityType) {
          const targetId = entityTypeName ? entityTypeName : entityTypeProject.id;
          if (minecraftEntityType.id?.endsWith(targetId)) {
            minecraftEntityType.runtimeIdentifier = entityTypeProject.targetRuntimeIdentifier ? entityTypeProject.targetRuntimeIdentifier : "minecraft:" + entityTypeProject.id;
            minecraftEntityType.persist();
          }
        }
      }
    }
    await project.save();
  }
  static getReplacedCreationData(project, galleryItem, newName) {
    if (galleryItem.creationData === void 0) {
      return void 0;
    }
    try {
      let creationDataStr = JSON.stringify(galleryItem.creationData);
      creationDataStr = this.replaceNamesInContent(creationDataStr, project, galleryItem, newName, []);
      return JSON.parse(creationDataStr);
    } catch (e) {
      return galleryItem.creationData;
    }
  }
  static getReplacedCreationDataInObject(project, creationObject, newName) {
    if (creationObject === void 0) {
      return void 0;
    }
    try {
      let creationDataStr = JSON.stringify(creationObject);
      creationDataStr = this.replaceNamesInContentFromReplacers(
        creationDataStr,
        project,
        [STANDARD_NAME_TOKEN],
        newName,
        []
      );
      return JSON.parse(creationDataStr);
    } catch (e) {
      return creationObject;
    }
  }
  static async addBlockTypeFromGallery(project, blockTypeProject, blockTypeName) {
    blockTypeName = await ProjectCreateManager.copyGalleryPackFilesAndFixupIds(
      project,
      blockTypeProject,
      blockTypeName
    );
    await project.inferProjectItemsFromFiles(true);
    const blockTypeItem = import_ProjectItemUtilities.default.getItemByTypeAndName(
      project,
      blockTypeName,
      import_IProjectItemData.ProjectItemType.blockTypeBehavior
    );
    if (blockTypeItem) {
      if (!blockTypeItem.isContentLoaded) {
        await blockTypeItem.loadContent();
      }
      if (blockTypeItem.primaryFile) {
        const blockType = await import_BlockTypeDefinition.default.ensureOnFile(blockTypeItem.primaryFile);
        const creationData = this.getReplacedCreationData(project, blockTypeProject, blockTypeName);
        if (blockType) {
          await blockType.ensureBlockAndTerrainLinks(project, creationData);
        }
      }
    }
    await project.save();
  }
  static async addItemTypeFromGallery(project, itemTypeProject, itemTypeName) {
    await ProjectCreateManager.copyGalleryPackFilesAndFixupIds(project, itemTypeProject, itemTypeName);
    await project.inferProjectItemsFromFiles(true);
    await project.save();
  }
  /**
   * Adds a model design template to a project.
   * Creates both a .model.json file and the exported .geo.json + texture.png in the resource pack.
   * @param project The project to add the model design to
   * @param modelDesignItem The gallery item containing the model design template info
   * @param modelName The name for the new model
   */
  static async addModelDesignFromGallery(project, modelDesignItem, modelName) {
    if (!modelName) {
      modelName = modelDesignItem.id;
    }
    const templateId = modelDesignItem.id;
    const modelDesign = await import_Database.default.ensureModelTemplateLoaded(templateId);
    if (!modelDesign) {
      import_Log.default.fail(`Could not load model template: ${templateId}`);
      return;
    }
    const designCopy = JSON.parse(JSON.stringify(modelDesign));
    designCopy.identifier = modelName;
    const rpFolder = await project.ensureDefaultResourcePackFolder();
    if (!rpFolder) {
      import_Log.default.fail("Could not ensure resource pack folder for model design");
      return;
    }
    const modelTemplatesFolder = rpFolder.ensureFolder("model_templates");
    await modelTemplatesFolder.ensureExists();
    const modelJsonFile = modelTemplatesFolder.ensureFile(modelName + ".model.json");
    modelJsonFile.setContent(JSON.stringify(designCopy, null, 2));
    await modelJsonFile.saveContent();
    const conversionResult = import_ModelDesignUtilities.default.convertToGeometry(designCopy);
    if (conversionResult.geometry) {
      const modelsFolder = rpFolder.ensureFolder("models");
      await modelsFolder.ensureExists();
      const entitiesFolder = modelsFolder.ensureFolder("entities");
      await entitiesFolder.ensureExists();
      const geoFile = entitiesFolder.ensureFile(modelName + ".geo.json");
      geoFile.setContent(JSON.stringify(conversionResult.geometry, null, 2));
      await geoFile.saveContent();
    }
    await project.inferProjectItemsFromFiles(true);
    await project.save();
  }
  static async copyGalleryPackFilesAndFixupIds(project, galleryProject, newTypeName, messagerUpdater, dontOverwriteExistingFiles) {
    const files = galleryProject.fileList;
    if (newTypeName === void 0) {
      newTypeName = galleryProject.id;
    }
    if (files === void 0) {
      import_Log.default.unexpectedUndefined("AETFLS");
      return newTypeName;
    }
    let sourceBpFolder = void 0;
    let sourceRpFolder = void 0;
    if (galleryProject.gitHubRepoName === "bedrock-samples") {
      sourceBpFolder = await import_Database.default.getReleaseVanillaBehaviorPackFolder();
      sourceRpFolder = await import_Database.default.getReleaseVanillaResourcePackFolder();
    } else {
      const url = import_Utilities.default.ensureEndsWithSlash(import_CreatorToolsHost.default.getVanillaContentRoot()) + "res/samples/" + import_Utilities.default.ensureEndsWithSlash(galleryProject.gitHubOwner) + galleryProject.gitHubRepoName + "-" + import_Utilities.default.ensureEndsWithSlash(galleryProject.gitHubBranch ? galleryProject.gitHubBranch : "main") + (galleryProject.gitHubFolder ? import_Utilities.default.ensureNotStartsWithSlash(galleryProject.gitHubFolder) : "");
      const gh = import_HttpStorage.default.get(url);
      if (!gh.rootFolder.isLoaded) {
        await gh.rootFolder.load();
      }
      const bps = gh.rootFolder.folders["behavior_packs"];
      const rps = gh.rootFolder.folders["resource_packs"];
      if (!bps || !rps) {
        import_Log.default.unexpectedUndefined("AETFLT");
        return newTypeName;
      }
      if (!rps.isLoaded) {
        await rps.load();
      }
      if (!bps.isLoaded) {
        await bps.load();
      }
      if (rps.folderCount < 1 || bps.folderCount < 1) {
        import_Log.default.unexpectedUndefined("AETFLY");
        return newTypeName;
      }
      sourceBpFolder = bps.getFolderByIndex(0);
      sourceRpFolder = rps.getFolderByIndex(0);
    }
    const targetBpFolder = await project.ensureDefaultBehaviorPackFolder();
    const targetRpFolder = await project.ensureDefaultResourcePackFolder();
    if (!sourceBpFolder || !sourceRpFolder || !sourceBpFolder || !sourceRpFolder || !targetBpFolder || !targetRpFolder) {
      import_Log.default.unexpectedUndefined("AETVA");
      return newTypeName;
    }
    let contentReplacements = ['"materials"'];
    for (const filePath of files) {
      if (filePath.startsWith("/behavior_pack")) {
        let subPath = void 0;
        if (filePath.startsWith("/behavior_pack/")) {
          subPath = filePath.substring(14);
        } else {
          const nextSlash = filePath.indexOf("/", 16);
          if (nextSlash < 0) {
            import_Log.default.unexpectedUndefined("AETVB");
            return newTypeName;
          }
          subPath = filePath.substring(nextSlash);
        }
        const targetPath = ProjectCreateManager.replaceNamesInPath(subPath, project, galleryProject, newTypeName);
        const sourceFile = await sourceBpFolder.getFileFromRelativePath(subPath);
        if (!sourceFile) {
          import_Log.default.debugAlert("Could not find file '" + subPath + "'  (GPFFIA)");
        } else {
          const targetFile = await targetBpFolder.ensureFileFromRelativePath(targetPath);
          let update = true;
          if (dontOverwriteExistingFiles) {
            const targetExists = await targetFile.exists();
            if (targetExists) {
              update = false;
            }
          }
          if (update) {
            if (!sourceFile.isContentLoaded) {
              await sourceFile.loadContent();
            }
            let content = sourceFile.content;
            if (typeof content === "string") {
              content = ProjectCreateManager.replaceNamesInContent(
                content,
                project,
                galleryProject,
                newTypeName,
                contentReplacements
              );
            }
            if (content !== null) {
              if (messagerUpdater) {
                messagerUpdater("Updating '" + targetFile.fullPath + "'");
              }
              targetFile.setContent(content);
            }
          }
        }
      } else if (filePath.startsWith("/resource_pack")) {
        let subPath = void 0;
        if (filePath.startsWith("/resource_pack/")) {
          subPath = filePath.substring(14);
        } else {
          const nextSlash = filePath.indexOf("/", 16);
          if (nextSlash < 0) {
            import_Log.default.unexpectedUndefined("AETVC");
            return newTypeName;
          }
          subPath = filePath.substring(nextSlash);
        }
        const targetPath = ProjectCreateManager.replaceNamesInPath(subPath, project, galleryProject, newTypeName);
        if (subPath.indexOf("cow.v2.render_controllers") >= 0) {
          subPath = subPath.replace("cow.v2.render_controllers", "cow.v2.render_controllres");
        }
        const sourceFile = await sourceRpFolder.getFileFromRelativePath(subPath);
        if (!sourceFile) {
          import_Log.default.debugAlert("Could not find file '" + subPath + "' (GPFFIB)");
        } else {
          const targetFile = await targetRpFolder.ensureFileFromRelativePath(targetPath);
          let update = true;
          if (update) {
            if (dontOverwriteExistingFiles) {
              const targetExists = await targetFile.exists();
              if (targetExists) {
                update = false;
              }
            }
            if (!sourceFile.isContentLoaded) {
              await sourceFile.loadContent();
            }
            let content = sourceFile.content;
            if (typeof content === "string") {
              content = ProjectCreateManager.replaceNamesInContent(
                content,
                project,
                galleryProject,
                newTypeName,
                contentReplacements
              );
            }
            if (content !== null) {
              if (messagerUpdater) {
                messagerUpdater("Updating '" + targetFile.fullPath + "'");
              }
              targetFile.setContent(content);
            }
          }
        }
      }
    }
    return newTypeName;
  }
  static replaceNamesInPath(path, project, galleryProject, newName) {
    let pathReplacers = galleryProject.nameReplacers;
    if (!pathReplacers) {
      pathReplacers = [galleryProject.id];
    }
    newName = newName.toLowerCase();
    newName = newName.replace(/[- ]/g, "_");
    const tempName = import_Utilities.default.createRandomId(10);
    for (const pathReplacer of pathReplacers) {
      path = import_Utilities.default.replaceAll(path, "/" + pathReplacer + ".", "/" + tempName + ".");
      path = import_Utilities.default.replaceAll(path, "\\" + pathReplacer + ".", "\\" + tempName + ".");
      path = import_Utilities.default.replaceAll(path, "/" + pathReplacer + "/", "/" + tempName + "/");
      path = import_Utilities.default.replaceAll(path, "\\" + pathReplacer + "\\", "\\" + tempName + "\\");
      path = import_Utilities.default.replaceAll(path, "\\" + pathReplacer + "_", "\\" + tempName + "_");
      path = import_Utilities.default.replaceAll(path, "/" + pathReplacer + "_", "/" + tempName + "_");
      path = import_Utilities.default.replaceAll(path, "/" + pathReplacer + "_ico.", "/" + tempName + "_ico.");
      path = import_Utilities.default.replaceAll(path, "\\" + pathReplacer + "_ico.", "\\" + tempName + "_ico.");
      path = import_Utilities.default.replaceAll(path, "/" + pathReplacer + "_ico/", "/" + tempName + "_ico/");
      path = import_Utilities.default.replaceAll(path, "\\" + pathReplacer + "_ico\\", "\\" + tempName + "_ico\\");
    }
    path = import_Utilities.default.replaceAll(path, tempName, newName);
    return path;
  }
  static replaceNamesInContent(content, project, galleryProject, newName, replaceAllExclusions) {
    let replacers = galleryProject.nameReplacers;
    if (!replacers) {
      replacers = [galleryProject.id];
    }
    replacers = replacers.slice();
    replacers.push(STANDARD_NAME_TOKEN);
    return this.replaceNamesInContentFromReplacers(content, project, replacers, newName, replaceAllExclusions);
  }
  static replaceNamesInContentFromReplacers(content, project, replacers, newName, replaceAllExclusions) {
    newName = newName.toLowerCase();
    newName = newName.replace(/-/g, "_");
    newName = newName.replace(/ /g, "_");
    const tempName = import_Utilities.default.createRandomLowerId(10);
    for (const replacer of replacers) {
      content = import_Utilities.default.replaceAll(
        content,
        "minecraft:" + replacer,
        project.effectiveDefaultNamespace + ":" + tempName
      );
      content = import_Utilities.default.replaceAll(content, "demo:" + replacer, project.effectiveDefaultNamespace + ":" + tempName);
      content = import_Utilities.default.replaceAll(
        content,
        "starter:" + replacer,
        project.effectiveDefaultNamespace + ":" + tempName
      );
      content = import_Utilities.default.replaceAll(content, "sample:" + replacer, project.effectiveDefaultNamespace + ":" + tempName);
      content = import_Utilities.default.replaceAllExceptInLines(content, ":" + replacer, ":" + tempName, replaceAllExclusions);
      content = import_Utilities.default.replaceAllExceptInLines(content, "/" + replacer, "/" + tempName, replaceAllExclusions);
      content = import_Utilities.default.replaceAllExceptInLines(content, "." + replacer, "." + tempName, replaceAllExclusions);
      content = import_Utilities.default.replaceAllExceptInLines(content, replacer + "_", tempName + "_", replaceAllExclusions);
      content = import_Utilities.default.replaceAllExceptInLines(
        content,
        '"' + replacer + '"',
        '"' + tempName + '"',
        replaceAllExclusions
      );
      for (const materialName of MATERIAL_NAMES_TO_FIXUP) {
        content = import_Utilities.default.replaceAll(content, materialName + '": "' + tempName, materialName + '": "' + replacer);
      }
    }
    content = import_Utilities.default.replaceAll(content, tempName, newName);
    return content;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  MATERIAL_NAMES_TO_FIXUP,
  STANDARD_NAME_TOKEN
});
//# sourceMappingURL=ProjectCreateManager.js.map
