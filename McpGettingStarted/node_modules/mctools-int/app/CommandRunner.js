"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CommandRunner_exports = {};
__export(CommandRunner_exports, {
  default: () => CommandRunner
});
module.exports = __toCommonJS(CommandRunner_exports);
var import_Utilities = __toESM(require("../core/Utilities"));
var import_Command = __toESM(require("../minecraft/Command"));
var import_CreatorTools = require("./CreatorTools");
class CommandRunner {
  static async runCustomTool(creatorTools, commandNumber) {
    if (creatorTools.activeMinecraftState !== import_CreatorTools.CreatorToolsMinecraftState.started) {
    }
    const cartoCommand = creatorTools.getCustomTool(commandNumber - 1);
    let commandName = commandNumber + "";
    if (cartoCommand.name !== void 0 && cartoCommand.name.length > 0) {
      commandName = cartoCommand.name;
    }
    const operId = await creatorTools.notifyOperationStarted("Running command " + commandName);
    if (cartoCommand.text !== void 0) {
      await CommandRunner.runCommandText(creatorTools, cartoCommand.text);
    } else {
    }
    await creatorTools.notifyOperationEnded(operId, "Command " + commandName + " complete.");
  }
  static async runCommandText(creatorTools, commandText) {
    const commandItems = commandText.split("\n");
    await this.runCommandList(creatorTools, commandItems);
  }
  static async runCommandList(creatorTools, commandItems) {
    const resultItems = [];
    let lastStoredBlockX = -1;
    let lastStoredBlockY = -1;
    let lastStoredBlockZ = -1;
    let firstRelativeX;
    let firstRelativeY;
    let firstRelativeZ;
    let setFirstPosition = false;
    for (let i = 0; i < commandItems.length; i++) {
      let commandText = commandItems[i].trim();
      if (commandText.length > 4 && !commandText.startsWith("#")) {
        if (!commandText.startsWith("/")) {
          commandText = "/" + commandText;
        }
        const commandStart = /* @__PURE__ */ new Date();
        const command = new import_Command.default(commandText);
        if (command.hasRelativeOrLocalCoordinates && setFirstPosition && firstRelativeX !== void 0 && firstRelativeY !== void 0 && firstRelativeZ !== void 0) {
          command.absolutizeCoordinates(
            lastStoredBlockX - firstRelativeX,
            lastStoredBlockY - firstRelativeY,
            lastStoredBlockZ - firstRelativeZ
          );
        }
        commandText = command.toString();
        await creatorTools.notifyStatusUpdate("Running command '" + commandText + "'");
        const commandResult = await creatorTools.runCommand(commandText);
        const result = commandResult?.data;
        let resultData;
        if (result !== void 0 && result !== null && result.indexOf("{") >= 0) {
          resultData = import_Utilities.default.parseJson(result);
          if (resultData !== void 0) {
            if (resultData.position !== void 0 && !setFirstPosition) {
              lastStoredBlockX = resultData.position.x;
              lastStoredBlockY = resultData.position.y;
              lastStoredBlockZ = resultData.position.z;
              const argX = command.firstX;
              if (argX !== void 0) {
                firstRelativeX = argX.position;
              }
              const argY = command.firstY;
              if (argY !== void 0) {
                firstRelativeY = argY.position;
              }
              const argZ = command.firstZ;
              if (argZ !== void 0) {
                firstRelativeZ = argZ.position;
              }
              setFirstPosition = true;
            }
          }
        }
        const resultItem = {
          lineNumber: i,
          dateTime: commandStart,
          response: resultData
        };
        resultItems.push(resultItem);
      }
    }
    return resultItems;
  }
}
//# sourceMappingURL=CommandRunner.js.map
