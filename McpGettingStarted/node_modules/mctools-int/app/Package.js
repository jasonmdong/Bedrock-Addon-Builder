"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var Package_exports = {};
__export(Package_exports, {
  PackageType: () => PackageType,
  default: () => Package
});
module.exports = __toCommonJS(Package_exports);
var import_Constants = require("../core/Constants");
var import_Database = __toESM(require("../minecraft/Database"));
var import_IFile = require("../storage/IFile");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_ZipStorage = __toESM(require("../storage/ZipStorage"));
var import_Project = __toESM(require("./Project"));
var import_Utilities = __toESM(require("../core/Utilities"));
var PackageType = /* @__PURE__ */ ((PackageType2) => {
  PackageType2[PackageType2["packSet"] = 0] = "packSet";
  PackageType2[PackageType2["world"] = 1] = "world";
  PackageType2[PackageType2["worldTemplate"] = 2] = "worldTemplate";
  PackageType2[PackageType2["generic"] = 3] = "generic";
  PackageType2[PackageType2["project"] = 4] = "project";
  return PackageType2;
})(PackageType || {});
class Package {
  storagePath;
  name;
  baseName;
  type;
  file;
  reportFile;
  cacheFolder;
  data;
  get isWorldType() {
    return this.type === 1 /* world */ || this.type === 2 /* worldTemplate */;
  }
  constructor(name, path) {
    this.name = name;
    this.storagePath = path;
    this.baseName = import_StorageUtilities.default.getBaseFromName(name);
    this.type = this._getPackTypeFromPath(path);
  }
  static ensureMinecraftCreatorToolsPackageReference(packRefs) {
    for (const packRef of packRefs) {
      if (packRef.behaviorPackReferences) {
        for (const bpRef of packRef.behaviorPackReferences) {
          if (bpRef.uuid === import_Database.default.creatorToolsIngameBehaviorPackUUID) {
            return;
          }
        }
      }
    }
    packRefs.push({
      name: "creator_tools_ingame.mcaddon",
      behaviorPackReferences: [
        { uuid: import_Database.default.creatorToolsIngameBehaviorPackUUID, version: import_Database.default.creatorToolsIngameBehaviorPackVersion }
      ],
      resourcePackReferences: [
        { uuid: import_Database.default.creatorToolsIngameResourcePackUUID, version: import_Database.default.creatorToolsIngameResourcePackVersion }
      ]
    });
    return packRefs;
  }
  _getPackTypeFromPath(path) {
    const type = import_StorageUtilities.default.getTypeFromName(path);
    switch (type) {
      case "mcworld":
        return 1 /* world */;
      case "mcproject":
        return 4 /* project */;
      case "mctemplate":
        return 2 /* worldTemplate */;
      case "mcaddon":
      case "mcpack":
        return 0 /* packSet */;
      default:
        return 3 /* generic */;
    }
  }
  async ensureData(creatorTools, file) {
    this.file = file;
    const summaryFile = file.parentFolder.ensureFile(file.name + ".report.html");
    const summaryFileExists = await summaryFile.exists();
    let summaryObject;
    if (summaryFileExists) {
      if (!summaryFile.isContentLoaded) {
        await summaryFile.loadContent();
      }
      const content = summaryFile.content;
      if (content && typeof content === "string") {
        const adderFunctionDec = content.indexOf("function _addReportJson(");
        let adderFunction = content.indexOf("_addReportJson(");
        if (adderFunction >= 0) {
          if (adderFunctionDec === adderFunction - 9) {
            adderFunction = content.indexOf("_addReportJson(", adderFunction + 10);
          }
          if (adderFunction >= 0) {
            const endOfFunction = content.indexOf("</script>", adderFunction);
            if (endOfFunction > adderFunction) {
              const previousEnd = content.lastIndexOf(");", endOfFunction);
              if (previousEnd > adderFunction && previousEnd < endOfFunction) {
                const jsonContent = content.substring(adderFunction + 15, previousEnd);
                try {
                  summaryObject = JSON.parse(jsonContent);
                } catch (e) {
                }
                if (summaryObject) {
                  this.reportFile = summaryFile;
                }
                if (summaryObject && summaryObject.generatorVersion !== import_Constants.constants.version) {
                  summaryObject = void 0;
                }
              }
            }
          }
        }
      }
    }
    if (!summaryObject) {
      if (!file.isContentLoaded) {
        await file.loadContent();
      }
      if (file.content && file.content instanceof Uint8Array) {
        let packRootZipFolder = void 0;
        if (!file.fileContainerStorage) {
          const zipStorage = new import_ZipStorage.default();
          zipStorage.storagePath = file.storageRelativePath + "#";
          await zipStorage.loadFromUint8Array(file.content, file.name);
          zipStorage.containerFile = file;
          file.fileContainerStorage = zipStorage;
        }
        packRootZipFolder = file.fileContainerStorage.rootFolder;
        const packProject = new import_Project.default(creatorTools, file.name, null);
        packProject.setProjectFolder(packRootZipFolder);
        await packProject.inferProjectItemsFromFiles();
        const pis = packProject.indevInfoSet;
        await pis.generateForProject();
        const hash = await file.getHash();
        const reportHtml = pis.getReportHtml(file.name, file.storageRelativePath, hash);
        summaryFile.setContent(reportHtml, import_IFile.FileUpdateType.versionlessEdit);
        await summaryFile.saveContent();
        this.reportFile = summaryFile;
        summaryObject = pis.getDataObject(file.name, file.storageRelativePath, hash);
      }
    }
    if (summaryObject) {
      this.data = summaryObject;
    }
  }
  createReference() {
    const bpRefs = [];
    const rpRefs = [];
    if (this.data && this.data.items) {
      for (let i = 0; i < this.data.items.length; i++) {
        const item = this.data.items[i];
        if (item.gId === "PACK" && item.gIx === 6 && item.d && typeof item.d === "string") {
          const ref = this.getRefFromString(item.d);
          if (ref) {
            bpRefs.push(ref);
          }
        } else if (item.gId === "PACK" && item.gIx === 16 && item.d && typeof item.d === "string") {
          const ref = this.getRefFromString(item.d);
          if (ref) {
            rpRefs.push(ref);
          }
        }
      }
    }
    const packRef = {
      name: this.name,
      hash: this.data?.sourceHash,
      behaviorPackReferences: bpRefs,
      resourcePackReferences: rpRefs
    };
    return packRef;
  }
  getRefFromString(uuidPlusVersion) {
    const sections = import_Utilities.default.splitUntil(uuidPlusVersion, "|", 1);
    if (sections.length !== 2) {
      return void 0;
    }
    const verNumbers = sections[1].split(".");
    if (verNumbers.length !== 3) {
      return void 0;
    }
    const targetVerNumbers = [];
    try {
      targetVerNumbers.push(parseInt(verNumbers[0]));
      targetVerNumbers.push(parseInt(verNumbers[1]));
      targetVerNumbers.push(parseInt(verNumbers[2]));
    } catch (e) {
    }
    if (targetVerNumbers.length !== 3) {
      return void 0;
    }
    return { uuid: sections[0], version: targetVerNumbers };
  }
  matches(packName, isWorldFocused) {
    const packCoreName = this.name.toLowerCase();
    const packBaseName = this.baseName.toLowerCase();
    packName = packName.toLowerCase();
    if (packBaseName.toLowerCase() === packName && (isWorldFocused === void 0 || isWorldFocused === true && (this.type === 1 /* world */ || this.type === 2 /* worldTemplate */) || isWorldFocused === false && this.type !== 1 /* world */ && this.type !== 2 /* worldTemplate */) || packCoreName === packName) {
      return true;
    }
    return false;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  PackageType
});
//# sourceMappingURL=Package.js.map
