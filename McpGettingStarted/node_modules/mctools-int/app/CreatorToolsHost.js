"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CreatorToolsHost_exports = {};
__export(CreatorToolsHost_exports, {
  CreatorToolsThemeStyle: () => CreatorToolsThemeStyle,
  HostType: () => HostType,
  default: () => CreatorToolsHost
});
module.exports = __toCommonJS(CreatorToolsHost_exports);
var import_CreatorTools = __toESM(require("./CreatorTools"));
var import_Database = __toESM(require("../minecraft/Database"));
var import_BrowserStorage = __toESM(require("../storage/BrowserStorage"));
var import_ElectronStorage = __toESM(require("../electronclient/ElectronStorage"));
var import_ste_events = require("ste-events");
var import_GitHubManager = __toESM(require("../github/GitHubManager"));
var import_AppServiceProxy = __toESM(require("../core/AppServiceProxy"));
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_CreatorToolsCommands = __toESM(require("./CreatorToolsCommands"));
var import_DeploymentTarget = require("./DeploymentTarget");
var CreatorToolsThemeStyle = /* @__PURE__ */ ((CreatorToolsThemeStyle2) => {
  CreatorToolsThemeStyle2[CreatorToolsThemeStyle2["dark"] = 0] = "dark";
  CreatorToolsThemeStyle2[CreatorToolsThemeStyle2["light"] = 1] = "light";
  CreatorToolsThemeStyle2[CreatorToolsThemeStyle2["highContrast"] = 2] = "highContrast";
  return CreatorToolsThemeStyle2;
})(CreatorToolsThemeStyle || {});
var HostType = /* @__PURE__ */ ((HostType2) => {
  HostType2[HostType2["web"] = 0] = "web";
  HostType2[HostType2["toolsNodejs"] = 1] = "toolsNodejs";
  HostType2[HostType2["electronNodeJs"] = 2] = "electronNodeJs";
  HostType2[HostType2["electronWeb"] = 3] = "electronWeb";
  HostType2[HostType2["vsCodeMainNodeJs"] = 4] = "vsCodeMainNodeJs";
  HostType2[HostType2["vsCodeMainWeb"] = 5] = "vsCodeMainWeb";
  HostType2[HostType2["vsCodeWebService"] = 6] = "vsCodeWebService";
  HostType2[HostType2["vsCodeWebWeb"] = 7] = "vsCodeWebWeb";
  HostType2[HostType2["webPlusServices"] = 8] = "webPlusServices";
  HostType2[HostType2["testLocal"] = 9] = "testLocal";
  return HostType2;
})(HostType || {});
class CreatorToolsHost {
  static _creatorTools;
  static _onInitialized = new import_ste_events.EventDispatcher();
  static _onThemeChanged = new import_ste_events.EventDispatcher();
  static _initializing = false;
  static _initialized = false;
  static isLocalNode = false;
  static fullLocalStorage = false;
  static hostType = 0 /* web */;
  static hostManager = void 0;
  static _theme = 0 /* dark */;
  static retrieveDataFromWebContentRoot = false;
  static contentWebRoot = "";
  static vanillaContentRoot = "";
  // URL root for vanilla textures, samples, definitions (e.g., https://mctools.dev/)
  static projectPath = "";
  static focusPath = void 0;
  static baseUrl = "";
  /**
   * Gets the effective root URL for vanilla content (textures, samples, definitions).
   * Returns vanillaContentRoot if set, otherwise falls back to contentRoot.
   */
  static getVanillaContentRoot() {
    return this.vanillaContentRoot || this.contentWebRoot || "";
  }
  /**
   * Gets the current theme style
   */
  static get theme() {
    return this._theme;
  }
  /**
   * Sets the theme style and notifies listeners
   */
  static set theme(value) {
    if (this._theme !== value) {
      this._theme = value;
      this._onThemeChanged.dispatch(CreatorToolsHost, value);
    }
  }
  /**
   * Event fired when theme changes. Use this to update FluentUI components
   * that are initialized outside of React's render cycle.
   */
  static get onThemeChanged() {
    return this._onThemeChanged.asEvent();
  }
  static initialMode = null;
  static modeParameter = null;
  static contentUrl = null;
  static readOnly = false;
  static postMessage;
  static prefsStorage = null;
  static projectsStorage = null;
  static deploymentStorage = [];
  static worldStorage = null;
  static packStorage = null;
  static workingStorage = null;
  static generateCryptoRandomNumber;
  static generateUuid;
  static localFolderExists;
  static localFileExists;
  static ensureLocalFolder;
  static createMinecraft;
  static canCreateMinecraft;
  // Image codec thunks - set by Node.js environments to provide platform-specific implementations
  static decodePng;
  static encodeToPng;
  static _onMessage = new import_ste_events.EventDispatcher();
  static get isNodeJs() {
    return this.hostType === 2 /* electronNodeJs */ || this.hostType === 1 /* toolsNodejs */ || this.hostType === 4 /* vsCodeMainNodeJs */;
  }
  static get isVsCode() {
    return this.hostType === 4 /* vsCodeMainNodeJs */ || this.hostType === 6 /* vsCodeWebService */ || this.hostType === 5 /* vsCodeMainWeb */ || this.hostType === 7 /* vsCodeWebWeb */;
  }
  static get isWeb() {
    return this.hostType === 8 /* webPlusServices */ || this.hostType === 0 /* web */ || this.hostType === 3 /* electronWeb */ || this.hostType === 5 /* vsCodeMainWeb */ || this.hostType === 7 /* vsCodeWebWeb */;
  }
  static get isAppServiceWeb() {
    return this.hostType === 3 /* electronWeb */ || this.hostType === 5 /* vsCodeMainWeb */;
  }
  static get onMessage() {
    return CreatorToolsHost._onMessage.asEvent();
  }
  static get onInitialized() {
    return CreatorToolsHost._onInitialized.asEvent();
  }
  static getCreatorTools() {
    if (!this._initialized) {
      this.init();
    }
    return CreatorToolsHost._creatorTools;
  }
  static notifyNewMessage(source, message) {
    CreatorToolsHost._onMessage.dispatch(source, message);
  }
  static setHostType(type) {
    this.hostType = type;
  }
  static init() {
    if (CreatorToolsHost._initializing || CreatorToolsHost._initialized) {
      return;
    }
    CreatorToolsHost._initializing = true;
    if (typeof g_contentRoot !== "undefined") {
      CreatorToolsHost.contentWebRoot = import_StorageUtilities.default.ensureEndsWithDelimiter(g_contentRoot);
    }
    if (typeof g_vanillaContentRoot !== "undefined") {
      CreatorToolsHost.vanillaContentRoot = import_StorageUtilities.default.ensureEndsWithDelimiter(g_vanillaContentRoot);
    }
    if (typeof g_initialMode !== "undefined") {
      CreatorToolsHost.initialMode = g_initialMode;
    }
    if (typeof g_modeParameter !== "undefined") {
      CreatorToolsHost.modeParameter = g_modeParameter;
    }
    if (typeof g_isVsCodeMain !== "undefined") {
      if (g_isVsCodeMain) {
        CreatorToolsHost.hostType = 5 /* vsCodeMainWeb */;
      }
    }
    if (typeof g_isVsCodeWeb !== "undefined") {
      if (g_isVsCodeWeb) {
        CreatorToolsHost.hostType = 7 /* vsCodeWebWeb */;
      }
    }
    if (typeof g_projectPath !== "undefined") {
      CreatorToolsHost.projectPath = g_projectPath;
    }
    if (typeof g_baseUrl !== "undefined") {
      CreatorToolsHost.baseUrl = g_baseUrl;
    }
    if (typeof g_contentUrl !== "undefined") {
      CreatorToolsHost.contentUrl = g_contentUrl;
    }
    if (typeof g_readOnly !== "undefined") {
      CreatorToolsHost.readOnly = g_readOnly === true;
    }
    import_AppServiceProxy.default.init();
    import_GitHubManager.default.init();
    import_Database.default.loadVanillaCatalog();
    import_CreatorToolsCommands.default.registerCommonCommands();
    CreatorToolsHost.generateCryptoRandomNumber = (toVal) => {
      const ct = CreatorToolsHost.getCreatorTools();
      if (!ct || !ct.local) {
        throw new Error("Could not generate key.");
      }
      return ct.local.generateCryptoRandomNumber(toVal);
    };
    CreatorToolsHost.generateUuid = () => {
      const ct = CreatorToolsHost.getCreatorTools();
      if (!ct || !ct.local) {
        throw new Error("Could not generate UUID.");
      }
      return ct.local.generateUuid();
    };
    CreatorToolsHost.decodePng = (data) => {
      const ct = CreatorToolsHost.getCreatorTools();
      if (!ct || !ct.local) {
        return void 0;
      }
      return ct.local.decodePng(data);
    };
    CreatorToolsHost.encodeToPng = (pixels, width, height) => {
      const ct = CreatorToolsHost.getCreatorTools();
      if (!ct || !ct.local) {
        return void 0;
      }
      return ct.local.encodeToPng(pixels, width, height);
    };
    if (typeof window !== "undefined" && window.crypto) {
      CreatorToolsHost.generateCryptoRandomNumber = (toVal) => {
        const maxUint32 = 4294967295;
        const limit = maxUint32 - maxUint32 % toVal;
        let randomValue;
        do {
          randomValue = window.crypto.getRandomValues(new Uint32Array(1))[0];
        } while (randomValue >= limit);
        return randomValue % toVal;
      };
      if (window.crypto.randomUUID) {
        CreatorToolsHost.generateUuid = () => {
          return window.crypto.randomUUID();
        };
      } else {
        CreatorToolsHost.generateUuid = () => {
          const bytes = window.crypto.getRandomValues(new Uint8Array(16));
          bytes[6] = bytes[6] & 15 | 64;
          bytes[8] = bytes[8] & 63 | 128;
          const hex = Array.from(bytes, (b) => b.toString(16).padStart(2, "0")).join("");
          return `${hex.slice(0, 8)}-${hex.slice(8, 12)}-${hex.slice(12, 16)}-${hex.slice(16, 20)}-${hex.slice(20, 32)}`;
        };
      }
    }
    if (CreatorToolsHost.projectsStorage !== null && CreatorToolsHost.prefsStorage !== null) {
    } else if (import_AppServiceProxy.default.hasAppService) {
      let ls = new import_ElectronStorage.default("<DOCP>", "prefs");
      ls.rootFolder.ensureExists();
      CreatorToolsHost.prefsStorage = ls;
      ls = new import_ElectronStorage.default("<DOCP>", "working");
      ls.rootFolder.ensureExists();
      CreatorToolsHost.workingStorage = ls;
      ls = new import_ElectronStorage.default("<DOCP>", "projects");
      ls.rootFolder.ensureExists();
      CreatorToolsHost.projectsStorage = ls;
      ls = new import_ElectronStorage.default("<DOCP>", "worlds");
      ls.rootFolder.ensureExists();
      CreatorToolsHost.worldStorage = ls;
      ls = new import_ElectronStorage.default("<DOCP>", "packs");
      ls.rootFolder.ensureExists();
      CreatorToolsHost.packStorage = ls;
      if (typeof window !== "undefined") {
        let basePath = window.location.href;
        const lastSlash = basePath.lastIndexOf("/");
        if (lastSlash >= 0) {
          CreatorToolsHost.contentWebRoot = basePath.substring(0, lastSlash + 1);
        }
      }
      let minecraftPath = "<BDRK>";
      ls = new import_ElectronStorage.default(minecraftPath, "");
      ls.getAvailable();
      CreatorToolsHost.deploymentStorage[import_DeploymentTarget.DeploymentTargetType.bedrock] = ls;
      let minecraftPreviewPath = "<BDPV>";
      ls = new import_ElectronStorage.default(minecraftPreviewPath, "");
      ls.getAvailable();
      CreatorToolsHost.deploymentStorage[import_DeploymentTarget.DeploymentTargetType.bedrockPreview] = ls;
    } else if (typeof window !== "undefined") {
      if (typeof window.api !== "undefined") {
        throw new Error(
          "BrowserStorage initialization attempted in Electron context. Electron renderer must use ElectronStorage. Ensure AppServiceProxy.hasAppService is true before CreatorToolsHost.init() is called."
        );
      }
      CreatorToolsHost.prefsStorage = new import_BrowserStorage.default("mctprefs");
      CreatorToolsHost.projectsStorage = new import_BrowserStorage.default("mctprojects");
      CreatorToolsHost.deploymentStorage[import_DeploymentTarget.DeploymentTargetType.bedrock] = new import_BrowserStorage.default("mctdeploy");
      CreatorToolsHost.workingStorage = new import_BrowserStorage.default("mctworking");
      CreatorToolsHost.worldStorage = new import_BrowserStorage.default("mctworlds");
      CreatorToolsHost.packStorage = new import_BrowserStorage.default("mctpacks");
    }
    if (CreatorToolsHost.prefsStorage === null || CreatorToolsHost.projectsStorage === null) {
      throw new Error("Unexpected uninitialized storage.");
    }
    CreatorToolsHost._creatorTools = new import_CreatorTools.default(
      CreatorToolsHost.prefsStorage,
      CreatorToolsHost.projectsStorage,
      CreatorToolsHost.deploymentStorage,
      CreatorToolsHost.worldStorage,
      CreatorToolsHost.packStorage,
      CreatorToolsHost.workingStorage,
      CreatorToolsHost.contentWebRoot
    );
    if (CreatorToolsHost.ensureLocalFolder) {
      CreatorToolsHost._creatorTools.ensureLocalFolder = CreatorToolsHost.ensureLocalFolder;
      CreatorToolsHost._creatorTools.localFolderExists = CreatorToolsHost.localFolderExists;
      CreatorToolsHost._creatorTools.localFileExists = CreatorToolsHost.localFileExists;
    } else if (CreatorToolsHost.hostType === 3 /* electronWeb */) {
      CreatorToolsHost._creatorTools.ensureLocalFolder = CreatorToolsHost._ensureElectronLocalFolder;
      CreatorToolsHost._creatorTools.localFolderExists = CreatorToolsHost._localFolderExists;
      CreatorToolsHost._creatorTools.localFileExists = CreatorToolsHost._localFileExists;
    }
    CreatorToolsHost._creatorTools.createMinecraft = CreatorToolsHost.createMinecraft;
    CreatorToolsHost._creatorTools.canCreateMinecraft = CreatorToolsHost.canCreateMinecraft;
    CreatorToolsHost._initialized = true;
    this._onInitialized.dispatch(CreatorToolsHost._creatorTools, CreatorToolsHost._creatorTools);
  }
  static async _localFolderExists(path) {
    const ls = new import_ElectronStorage.default(path, "");
    let result = ls.available;
    if (!result) {
      result = await ls.getAvailable();
      if (!result) {
        return false;
      }
    }
    return await ls.rootFolder.exists();
  }
  static async _localFileExists(path) {
    const folderPath = import_StorageUtilities.default.getFolderPath(path);
    const fileName = import_StorageUtilities.default.getLeafName(path);
    if (!fileName || fileName.length < 2 || !folderPath || folderPath.length < 2) {
      throw new Error("Could not process file with path: `" + path + "`");
    }
    const ls = new import_ElectronStorage.default(folderPath, "");
    const file = ls.rootFolder.ensureFile(fileName);
    return await file.exists();
  }
  static _ensureElectronLocalFolder(path) {
    const ls = new import_ElectronStorage.default(path, "");
    return ls.rootFolder;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CreatorToolsThemeStyle,
  HostType
});
//# sourceMappingURL=CreatorToolsHost.js.map
