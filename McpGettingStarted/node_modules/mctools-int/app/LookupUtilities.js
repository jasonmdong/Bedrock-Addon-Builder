"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var LookupUtilities_exports = {};
__export(LookupUtilities_exports, {
  default: () => LookupUtilities
});
module.exports = __toCommonJS(LookupUtilities_exports);
var import_Utilities = __toESM(require("../core/Utilities"));
var import_VanillaProjectManager = __toESM(require("../minecraft/VanillaProjectManager"));
var import_Database = __toESM(require("../minecraft/Database"));
var import_MinecraftUtilities = __toESM(require("../minecraft/MinecraftUtilities"));
class LookupUtilities {
  static blockTypeRefs = void 0;
  static entityTypeRefs = void 0;
  static itemTypeRefs = void 0;
  static soundDefRefs = void 0;
  static terrainTextureRefs = void 0;
  static itemTextureRefs = void 0;
  static async getLookup(lookupId) {
    switch (lookupId) {
      case "blockType":
        return await this.getBlockTypeReferences();
      case "entityType":
        return await this.getEntityTypeReferences();
      case "itemType":
        return await this.getItemTypeReferences();
      case "soundDefinition":
        return await this.getSoundDefinitionReferences();
      case "terrainTexture":
        return await this.getTerrainTextureReferences();
      case "entityTypeEvents":
        break;
      case "difficulty":
        break;
      case "damageCause":
        break;
      case "lootTable":
        break;
      case "entityTypePlusVariants":
        break;
    }
    return void 0;
  }
  static async getSoundDefinitionReferences() {
    if (this.soundDefRefs) {
      return this.soundDefRefs;
    }
    const soundData = await import_VanillaProjectManager.default.getSoundDefinitionCatalog();
    const refs = [];
    if (soundData) {
      const soundSets = soundData.getSoundDefinitionSetNameList();
      if (soundSets) {
        for (const soundSet of soundSets) {
          refs.push({
            id: soundSet,
            title: import_Utilities.default.humanifyMinecraftName(soundSet)
          });
        }
      }
    }
    LookupUtilities.sortReferences(refs);
    this.soundDefRefs = refs;
    return this.soundDefRefs;
  }
  static async getItemTextureReferences() {
    if (this.itemTextureRefs) {
      return this.itemTextureRefs;
    }
    const itemData = await import_VanillaProjectManager.default.getItemTexturesCatalog();
    const refs = [];
    if (itemData) {
      const textureRefs = itemData.getTextureReferences();
      if (textureRefs) {
        for (const textureRef of textureRefs) {
          refs.push({
            id: textureRef,
            title: import_Utilities.default.humanifyMinecraftName(textureRef)
          });
        }
      }
    }
    LookupUtilities.sortReferences(refs);
    this.itemTextureRefs = refs;
    return this.itemTextureRefs;
  }
  static async getTerrainTextureReferences() {
    if (this.terrainTextureRefs) {
      return this.terrainTextureRefs;
    }
    const terrainData = await import_VanillaProjectManager.default.getTerrainTexturesCatalog();
    const refs = [];
    if (terrainData) {
      const textureRefs = terrainData.getTextureReferences();
      if (textureRefs) {
        for (const textureRef of textureRefs) {
          refs.push({
            id: textureRef,
            title: import_Utilities.default.humanifyMinecraftName(textureRef)
          });
        }
      }
    }
    LookupUtilities.sortReferences(refs);
    this.terrainTextureRefs = refs;
    return this.terrainTextureRefs;
  }
  static async getBlockTypeReferences() {
    if (this.blockTypeRefs) {
      return this.blockTypeRefs;
    }
    const blockData = await import_Database.default.getBlocksMetadata();
    const refs = [];
    if (blockData && blockData.data_items) {
      for (const dataItem of blockData.data_items) {
        if (dataItem.name && import_MinecraftUtilities.default.isBedrockItem(dataItem.name)) {
          const iconImage = import_MinecraftUtilities.default.getBlockDefaultTexturePath(dataItem.name);
          refs.push({
            id: dataItem.name,
            title: import_Utilities.default.humanifyMinecraftName(dataItem.name),
            iconImage: iconImage ? "/res/latest/van/serve/resource_pack/" + iconImage + ".png" : void 0
          });
        }
      }
    }
    LookupUtilities.sortReferences(refs);
    this.blockTypeRefs = refs;
    return this.blockTypeRefs;
  }
  static async getEntityTypeReferences() {
    if (this.entityTypeRefs) {
      return this.entityTypeRefs;
    }
    const entitiesData = await import_Database.default.getEntitiesMetadata();
    const refs = [];
    if (entitiesData && entitiesData.data_items) {
      for (const dataItem of entitiesData.data_items) {
        if (dataItem.name && import_MinecraftUtilities.default.isBedrockItem(dataItem.name)) {
          refs.push({
            id: dataItem.name,
            title: import_Utilities.default.humanifyMinecraftName(dataItem.name)
          });
        }
      }
    }
    LookupUtilities.sortReferences(refs);
    LookupUtilities.entityTypeRefs = refs;
    return LookupUtilities.entityTypeRefs;
  }
  static async getItemTypeReferences() {
    if (this.itemTypeRefs) {
      return this.itemTypeRefs;
    }
    const itemsData = await import_Database.default.getItemsMetadata();
    const refs = [];
    if (itemsData && itemsData.data_items) {
      for (const dataItem of itemsData.data_items) {
        if (dataItem.name && import_MinecraftUtilities.default.isBedrockItem(dataItem.name)) {
          refs.push({
            id: dataItem.name,
            title: import_Utilities.default.humanifyMinecraftName(dataItem.name)
          });
        }
      }
    }
    LookupUtilities.sortReferences(refs);
    LookupUtilities.itemTypeRefs = refs;
    return LookupUtilities.itemTypeRefs;
  }
  static appendReferences(core, references) {
    if (references === void 0) {
      return;
    }
    for (const ref of references) {
      let foundRef = false;
      for (const targetRef of core) {
        if (targetRef.id === ref.id) {
          foundRef = true;
        }
      }
      if (!foundRef) {
        core.push(ref);
      }
    }
  }
  static sortReferences(references) {
    references.sort((a, b) => {
      if (a.title && b.title) {
        return a.title.localeCompare(b.title);
      }
      if (a.id && b.id) {
        return import_Utilities.default.staticCompare(a.id.toString(), b.id.toString());
      }
      return 0;
    });
  }
  static getReferencesFromAnnotatedValues(paths, annotationCategory, description, startsWithFilter) {
    if (!paths) {
      return void 0;
    }
    let simpleRefs = [];
    for (let path in paths) {
      const val = paths[path];
      if (path && val && (!startsWithFilter || path.startsWith(startsWithFilter))) {
        if (startsWithFilter) {
          path = path.substring(startsWithFilter.length);
        }
        for (const annotatedVal of val) {
          if (annotatedVal.annotation === annotationCategory) {
            simpleRefs.push({
              id: path,
              title: import_Utilities.default.humanifyMinecraftName(path),
              description: description ? import_Utilities.default.stringFormat(description, import_Utilities.default.humanifyMinecraftName(path)) : void 0
            });
            continue;
          }
        }
      }
    }
    if (simpleRefs && simpleRefs.length === 0) {
      return void 0;
    }
    return simpleRefs;
  }
}
//# sourceMappingURL=LookupUtilities.js.map
