"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CommandRegistry_exports = {};
__export(CommandRegistry_exports, {
  CommandScope: () => CommandScope,
  default: () => CommandRegistry
});
module.exports = __toCommonJS(CommandRegistry_exports);
var import_Log = __toESM(require("../core/Log"));
var import_Utilities = __toESM(require("../core/Utilities"));
var import_CommandStructure = __toESM(require("./CommandStructure"));
var import_ICommand = require("./ICommand");
var CommandScope = /* @__PURE__ */ ((CommandScope2) => {
  CommandScope2[CommandScope2["any"] = 1] = "any";
  CommandScope2[CommandScope2["project"] = 2] = "project";
  CommandScope2[CommandScope2["minecraft"] = 3] = "minecraft";
  CommandScope2[CommandScope2["carto"] = 4] = "carto";
  CommandScope2[CommandScope2["host"] = 5] = "host";
  CommandScope2[CommandScope2["debug"] = 10] = "debug";
  CommandScope2[CommandScope2["debugProject"] = 11] = "debugProject";
  CommandScope2[CommandScope2["debugMinecraft"] = 12] = "debugMinecraft";
  CommandScope2[CommandScope2["debugHost"] = 14] = "debugHost";
  return CommandScope2;
})(CommandScope || {});
const MinecraftCommands = [
  "aimassist",
  "allowlist",
  "alwaysday",
  "camera",
  "camerashake",
  "changesetting",
  "clear",
  "clearspawnpoint",
  "clone",
  "commandbuilder",
  "connect",
  "controlscheme",
  "damage",
  "daylock",
  "deop",
  "dialogue",
  "difficulty",
  "effect",
  "enchant",
  "event",
  "execute",
  "fill",
  "fog",
  "function",
  "gamemode",
  "gamerule",
  "gametest",
  "give",
  "help",
  "hud",
  "inputpermission",
  "kick",
  "kill",
  "list",
  "locate",
  "loot",
  "me",
  "msg",
  "mobevent",
  "music",
  "op",
  "ops",
  "particle",
  "permission",
  "place",
  "playanimation",
  "playsound",
  "project",
  "recipe",
  "reload",
  "reloadconfig",
  "reloadpacketlimitconfig",
  "replaceitem",
  "ride",
  "save",
  "say",
  "schedule",
  "scoreboard",
  "script",
  "scriptevent",
  "sendshowstoreoffer",
  "setblock",
  "setmaxplayers",
  "setworldspawn",
  "simulationtype",
  "spawnpoint",
  "spreadplayers",
  "stop",
  "stopsound",
  "structure",
  "summon",
  "tag",
  "teleport",
  "tell",
  "tellraw",
  "testfor",
  "testforblock",
  "testforblocks",
  "tickingarea",
  "tp",
  "time",
  "title",
  "titleraw",
  "toggledownfall",
  "transfer",
  "volumearea",
  "weather",
  "wsserver",
  "whitelist",
  "?",
  "w",
  "xp"
];
const MinecraftAddOnBlockedCommands = [
  "allowlist",
  "alwaysday",
  "changesetting",
  "connect",
  "daylock",
  "deop",
  "difficulty",
  "gamemode",
  "gamerule",
  "gametest",
  "help",
  "kick",
  "list",
  "locate",
  "op",
  "ops",
  "permission",
  "project",
  "reload",
  "reloadconfig",
  "save",
  "script",
  "setmaxplayers",
  "setworldspawn",
  "simulationtype",
  "stop",
  "tickingarea",
  "time",
  "transfer",
  "wsserver",
  "whitelist",
  "?"
];
class CommandRegistry {
  static _registry;
  _commandsByName = {};
  _commandsByScope = {};
  static get main() {
    if (!this._registry) {
      this._registry = new CommandRegistry();
      this._registry.registerCommand(
        "sleep",
        1 /* any */,
        async (context, name, args) => {
          if (args.length === 1) {
            let delay = 0;
            try {
              delay = parseInt(args[0]);
            } catch (e) {
            }
            if (delay > 0) {
              context.creatorTools.notifyStatusUpdate("(Delaying commands for " + delay + "ms).");
              await import_Utilities.default.sleep(delay);
            }
          }
          return { status: import_ICommand.CommandStatus.completed };
        }
      );
    }
    return this._registry;
  }
  registerCommand(commandName, commandScope, command) {
    commandName = commandName.toLowerCase();
    if (import_Utilities.default.isUsableAsObjectKey(commandName)) {
      this._commandsByName[commandName] = command;
      this._commandsByScope[commandName] = commandScope;
    } else {
      import_Log.default.unsupportedToken(commandName);
    }
  }
  logHelp() {
    let commandArr = [];
    for (const commandName in this._commandsByName) {
      commandArr.push(commandName);
    }
    commandArr = commandArr.sort();
    import_Log.default.message("Use help <command name> for more detailed help on a command.");
    for (let i = 0; i < commandArr.length; i++) {
      import_Log.default.message(commandArr[i]);
    }
  }
  static isMinecraftBuiltInCommand(name) {
    return MinecraftCommands.includes(name) || name === "#";
  }
  static isAddOnBlockedCommand(name) {
    return MinecraftAddOnBlockedCommands.includes(name);
  }
  async runCommand(context, commandText) {
    const command = import_CommandStructure.default.parse(commandText);
    if (!command || !command.fullName || !command.commandArguments) {
      return void 0;
    }
    const scope = this._commandsByScope[command.fullName];
    const commandName = this._commandsByName[command.fullName];
    if (command.fullName === "help") {
      this.logHelp();
      return { status: import_ICommand.CommandStatus.completed };
    } else if (CommandRegistry.isMinecraftBuiltInCommand(command.fullName) && context.minecraft) {
      import_Log.default.debug("Sending '" + commandText + "' to Minecraft.");
      let result = await context.minecraft.runCommand(commandText);
      return {
        data: result,
        status: import_ICommand.CommandStatus.completed
      };
    } else if (scope && commandName) {
      if ((scope === 10 /* debug */ || scope === 11 /* debugProject */ || scope === 12 /* debugMinecraft */) && !import_Utilities.default.isDebug) {
        return void 0;
      }
      if ((scope === 2 /* project */ || scope === 11 /* debugProject */) && !context.project) {
        import_Log.default.message("Could not run command '" + command.fullName + "'; no active project.");
        return void 0;
      }
      if ((scope === 3 /* minecraft */ || scope === 12 /* debugMinecraft */) && !context.minecraft) {
        import_Log.default.message("Could not run command '" + command.fullName + "'; no active Minecraft deploy target was set.");
        return void 0;
      }
      if ((scope === 5 /* host */ || scope === 14 /* debugHost */) && !context.host) {
        import_Log.default.message("Could not run command '" + command.fullName + "'; no host was set.");
        return void 0;
      }
      const result = await commandName(context, command.fullName, command.commandArguments);
      if (result.status === import_ICommand.CommandStatus.invalidEnvironment) {
        import_Log.default.error("'" + command.fullName + "' was not set up properly.");
      } else if (result.status === import_ICommand.CommandStatus.invalidArguments) {
        import_Log.default.error("'" + command.fullName + "' command arguments were not set up.");
      }
      return result;
    }
    context.creatorTools.notifyStatusUpdate("Could not find a command '" + command.fullName + "'.");
    return void 0;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CommandScope
});
//# sourceMappingURL=CommandRegistry.js.map
