"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ProjectAutogeneration_exports = {};
__export(ProjectAutogeneration_exports, {
  default: () => ProjectAutogeneration
});
module.exports = __toCommonJS(ProjectAutogeneration_exports);
var import_WorldTestManager = __toESM(require("../script/WorldTestManager"));
var import_IProjectItemData = require("./IProjectItemData");
var import_Project = require("./Project");
var import_Log = __toESM(require("../core/Log"));
var import_IProjectData = require("./IProjectData");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_EntityTypeDefinition = __toESM(require("../minecraft/EntityTypeDefinition"));
var import_BehaviorManifestDefinition = __toESM(require("../minecraft/BehaviorManifestDefinition"));
var import_EnvSettings = __toESM(require("../devproject/EnvSettings"));
var import_VsCodeLaunchDefinition = __toESM(require("../devproject/VsCodeLaunchDefinition"));
var import_VsCodeTasksDefinition = __toESM(require("../devproject/VsCodeTasksDefinition"));
var import_VsCodeExtensionsDefinition = __toESM(require("../devproject/VsCodeExtensionsDefinition"));
var import_VsCodeSettingsDefinition = __toESM(require("../devproject/VsCodeSettingsDefinition"));
var import_PrettierRcConfig = __toESM(require("../devproject/PrettierRcConfig"));
var import_JustConfig = __toESM(require("../devproject/JustConfig"));
var import_NpmPackageDefinition = __toESM(require("../devproject/NpmPackageDefinition"));
var import_IFile = require("../storage/IFile");
class ProjectAutogeneration {
  static async updateProjectAutogeneration(project, createNewArtifacts) {
    let packageItems = 0;
    let envItems = 0;
    let justConfigItems = 0;
    let needsJustConfig = false;
    let needsEslintConfig = false;
    let eslintConfigItems = 0;
    let vsCodeLaunchItems = 0;
    let vsCodeExtensionsItems = 0;
    let vsCodeSettingsItems = 0;
    let vsCodeTasksItems = 0;
    let prettierRcItems = 0;
    const items = project.getItemsCopy();
    for (let i = 0; i < items.length; i++) {
      const item = items[i];
      if (item.itemType === import_IProjectItemData.ProjectItemType.packageJson) {
        packageItems++;
        const itemFile = item.primaryFile;
        if (itemFile) {
          if (!itemFile.isContentLoaded) {
            await itemFile.loadContent();
          }
          if (itemFile.content && typeof itemFile.content === "string") {
            if (itemFile.content.indexOf("just-scripts") > 0) {
              needsJustConfig = true;
            }
            if (itemFile.content.indexOf("eslint") > 0) {
              needsEslintConfig = true;
            }
          }
        }
      } else if (item.itemType === import_IProjectItemData.ProjectItemType.env) {
        envItems++;
      } else if (item.itemType === import_IProjectItemData.ProjectItemType.vsCodeLaunchJson) {
        vsCodeLaunchItems++;
      } else if (item.itemType === import_IProjectItemData.ProjectItemType.prettierRcJson) {
        prettierRcItems++;
      } else if (item.itemType === import_IProjectItemData.ProjectItemType.esLintConfigMjs) {
        eslintConfigItems++;
      } else if (item.itemType === import_IProjectItemData.ProjectItemType.justConfigTs) {
        justConfigItems++;
      }
    }
    if (envItems === 0 && (justConfigItems > 0 || needsJustConfig) && createNewArtifacts) {
      project.ensureItemByProjectPath(
        "/.env",
        import_IProjectItemData.ProjectItemStorageType.singleFile,
        ".env",
        import_IProjectItemData.ProjectItemType.env,
        import_Project.FolderContext.unknown,
        void 0,
        import_IProjectItemData.ProjectItemCreationType.generated
      );
    }
    if (prettierRcItems === 0 && packageItems > 0 && createNewArtifacts) {
      project.ensureItemByProjectPath(
        "/.prettierrc.json",
        import_IProjectItemData.ProjectItemStorageType.singleFile,
        ".prettierrc.json",
        import_IProjectItemData.ProjectItemType.prettierRcJson,
        import_Project.FolderContext.unknown,
        void 0,
        import_IProjectItemData.ProjectItemCreationType.generated
      );
    }
    if (justConfigItems === 0 && needsJustConfig && createNewArtifacts) {
      project.ensureItemByProjectPath(
        "/just.config.ts",
        import_IProjectItemData.ProjectItemStorageType.singleFile,
        "just.config.ts",
        import_IProjectItemData.ProjectItemType.justConfigTs,
        import_Project.FolderContext.unknown,
        void 0,
        import_IProjectItemData.ProjectItemCreationType.generated
      );
    }
    if (eslintConfigItems === 0 && needsEslintConfig && createNewArtifacts) {
      project.ensureItemByProjectPath(
        "/eslint.config.mjs",
        import_IProjectItemData.ProjectItemStorageType.singleFile,
        "eslint.config.mjs",
        import_IProjectItemData.ProjectItemType.esLintConfigMjs,
        import_Project.FolderContext.unknown,
        void 0,
        import_IProjectItemData.ProjectItemCreationType.generated
      );
    }
    if (vsCodeLaunchItems === 0 && packageItems > 0 && project.projectFolder && createNewArtifacts) {
      project.ensureItemByProjectPath(
        "/.vscode/launch.json",
        import_IProjectItemData.ProjectItemStorageType.singleFile,
        "launch.json",
        import_IProjectItemData.ProjectItemType.vsCodeLaunchJson,
        import_Project.FolderContext.unknown,
        void 0,
        import_IProjectItemData.ProjectItemCreationType.generated
      );
    }
    if (vsCodeSettingsItems === 0 && packageItems > 0 && project.projectFolder && createNewArtifacts) {
      project.ensureItemByProjectPath(
        "/.vscode/settings.json",
        import_IProjectItemData.ProjectItemStorageType.singleFile,
        "settings.json",
        import_IProjectItemData.ProjectItemType.vsCodeSettingsJson,
        import_Project.FolderContext.unknown,
        void 0,
        import_IProjectItemData.ProjectItemCreationType.generated
      );
    }
    if (vsCodeTasksItems === 0 && packageItems > 0 && project.projectFolder && createNewArtifacts) {
      project.ensureItemByProjectPath(
        "/.vscode/tasks.json",
        import_IProjectItemData.ProjectItemStorageType.singleFile,
        "tasks.json",
        import_IProjectItemData.ProjectItemType.vsCodeTasksJson,
        import_Project.FolderContext.unknown,
        void 0,
        import_IProjectItemData.ProjectItemCreationType.generated
      );
    }
    if (vsCodeExtensionsItems === 0 && packageItems > 0 && project.projectFolder && createNewArtifacts) {
      project.ensureItemByProjectPath(
        "/.vscode/extensions.json",
        import_IProjectItemData.ProjectItemStorageType.singleFile,
        "extensions.json",
        import_IProjectItemData.ProjectItemType.vsCodeExtensionsJson,
        import_Project.FolderContext.unknown,
        void 0,
        import_IProjectItemData.ProjectItemCreationType.generated
      );
    }
    for (let i = 0; i < items.length; i++) {
      const item = items[i];
      if (item.creationType === import_IProjectItemData.ProjectItemCreationType.generated) {
        await ProjectAutogeneration.updateItemAutogeneration(item);
      }
      await ProjectAutogeneration.updateItemAutogeneratedSideFiles(item);
    }
  }
  static async updateItemAutogeneratedSideFiles(item) {
    if (!item.primaryFile || !item.project) {
      return;
    }
    if (item.itemType === import_IProjectItemData.ProjectItemType.worldTest) {
      const worldTestManager = await import_WorldTestManager.default.ensureOnFile(item.primaryFile);
      if (worldTestManager) {
        await worldTestManager.persistSideFiles(item.project);
      }
    }
  }
  static async updateItemAutogeneration(item, contentOnlyUpdate) {
    if (!contentOnlyUpdate && !item.isContentLoaded) {
      await item.loadContent();
    }
    if (item.primaryFile == null || item.creationType !== import_IProjectItemData.ProjectItemCreationType.generated) {
      return;
    }
    if (item.itemType === import_IProjectItemData.ProjectItemType.catalogIndexJs) {
      if (!item.primaryFile.isContentLoaded) {
        await item.primaryFile.loadContent();
      }
      ProjectAutogeneration.updateCatalogJsContent(item, await ProjectAutogeneration.getCatalogJsContent(item.project));
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.env) {
      item.primaryFile.setContentIfSemanticallyDifferent(
        await import_EnvSettings.default.getContent(
          item.project,
          typeof item.primaryFile.content === "string" ? item.primaryFile.content : void 0
        ),
        import_IFile.FileUpdateType.versionlessEdit
      );
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.justConfigTs) {
      const justConfigTs = await import_JustConfig.default.ensureOnFile(item.primaryFile);
      if (justConfigTs) {
        await justConfigTs.ensureDefault();
        await justConfigTs.save();
      }
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.vsCodeLaunchJson) {
      const vscodelaunch = await import_VsCodeLaunchDefinition.default.ensureOnFile(item.primaryFile);
      if (vscodelaunch) {
        await vscodelaunch.ensureMinContent();
        await vscodelaunch.save();
      }
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.vsCodeTasksJson) {
      const vscodetasks = await import_VsCodeTasksDefinition.default.ensureOnFile(item.primaryFile);
      if (vscodetasks) {
        await vscodetasks.ensureMinContent();
        await vscodetasks.save();
      }
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.vsCodeExtensionsJson) {
      const vscodeextensions = await import_VsCodeExtensionsDefinition.default.ensureOnFile(item.primaryFile);
      if (vscodeextensions) {
        await vscodeextensions.ensureMinContent();
        await vscodeextensions.save();
      }
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.vsCodeSettingsJson) {
      const vscodesettings = await import_VsCodeSettingsDefinition.default.ensureOnFile(item.primaryFile);
      if (vscodesettings) {
        await vscodesettings.ensureMinContent();
        await vscodesettings.save();
      }
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.prettierRcJson) {
      const prettierrc = await import_PrettierRcConfig.default.ensureOnFile(item.primaryFile);
      if (prettierrc) {
        await prettierrc.ensureMinContent();
        await prettierrc.save();
      }
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.packageJson) {
      const packageJson = await import_NpmPackageDefinition.default.ensureOnFile(item.primaryFile);
      if (packageJson) {
        await packageJson.ensureMinContent(item.project);
        await packageJson.save();
      }
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.behaviorPackManifestJson) {
      item.primaryFile.setContent(await import_BehaviorManifestDefinition.default.getContent(item.project, contentOnlyUpdate));
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.entityTypeBaseJs) {
      const content = await ProjectAutogeneration.getGeneratedEntityTypeScript(item);
      if (content.javaScript !== void 0) {
        item.primaryFile.setContent(content.javaScript, import_IFile.FileUpdateType.versionlessEdit);
      }
      if (item.project.preferredScriptLanguage === import_IProjectData.ProjectScriptLanguage.typeScript && content.typeScript !== void 0) {
        const file = item.primaryFile.parentFolder.ensureFile(
          import_StorageUtilities.default.getBaseFromName(item.primaryFile.name) + ".ts"
        );
        file.setContent(content.typeScript, import_IFile.FileUpdateType.versionlessEdit);
      }
      if (item.project.preferredScriptLanguage === import_IProjectData.ProjectScriptLanguage.typeScript && content.typeScript !== void 0) {
        const file = item.primaryFile.parentFolder.ensureFile(
          import_StorageUtilities.default.getBaseFromName(item.primaryFile.name) + ".ts"
        );
        file.setContent(content.typeScript, import_IFile.FileUpdateType.versionlessEdit);
      }
    } else if (item.itemType === import_IProjectItemData.ProjectItemType.entityTypeBaseTs) {
      const content = await ProjectAutogeneration.getGeneratedEntityTypeScript(item);
      if (content.typeScript !== void 0) {
        item.primaryFile.setContent(content.typeScript, import_IFile.FileUpdateType.versionlessEdit);
      }
    }
  }
  static async getCatalogJsContent(project) {
    const content = [];
    const behaviorPackRootFolder = await project.ensureDefaultBehaviorPackFolder();
    const items = project.getItemsCopy();
    for (let i = 0; i < items.length; i++) {
      const projectItem = items[i];
      if (projectItem.itemType === import_IProjectItemData.ProjectItemType.worldTest) {
        const file = await projectItem.loadFileContent();
        if (file) {
          const newFileName = import_StorageUtilities.default.getBaseFromName(file.name) + ".gen.js";
          content.push('import "scripts/' + newFileName + '";');
        }
      } else if (projectItem.itemType === import_IProjectItemData.ProjectItemType.js || projectItem.itemType === import_IProjectItemData.ProjectItemType.buildProcessedJs || projectItem.itemType === import_IProjectItemData.ProjectItemType.entityTypeBaseJs || projectItem.itemType === import_IProjectItemData.ProjectItemType.testJs) {
        const file = await projectItem.loadFileContent();
        if (file) {
          let filePath = file.getFolderRelativePath(behaviorPackRootFolder);
          if (!file.isContentLoaded) {
            await file.loadContent(false);
          }
          if ((file.content !== null || filePath !== null) && filePath !== void 0) {
            if (filePath.startsWith("/scripts/")) {
              filePath = filePath.substring(9, filePath.length);
            }
            if (filePath.startsWith("/")) {
              filePath = filePath.substring(1, filePath.length);
            }
            content.push('import "' + filePath + '";');
          }
        }
      }
    }
    return import_Project.AUTOGENERATED_JS_SEPARATOR + "\n\n" + content.join("\n");
  }
  static async updateCatalogJsContent(item, newContent) {
    if (item.primaryFile == null || item.primaryFile.content instanceof Uint8Array) {
      import_Log.default.unexpectedContentState();
      return;
    }
    if (item.primaryFile.content === void 0 || item.primaryFile.content === null || item.primaryFile.content.trim() === "") {
      item.primaryFile.setContent(newContent, import_IFile.FileUpdateType.versionlessEdit);
      return;
    }
    let updatedContent = item.primaryFile.content;
    const autogenToken = updatedContent.indexOf(import_Project.AUTOGENERATED_CONTENT_TOKEN);
    if (autogenToken >= 0) {
      let previous = updatedContent.lastIndexOf("\r", autogenToken);
      if (previous < 0) {
        previous = updatedContent.lastIndexOf("\n", autogenToken);
        if (previous < 0) {
          previous = 0;
        }
      }
      const previousComment = updatedContent.lastIndexOf("//", autogenToken);
      if (previousComment > previous + 2) {
        previous = previousComment;
      }
      if (previous >= 0) {
        updatedContent = updatedContent.substring(0, previous);
      }
    }
    updatedContent += newContent;
    item.primaryFile.setContent(updatedContent, import_IFile.FileUpdateType.versionlessEdit);
  }
  static async getGeneratedEntityTypeScript(item) {
    if (item.projectPath === null || item.projectPath === void 0) {
      return { javaScript: "", typeScript: void 0 };
    }
    let typeName = import_StorageUtilities.default.getBaseFromName(import_StorageUtilities.default.getLeafName(item.projectPath));
    if (typeName.endsWith(".base")) {
      typeName = typeName.substring(0, typeName.length - 5);
    }
    const items = item.project.getItemsCopy();
    for (let i = 0; i < items.length; i++) {
      const candItem = items[i];
      if (candItem.itemType === import_IProjectItemData.ProjectItemType.entityTypeBehavior && candItem.projectPath !== null && candItem.projectPath !== void 0) {
        const jsonName = import_StorageUtilities.default.getBaseFromName(import_StorageUtilities.default.getLeafName(candItem.projectPath));
        if (jsonName === typeName) {
          const jsonFile = await candItem.loadFileContent();
          if (jsonFile !== void 0 && jsonFile !== null) {
            if (jsonFile.manager === void 0) {
              await import_EntityTypeDefinition.default.ensureOnFile(jsonFile);
            }
            if (jsonFile.manager) {
              const et = jsonFile.manager;
              const js = et.getScript(false);
              let ts = void 0;
              if (item.project.preferredScriptLanguage === import_IProjectData.ProjectScriptLanguage.typeScript) {
                ts = et.getScript(true);
              }
              return { javaScript: js, typeScript: ts };
            }
          }
        }
      }
    }
    return { javaScript: "", typeScript: void 0 };
  }
}
//# sourceMappingURL=ProjectAutogeneration.js.map
