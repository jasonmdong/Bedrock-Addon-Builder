"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ProjectItem_exports = {};
__export(ProjectItem_exports, {
  default: () => ProjectItem
});
module.exports = __toCommonJS(ProjectItem_exports);
var import_IProjectItemData = require("./IProjectItemData");
var import_Log = __toESM(require("./../core/Log"));
var import_IProjectItemData2 = require("./IProjectItemData");
var import_ste_events = require("ste-events");
var import_IFile = require("../storage/IFile");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_EntityTypeDefinition = __toESM(require("../minecraft/EntityTypeDefinition"));
var import_IProjectData = require("./IProjectData");
var import_MCWorld = __toESM(require("../minecraft/MCWorld"));
var import_ZipStorage = __toESM(require("../storage/ZipStorage"));
var import_Utilities = __toESM(require("../core/Utilities"));
var import_IStorage = require("../storage/IStorage");
var import_ProjectItemUtilities = __toESM(require("./ProjectItemUtilities"));
var import_ProjectAutogeneration = __toESM(require("./ProjectAutogeneration"));
var import_ProjectItemRelations = __toESM(require("./ProjectItemRelations"));
var import_ProjectItemVariant = __toESM(require("./ProjectItemVariant"));
var import_IProjectItemVariant = require("./IProjectItemVariant");
var import_Database = __toESM(require("../minecraft/Database"));
var import_ProjectVariant = __toESM(require("./ProjectVariant"));
var import_MinecraftUtilities = __toESM(require("../minecraft/MinecraftUtilities"));
class ProjectItem {
  _data;
  _project;
  _onPropertyChanged = new import_ste_events.EventDispatcher();
  _onFileRetrieved = new import_ste_events.EventDispatcher();
  _onFolderRetrieved = new import_ste_events.EventDispatcher();
  _onLoaded = new import_ste_events.EventDispatcher();
  _defaultFile;
  _defaultFolder;
  _pendingLoadRequests = [];
  _isLoading = false;
  _isFileContentProcessed = false;
  _imageUrlBase64Cache;
  _pack;
  _primaryFile;
  _accessoryFolder;
  parentItems;
  childItems;
  unfulfilledRelationships;
  _variants;
  constructor(parent, incomingData) {
    this._project = parent;
    this._defaultFile = null;
    this._defaultFolder = null;
    this._accessoryFolder = null;
    this._isFileContentProcessed = false;
    this._variants = {};
    this._handleMCWorldLoaded = this._handleMCWorldLoaded.bind(this);
    this.sortVariantsMostImportantFirst = this.sortVariantsMostImportantFirst.bind(this);
    this.sortVariantsMostImportantLast = this.sortVariantsMostImportantLast.bind(this);
    if (incomingData) {
      this._data = incomingData;
    } else {
      this._data = {
        variants: {},
        itemType: import_IProjectItemData2.ProjectItemType.unknown,
        projectPath: null,
        storageType: import_IProjectItemData.ProjectItemStorageType.singleFile,
        tags: [],
        name: ""
      };
    }
  }
  get parentItemCount() {
    if (this.parentItems === void 0) {
      return 0;
    }
    return this.parentItems.length;
  }
  get childItemCount() {
    if (this.childItems === void 0) {
      return 0;
    }
    return this.childItems.length;
  }
  get unfulfilledRelationshipsCount() {
    if (this.unfulfilledRelationships === void 0) {
      return 0;
    }
    return this.unfulfilledRelationships.length;
  }
  get isInWorld() {
    return this._data.isInWorld;
  }
  set isInWorld(isInWorld) {
    this._data.isInWorld = isInWorld;
  }
  get project() {
    return this._project;
  }
  get onPropertyChanged() {
    return this._onPropertyChanged.asEvent();
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  get onFileRetrieved() {
    return this._onFileRetrieved.asEvent();
  }
  get onFolderRetrieved() {
    return this._onFolderRetrieved.asEvent();
  }
  get gitHubReference() {
    return this._data.gitHubReference;
  }
  get isInFileContainer() {
    if (!this.projectPath) {
      return false;
    }
    return this.projectPath.indexOf("#") >= 0;
  }
  get isFileContainerStorageItem() {
    return this.itemType === import_IProjectItemData2.ProjectItemType.zip || this.itemType === import_IProjectItemData2.ProjectItemType.MCWorld || this.itemType === import_IProjectItemData2.ProjectItemType.MCProject || this.itemType === import_IProjectItemData2.ProjectItemType.MCAddon || this.itemType === import_IProjectItemData2.ProjectItemType.MCPack || this.itemType === import_IProjectItemData2.ProjectItemType.MCTemplate;
  }
  hasCustomVariants() {
    for (const key in this._data.variants) {
      if (key !== "") {
        return true;
      }
    }
    return false;
  }
  hasVersionSliceCustomVariants() {
    for (const key in this._data.variants) {
      const varType = this._data.variants[key].variantType;
      if (key !== "" && (varType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAltPacks || varType === import_IProjectItemVariant.ProjectItemVariantType.versionSlice || varType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAlt)) {
        return true;
      }
    }
    return false;
  }
  _shouldEnsureDefaultVariant() {
    return this.defaultFile && (this.defaultFile.content || !this.hasCustomVariants()) || this.defaultFolder;
  }
  getVariant(variantName) {
    if (variantName === "" && this._shouldEnsureDefaultVariant()) {
      return this.ensureDefaultVariant();
    }
    return this._variants[variantName];
  }
  get isWorld() {
    return this.itemType === import_IProjectItemData2.ProjectItemType.MCProject || this.itemType === import_IProjectItemData2.ProjectItemType.MCWorld || this.itemType === import_IProjectItemData2.ProjectItemType.MCTemplate || this.itemType === import_IProjectItemData2.ProjectItemType.worldFolder;
  }
  static getGitHubSignature(info) {
    let sig = info.owner + "|" + info.repoName + "|";
    if (info.branch !== void 0) {
      sig += info.branch;
    }
    if (info.folder !== void 0) {
      sig += "|" + info.folder;
    }
    return sig;
  }
  get defaultVariant() {
    if (this._shouldEnsureDefaultVariant()) {
      return this.ensureDefaultVariant();
    }
    return this._variants[""];
  }
  async ensureAccessoryFolder() {
    if (this._accessoryFolder) {
      return this._accessoryFolder;
    }
    const rootAccessoryFolder = await this.project.ensureProjectItemAccessoryFolder();
    const folderPath = import_ProjectItemUtilities.default.getAccessoryFolderPathFromFilePath(this);
    this._accessoryFolder = await rootAccessoryFolder.ensureFolderFromRelativePath(folderPath);
    return this._accessoryFolder;
  }
  ensureDefaultVariant() {
    return this.ensureVariant("");
  }
  getVariantList() {
    if (this.defaultFile || this.defaultFolder) {
      this.ensureDefaultVariant();
    }
    return this._getVariantList();
  }
  _getVariantList() {
    const vararr = [];
    for (const key in this._data.variants) {
      const variant = this.ensureVariant(key);
      vararr.push(variant);
    }
    return vararr;
  }
  getVariantListMostImportantLast() {
    const vararr = [];
    const varKeys = Object.keys(this._data.variants);
    varKeys.sort(this.sortVariantsMostImportantLast);
    for (const key of varKeys) {
      const variant = this.ensureVariant(key);
      vararr.push(variant);
    }
    return vararr;
  }
  getVariantListMostImportantFirst() {
    const vararr = [];
    const varKeys = Object.keys(this._data.variants);
    varKeys.sort(this.sortVariantsMostImportantFirst);
    for (const key of varKeys) {
      const variant = this.ensureVariant(key);
      vararr.push(variant);
    }
    return vararr;
  }
  hasNonDefaultVariant() {
    const vararr = this._getVariantList();
    if (vararr.length === 0) {
      return false;
    }
    if (vararr.length > 1) {
      return true;
    }
    if (vararr[0].label !== "") {
      return true;
    }
    return false;
  }
  ensureVariant(label) {
    label = import_ProjectVariant.default.canonicalizeVariantLabel(label);
    if (!import_Utilities.default.isUsableAsObjectKey(label)) {
      import_Log.default.unsupportedToken(label);
      throw new Error();
    }
    if (!this._variants[label]) {
      const pv = this.project.ensureVariant(label);
      if (!this._data.variants) {
        this._data.variants = {};
      }
      if (this._data.variants[label] === void 0) {
        this._data.variants[label] = { label, variantType: import_IProjectItemVariant.ProjectItemVariantType.general };
      }
      this._primaryFile = void 0;
      this._variants[label] = new import_ProjectItemVariant.default(this, this._data.variants[label], pv);
    }
    return this._variants[label];
  }
  /// NOTE: ProjectItem.ensureFileStorage or ensureStorage should be called before this method.
  getFile(variantName) {
    if (variantName === void 0 || variantName === "") {
      return this._defaultFile;
    }
    const variant = this.ensureVariant(variantName);
    return variant.file;
  }
  /// NOTE: ProjectItem.ensureFolderStorage or ensureStorage should be called before this method.
  getFolder(variantName) {
    if (variantName === void 0 || variantName === "") {
      return this._defaultFolder;
    }
    const variant = this.ensureVariant(variantName);
    return variant.folder;
  }
  /// NOTE: ProjectItem.ensure*Storage should be called before this method.
  async getPack() {
    if (this._pack) {
      return this._pack;
    }
    let thisPath = void 0;
    if (this._defaultFile) {
      thisPath = this._defaultFile.storageRelativePath;
    } else if (this._defaultFolder) {
      thisPath = this._defaultFolder.storageRelativePath;
    }
    if (thisPath === void 0) {
      return void 0;
    }
    thisPath = import_StorageUtilities.default.canonicalizePath(thisPath);
    await this.project.ensurePacks();
    for (const pack of this.project.packs) {
      if (thisPath.startsWith(import_StorageUtilities.default.canonicalizePath(pack.folder.storageRelativePath))) {
        this._pack = pack;
        return this._pack;
      }
    }
    return void 0;
  }
  addUnfulfilledRelationship(path, itemType, isVanillaDependent) {
    let pir = {
      parentItem: this,
      path,
      itemType,
      isVanillaDependent: isVanillaDependent === true
    };
    if (this.unfulfilledRelationships === void 0) {
      this.unfulfilledRelationships = [];
    }
    this.unfulfilledRelationships.push(pir);
  }
  async ensureDependencies() {
    await import_ProjectItemRelations.default.calculateForItem(this);
  }
  addChildItem(childItem) {
    if (this.childItems) {
      for (const rel of this.childItems) {
        if (rel.childItem === childItem && rel.parentItem === this) {
          return;
        }
      }
    }
    if (childItem === this) {
      return;
    }
    if (import_ProjectItemUtilities.default.wouldBeCircular(childItem)) {
      return;
    }
    let hasChild = false;
    let childHasParent = false;
    if (this.childItems === void 0) {
      this.childItems = [];
    }
    if (childItem.parentItems === void 0) {
      childItem.parentItems = [];
    }
    for (const existingRelation of this.childItems) {
      if (existingRelation.childItem === childItem && existingRelation.parentItem === this) {
        hasChild = true;
      }
    }
    for (const existingRelation of childItem.parentItems) {
      if (existingRelation.childItem === childItem && existingRelation.parentItem === this) {
        childHasParent = true;
      }
    }
    const pir = {
      parentItem: this,
      childItem
    };
    if (!hasChild) {
      this.childItems.push(pir);
    }
    if (!childHasParent) {
      childItem.parentItems.push(pir);
    }
  }
  addParentItem(parentItem) {
    if (import_ProjectItemUtilities.default.wouldBeCircular(parentItem)) {
      return;
    }
    if (parentItem === this) {
      return;
    }
    if (this.parentItems === void 0) {
      this.parentItems = [];
    }
    if (parentItem.childItems === void 0) {
      parentItem.childItems = [];
    }
    let hasParent = false;
    let parentHasChild = false;
    for (const existingRelation of this.parentItems) {
      if (existingRelation.parentItem === parentItem && existingRelation.childItem === this) {
        hasParent = true;
      }
    }
    for (const existingRelation of parentItem.childItems) {
      if (existingRelation.parentItem === parentItem && existingRelation.childItem === this) {
        parentHasChild = true;
      }
    }
    const pir = {
      parentItem,
      childItem: this
    };
    if (!hasParent) {
      this.parentItems.push(pir);
    }
    if (!parentHasChild) {
      parentItem.childItems.push(pir);
    }
  }
  toString() {
    return this.itemType + ": " + this.projectPath;
  }
  /// NOTE: ProjectItem.ensure*Storage should be called before this method.
  async getPackRelativePath() {
    const pack = await this.getPack();
    if (!pack) {
      return void 0;
    }
    if (this._defaultFile) {
      return this._defaultFile.getFolderRelativePath(pack.folder);
    } else if (this._defaultFolder) {
      return this._defaultFolder.getFolderRelativePath(pack.folder);
    }
    return void 0;
  }
  static gitHubReferencesEqual(refA, refB) {
    if (refA === refB) {
      return true;
    }
    if (refA === void 0 && refB === void 0) {
      return true;
    }
    if (refA !== void 0 && refB !== void 0 && refA.owner === refB.owner && refA.repoName === refB.repoName && refA.folder === refB.folder && refA.branch === refB.branch) {
      return true;
    }
    return false;
  }
  set gitHubReference(value) {
    if (ProjectItem.gitHubReferencesEqual(this._data.gitHubReference, value)) {
      return;
    }
    this._data.gitHubReference = value;
    this._project.notifyProjectItemChanged(this);
  }
  get title() {
    return import_StorageUtilities.default.getContaineredFileLeafPath(this.projectPath) + " (" + import_ProjectItemUtilities.default.getDescriptionForType(this._data.itemType).toLowerCase() + ")";
  }
  get typeTitle() {
    return import_ProjectItemUtilities.default.getDescriptionForType(this._data.itemType);
  }
  getPackFolderName() {
    if (this.projectPath === void 0 || this.projectPath === null) {
      return void 0;
    }
    let folderStoragePath = import_StorageUtilities.default.getFolderPath(this.projectPath);
    if (folderStoragePath === void 0) {
      return void 0;
    }
    let folderStoragePathLower = folderStoragePath.toLowerCase();
    if (this.project.packs.length > 2) {
      const endOfPacks = folderStoragePathLower.indexOf("_packs/");
      if (endOfPacks > 0) {
        const endOfPacksSegment = folderStoragePathLower.indexOf("/", endOfPacks + 7);
        if (endOfPacksSegment > 0) {
          return folderStoragePath.substring(endOfPacks + 7, endOfPacksSegment);
        }
      }
    }
    return void 0;
  }
  get folderPath() {
    if (this.projectPath === void 0 || this.projectPath === null) {
      return void 0;
    }
    return import_StorageUtilities.default.getFolderPath(this.projectPath);
  }
  getFolderGroupingPath() {
    if (this.projectPath === void 0 || this.projectPath === null) {
      return void 0;
    }
    let folderStoragePath = import_StorageUtilities.default.getFolderPath(this.projectPath);
    if (folderStoragePath === void 0) {
      return void 0;
    }
    folderStoragePath = import_MinecraftUtilities.default.clearCommonTerms(folderStoragePath);
    let folderStoragePathLower = folderStoragePath.toLowerCase();
    const folderTypeRoots = import_ProjectItemUtilities.default.getFolderRootsForType(this.itemType);
    folderTypeRoots.push("zip");
    for (const folderTypeRoot of folderTypeRoots) {
      if (this.project.hasMultiplePacksOfSameType) {
        folderStoragePath = import_Utilities.default.replaceAllCaseInsensitive(folderStoragePath, "/" + folderTypeRoot + "/", "/");
      } else {
        const start = folderStoragePathLower.indexOf("/" + folderTypeRoot + "/");
        if (start >= 0) {
          let packPrefix = this.getPackFolderName();
          if (packPrefix === void 0) {
            packPrefix = "";
          } else {
            packPrefix += " ";
          }
          folderStoragePath = packPrefix + folderStoragePath.substring(start + 2 + folderTypeRoot.length);
          folderStoragePathLower = folderStoragePath.toLowerCase();
        }
      }
    }
    folderStoragePath = folderStoragePath.replace(/#/gi, " ").trim();
    return folderStoragePath;
  }
  getCommunitySchemaPath() {
    return import_ProjectItemUtilities.default.getCommunitySchemaPathForType(this.itemType);
  }
  getOfficialSchemaPath() {
    return import_ProjectItemUtilities.default.getOfficialSchemaPathForType(this.itemType);
  }
  getFormPath() {
    if (this.itemType === import_IProjectItemData2.ProjectItemType.modelGeometryJson && this.primaryFile) {
      const content = import_StorageUtilities.default.getJsonObject(this.primaryFile);
      if (content) {
        const rootKeys = Object.keys(content);
        const hasLegacyGeometry = rootKeys.some((key) => key.toLowerCase().startsWith("geometry."));
        if (hasLegacyGeometry) {
          return "visual/geometry.v1.8.0";
        }
      }
    }
    return import_ProjectItemUtilities.default.getFormPathForType(this.itemType);
  }
  get errorStatus() {
    return this._data.errorStatus;
  }
  set errorStatus(newErrorStatus) {
    this._data.errorStatus = newErrorStatus;
  }
  get source() {
    return this._data.source;
  }
  set source(newSource) {
    this._data.source = newSource;
  }
  get errorMessage() {
    return this._data.errorMessage;
  }
  set errorMessage(newErrorMessage) {
    this._data.errorMessage = newErrorMessage;
  }
  get projectPath() {
    if (this._data.projectPath === void 0 && this._data.storagePath) {
      this._data.projectPath = this._data.storagePath;
      return this._data.storagePath;
    }
    return this._data.projectPath;
  }
  set projectPath(newBasePath) {
    this._data.projectPath = newBasePath;
  }
  get effectiveEditPreference() {
    const ep = this.editPreference;
    if (ep === import_IProjectItemData.ProjectItemEditPreference.projectDefault) {
      return this._project.effectiveEditPreference;
    } else if (ep === import_IProjectItemData.ProjectItemEditPreference.forceRaw) {
      return import_IProjectData.ProjectEditPreference.raw;
    } else {
      return import_IProjectData.ProjectEditPreference.summarized;
    }
  }
  get editPreference() {
    if (this._data.editPreference === void 0) {
      return import_IProjectItemData.ProjectItemEditPreference.projectDefault;
    }
    return this._data.editPreference;
  }
  set editPreference(newEditPreference) {
    this._data.editPreference = newEditPreference;
  }
  get storageType() {
    if (this._data.storageType === void 0) {
      return import_IProjectItemData.ProjectItemStorageType.singleFile;
    }
    return this._data.storageType;
  }
  set storageType(newStorageType) {
    this._data.storageType = newStorageType;
  }
  get creationType() {
    return this._data.creationType;
  }
  set creationType(newCreationType) {
    this._data.creationType = newCreationType;
  }
  get itemType() {
    return this._data.itemType;
  }
  get defaultFile() {
    return this._defaultFile;
  }
  /**
   * Get the cached thumbnail data URL for this item.
   * Returns undefined if no thumbnail has been generated.
   */
  get cachedThumbnail() {
    return this._data.cachedThumbnail;
  }
  /**
   * Set the cached thumbnail for this item.
   * Also updates the thumbnailGeneratedAt timestamp and clears the imageUrl cache.
   */
  set cachedThumbnail(value) {
    this._data.cachedThumbnail = value;
    this._data.thumbnailGeneratedAt = value ? Date.now() : void 0;
    this._imageUrlBase64Cache = void 0;
  }
  /**
   * Get the timestamp when the thumbnail was last generated.
   */
  get thumbnailGeneratedAt() {
    return this._data.thumbnailGeneratedAt;
  }
  /**
   * Get the thumbnail link - path to another item whose thumbnail should be used.
   */
  get thumbnailLink() {
    return this._data.thumbnailLink;
  }
  /**
   * Set the thumbnail link - path to another item whose thumbnail should be used.
   */
  set thumbnailLink(value) {
    this._data.thumbnailLink = value;
  }
  /**
   * Check if this item type supports thumbnail generation.
   */
  get supportsThumbnail() {
    return this.itemType === import_IProjectItemData2.ProjectItemType.modelGeometryJson;
  }
  /**
   * Clear the cached thumbnail and imageUrl cache.
   * Call this when the file content changes.
   */
  clearThumbnailCache() {
    this._data.cachedThumbnail = void 0;
    this._data.thumbnailGeneratedAt = void 0;
    this._imageUrlBase64Cache = void 0;
  }
  sortVariantsMostImportantLast(a, b) {
    if (!this._variants) {
      this._getVariantList();
    }
    if (!this._variants) {
      return import_Utilities.default.staticCompare(a, b);
    }
    const va = this._variants[a];
    const vb = this._variants[b];
    if (!va || !vb || !va.label || !vb.label) {
      return 0;
    }
    if (va.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSlice && vb.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSlice || va.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAlt && vb.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAlt || va.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAltPacks && vb.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAltPacks) {
      const versionIndexA = import_Database.default.getVersionIndexFromVersionStr(va.label);
      const versionIndexB = import_Database.default.getVersionIndexFromVersionStr(vb.label);
      return versionIndexA - versionIndexB;
    }
    return import_Utilities.default.staticCompare(va.label.toString(), vb.label.toString());
  }
  sortVariantsMostImportantFirst(a, b) {
    if (!this._variants) {
      this._getVariantList();
    }
    if (!this._variants) {
      return import_Utilities.default.staticCompare(a, b);
    }
    const va = this._variants[a];
    const vb = this._variants[b];
    if (!va || !vb || !va.label || !vb.label) {
      return 0;
    }
    if (va.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSlice && vb.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSlice || va.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAlt && vb.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAlt || va.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAltPacks && vb.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAltPacks) {
      const versionIndexA = import_Database.default.getVersionIndexFromVersionStr(va.label);
      const versionIndexB = import_Database.default.getVersionIndexFromVersionStr(vb.label);
      return versionIndexB - versionIndexA;
    }
    return vb.label.localeCompare(va.label);
  }
  get primaryVariantLabel() {
    const variantKeys = Object.keys(this._variants);
    if (this.hasVersionSliceCustomVariants()) {
      variantKeys.sort(this.sortVariantsMostImportantFirst);
      for (const variantName of variantKeys) {
        const variant = this._variants[variantName];
        if (variant.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSlice || variant.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAlt || variant.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAltPacks) {
          if (variant.file) {
            return variant.label;
          }
        }
      }
    }
    if (this._defaultFile) {
      return void 0;
    }
    for (const variantName of variantKeys) {
      if (variantName) {
        const variant = this._variants[variantName];
        if (variant.file) {
          return variant.label;
        }
      }
    }
    return void 0;
  }
  get primaryFile() {
    if (this._primaryFile) {
      return this._primaryFile;
    }
    if (this.hasVersionSliceCustomVariants()) {
      const variantKeys2 = Object.keys(this._variants);
      variantKeys2.sort(this.sortVariantsMostImportantFirst);
      for (const variantName of variantKeys2) {
        const variant = this._variants[variantName];
        if (variant.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSlice || variant.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAlt || variant.variantType === import_IProjectItemVariant.ProjectItemVariantType.versionSliceAltPacks) {
          if (variant.file) {
            this._primaryFile = variant.file;
            return this._primaryFile;
          }
        }
      }
    }
    if (this._defaultFile && (!this._defaultFile.isContentLoaded || this._defaultFile.content)) {
      return this._defaultFile;
    }
    const variantKeys = Object.keys(this._variants);
    variantKeys.sort(this.sortVariantsMostImportantFirst);
    for (const variantName of variantKeys) {
      if (variantName) {
        const variant = this._variants[variantName];
        if (variant.file) {
          this._primaryFile = variant.file;
          return this._primaryFile;
        }
      }
    }
    if (this._defaultFile) {
      this._primaryFile = this._defaultFile;
      return this._primaryFile;
    }
    return null;
  }
  get defaultFolder() {
    return this._defaultFolder;
  }
  set itemType(newItemType) {
    this._data.itemType = newItemType;
    this._onPropertyChanged.dispatch(this, "itemType");
    this._project.notifyProjectItemChanged(this);
  }
  get isContentLoaded() {
    if (this.storageType === import_IProjectItemData.ProjectItemStorageType.folder) {
      if (!this.defaultFolder) {
        return false;
      }
      if (!this.defaultFolder.isLoaded) {
        return false;
      }
      if (this.itemType === import_IProjectItemData2.ProjectItemType.worldFolder) {
        if (!this.defaultFolder.manager || !(this.defaultFolder.manager instanceof import_MCWorld.default)) {
          return false;
        }
        const world = this.defaultFolder.manager;
        return world.isLoaded;
      }
      return true;
    } else if (this.storageType === import_IProjectItemData.ProjectItemStorageType.singleFile) {
      const itemFile = this.primaryFile;
      if (!itemFile) {
        return false;
      }
      if (!itemFile.isContentLoaded) {
        return false;
      }
      if (this.isWorld) {
        if (!itemFile.manager || !(itemFile.manager instanceof import_MCWorld.default)) {
          return false;
        }
        const world = itemFile.manager;
        return world.isLoaded;
      }
      return this._isFileContentProcessed;
    }
    import_Log.default.unexpectedContentState();
    return false;
  }
  get tags() {
    return this._data.tags;
  }
  async setFile(file) {
    if (file !== this._defaultFile) {
      this._defaultFile = file;
      this._isFileContentProcessed = false;
    }
  }
  get needsSave() {
    if (this._defaultFile === null && this.creationType === import_IProjectItemData.ProjectItemCreationType.generated && this.storageType === import_IProjectItemData.ProjectItemStorageType.singleFile) {
      return true;
    }
    if (this._defaultFile === null) {
      return false;
    }
    let val = this._defaultFile.needsSave;
    if (!val) {
      if (this.isFileContainerStorageItem && this._defaultFile.fileContainerStorage && this._defaultFile.fileContainerStorage instanceof import_ZipStorage.default) {
        val = this._defaultFile.fileContainerStorage.isContentUpdated;
      }
      if (!val) {
        const jsFile = this.getJavaScriptLibTwin();
        if (jsFile !== void 0) {
          val = jsFile.needsSave;
        }
      }
    }
    return val;
  }
  updateProjectPath() {
    if (this._project && this._project.projectFolder) {
      if (this._defaultFile) {
        this.projectPath = this._defaultFile.getFolderRelativePath(this._project.projectFolder);
      } else if (this._defaultFolder) {
        this.projectPath = this._defaultFolder.getFolderRelativePath(this._project.projectFolder);
      }
    }
  }
  async rename(newFileBaseName) {
    if (!this.isContentLoaded) {
      await this.loadContent();
    }
    await this._project.ensureProjectFolder();
    if (this._project.projectFolder === void 0 || this._project.projectFolder === null) {
      return;
    }
    if (this._defaultFile !== null) {
      await this._defaultFile.moveTo(
        this._defaultFile.parentFolder.storageRelativePath + newFileBaseName + "." + this._defaultFile.type
      );
      this._data.name = newFileBaseName + "." + this._defaultFile.type;
      this.projectPath = this._defaultFile.getFolderRelativePath(this._project.projectFolder);
      this.storageType = import_IProjectItemData.ProjectItemStorageType.singleFile;
    } else {
      this._data.name = newFileBaseName;
    }
    this._onPropertyChanged.dispatch(this, "name");
    this._project.notifyProjectItemChanged(this);
  }
  async deleteItem() {
    if (!this.isContentLoaded) {
      await this.loadContent();
    }
    await import_ProjectItemRelations.default.deleteLinksFromParents(this);
    if (this._defaultFile !== null) {
      await this._defaultFile.deleteThisFile();
    }
    this._project.removeItem(this);
  }
  get imageUrl() {
    if (this._imageUrlBase64Cache === null) {
      return void 0;
    }
    if (this._imageUrlBase64Cache) {
      return this._imageUrlBase64Cache;
    }
    if (this._data.cachedThumbnail) {
      this._imageUrlBase64Cache = this._data.cachedThumbnail;
      return this._imageUrlBase64Cache;
    }
    if (this.itemType === import_IProjectItemData2.ProjectItemType.worldFolder) {
      if (this.defaultFolder) {
        if (this.defaultFolder.manager instanceof import_MCWorld.default) {
          const world = this.defaultFolder.manager;
          if (world.isLoaded) {
            this._imageUrlBase64Cache = "data:image/jpg;base64, " + world.imageBase64;
            return this._imageUrlBase64Cache;
          }
        }
      }
    } else if (this.isWorld) {
      const itemFile = this.primaryFile;
      if (itemFile) {
        if (itemFile.manager instanceof import_MCWorld.default) {
          const world = itemFile.manager;
          if (world.isLoaded) {
            return "data:image/jpg;base64, " + world.imageBase64;
          }
        }
      }
    } else if (this.projectPath && import_ProjectItemUtilities.default.isImageType(this.itemType)) {
      if (this.projectPath && this.projectPath.endsWith(".tga")) {
        this._imageUrlBase64Cache = null;
      } else if (this._defaultFile && this._defaultFile.content && this._defaultFile.content instanceof Uint8Array) {
        if (this._defaultFile.content.length > 1e4) {
          this._imageUrlBase64Cache = null;
          return void 0;
        }
        const ext = import_StorageUtilities.default.getTypeFromName(this.projectPath || "");
        let mimeType = "image/png";
        if (ext === "jpg" || ext === "jpeg") {
          mimeType = "image/jpeg";
        } else if (ext === "gif") {
          mimeType = "image/gif";
        } else if (ext === "webp") {
          mimeType = "image/webp";
        }
        this._imageUrlBase64Cache = "data:" + mimeType + ";base64," + import_Utilities.default.uint8ArrayToBase64(this._defaultFile.content);
        return this._imageUrlBase64Cache;
      }
    }
    return void 0;
  }
  get name() {
    if (this.itemType === import_IProjectItemData2.ProjectItemType.worldFolder) {
      if (this.defaultFolder) {
        if (this.defaultFolder.manager instanceof import_MCWorld.default) {
          const world = this.defaultFolder.manager;
          if (world.isLoaded) {
            return world.name;
          }
        }
      }
    } else if (this.itemType === import_IProjectItemData2.ProjectItemType.MCWorld || this.itemType === import_IProjectItemData2.ProjectItemType.MCTemplate) {
      const itemFile = this.primaryFile;
      if (itemFile) {
        if (itemFile.manager instanceof import_MCWorld.default) {
          const world = itemFile.manager;
          if (world.isLoaded) {
            return world.name;
          }
        }
      }
    }
    if (this.projectPath && (this.itemType === import_IProjectItemData2.ProjectItemType.js || this.itemType === import_IProjectItemData2.ProjectItemType.ts || this.itemType === import_IProjectItemData2.ProjectItemType.testJs || this.itemType === import_IProjectItemData2.ProjectItemType.structure)) {
      return import_StorageUtilities.default.getLeafName(this.projectPath);
    }
    if (this._data.name !== void 0) {
      return this._data.name;
    }
    if (this.projectPath) {
      return import_StorageUtilities.default.getLeafName(this.projectPath);
    }
    return "untitled";
  }
  async loadFolder() {
    if (this.storageType === import_IProjectItemData.ProjectItemStorageType.folder) {
      if (this._defaultFolder === null && this.projectPath !== null && this.projectPath !== void 0 && this.projectPath.startsWith("/") && this._project.projectFolder !== null && this._project.projectFolder !== void 0) {
        const prefixPaths = this.projectPath.split("#");
        if (prefixPaths.length > 1) {
          let folderToLoadFrom = this._project.projectFolder;
          for (let i = 0; i < prefixPaths.length - 1; i++) {
            if (folderToLoadFrom) {
              const zipFile = await folderToLoadFrom.ensureFileFromRelativePath(prefixPaths[i]);
              if (!zipFile.isContentLoaded) {
                await zipFile.loadContent();
              }
              if (zipFile.content && zipFile.content instanceof Uint8Array) {
                if (!zipFile.fileContainerStorage) {
                  const zipStorage = new import_ZipStorage.default();
                  zipStorage.storagePath = zipFile.storageRelativePath + "#";
                  await zipStorage.loadFromUint8Array(zipFile.content, zipFile.name);
                  zipStorage.containerFile = zipFile;
                  zipFile.fileContainerStorage = zipStorage;
                }
                folderToLoadFrom = zipFile.fileContainerStorage.rootFolder;
              } else {
                folderToLoadFrom = void 0;
              }
            }
          }
          if (folderToLoadFrom) {
            this._defaultFolder = await folderToLoadFrom.ensureFolderFromRelativePath(
              prefixPaths[prefixPaths.length - 1]
            );
          } else {
            return null;
          }
        } else {
          this._defaultFolder = await this._project.projectFolder.ensureFolderFromRelativePath(this.projectPath);
        }
        await this._defaultFolder.load();
        this._onFolderRetrieved.dispatch(this, this._defaultFolder);
        if (this.itemType === import_IProjectItemData2.ProjectItemType.worldFolder) {
          const mcworld = await import_MCWorld.default.ensureMCWorldOnFolder(
            this._defaultFolder,
            this._project,
            this._handleMCWorldLoaded
          );
          if (mcworld) {
            this.errorMessage = mcworld.storageErrorMessage;
            if (mcworld.storageErrorStatus === import_IStorage.StorageErrorStatus.unprocessable) {
              this.errorStatus = import_IProjectItemData.ProjectItemErrorStatus.unprocessable;
            } else {
              this.errorStatus = import_IProjectItemData.ProjectItemErrorStatus.none;
            }
          }
        } else {
          this._fireLoadedEvent();
        }
      }
      return this._defaultFolder;
    }
    return void 0;
  }
  _handleMCWorldLoaded(world, worldA) {
    this._fireLoadedEvent();
  }
  _fireLoadedEvent() {
    if (this._onLoaded && this.isContentLoaded) {
      this._onLoaded.dispatch(this, this);
    }
  }
  invalidateContentProcessedState() {
    if (this._defaultFile) {
      this._defaultFile.manager = void 0;
    }
    for (const varLabel in this._variants) {
      const variant = this._variants[varLabel];
      if (variant.file) {
        variant.file.manager = void 0;
      }
    }
    this._isFileContentProcessed = false;
  }
  async getManager() {
    if (!this.isContentLoaded) {
      await this.loadContent();
    }
    if (this.storageType === import_IProjectItemData.ProjectItemStorageType.singleFile && this._defaultFile) {
      return this._defaultFile.manager;
    }
    if (this.storageType === import_IProjectItemData.ProjectItemStorageType.folder && this._defaultFolder) {
      return this._defaultFolder.manager;
    }
    return void 0;
  }
  async ensureStorage() {
    if (this.storageType === import_IProjectItemData.ProjectItemStorageType.folder) {
      await this.loadFolder();
    } else if (this.storageType === import_IProjectItemData.ProjectItemStorageType.singleFile) {
      await this.ensureFileStorage();
    }
  }
  get isStorageEnsured() {
    if (this.storageType === import_IProjectItemData.ProjectItemStorageType.folder) {
      return !!this._defaultFolder;
    }
    return !!this.primaryFile;
  }
  async loadContentDirect() {
    if (this.storageType === import_IProjectItemData.ProjectItemStorageType.folder) {
      await this.loadFolder();
    } else if (this.storageType === import_IProjectItemData.ProjectItemStorageType.singleFile) {
      await this.loadFileContent();
    }
  }
  async ensureFileStorage() {
    if (this.storageType === import_IProjectItemData.ProjectItemStorageType.singleFile && this._defaultFile === null && this.projectPath !== null && this.projectPath !== void 0 && this.projectPath.startsWith("/") && this._project.projectFolder !== null && this._project.projectFolder !== void 0) {
      const prefixPaths = this.projectPath.split("#");
      if (prefixPaths.length > 1) {
        let folderToLoadFrom = this._project.projectFolder;
        for (let i = 0; i < prefixPaths.length - 1; i++) {
          if (folderToLoadFrom) {
            const zipFile = await folderToLoadFrom.ensureFileFromRelativePath(prefixPaths[i]);
            if (!zipFile.isContentLoaded) {
              await zipFile.loadContent();
            }
            if (zipFile.content && zipFile.content instanceof Uint8Array) {
              if (!zipFile.fileContainerStorage) {
                const zipStorage = new import_ZipStorage.default();
                zipStorage.storagePath = zipFile.storageRelativePath + "#";
                await zipStorage.loadFromUint8Array(zipFile.content, zipFile.name);
                zipStorage.containerFile = zipFile;
                zipFile.fileContainerStorage = zipStorage;
              }
              folderToLoadFrom = zipFile.fileContainerStorage.rootFolder;
            } else {
              folderToLoadFrom = void 0;
            }
          }
        }
        if (folderToLoadFrom) {
          this._defaultFile = await folderToLoadFrom.ensureFileFromRelativePath(prefixPaths[prefixPaths.length - 1]);
          this._isFileContentProcessed = false;
        } else {
          import_Log.default.debugAlert("Unable to parse a containerized file path of '" + this.projectPath + "'");
          return null;
        }
      } else {
        this._defaultFile = await this._project.projectFolder.ensureFileFromRelativePath(this.projectPath);
        this._isFileContentProcessed = false;
      }
    }
    return this._defaultFile;
  }
  async loadFileContent() {
    if (this._isFileContentProcessed && this._defaultFile) {
      return this._defaultFile;
    }
    if (!this._defaultFile) {
      await this.ensureFileStorage();
    }
    const variants = this._getVariantList();
    for (const variant of variants) {
      await variant.ensureAndLoadFileStorage();
    }
    if (this._defaultFile) {
      await this.loadFileStorage();
    }
    return this._defaultFile;
  }
  async loadFileStorage() {
    if (!this._isFileContentProcessed && this._defaultFile) {
      if (this._data.creationType === import_IProjectItemData.ProjectItemCreationType.generated) {
        await import_ProjectAutogeneration.default.updateItemAutogeneration(this, true);
      } else {
        if (!this._defaultFile.isContentLoaded) {
          await this._defaultFile.loadContent();
        }
      }
      await import_ProjectAutogeneration.default.updateItemAutogeneratedSideFiles(this);
      this._isFileContentProcessed = true;
      this._onFileRetrieved.dispatch(this, this._defaultFile);
      if (this.itemType === import_IProjectItemData2.ProjectItemType.MCWorld || this.itemType === import_IProjectItemData2.ProjectItemType.MCTemplate) {
        const mcworld = await import_MCWorld.default.ensureOnFile(this._defaultFile, this._project, this._handleMCWorldLoaded);
        if (mcworld) {
          this.errorMessage = mcworld.storageErrorMessage;
          if (mcworld.storageErrorStatus === import_IStorage.StorageErrorStatus.unprocessable) {
            this.errorStatus = import_IProjectItemData.ProjectItemErrorStatus.unprocessable;
          } else {
            this.errorStatus = import_IProjectItemData.ProjectItemErrorStatus.none;
          }
        }
      } else if (this.itemType === import_IProjectItemData2.ProjectItemType.entityTypeBehavior) {
        await import_EntityTypeDefinition.default.ensureOnFile(this._defaultFile);
        this._fireLoadedEvent();
      } else {
        this._fireLoadedEvent();
      }
    }
  }
  async getJsonObject() {
    let strContent = await this.getStringContent();
    if (!strContent) {
      return void 0;
    }
    let obj = void 0;
    try {
      strContent = import_Utilities.default.fixJsonContent(strContent);
      obj = JSON.parse(strContent);
    } catch (e) {
      import_Log.default.debug("Could not parse content '" + strContent + "': " + e, this.project.name);
    }
    return obj;
  }
  async getStringContent() {
    if (!this.isContentLoaded) {
      await this.loadContent();
    }
    const pf = this.primaryFile;
    if (!pf) {
      return void 0;
    }
    if (!pf.isContentLoaded) {
      await pf.loadContent();
    }
    if (pf.content instanceof Uint8Array || pf.content === null) {
      return void 0;
    }
    return pf.content;
  }
  async getContentAsJson() {
    const file = await this.loadFileContent();
    return file && await import_StorageUtilities.default.getJsonObject(file);
  }
  async loadContent() {
    if (this.isContentLoaded) {
      return true;
    }
    if (this._isLoading) {
      const pendingLoad = this._pendingLoadRequests;
      const prom = (resolve, reject) => {
        pendingLoad.push(resolve);
      };
      await new Promise(prom);
      return true;
    } else {
      this._isLoading = true;
      await this.loadContentDirect();
      this._isLoading = false;
      const pendingLoad = this._pendingLoadRequests;
      this._pendingLoadRequests = [];
      for (const prom of pendingLoad) {
        prom(void 0);
      }
      return true;
    }
  }
  async prepareToSave() {
    if (!this.isContentLoaded) {
      await this.loadContent();
    }
    await import_ProjectAutogeneration.default.updateItemAutogeneration(this);
    await import_ProjectAutogeneration.default.updateItemAutogeneratedSideFiles(this);
    if (!this.isInFileContainer && this.isFileContainerStorageItem && this.primaryFile && this.primaryFile.fileContainerStorage && this.primaryFile.fileContainerStorage instanceof import_ZipStorage.default) {
      const zs = this.primaryFile.fileContainerStorage;
      if (zs.isContentUpdated && (zs.errorStatus === void 0 || zs.errorStatus === import_IStorage.StorageErrorStatus.none)) {
        const op = await this._project.creatorTools.notifyOperationStarted(
          "Zipping file '" + this.primaryFile.storageRelativePath + "'..."
        );
        const bytes = await zs.generateUint8ArrayAsync();
        await this._project.creatorTools.notifyOperationEnded(
          op,
          "Done zipping file '" + this.primaryFile.storageRelativePath + "'."
        );
        this.primaryFile.setContent(bytes, import_IFile.FileUpdateType.versionlessEdit);
      }
    }
  }
  getJavaScriptLibTwin() {
    if (!this._defaultFile) {
      return void 0;
    }
    if (this.itemType === import_IProjectItemData2.ProjectItemType.ts) {
      const libScriptsFolder = this.project.getLibScriptsFolder();
      if (!libScriptsFolder) {
        return void 0;
      }
      const jsTwinName = import_StorageUtilities.default.canonicalizeName(
        import_StorageUtilities.default.getBaseFromName(this._defaultFile.name) + ".js"
      );
      return libScriptsFolder.ensureFile(jsTwinName);
    }
    return void 0;
  }
  getFunctionTwin() {
    if (!this._defaultFile) {
      return void 0;
    }
    if (this.itemType === import_IProjectItemData2.ProjectItemType.actionSet) {
      const functionTwinName = import_StorageUtilities.default.canonicalizeName(
        import_StorageUtilities.default.getBaseFromName(this._defaultFile.name) + ".mcfunction"
      );
      let functionFolder = this._defaultFile.parentFolder;
      if (functionFolder.name === "scripts" && functionFolder.parentFolder) {
        functionFolder = functionFolder.parentFolder.ensureFolder("functions");
      }
      return functionFolder.files[functionTwinName];
    }
    return void 0;
  }
  async saveContent() {
    if (this._defaultFile) {
      await this._defaultFile.saveContent();
      const jsFile = this.getJavaScriptLibTwin();
      if (jsFile !== void 0 && jsFile.needsSave) {
        await jsFile.saveContent();
      }
      const functionFile = this.getFunctionTwin();
      if (functionFile !== void 0 && functionFile.needsSave) {
        await functionFile.saveContent();
      }
    }
  }
  hasTag(name) {
    return this.tags.includes(name);
  }
  ensureTag(name) {
    if (this.hasTag(name)) {
      return;
    }
    this.tags.push(name);
  }
}
//# sourceMappingURL=ProjectItem.js.map
