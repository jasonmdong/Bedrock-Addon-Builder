"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CreatorTools_exports = {};
__export(CreatorTools_exports, {
  CreatorToolsMinecraftErrorStatus: () => CreatorToolsMinecraftErrorStatus,
  CreatorToolsMinecraftState: () => CreatorToolsMinecraftState,
  CreatorToolsTargetSettings: () => CreatorToolsTargetSettings,
  DefaultCreatorName: () => DefaultCreatorName,
  SidePaneMaxWidth: () => SidePaneMaxWidth,
  SidePaneMinWidth: () => SidePaneMinWidth,
  default: () => CreatorTools
});
module.exports = __toCommonJS(CreatorTools_exports);
var import_ICreatorToolsData = require("./ICreatorToolsData");
var import_Project = __toESM(require("./Project"));
var import_ste_events = require("ste-events");
var import_Status = require("./Status");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_Utilities = __toESM(require("../core/Utilities"));
var import_GitHubManager = __toESM(require("../github/GitHubManager"));
var import_axios = __toESM(require("axios"));
var import_Log = __toESM(require("../core/Log"));
var import_AppServiceProxy = __toESM(require("../core/AppServiceProxy"));
var import_CommandRunner = __toESM(require("./CommandRunner"));
var import_RemoteMinecraft = __toESM(require("./RemoteMinecraft"));
var import_CreatorToolsHost = __toESM(require("./CreatorToolsHost"));
var import_ProcessHostedProxyMinecraft = __toESM(require("../clientapp/ProcessHostedProxyMinecraft"));
var import_MinecraftGameProxyMinecraft = __toESM(require("../clientapp/MinecraftGameProxyMinecraft"));
var import_WorldLevelDat = require("../minecraft/WorldLevelDat");
var import_IWorldSettings = require("../minecraft/IWorldSettings");
var import_Package = __toESM(require("./Package"));
var import_CommandRegistry = __toESM(require("./CommandRegistry"));
var import_ZipStorage = __toESM(require("../storage/ZipStorage"));
var import_IProjectItemData = require("./IProjectItemData");
var import_DeploymentStorageMinecraft = __toESM(require("./DeploymentStorageMinecraft"));
var import_MinecraftUtilities = __toESM(require("../minecraft/MinecraftUtilities"));
var import_IGalleryItem = require("./IGalleryItem");
var import_ProjectUtilities = __toESM(require("./ProjectUtilities"));
var import_ElectronStorage = __toESM(require("../electronclient/ElectronStorage"));
var import_Storage = __toESM(require("../storage/Storage"));
var import_Database = __toESM(require("../minecraft/Database"));
var import_DeploymentTarget = __toESM(require("./DeploymentTarget"));
var CreatorToolsMinecraftState = /* @__PURE__ */ ((CreatorToolsMinecraftState2) => {
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["none"] = 0] = "none";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["initializing"] = 1] = "initializing";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["initialized"] = 2] = "initialized";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["preparing"] = 3] = "preparing";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["prepared"] = 4] = "prepared";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["starting"] = 5] = "starting";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["started"] = 6] = "started";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["stopping"] = 7] = "stopping";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["stopped"] = 8] = "stopped";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["error"] = 9] = "error";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["newMinecraft"] = 10] = "newMinecraft";
  CreatorToolsMinecraftState2[CreatorToolsMinecraftState2["disconnected"] = 11] = "disconnected";
  return CreatorToolsMinecraftState2;
})(CreatorToolsMinecraftState || {});
var CreatorToolsMinecraftErrorStatus = /* @__PURE__ */ ((CreatorToolsMinecraftErrorStatus2) => {
  CreatorToolsMinecraftErrorStatus2[CreatorToolsMinecraftErrorStatus2["none"] = 0] = "none";
  CreatorToolsMinecraftErrorStatus2[CreatorToolsMinecraftErrorStatus2["actionInProgress"] = 1] = "actionInProgress";
  CreatorToolsMinecraftErrorStatus2[CreatorToolsMinecraftErrorStatus2["serverUnavailable"] = 2] = "serverUnavailable";
  CreatorToolsMinecraftErrorStatus2[CreatorToolsMinecraftErrorStatus2["serverError"] = 3] = "serverError";
  CreatorToolsMinecraftErrorStatus2[CreatorToolsMinecraftErrorStatus2["loginFailed"] = 4] = "loginFailed";
  CreatorToolsMinecraftErrorStatus2[CreatorToolsMinecraftErrorStatus2["generalError"] = 5] = "generalError";
  CreatorToolsMinecraftErrorStatus2[CreatorToolsMinecraftErrorStatus2["configuration"] = 6] = "configuration";
  return CreatorToolsMinecraftErrorStatus2;
})(CreatorToolsMinecraftErrorStatus || {});
const CreatorToolsTargetSettings = [
  "Latest Minecraft Bedrock",
  "Latest Minecraft Bedrock preview",
  "Latest Minecraft Education",
  "Latest Minecraft Education preview"
];
const SidePaneMaxWidth = 880;
const SidePaneMinWidth = 280;
const DefaultCreatorName = "Minecraft Creator";
class CreatorTools {
  _isLoaded;
  _userGitHub;
  _anonGitHub;
  contentRoot = "";
  processHostedMinecraft;
  deploymentStorageMinecraft;
  gameMinecraft;
  remoteMinecraft;
  activeMinecraft;
  prefsStorage;
  projectsStorage;
  deploymentStorage;
  worldStorage;
  packStorage;
  workingStorage;
  deploymentTargets = [];
  localFolderExists;
  localFileExists;
  ensureLocalFolder;
  createMinecraft;
  canCreateMinecraft;
  _pendingPackLoadRequests = [];
  _arePacksLoading = false;
  // Private instance variable for auth token - not persisted in #data
  // This is used for in-memory session tokens (e.g., from mct view command)
  _remoteServerAuthToken;
  // Runtime property for remote server EULA acceptance status
  // This is set from the server's auth response and indicates whether the
  // server has accepted the Minecraft EULA. Not persisted locally.
  _remoteServerEulaAccepted;
  local;
  #data;
  projects;
  status;
  activeOperations;
  packs;
  mcLogs = {};
  _gallery;
  _galleryLoaded = false;
  isDeployingToMinecraft = false;
  hasAttemptedPersistentBrowserStorageSwitch = false;
  _onDeploymentStorageChanged = new import_ste_events.EventDispatcher();
  _onMinecraftStateChanged = new import_ste_events.EventDispatcher();
  _onMinecraftRefreshed = new import_ste_events.EventDispatcher();
  _onPropertyChanged = new import_ste_events.EventDispatcher();
  _onLoaded = new import_ste_events.EventDispatcher();
  _onStatusAdded = new import_ste_events.EventDispatcher();
  _onOperationCompleted = new import_ste_events.EventDispatcher();
  _onStatusAddedAsync = [];
  _onGalleryLoaded = new import_ste_events.EventDispatcher();
  get defaultDeploymentTargetType() {
    if (this.#data.defaultDeploymentTarget !== void 0) {
      return this.#data.defaultDeploymentTarget;
    }
    for (let i = 0; i < import_DeploymentTarget.MaxDeploymentTargets; i++) {
      if (this.deploymentStorage[i]) {
        return i;
      }
    }
    return import_DeploymentTarget.DeploymentTargetType.bedrock;
  }
  set defaultDeploymentTargetType(newType) {
    this.#data.defaultDeploymentTarget = newType;
  }
  get isLoaded() {
    return this._isLoaded;
  }
  get collapsedTypes() {
    if (!this.#data.collapsedTypes) {
      this.#data.collapsedTypes = [];
    }
    return this.#data.collapsedTypes;
  }
  get data() {
    return this.#data;
  }
  get defaultDeploymentStorage() {
    return this.deploymentStorage[this.defaultDeploymentTargetType];
  }
  get defaultDeploymentTarget() {
    return this.getDeploymentTarget(this.defaultDeploymentTargetType);
  }
  set collapsedTypes(newCollapsedTypes) {
    this.#data.collapsedTypes = newCollapsedTypes;
  }
  get showMruPane() {
    if (this.#data.showMruPane === void 0) {
      return true;
    }
    return this.#data.showMruPane;
  }
  set showMruPane(newValue) {
    this.#data.showMruPane = newValue;
  }
  get mruItemPaths() {
    if (!this.#data.mruItemPaths) {
      this.#data.mruItemPaths = [];
    }
    return this.#data.mruItemPaths;
  }
  set mruItemPaths(newPaths) {
    this.#data.mruItemPaths = newPaths;
  }
  get viewAsFiles() {
    if (this.#data.viewAsFiles === void 0) {
      return false;
    }
    return this.#data.viewAsFiles;
  }
  set viewAsFiles(newValue) {
    this.#data.viewAsFiles = newValue;
  }
  /**
   * Adds a project item path to the MRU list.
   * Moves existing entries to front and maintains max 10 items.
   */
  addToMru(projectPath) {
    if (!projectPath) return;
    const paths = this.mruItemPaths;
    const existingIndex = paths.indexOf(projectPath);
    if (existingIndex !== -1) {
      paths.splice(existingIndex, 1);
    }
    paths.unshift(projectPath);
    if (paths.length > 10) {
      paths.pop();
    }
    this.#data.mruItemPaths = paths;
  }
  get editPreference() {
    return this.#data.editPreference;
  }
  set editPreference(newValue) {
    this.#data.editPreference = newValue;
  }
  get disableFirstRun() {
    return this.#data.disableFirstRun === true;
  }
  set disableFirstRun(newValue) {
    this.#data.disableFirstRun = newValue;
  }
  get worldSettings() {
    return this.#data.worldSettings;
  }
  get editorWorldSettings() {
    return this.#data.editorWorldSettings;
  }
  get activeMinecraftState() {
    if (this.activeMinecraft === void 0) {
      return 0 /* none */;
    }
    return this.activeMinecraft.state;
  }
  get onMinecraftStateChanged() {
    return this._onMinecraftStateChanged.asEvent();
  }
  get onDeploymentStorageChanged() {
    return this._onDeploymentStorageChanged.asEvent();
  }
  get formatBeforeSave() {
    if (this.#data.formatBeforeSave === void 0) {
      return true;
    }
    return this.#data.formatBeforeSave;
  }
  set formatBeforeSave(newValue) {
    this.#data.formatBeforeSave = newValue;
  }
  get showLivePreview() {
    if (this.#data.showLivePreview === void 0) {
      return true;
    }
    return this.#data.showLivePreview;
  }
  set showLivePreview(newValue) {
    this.#data.showLivePreview = newValue;
  }
  get conversionJarPath() {
    return this.#data.conversionJarPath;
  }
  set conversionJarPath(newValue) {
    this.#data.conversionJarPath = newValue;
  }
  get preferredTextSize() {
    if (this.#data.preferredTextSize === void 0) {
      return 16;
    }
    return this.#data.preferredTextSize;
  }
  set preferredTextSize(newValue) {
    this.#data.preferredTextSize = newValue;
  }
  get livePreviewWidth() {
    if (this.#data.livePreviewWidth === void 0) {
      return 260;
    }
    if (this.#data.livePreviewWidth < 200) {
      return 200;
    }
    if (this.#data.livePreviewWidth > 600) {
      return 600;
    }
    return this.#data.livePreviewWidth;
  }
  set livePreviewWidth(newValue) {
    if (newValue < 200) {
      newValue = 200;
    }
    if (newValue > 600) {
      newValue = 600;
    }
    this.#data.livePreviewWidth = newValue;
  }
  get itemSidePaneWidth() {
    if (this.#data.itemSidePaneWidth === void 0) {
      return 300;
    }
    if (this.#data.itemSidePaneWidth < SidePaneMinWidth) {
      return SidePaneMinWidth;
    }
    if (this.#data.itemSidePaneWidth > SidePaneMaxWidth) {
      return SidePaneMaxWidth;
    }
    return this.#data.itemSidePaneWidth;
  }
  set itemSidePaneWidth(newValue) {
    this.#data.itemSidePaneWidth = newValue;
  }
  get toolPaneWidth() {
    if (this.#data.toolPaneWidth === void 0) {
      return 380;
    }
    if (this.#data.toolPaneWidth < SidePaneMinWidth) {
      return SidePaneMinWidth;
    }
    if (this.#data.toolPaneWidth > SidePaneMaxWidth) {
      return SidePaneMaxWidth;
    }
    return this.#data.toolPaneWidth;
  }
  set toolPaneWidth(newValue) {
    this.#data.toolPaneWidth = newValue;
  }
  get preferredSuite() {
    return this.#data.preferredSuite;
  }
  set preferredSuite(newValue) {
    this.#data.preferredSuite = newValue;
  }
  get track() {
    return this.#data.track;
  }
  set track(newTrack) {
    if (newTrack !== this.#data.track) {
      this.#data.track = newTrack;
      this._onPropertyChanged.dispatch(this, "track");
    }
  }
  get effectiveTrack() {
    if (this.#data.track !== void 0) {
      return this.#data.track;
    }
    return import_ICreatorToolsData.MinecraftTrack.main;
  }
  get useEditor() {
    return this.#data.useEditor;
  }
  set useEditor(newUseEditor) {
    if (newUseEditor !== this.#data.useEditor) {
      this.#data.useEditor = newUseEditor;
    }
  }
  get windowX() {
    if (this.#data.windowX === void 0) {
      return 0;
    }
    return this.#data.windowX;
  }
  set windowX(newVal) {
    this.#data.windowX = newVal;
  }
  get windowY() {
    if (this.#data.windowY === void 0) {
      return 0;
    }
    return this.#data.windowY;
  }
  set windowY(newVal) {
    this.#data.windowY = newVal;
  }
  get windowWidth() {
    if (this.#data.windowWidth === void 0) {
      return 1200;
    }
    return this.#data.windowWidth;
  }
  set windowWidth(newVal) {
    this.#data.windowWidth = newVal;
  }
  get windowHeight() {
    if (this.#data.windowHeight === void 0) {
      return 900;
    }
    return this.#data.windowHeight;
  }
  set windowHeight(newVal) {
    this.#data.windowHeight = newVal;
  }
  get windowSlot() {
    if (this.#data.windowSlot === void 0) {
      return 0;
    }
    return this.#data.windowSlot;
  }
  set windowSlot(newVal) {
    this.#data.windowSlot = newVal;
  }
  get creator() {
    return this.#data.creator;
  }
  set creator(newCreator) {
    if (this.#data.creator !== newCreator) {
      this.#data.creator = newCreator;
      this._onPropertyChanged.dispatch(this, "creator");
    }
  }
  get windowState() {
    if (this.#data.windowState === void 0) {
      return import_ICreatorToolsData.WindowState.regular;
    }
    return this.#data.windowState;
  }
  set windowState(newVal) {
    this.#data.windowState = newVal;
  }
  get lastActiveMinecraftFlavor() {
    return this.#data.lastActiveMinecraftFlavor;
  }
  set lastActiveMinecraftFlavor(lastActiveMinecraftFlavor) {
    if (lastActiveMinecraftFlavor !== this.#data.lastActiveMinecraftFlavor) {
      this.#data.lastActiveMinecraftFlavor = lastActiveMinecraftFlavor;
    }
  }
  get remoteServerUrl() {
    if (this.#data.remoteServerUrl === void 0 && import_CreatorToolsHost.default.baseUrl) {
      return import_Utilities.default.getBaseUrl(import_CreatorToolsHost.default.baseUrl);
    }
    return this.#data.remoteServerUrl;
  }
  get fullRemoteServerUrl() {
    if (!this.remoteServerUrl) {
      return void 0;
    }
    let url = this.remoteServerUrl.toLowerCase();
    if (!url.startsWith("http") && url.indexOf("//") < 0) {
      if (url.indexOf("localhost") >= 0) {
        url = "http://" + url;
      } else {
        url = "https://" + url;
      }
    }
    url = import_Utilities.default.ensureEndsWithSlash(url);
    return url;
  }
  set remoteServerUrl(newPath) {
    if (newPath !== this.#data.remoteServerUrl) {
      this.#data.remoteServerUrl = newPath;
    }
  }
  get iAgreeToTheMinecraftEndUserLicenseAgreementAndPrivacyStatementAtMinecraftDotNetSlashEula() {
    return this.#data.iAgreeToTheMinecraftEndUserLicenseAgreementAndPrivacyStatementAtMinecraftDotNetSlashEula;
  }
  set iAgreeToTheMinecraftEndUserLicenseAgreementAndPrivacyStatementAtMinecraftDotNetSlashEula(newPort) {
    this.#data.iAgreeToTheMinecraftEndUserLicenseAgreementAndPrivacyStatementAtMinecraftDotNetSlashEula = newPort;
  }
  get dedicatedServerSlotCount() {
    return this.#data.dedicatedServerSlotCount;
  }
  set dedicatedServerSlotCount(newPort) {
    this.#data.dedicatedServerSlotCount = newPort;
  }
  get dedicatedServerMode() {
    if (this.#data.dedicatedServerMode === void 0) {
      return import_ICreatorToolsData.DedicatedServerMode.auto;
    }
    return this.#data.dedicatedServerMode;
  }
  set dedicatedServerMode(newMode) {
    this.#data.dedicatedServerMode = newMode;
  }
  get dedicatedServerPath() {
    return this.#data.dedicatedServerPath;
  }
  set minecraftGameMode(newMode) {
    this.#data.webSocketMode = newMode;
  }
  get minecraftGameMode() {
    return this.#data.webSocketMode;
  }
  set dedicatedServerPath(newPath) {
    this.#data.dedicatedServerPath = newPath;
  }
  get remoteServerPort() {
    if (this.#data.remoteServerPort === void 0) {
      return 0;
    }
    return this.#data.remoteServerPort;
  }
  set remoteServerPort(newPort) {
    this.#data.remoteServerPort = newPort;
  }
  get remoteServerAccessLevel() {
    return this.#data.remoteServerAccessLevel;
  }
  set remoteServerAccessLevel(newAccessLevel) {
    this.#data.remoteServerAccessLevel = newAccessLevel;
  }
  get remoteServerPasscode() {
    return this.#data.remoteServerPasscode;
  }
  set remoteServerPasscode(newPath) {
    this.#data.remoteServerPasscode = newPath;
  }
  get remoteServerAuthToken() {
    return this._remoteServerAuthToken;
  }
  set remoteServerAuthToken(newToken) {
    this._remoteServerAuthToken = newToken;
  }
  get remoteServerEulaAccepted() {
    return this._remoteServerEulaAccepted;
  }
  set remoteServerEulaAccepted(newValue) {
    this._remoteServerEulaAccepted = newValue;
  }
  get editorViewMode() {
    if (this.#data.editorViewMode === void 0) {
      return import_ICreatorToolsData.CreatorToolsEditorViewMode.itemsOnLeft;
    }
    return this.#data.editorViewMode;
  }
  set editorViewMode(newViewMode) {
    this.#data.editorViewMode = newViewMode;
  }
  get gallery() {
    return this._gallery;
  }
  get galleryLoaded() {
    return this._galleryLoaded;
  }
  get userGitHub() {
    if (this._userGitHub === void 0) {
      this._userGitHub = new import_GitHubManager.default(this.prefsStorage.rootFolder.ensureFile("github.json"));
    }
    return this._userGitHub;
  }
  get anonGitHub() {
    if (this._anonGitHub === void 0) {
      this._anonGitHub = new import_GitHubManager.default();
    }
    return this._anonGitHub;
  }
  get onLoaded() {
    return this._onLoaded.asEvent();
  }
  get onGalleryLoaded() {
    return this._onGalleryLoaded.asEvent();
  }
  get onPropertyChanged() {
    return this._onPropertyChanged.asEvent();
  }
  get onStatusAdded() {
    return this._onStatusAdded.asEvent();
  }
  get onOperationCompleted() {
    return this._onOperationCompleted.asEvent();
  }
  get successfullyConnectedWebSocketToMinecraft() {
    if (this.#data.successfullyConnectedWebSocketToMinecraft === void 0) {
      return false;
    }
    return this.#data.successfullyConnectedWebSocketToMinecraft;
  }
  set successfullyConnectedWebSocketToMinecraft(newValue) {
    this.#data.successfullyConnectedWebSocketToMinecraft = newValue;
  }
  get successfullyStartedMinecraftServer() {
    if (this.#data.successfullyStartedMinecraftServer === void 0) {
      return false;
    }
    return this.#data.successfullyStartedMinecraftServer;
  }
  set successfullyStartedMinecraftServer(newValue) {
    this.#data.successfullyStartedMinecraftServer = newValue;
  }
  get successfullyConnectedToRemoteMinecraft() {
    if (this.#data.successfullyConnectedToRemoteMinecraft === void 0) {
      return false;
    }
    return this.#data.successfullyConnectedToRemoteMinecraft;
  }
  set successfullyConnectedToRemoteMinecraft(newValue) {
    this.#data.successfullyConnectedToRemoteMinecraft = newValue;
  }
  get defaultMinecraftFlavor() {
    if (this.#data.lastActiveMinecraftFlavor === void 0) {
      if (import_CreatorToolsHost.default.isAppServiceWeb) {
        return import_ICreatorToolsData.MinecraftFlavor.processHostedProxy;
      } else {
        return import_ICreatorToolsData.MinecraftFlavor.remote;
      }
    }
    return this.#data.lastActiveMinecraftFlavor;
  }
  subscribeStatusAddedAsync(fn) {
    this._onStatusAddedAsync.push(fn);
  }
  unsubscribeStatusAddedAsync(fn) {
    let newStatusAddedArr = [];
    for (let i = 0; i < this._onStatusAddedAsync.length; i++) {
      if (this._onStatusAddedAsync[i] !== fn) {
        newStatusAddedArr.push(this._onStatusAddedAsync[i]);
      }
    }
    this._onStatusAddedAsync = newStatusAddedArr;
  }
  setMinecraftFlavor(newValue) {
    this.ensureMinecraft(newValue);
  }
  get autoStartMinecraft() {
    if (this.#data.autoStartMinecraft === void 0) {
      return true;
    }
    return this.#data.autoStartMinecraft;
  }
  set autoStartMinecraft(newValue) {
    this.#data.autoStartMinecraft = newValue;
  }
  get file() {
    return this.prefsStorage.rootFolder.ensureFile("mctools.json");
  }
  get prefsProjectsFolder() {
    return this.prefsStorage.rootFolder.ensureFolder("projects");
  }
  constructor(settingsStorage, projectsStorage, deploymentsStorage, worldStorage, packStorage, workingStorage, contentRoot) {
    this.prefsStorage = settingsStorage;
    this.projectsStorage = projectsStorage;
    this.deploymentStorage = deploymentsStorage;
    this.packStorage = packStorage;
    this.worldStorage = worldStorage;
    this.workingStorage = workingStorage;
    if (contentRoot) {
      this.contentRoot = contentRoot;
    }
    this._handleMessageFromAppService = this._handleMessageFromAppService.bind(this);
    this._bubbleMinecraftStateChanged = this._bubbleMinecraftStateChanged.bind(this);
    this._bubbleMinecraftRefreshed = this._bubbleMinecraftRefreshed.bind(this);
    import_AppServiceProxy.default.onMessage.subscribe(this._handleMessageFromAppService);
    this.#data = {
      successfullyConnectedWebSocketToMinecraft: false,
      successfullyStartedMinecraftServer: false,
      successfullyConnectedToRemoteMinecraft: false,
      autoStartMinecraft: true,
      showScreenOnConnect: true,
      customTools: []
    };
    if (import_CreatorToolsHost.default.baseUrl) {
      this.setMinecraftFlavor(import_ICreatorToolsData.MinecraftFlavor.remote);
      this.successfullyConnectedWebSocketToMinecraft = true;
    }
    this._isLoaded = false;
    this.projects = [];
    this.status = [];
    this.activeOperations = [];
  }
  initializeWorldSettings() {
    if (this.#data.worldSettings === void 0) {
      this.#data.worldSettings = {
        gameType: import_WorldLevelDat.GameType.creative,
        generator: import_WorldLevelDat.Generator.infinite,
        randomSeed: "2000",
        backupType: import_IWorldSettings.BackupType.every5Minutes,
        useCustomSettings: false,
        isEditor: false,
        deployCreatorToolsInfrastructure: true,
        packageReferences: []
      };
      this.ensureDefaultWorldName();
    }
  }
  getDeploymentTarget(target) {
    let dt = this.deploymentTargets[target];
    if (!dt && this.deploymentStorage[target]) {
      dt = new import_DeploymentTarget.default(this.deploymentStorage[target], target);
      this.deploymentTargets[target] = dt;
    }
    return dt;
  }
  ensureDefaultWorldName() {
    if (this.worldSettings && this.worldSettings.name === void 0) {
      this.worldSettings.name = "world " + import_Utilities.default.getDateStr(/* @__PURE__ */ new Date());
    }
  }
  initializeEditorWorldSettings() {
    if (this.#data.editorWorldSettings === void 0) {
      this.#data.editorWorldSettings = {
        gameType: import_WorldLevelDat.GameType.creative,
        generator: import_WorldLevelDat.Generator.infinite,
        backupType: import_IWorldSettings.BackupType.every5Minutes,
        useCustomSettings: false,
        isEditor: true,
        packageReferences: []
      };
      this.ensureDefaultEditorWorldName();
    }
  }
  ensureDefaultEditorWorldName() {
    if (this.editorWorldSettings && this.editorWorldSettings.name === void 0) {
      this.editorWorldSettings.name = "editor project " + import_Utilities.default.getDateStr(/* @__PURE__ */ new Date());
    }
  }
  getCustomTool(index) {
    if (this.#data.customTools === void 0) {
      this.#data.customTools = [];
    }
    while (this.#data.customTools.length <= index) {
      this.#data.customTools.push({
        name: "",
        type: 0,
        text: void 0,
        lastRunResult: void 0
      });
    }
    return this.#data.customTools[index];
  }
  get defaultFunction() {
    if (this.#data === void 0 || this.#data.defaultFunction === void 0) {
      return "";
    }
    return this.#data.defaultFunction;
  }
  set defaultFunction(newFunction) {
    if (this.#data === void 0) {
      return;
    }
    this.#data.defaultFunction = import_Utilities.default.makeSafeForJson(newFunction);
  }
  async runCommand(command, project) {
    return await import_CommandRegistry.default.main.runCommand(
      {
        creatorTools: this,
        project,
        minecraft: this.activeMinecraft,
        host: import_CreatorToolsHost.default.hostManager
      },
      command
    );
  }
  async runMinecraftCommand(command) {
    if (this.activeMinecraft === void 0) {
      throw new Error("No minecraft active.");
    }
    const result = await this.activeMinecraft.runCommand(command);
    return result;
  }
  async loadPacks() {
    if (!this.packStorage) {
      throw new Error("Could not find pack storage");
    }
    if (this._arePacksLoading) {
      const pendingLoad = this._pendingPackLoadRequests;
      const prom = (resolve, reject) => {
        pendingLoad.push(resolve);
      };
      await new Promise(prom);
    } else {
      this._arePacksLoading = true;
      this.packs = [];
      const creatorToolsIngame = await import_Database.default.ensureCreatorToolsIngameFile();
      if (creatorToolsIngame) {
        await this.ensurePackForFile(creatorToolsIngame);
      }
      import_Log.default.message("Loading packs from '" + this.packStorage.rootFolder.fullPath + "'");
      await this.loadPacksFromFolder(this.packStorage.rootFolder);
      this._arePacksLoading = false;
      const pendingLoad = this._pendingPackLoadRequests;
      this._pendingPackLoadRequests = [];
      for (const prom of pendingLoad) {
        prom(void 0);
      }
    }
  }
  ensureAllTypesCollapsedExcept(itemType) {
    const newCollapsedTypes = [];
    for (let i = 0; i < import_IProjectItemData.MaxItemTypes; i++) {
      if (i !== itemType) {
        newCollapsedTypes.push(i);
      }
    }
    this.collapsedTypes = newCollapsedTypes;
  }
  ensureTypeIsCollapsed(itemType) {
    for (const itemTypeCollapsed of this.collapsedTypes) {
      if (itemTypeCollapsed === itemType) {
        return;
      }
    }
    this.collapsedTypes.push(itemType);
  }
  ensureTypeIsNotCollapsed(itemType) {
    const newCollapsedTypes = [];
    for (const itemTypeCollapsed of this.collapsedTypes) {
      if (itemTypeCollapsed !== itemType) {
        newCollapsedTypes.push(itemTypeCollapsed);
      }
    }
    this.#data.collapsedTypes = newCollapsedTypes;
  }
  async loadPacksFromFolder(folder) {
    await folder.load();
    for (let fileName in folder.files) {
      const file = folder.files[fileName];
      if (file && import_StorageUtilities.default.isContainerFile(file.storageRelativePath)) {
        await this.ensurePackForFile(file);
      }
    }
  }
  getPackByName(packName, isWorldFocused) {
    if (!this.packs) {
      import_Log.default.unexpectedUndefined("GPN");
      return;
    }
    for (let i = 0; i < this.packs.length; i++) {
      if (this.packs[i].matches(packName, isWorldFocused)) {
        return this.packs[i];
      }
    }
    return void 0;
  }
  getPackageByNameAndHash(packName, hash) {
    if (!this.packs) {
      import_Log.default.unexpectedUndefined("GPNH");
      return;
    }
    for (let i = 0; i < this.packs.length; i++) {
      if (this.packs[i].matches(packName, false) && (hash === void 0 || hash === this.packs[i].data?.sourceHash)) {
        return this.packs[i];
      }
    }
    return this.getPackByName(packName);
  }
  async ensurePackForFile(file) {
    const pack = this._ensurePack(file.storageRelativePath);
    await pack.ensureData(this, file);
    return pack;
  }
  _ensurePack(storagePath) {
    if (this.packs === void 0) {
      this.packs = [];
    }
    for (let i = 0; i < this.packs.length; i++) {
      if (this.packs[i].storagePath === storagePath) {
        return this.packs[i];
      }
    }
    const pack = new import_Package.default(import_StorageUtilities.default.getLeafName(storagePath), storagePath);
    this.packs.push(pack);
    return pack;
  }
  _handleMessageFromAppService(command, data) {
    switch (command) {
      case "externalKeyPress":
        if (data.startsWith("command")) {
          const commandIndex = parseInt(data.substring(7, data.length));
          import_CommandRunner.default.runCustomTool(this, commandIndex);
        }
        break;
      case "localFileUpdate":
        import_ElectronStorage.default.processLocalFileUpdate(data);
        break;
      case "localFileAdded":
        import_ElectronStorage.default.processLocalFileAdded(data);
        break;
      case "localFileRemoved":
        import_ElectronStorage.default.processLocalFileRemoved(data);
        break;
      case "statusMessage":
        const firstPipe = data.indexOf("|");
        const content = data.substring(firstPipe + 1, data.length);
        try {
          const contentO = JSON.parse(content);
          this.notifyExternalStatus(contentO);
        } catch (e) {
        }
        break;
      case "mctSavedInAppService":
        this.load(true);
        break;
      case "logFileUpdated":
        try {
          if (data) {
            const firstPipe2 = data.indexOf("|");
            if (firstPipe2 > 0) {
              const fileName = data.substring(0, firstPipe2);
              const content2 = data.substring(firstPipe2 + 1, data.length);
              this._handleLogFileUpdated(fileName, content2);
            }
          }
        } catch (e) {
          import_Log.default.fail("Error parsing inbound log: " + e);
        }
        break;
      default:
        if (this.deploymentStorageMinecraft) {
          this.deploymentStorageMinecraft.processExternalMessage(command, data);
        }
        if (this.processHostedMinecraft) {
          this.processHostedMinecraft.processExternalMessage(command, data);
        }
        if (this.gameMinecraft) {
          this.gameMinecraft.processExternalMessage(command, data);
        }
        if (this.remoteMinecraft) {
          this.remoteMinecraft.processExternalMessage(command, data);
        }
        break;
    }
  }
  _handleLogFileUpdated(fileName, contents) {
    if (contents === null || contents === void 0 || fileName === null || fileName === void 0) {
      return;
    }
    let arr = this.mcLogs[fileName];
    if (arr === void 0) {
      arr = [];
      if (import_Utilities.default.isUsableAsObjectKey(fileName)) {
        this.mcLogs[fileName] = arr;
      }
    }
    const logItems = contents.split("\r");
    for (let i = logItems.length - 1; i >= 0; i--) {
      const logItem = logItems[i];
      if (logItem !== void 0 && logItem.length > 3) {
        if (!arr.includes(logItem)) {
          arr.push(logItem);
          this.notifyStatusUpdate(logItem);
        }
      }
    }
  }
  async notifyStatusUpdate(message, topic) {
    const messageCanon = message.trim().toLowerCase();
    if (messageCanon.length > 1) {
      const status = {
        time: /* @__PURE__ */ new Date(),
        message,
        topic,
        type: import_Status.StatusType.message
      };
      this.status.push(status);
      await this.callStatusAddedListeners(status);
      this.ensureStatusArrayIsTrimmed();
    }
  }
  ensureStatusArrayIsTrimmed() {
    if (this.status.length > 1e4) {
      const newStatusArr = [];
      for (let i = this.status.length - 9e3; i < this.status.length; i++) {
        newStatusArr.push(this.status[i]);
      }
      this.status = newStatusArr;
    }
  }
  async callStatusAddedListeners(status) {
    this._onStatusAdded.dispatch(this, status);
    if (this._onStatusAddedAsync.length > 0) {
      let promises = [];
      for (let i = 0; i < this._onStatusAddedAsync.length; i++) {
        promises.push(this._onStatusAddedAsync[i](this, status));
      }
      await Promise.all(promises);
    }
  }
  notifyExternalStatus(status) {
    this.status.push(status);
    if (status.type === import_Status.StatusType.operationStarted) {
      this.activeOperations.push(status);
    } else if ((status.type === import_Status.StatusType.operationEndedComplete || status.type === import_Status.StatusType.operationEndedErrors) && status.operationId !== null && status.operationId !== void 0) {
      this.removeOperation(status.operationId);
    }
    this.callStatusAddedListeners(status);
    return status.operationId;
  }
  async notifyOperationUpdate(updateOperationId, message, topic) {
    this.ensureStatusArrayIsTrimmed();
    this.updateOperation(updateOperationId, message);
    const status = {
      message,
      operationId: updateOperationId,
      type: import_Status.StatusType.operationUpdate,
      time: /* @__PURE__ */ new Date(),
      topic
    };
    this.status.push(status);
    await this.callStatusAddedListeners(status);
  }
  async notifyOperationStarted(message, topic) {
    const status = {
      time: /* @__PURE__ */ new Date(),
      message,
      type: import_Status.StatusType.operationStarted,
      topic
    };
    status.operationId = status.time.getTime();
    this.status.push(status);
    this.activeOperations.push(status);
    this.ensureStatusArrayIsTrimmed();
    await this.callStatusAddedListeners(status);
    return status.operationId;
  }
  async notifyOperationEnded(endedOperationId, message, topic, endedWithErrors) {
    this.ensureStatusArrayIsTrimmed();
    this.removeOperation(endedOperationId);
    this._onOperationCompleted.dispatch(this, endedOperationId);
    if (!message) {
      return;
    }
    const status = {
      message,
      operationId: endedOperationId,
      type: endedWithErrors ? import_Status.StatusType.operationEndedErrors : import_Status.StatusType.operationEndedComplete,
      time: /* @__PURE__ */ new Date(),
      topic
    };
    this.status.push(status);
    await this.callStatusAddedListeners(status);
  }
  updateOperation(id, newMessage) {
    for (let i = 0; i < this.activeOperations.length; i++) {
      const oper = this.activeOperations[i];
      if (oper.operationId === id) {
        oper.message = newMessage;
      }
    }
  }
  removeOperation(id) {
    const newActiveOperations = [];
    for (let i = 0; i < this.activeOperations.length; i++) {
      const oper = this.activeOperations[i];
      if (oper.operationId !== id) {
        newActiveOperations.push(oper);
      }
    }
    this.activeOperations = newActiveOperations;
  }
  async save() {
    const configFile = this.file;
    configFile.setObjectContentIfSemanticallyDifferent(this.#data);
    await configFile.saveContent();
    if (import_AppServiceProxy.default.hasAppService) {
      import_AppServiceProxy.default.sendAsync(import_AppServiceProxy.AppServiceProxyCommands.reloadMct, "");
    }
  }
  async loadGallery() {
    let result = null;
    if (typeof window !== "undefined") {
      const url = this.contentRoot + "data/gallery.json";
      try {
        result = await import_axios.default.get(url);
        if (result) {
          this._gallery = result.data;
        }
      } catch (e) {
        import_Log.default.fail("Could not load gallery: " + e + " from '" + url + "'");
      }
    } else if (this.local) {
      try {
        result = await this.local.readJsonFile("data/gallery.json");
      } catch (e) {
        import_Log.default.fail("Could not load local file: " + e + " from 'data/gallery.json'");
      }
      if (result !== null) {
        this._gallery = result;
      }
    }
    if (this._gallery) {
      for (const item of this._gallery.items) {
        if (item.sampleSet) {
          item.gitHubOwner = "microsoft";
          item.gitHubRepoName = "minecraft-scripting-samples";
          if (item.type === import_IGalleryItem.GalleryItemType.editorCodeSample) {
            item.gitHubFolder = "/editor-script-box";
            if (!item.tags) {
              item.tags = ["editor"];
            }
          } else {
            item.gitHubFolder = "/script-box";
          }
        }
        if (item.fileList) {
          const newFileList = [];
          for (const fileItem of item.fileList) {
            newFileList.push(import_MinecraftUtilities.default.replaceMinecraftPathTokens(fileItem));
          }
          item.fileList = newFileList;
        }
      }
    }
    this._galleryLoaded = true;
    this._onGalleryLoaded.dispatch(this, this._gallery);
    return this._gallery;
  }
  async getGalleryProjectByGitHub(repoName, owner, branch, folder, withFiles) {
    if (!this._galleryLoaded) {
      await this.loadGallery();
    }
    if (this._galleryLoaded === false || this._gallery === void 0 || this._gallery.items === void 0) {
      return void 0;
    }
    repoName = repoName.toLowerCase();
    owner = owner.toLowerCase();
    for (const galProj of this._gallery.items) {
      if (galProj.gitHubRepoName && galProj.gitHubOwner && galProj.gitHubRepoName.toLowerCase() === repoName && galProj.gitHubOwner.toLowerCase() === owner && branch === galProj.gitHubBranch && folder === galProj.gitHubFolder && (!withFiles || galProj.fileList)) {
        return galProj;
      }
    }
    return void 0;
  }
  async getGalleryProjectById(galleryProjectId) {
    if (!this._galleryLoaded) {
      await this.loadGallery();
    }
    if (this._galleryLoaded === false || this._gallery === void 0 || this._gallery.items === void 0) {
      return void 0;
    }
    galleryProjectId = galleryProjectId.toLowerCase();
    galleryProjectId = galleryProjectId.replace(/-/gi, "");
    for (const galProj of this._gallery.items) {
      if (galProj.id.toLowerCase() === galleryProjectId) {
        return galProj;
      }
    }
    return void 0;
  }
  async getGalleryProjectByCaption(galleryProjectTitle) {
    if (this._galleryLoaded === false || this._gallery === void 0 || this._gallery.items === void 0) {
      return void 0;
    }
    for (const galProj of this._gallery.items) {
      if (galProj.title === galleryProjectTitle) {
        return galProj;
      }
    }
    return void 0;
  }
  getGalleryProjectByType(itemType) {
    if (this._galleryLoaded === false || this._gallery === void 0 || this._gallery.items === void 0) {
      return void 0;
    }
    const galleryItems = [];
    for (const galProj of this._gallery.items) {
      if (galProj.type === itemType) {
        galleryItems.push(galProj);
      }
    }
    return galleryItems;
  }
  async getNewProjectName(seedName) {
    if (!this._isLoaded) {
      await this.load();
    }
    let newProjectName = import_StorageUtilities.default.convertFolderPlaceholders(seedName);
    let counter = 0;
    while (this.prefsProjectsFolder.fileExists(newProjectName + ".json") || this.projectsStorage.rootFolder.folderExists(newProjectName)) {
      counter++;
      newProjectName = import_StorageUtilities.default.convertFolderPlaceholders(seedName) + " " + counter;
    }
    return newProjectName;
  }
  async createNewProject(newProjectName, newProjectPath, newProjectFolder, newProjectFolderTitle, focus, includeDefaultItems, projectLanguage) {
    if (!this._isLoaded) {
      await this.load();
    }
    const targetProjectName = await this.getNewProjectName(newProjectName);
    let projectPrefs;
    if (newProjectFolder) {
      projectPrefs = await newProjectFolder.ensureFileFromRelativePath("/.mct/prefs.mctp.json");
    } else {
      projectPrefs = await this.prefsProjectsFolder.createFile(targetProjectName + ".json");
    }
    const newProject = new import_Project.default(this, targetProjectName, projectPrefs);
    if (newProjectPath && !newProjectFolder) {
      newProject.localFolderPath = newProjectPath;
    }
    if (projectLanguage) {
      newProject.preferredScriptLanguage = projectLanguage;
    }
    if (newProjectFolder) {
      newProject.setProjectFolder(newProjectFolder);
      newProject.projectFolderTitle = newProjectFolderTitle;
    } else {
      await newProject.ensureProjectFolder();
    }
    newProject.focus = focus;
    if (includeDefaultItems) {
      await import_ProjectUtilities.default.ensureDefaultItems(newProject);
    }
    this.projects.push(newProject);
    return newProject;
  }
  async createProjectFromContent(jsonStringOrBase64ZipContent) {
    let jsonO = void 0;
    let filteredContents = import_Utilities.default.fixJsonContent(jsonStringOrBase64ZipContent);
    try {
      jsonO = JSON.parse(filteredContents);
    } catch (e) {
      jsonO = void 0;
    }
    if (jsonO) {
      const tempStorage = new import_Storage.default();
      const tempFile = tempStorage.rootFolder.ensureFile("import.json");
      tempFile.setContent(jsonStringOrBase64ZipContent);
      return await this.createProjectFromFolder(tempStorage.rootFolder);
    }
    let zs = new import_ZipStorage.default();
    await zs.loadFromBase64(jsonStringOrBase64ZipContent);
    if (zs.errorStatus !== void 0) {
      return zs.errorMessage;
    }
    if (zs.rootFolder) {
      return await this.createProjectFromFolder(zs.rootFolder);
    }
    return void 0;
  }
  async createAndDeployProjectFromPath(path) {
    const project = await this.createAndDeployProjectFromPath(path);
    if (!project) {
      return "Could not create project from path: " + path;
    }
    if (typeof project === "string") {
      return project;
    }
    this.prepareAndStartToMinecraft({
      project
    });
    return project;
  }
  async createProjectFromPath(path) {
    const folderAndFile = import_StorageUtilities.default.getRootAndFocusPathFromInputPath(path);
    if (!folderAndFile.basePath || !import_CreatorToolsHost.default.localFolderExists || !import_CreatorToolsHost.default.ensureLocalFolder) {
      return "Invalid folder structure";
    }
    const exists = await import_CreatorToolsHost.default.localFolderExists(folderAndFile.basePath);
    if (!exists) {
      return "Local path does not exist: " + folderAndFile.basePath;
    }
    const folder = import_CreatorToolsHost.default.ensureLocalFolder(folderAndFile.basePath);
    if (!folder) {
      return "Local folder could not be created: " + folderAndFile.basePath;
    }
    return this.createProjectFromFolder(folder);
  }
  async createProjectFromFolder(folder) {
    if (!this._isLoaded) {
      await this.load();
    }
    const newProject = new import_Project.default(this, folder.name, null);
    newProject.setProjectFolder(folder);
    await newProject.ensureProjectFolder();
    await newProject.ensureInflated();
    await newProject.inferProjectItemsFromFiles();
    this.projects.push(newProject);
    return newProject;
  }
  async createProjectFromFolderPath(path) {
    if (!this._isLoaded) {
      await this.load();
    }
    let newProjectName = import_StorageUtilities.default.convertFolderPlaceholders(import_StorageUtilities.default.getLeafName(path));
    let counter = 0;
    while (this.prefsProjectsFolder.fileExists(newProjectName + ".json") || this.projectsStorage.rootFolder.folderExists(newProjectName)) {
      counter++;
      newProjectName = import_StorageUtilities.default.convertFolderPlaceholders(import_StorageUtilities.default.getLeafName(path)) + " " + counter;
    }
    const projectPrefs = await this.prefsProjectsFolder.createFile(newProjectName + ".json");
    const newProject = new import_Project.default(this, newProjectName, projectPrefs);
    newProject.localFolderPath = path;
    await newProject.ensureProjectFolder();
    await newProject.ensureInflated();
    await newProject.inferProjectItemsFromFiles();
    this.projects.push(newProject);
    return newProject;
  }
  async deleteProjectByName(projectName) {
    let projectToDelete;
    for (let i = 0; i < this.projects.length; i++) {
      const proj = this.projects[i];
      if (proj.name === projectName) {
        if (projectToDelete !== void 0) {
          import_Log.default.fail("Encountered multiple projects with the same name.");
          return;
        }
        projectToDelete = proj;
      }
    }
    if (projectToDelete) {
      await projectToDelete.deleteThisProject();
      this.removeProject(projectToDelete);
      projectToDelete.dispose();
    }
  }
  async removeProjectByName(projectName) {
    let projectToRemove;
    for (let i = 0; i < this.projects.length; i++) {
      const proj = this.projects[i];
      if (proj.name === projectName) {
        if (projectToRemove !== void 0) {
          import_Log.default.fail("Encountered multiple projects with the same name.");
          return;
        }
        projectToRemove = proj;
      }
    }
    if (!projectToRemove) {
      import_Log.default.fail("Could not find project to remove: " + projectName);
      return;
    }
    await projectToRemove.deletePreferencesFile();
    this.removeProject(projectToRemove);
    projectToRemove.dispose();
  }
  removeProject(project) {
    const newProjects = [];
    for (let i = 0; i < this.projects.length; i++) {
      const proj = this.projects[i];
      if (proj !== project) {
        newProjects.push(proj);
      }
    }
    this.projects = newProjects;
  }
  getProjectByName(projectName) {
    for (let i = 0; i < this.projects.length; i++) {
      const proj = this.projects[i];
      if (proj.name === projectName) {
        return proj;
      }
    }
    return void 0;
  }
  getProjectByMainLocalFolderPath(localPath) {
    localPath = import_StorageUtilities.default.canonicalizePath(localPath);
    for (let i = 0; i < this.projects.length; i++) {
      const proj = this.projects[i];
      if (proj.localFolderPath && import_StorageUtilities.default.canonicalizePath(proj.localFolderPath) === localPath) {
        return proj;
      }
    }
    return void 0;
  }
  async ensureProjectFromLocalStoragePath(messageProjectPath) {
    if (!this.local) {
      import_Log.default.fail("Could not find local utilities.");
      return;
    }
    if (!this._isLoaded) {
      await this.load();
    }
    let desiredProjectName = import_StorageUtilities.default.convertFolderPlaceholders(
      import_StorageUtilities.default.canonicalizePathAsFileName(messageProjectPath)
    );
    const project = this.getProjectByName(desiredProjectName);
    const canonPath = import_StorageUtilities.default.canonicalizePath(messageProjectPath);
    if (project !== void 0) {
      await project.loadPreferencesAndFolder();
      if (project.localFolderPath !== void 0) {
        if (canonPath === import_StorageUtilities.default.canonicalizePath(project.localFolderPath)) {
          await project.ensureInflated();
          await project.inferProjectItemsFromFiles();
          return project;
        }
      }
    }
    let counter = 0;
    while (this.prefsProjectsFolder.fileExists(desiredProjectName + ".json") || this.projectsStorage.rootFolder.folderExists(desiredProjectName)) {
      counter++;
      desiredProjectName = import_StorageUtilities.default.convertFolderPlaceholders(import_StorageUtilities.default.canonicalizePathAsFileName(messageProjectPath)) + " " + counter;
    }
    const projectPrefs = await this.prefsProjectsFolder.createFile(desiredProjectName + ".json");
    const newProject = new import_Project.default(this, desiredProjectName, projectPrefs);
    newProject.localFolderPath = messageProjectPath;
    newProject.originalFullPath = messageProjectPath;
    const localStorage = this.local.createStorage(messageProjectPath);
    if (localStorage === null) {
      import_Log.default.fail("Could not create local storage.");
      return;
    }
    newProject.setProjectFolder(localStorage.rootFolder);
    await newProject.ensureProjectFolder();
    await newProject.inferProjectItemsFromFiles();
    this.projects.push(newProject);
    return newProject;
  }
  async ensureProjectForLocalFolder(folderPath, newProjectName, openDirect) {
    if (!this._isLoaded) {
      await this.load();
    }
    folderPath = import_StorageUtilities.default.convertFolderPlaceholdersPartial(folderPath);
    let desiredProjectName = "";
    if (newProjectName !== void 0) {
      desiredProjectName = newProjectName;
    } else {
      desiredProjectName = import_StorageUtilities.default.getLeafName(folderPath);
    }
    desiredProjectName = import_StorageUtilities.default.convertFolderPlaceholders(desiredProjectName);
    let canonPath = import_StorageUtilities.default.canonicalizePath(folderPath);
    let project = this.getProjectByMainLocalFolderPath(folderPath);
    if (!project) {
      project = this.getProjectByName(desiredProjectName);
    }
    if (project !== void 0 && !openDirect) {
      await project.loadPreferencesAndFolder();
      if (project.localFolderPath !== void 0) {
        if (canonPath === import_StorageUtilities.default.canonicalizePath(project.localFolderPath)) {
          await project.ensureInflated();
          await project.inferProjectItemsFromFiles();
          return project;
        }
      }
    } else if (project && openDirect) {
      for (let i = 1; i < 99; i++) {
        desiredProjectName = import_StorageUtilities.default.convertFolderPlaceholders(import_StorageUtilities.default.getLeafName(folderPath)) + " " + i;
        canonPath = import_StorageUtilities.default.canonicalizePath(folderPath) + " " + i;
        if (newProjectName !== void 0) {
          desiredProjectName = newProjectName;
        }
        let project2 = this.getProjectByName(desiredProjectName);
        if (!project2) {
          break;
        }
      }
    }
    for (let i = 0; i < this.projects.length; i++) {
      project = this.projects[i];
      await project.loadPreferencesAndFolder();
      if (project.localFolderPath !== void 0) {
        if (canonPath === import_StorageUtilities.default.canonicalizePath(project.localFolderPath)) {
          await project.ensureInflated();
          await project.inferProjectItemsFromFiles();
          return project;
        }
      }
    }
    let counter = 0;
    while (this.prefsProjectsFolder.fileExists(desiredProjectName + ".json") || this.projectsStorage.rootFolder.folderExists(desiredProjectName)) {
      counter++;
      desiredProjectName = import_StorageUtilities.default.convertFolderPlaceholders(import_StorageUtilities.default.getLeafName(folderPath)) + " " + counter;
    }
    const projectPrefs = await this.prefsProjectsFolder.createFile(desiredProjectName + ".json");
    const newProject = new import_Project.default(this, desiredProjectName, projectPrefs, true);
    newProject.localFolderPath = folderPath;
    newProject.originalFullPath = folderPath;
    await newProject.ensureProjectFolder();
    await newProject.inferProjectItemsFromFiles();
    this.projects.push(newProject);
    return newProject;
  }
  async getExportZip() {
    const zs = new import_ZipStorage.default();
    const zipRoot = zs.rootFolder;
    if (!this._isLoaded) {
      await this.load();
    }
    for (let i = 0; i < this.projects.length; i++) {
      const project = this.projects[i];
      await project.loadPreferencesAndFolder();
      await project.ensureProjectFolder();
      if (project.projectFolder) {
        const projectTargetFolder = zipRoot.ensureFolder(project.name);
        await import_StorageUtilities.default.syncFolderTo(project.projectFolder, projectTargetFolder, true, true, false);
      }
    }
    await zipRoot.saveAll();
    return zs;
  }
  async load(force) {
    if (this._isLoaded && !force) {
      return;
    }
    const configFile = this.file;
    if (!configFile.isContentLoaded) {
      await configFile.loadContent(false);
    }
    if (configFile.content !== null && configFile.content !== void 0 && typeof configFile.content === "string") {
      this.#data = JSON.parse(configFile.content);
    }
    const projectsFolder = this.prefsProjectsFolder;
    await projectsFolder.load();
    await this.projectsStorage.rootFolder.load();
    this.projects = [];
    for (const fileName in projectsFolder.files) {
      const projectName = import_StorageUtilities.default.getBaseFromName(fileName);
      const projectFile = projectsFolder.files[fileName];
      if (projectFile !== void 0) {
        const project = new import_Project.default(this, projectName, projectFile);
        this.projects.push(project);
      }
    }
    this.considerStartingMinecraft();
    this.initializeWorldSettings();
    this.ensureDefaultWorldName();
    this._onLoaded.dispatch(this, this);
    this._isLoaded = true;
  }
  async considerStartingMinecraft() {
    if (import_CreatorToolsHost.default.hostType === import_CreatorToolsHost.HostType.webPlusServices || import_CreatorToolsHost.default.hostType === import_CreatorToolsHost.HostType.web) {
      if (this.successfullyConnectedWebSocketToMinecraft && (this.autoStartMinecraft || this.lastActiveMinecraftFlavor === import_ICreatorToolsData.MinecraftFlavor.remote)) {
        this.ensureRemoteMinecraft();
        const ct = import_CreatorToolsHost.default.getCreatorTools();
        if (this.remoteMinecraft && import_CreatorToolsHost.default.hostType === import_CreatorToolsHost.HostType.web && ct && ct.remoteServerUrl && ct.remoteServerUrl.length > 4) {
          this.remoteMinecraft.initialize();
          if (!this.activeMinecraft && this.lastActiveMinecraftFlavor === import_ICreatorToolsData.MinecraftFlavor.remote) {
            this.activeMinecraft = this.remoteMinecraft;
            this.lastActiveMinecraftFlavor = import_ICreatorToolsData.MinecraftFlavor.remote;
          }
        }
        if (import_CreatorToolsHost.default.isAppServiceWeb) {
          this.ensureGameMinecraft();
          if (this.gameMinecraft) {
            this.gameMinecraft.initialize();
          }
          if (!this.activeMinecraft && this.lastActiveMinecraftFlavor === import_ICreatorToolsData.MinecraftFlavor.minecraftGameProxy) {
            this.activeMinecraft = this.gameMinecraft;
            this.lastActiveMinecraftFlavor = import_ICreatorToolsData.MinecraftFlavor.minecraftGameProxy;
          }
        }
      }
      if (this.successfullyStartedMinecraftServer && this.lastActiveMinecraftFlavor === import_ICreatorToolsData.MinecraftFlavor.processHostedProxy && this.autoStartMinecraft) {
        await this.connectToMinecraft();
      }
    }
  }
  async connectToMinecraft() {
    if (!this.lastActiveMinecraftFlavor) {
      return;
    }
    this.ensureMinecraft(this.lastActiveMinecraftFlavor);
    if (this.activeMinecraft) {
      const status = await this.activeMinecraft.updateStatus();
      if (status === 11 /* disconnected */ || status === 9 /* error */ || status === 0 /* none */ || status === 8 /* stopped */ || status === 7 /* stopping */ || status === 10 /* newMinecraft */) {
        await this.activeMinecraft.initialize();
      }
    }
  }
  canPrepareAndStartMinecraft() {
    if (!this.activeMinecraft) {
      return false;
    }
    if (this.activeMinecraftState !== 2 /* initialized */ && this.activeMinecraftState !== 3 /* preparing */ && this.activeMinecraftState !== 4 /* prepared */ && this.activeMinecraftState !== 5 /* starting */ && this.activeMinecraftState !== 6 /* started */) {
      return false;
    }
    return true;
  }
  async prepareAndStartToMinecraft(push) {
    if (!this.canPrepareAndStartMinecraft || !this.activeMinecraft) {
      return;
    }
    await this.activeMinecraft.prepareAndStart(push);
  }
  _bubbleMinecraftRefreshed(minecraft, newState) {
    if (minecraft !== this.activeMinecraft) {
      return;
    }
    this._onMinecraftRefreshed.dispatch(minecraft, newState);
  }
  _bubbleMinecraftStateChanged(minecraft, newState) {
    if (minecraft !== this.activeMinecraft) {
      return;
    }
    if (!this.successfullyConnectedWebSocketToMinecraft && newState === 2 /* initialized */) {
      this.successfullyConnectedWebSocketToMinecraft = true;
      this.save();
    }
    if (newState === 4 /* prepared */) {
      import_Log.default.message("Minecraft is ready.");
    } else if (newState === 6 /* started */) {
    } else if (newState === 7 /* stopping */) {
      import_Log.default.message("Disconnecting from Minecraft.");
    }
    this._onMinecraftStateChanged.dispatch(minecraft, newState);
  }
  ensureRemoteMinecraft() {
    if (this.remoteMinecraft === void 0) {
      this.remoteMinecraft = new import_RemoteMinecraft.default(this);
    }
    return this.remoteMinecraft;
  }
  ensureProcessHostedMinecraft() {
    if (this.processHostedMinecraft === void 0) {
      this.processHostedMinecraft = new import_ProcessHostedProxyMinecraft.default(this);
    }
    return this.processHostedMinecraft;
  }
  ensureDeploymentStorageMinecraft() {
    if (this.deploymentStorageMinecraft === void 0) {
      this.deploymentStorageMinecraft = new import_DeploymentStorageMinecraft.default(this);
    }
    return this.deploymentStorageMinecraft;
  }
  ensureGameMinecraft() {
    if (this.gameMinecraft === void 0) {
      this.gameMinecraft = new import_MinecraftGameProxyMinecraft.default(this);
    }
    if (this.autoStartMinecraft && this.successfullyConnectedWebSocketToMinecraft) {
      this.gameMinecraft.start();
    }
    return this.gameMinecraft;
  }
  ensureMinecraft(flavor) {
    if (flavor === import_ICreatorToolsData.MinecraftFlavor.none) {
      return void 0;
    }
    if (this.activeMinecraft && this.lastActiveMinecraftFlavor === flavor) {
      return this.activeMinecraft;
    }
    this.ensureRemoteMinecraft();
    if (flavor === import_ICreatorToolsData.MinecraftFlavor.processHostedProxy) {
      this.ensureProcessHostedMinecraft();
    } else if (flavor === import_ICreatorToolsData.MinecraftFlavor.deploymentStorage) {
      this.ensureDeploymentStorageMinecraft();
    } else if (flavor === import_ICreatorToolsData.MinecraftFlavor.minecraftGameProxy) {
      this.ensureGameMinecraft();
    }
    if (this.activeMinecraft) {
      this.activeMinecraft.onStateChanged.unsubscribe(this._bubbleMinecraftStateChanged);
      this.activeMinecraft.onRefreshed.unsubscribe(this._bubbleMinecraftRefreshed);
    }
    if (flavor === import_ICreatorToolsData.MinecraftFlavor.remote) {
      if (!this.remoteMinecraft) {
        import_Log.default.throwUnexpectedUndefined("EMR");
        return;
      }
      this.activeMinecraft = this.remoteMinecraft;
      if (this.lastActiveMinecraftFlavor !== import_ICreatorToolsData.MinecraftFlavor.remote) {
        this.lastActiveMinecraftFlavor = import_ICreatorToolsData.MinecraftFlavor.remote;
        this.save();
      }
      this._onMinecraftStateChanged.dispatch(this.activeMinecraft, 10 /* newMinecraft */);
      if (this.activeMinecraft === void 0) {
        import_Log.default.unexpectedUndefined("EMA");
        return void 0;
      }
      this.activeMinecraft.onStateChanged.subscribe(this._bubbleMinecraftStateChanged);
      this.activeMinecraft.onRefreshed.subscribe(this._bubbleMinecraftRefreshed);
    } else if (flavor === import_ICreatorToolsData.MinecraftFlavor.minecraftGameProxy) {
      if (!this.gameMinecraft) {
        import_Log.default.throwUnexpectedUndefined("EMG");
        return void 0;
      }
      this.activeMinecraft = this.gameMinecraft;
      if (this.lastActiveMinecraftFlavor !== import_ICreatorToolsData.MinecraftFlavor.minecraftGameProxy) {
        this.lastActiveMinecraftFlavor = import_ICreatorToolsData.MinecraftFlavor.minecraftGameProxy;
        this.save();
      }
      this._onMinecraftStateChanged.dispatch(this.activeMinecraft, 10 /* newMinecraft */);
      if (this.activeMinecraft === void 0) {
        import_Log.default.unexpectedUndefined("EMB");
        return void 0;
      }
      this.activeMinecraft.onStateChanged.subscribe(this._bubbleMinecraftStateChanged);
      this.activeMinecraft.onRefreshed.subscribe(this._bubbleMinecraftRefreshed);
    } else if (flavor === import_ICreatorToolsData.MinecraftFlavor.processHostedProxy) {
      if (!this.processHostedMinecraft) {
        import_Log.default.throwUnexpectedUndefined("EMC");
        return void 0;
      }
      this.activeMinecraft = this.processHostedMinecraft;
      if (this.lastActiveMinecraftFlavor !== import_ICreatorToolsData.MinecraftFlavor.processHostedProxy) {
        this.lastActiveMinecraftFlavor = import_ICreatorToolsData.MinecraftFlavor.processHostedProxy;
        this.save();
      }
      this._onMinecraftStateChanged.dispatch(this.activeMinecraft, 10 /* newMinecraft */);
      if (this.activeMinecraft === void 0) {
        import_Log.default.unexpectedUndefined("EMD");
        return void 0;
      }
      this.activeMinecraft.onStateChanged.subscribe(this._bubbleMinecraftStateChanged);
      this.activeMinecraft.onRefreshed.subscribe(this._bubbleMinecraftRefreshed);
    } else if (flavor === import_ICreatorToolsData.MinecraftFlavor.deploymentStorage) {
      if (!this.deploymentStorageMinecraft) {
        import_Log.default.throwUnexpectedUndefined("EMCD");
        return void 0;
      }
      this.activeMinecraft = this.deploymentStorageMinecraft;
      if (this.lastActiveMinecraftFlavor !== import_ICreatorToolsData.MinecraftFlavor.deploymentStorage) {
        this.lastActiveMinecraftFlavor = import_ICreatorToolsData.MinecraftFlavor.deploymentStorage;
        this.save();
      }
      this._onMinecraftStateChanged.dispatch(this.activeMinecraft, 10 /* newMinecraft */);
      if (this.activeMinecraft === void 0) {
        import_Log.default.unexpectedUndefined("EMDD");
        return void 0;
      }
      this.activeMinecraft.onStateChanged.subscribe(this._bubbleMinecraftStateChanged);
      this.activeMinecraft.onRefreshed.subscribe(this._bubbleMinecraftRefreshed);
    } else if (this.createMinecraft === void 0 || this.canCreateMinecraft === void 0 || !this.canCreateMinecraft(flavor)) {
      import_Log.default.debugAlert("Requested creation of an unavailable Minecraft.");
      flavor = import_ICreatorToolsData.MinecraftFlavor.none;
    } else {
      const newMinecraft = this.createMinecraft(flavor, this);
      if (!newMinecraft) {
        import_Log.default.debugAlert("Could not create a requested Minecraft.");
        flavor = import_ICreatorToolsData.MinecraftFlavor.none;
      } else {
        this.activeMinecraft = newMinecraft;
        this.lastActiveMinecraftFlavor = flavor;
        this._onMinecraftStateChanged.dispatch(this.activeMinecraft, 10 /* newMinecraft */);
        this.activeMinecraft.onStateChanged.subscribe(this._bubbleMinecraftStateChanged);
        this.activeMinecraft.onRefreshed.subscribe(this._bubbleMinecraftRefreshed);
      }
    }
    this.lastActiveMinecraftFlavor = flavor;
    this.save();
    return this.activeMinecraft;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CreatorToolsMinecraftErrorStatus,
  CreatorToolsMinecraftState,
  CreatorToolsTargetSettings,
  DefaultCreatorName,
  SidePaneMaxWidth,
  SidePaneMinWidth
});
//# sourceMappingURL=CreatorTools.js.map
