"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var GalleryReader_exports = {};
__export(GalleryReader_exports, {
  default: () => GalleryReader
});
module.exports = __toCommonJS(GalleryReader_exports);
var import_Utilities = __toESM(require("../../core/Utilities"));
var import_CreatorToolsHost = __toESM(require("../CreatorToolsHost"));
class GalleryReader {
  constructor(defaultProjectImage) {
    this.defaultProjectImage = defaultProjectImage;
  }
  getStandinImage() {
    return this.defaultProjectImage;
  }
  getGalleryImage(item) {
    if (item.logoImage === void 0 && item.localLogo === void 0) {
      return this.getStandinImage();
    }
    let imagePath = item.logoImage;
    if (imagePath === void 0) {
      imagePath = import_CreatorToolsHost.default.getVanillaContentRoot() + "res/latest/van/serve/resource_pack/textures/" + item.localLogo;
    }
    if (item.logoImage === void 0) return imagePath;
    if (item.gitHubRepoName === "bedrock-samples") {
      imagePath = import_CreatorToolsHost.default.getVanillaContentRoot() + import_Utilities.default.ensureEndsWithSlash("res/latest/van/serve/");
    } else {
      imagePath = import_CreatorToolsHost.default.contentWebRoot + "res/samples/" + item.gitHubOwner + "/" + item.gitHubRepoName + "-";
      if (item.gitHubBranch !== void 0) {
        imagePath += import_Utilities.default.ensureEndsWithSlash(item.gitHubBranch);
      } else {
        imagePath += "main/";
      }
    }
    imagePath += import_Utilities.default.ensureNotStartsWithSlash(item.logoImage);
    return imagePath;
  }
}
//# sourceMappingURL=GalleryReader.js.map
