"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ProjectStandard_exports = {};
__export(ProjectStandard_exports, {
  default: () => ProjectStandard
});
module.exports = __toCommonJS(ProjectStandard_exports);
var import_EslintConfig = __toESM(require("../devproject/EslintConfig"));
var import_JustConfig = __toESM(require("../devproject/JustConfig"));
var import_NpmPackageDefinition = __toESM(require("../devproject/NpmPackageDefinition"));
var import_IProjectItemData = require("./IProjectItemData");
var import_Project = require("./Project");
class ProjectStandard {
  static async ensureIsStandard(project) {
    await project.ensureProjectFolder();
    if (project.projectFolder) {
      const justFile = await project.projectFolder.getFileFromRelativePath("/just.config.ts");
      if (justFile && await justFile.exists()) {
        justFile.setContent(import_JustConfig.default.getDefaultContent());
        justFile.saveContent();
      }
      const eslintFile = await project.projectFolder.getFileFromRelativePath("/eslint.config.mjs");
      if (eslintFile && await eslintFile.exists()) {
        eslintFile.setContent(import_EslintConfig.default.getDefaultContent());
        eslintFile.saveContent();
      }
      const packageJson = await project.projectFolder.getFileFromRelativePath("/package.json");
      if (packageJson && await packageJson.exists()) {
        project.ensureItemByProjectPath(
          "/package.json",
          import_IProjectItemData.ProjectItemStorageType.singleFile,
          "package.json",
          import_IProjectItemData.ProjectItemType.packageJson,
          import_Project.FolderContext.unknown,
          void 0,
          import_IProjectItemData.ProjectItemCreationType.generated
        );
      }
    }
    const itemsCopy = project.getItemsCopy();
    for (let i = 0; i < itemsCopy.length; i++) {
      const item = itemsCopy[i];
      if (item.itemType === import_IProjectItemData.ProjectItemType.justConfigTs && item.primaryFile) {
        item.creationType = import_IProjectItemData.ProjectItemCreationType.generated;
        const justConfig = await import_JustConfig.default.ensureOnFile(item.primaryFile);
        if (justConfig) {
          justConfig.ensureDefault();
          await justConfig.save();
        }
      } else if (item.itemType === import_IProjectItemData.ProjectItemType.esLintConfigMjs && item.primaryFile) {
        item.creationType = import_IProjectItemData.ProjectItemCreationType.generated;
        const eslintConfig = await import_EslintConfig.default.ensureOnFile(item.primaryFile);
        if (eslintConfig) {
          eslintConfig.ensureDefault();
          await eslintConfig.save();
        }
      } else if (item.itemType === import_IProjectItemData.ProjectItemType.packageJson && item.primaryFile) {
        item.creationType = import_IProjectItemData.ProjectItemCreationType.generated;
        const packageJson = await import_NpmPackageDefinition.default.ensureOnFile(item.primaryFile);
        if (packageJson) {
          await packageJson.ensureStandardContent();
          await packageJson.save();
        }
      }
    }
  }
}
//# sourceMappingURL=ProjectStandard.js.map
