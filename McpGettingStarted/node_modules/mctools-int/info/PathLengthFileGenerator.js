"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var PathLengthFileGenerator_exports = {};
__export(PathLengthFileGenerator_exports, {
  PathLengthFileGeneratorTest: () => PathLengthFileGeneratorTest,
  default: () => PathLengthFileGenerator
});
module.exports = __toCommonJS(PathLengthFileGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IInfoItemData = require("./IInfoItemData");
var import_StorageUtilities = require("../storage/StorageUtilities");
var PathLengthFileGeneratorTest = /* @__PURE__ */ ((PathLengthFileGeneratorTest2) => {
  PathLengthFileGeneratorTest2[PathLengthFileGeneratorTest2["filePathExceeds8DirectorySegments"] = 102] = "filePathExceeds8DirectorySegments";
  PathLengthFileGeneratorTest2[PathLengthFileGeneratorTest2["filePathExceedsCharacterLength"] = 103] = "filePathExceedsCharacterLength";
  PathLengthFileGeneratorTest2[PathLengthFileGeneratorTest2["filePathContainsNonLowercaseLetters"] = 104] = "filePathContainsNonLowercaseLetters";
  return PathLengthFileGeneratorTest2;
})(PathLengthFileGeneratorTest || {});
class PathLengthFileGenerator {
  id = "PATHLENGTH";
  title = "Path Length";
  canAlwaysProcess = true;
  summarize(info, infoSet) {
  }
  async generate(project, file, contentIndex) {
    const items = [];
    let path = file.storageRelativePath;
    let pathSub = path;
    pathSub = pathSub.replace("/Content/", "/content/");
    pathSub = pathSub.replace("/Marketing Art/", "/marketing art/");
    pathSub = pathSub.replace("/Store Art/", "/store art/");
    if (pathSub.indexOf("/marketing art/") >= 0 || pathSub.indexOf("/store art/") >= 0) {
      return items;
    }
    if (pathSub.startsWith("/content/")) {
      pathSub = pathSub.substring(9);
    }
    let packsIndex = pathSub.indexOf("_packs/");
    if (packsIndex > 0) {
      packsIndex = pathSub.indexOf("/", packsIndex + 7);
      if (packsIndex >= 0) {
        pathSub = pathSub.substring(packsIndex);
      }
    } else {
      packsIndex = pathSub.indexOf("/", 2);
      if (packsIndex >= 0) {
        pathSub = pathSub.substring(packsIndex + 1);
      }
    }
    if (pathSub.toLowerCase() !== pathSub && !pathSub.startsWith("/marketing art/") && !pathSub.startsWith("/store art/") && !pathSub.endsWith(".lang") && pathSub.indexOf("/texts/") < 0 && pathSub.indexOf("/scripts/") < 0) {
      items.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.recommendation,
          this.id,
          104 /* filePathContainsNonLowercaseLetters */,
          `File path contains non-lowercase letters`,
          project.getItemByExtendedOrProjectPath(file.extendedPath),
          pathSub
        )
      );
    }
    for (const hint of import_StorageUtilities.PackContainerFolderHints) {
      const hintIndex = path.toLowerCase().indexOf("/" + hint + "/");
      if (hintIndex >= 0) {
        path = path.substring(hintIndex + hint.length + 2);
      }
    }
    for (const hint of import_StorageUtilities.PackFolderHints) {
      if (path.toLowerCase().startsWith("/" + hint + "/")) {
        path = path.substring(hint.length + 2);
      }
      if (path.toLowerCase().startsWith(hint + "/")) {
        path = path.substring(hint.length + 1);
      }
    }
    const fSlashSegments = path.split("/");
    const bSlashSegments = path.split("\\");
    if (fSlashSegments.length > 9 || bSlashSegments.length > 9) {
      items.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          102 /* filePathExceeds8DirectorySegments */,
          `File path contains 8 or more directory segments, and may not run on all devices`,
          project.getItemByExtendedOrProjectPath(file.extendedPath),
          path
        )
      );
    }
    if (path.length > 100) {
      items.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          103 /* filePathExceedsCharacterLength */,
          `File path contains more than 100 characters, and may not run on all devices`,
          project.getItemByExtendedOrProjectPath(file.extendedPath),
          path
        )
      );
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  PathLengthFileGeneratorTest
});
//# sourceMappingURL=PathLengthFileGenerator.js.map
