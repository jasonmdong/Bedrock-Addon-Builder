"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CooperativeAddOnRequirementsGenerator_exports = {};
__export(CooperativeAddOnRequirementsGenerator_exports, {
  CooperativeAddOnRequirementsGeneratorTest: () => CooperativeAddOnRequirementsGeneratorTest,
  default: () => CooperativeAddOnRequirementsGenerator
});
module.exports = __toCommonJS(CooperativeAddOnRequirementsGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IInfoItemData = require("./IInfoItemData");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var import_BehaviorManifestDefinition = __toESM(require("../minecraft/BehaviorManifestDefinition"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_ResourceManifestDefinition = __toESM(require("../minecraft/ResourceManifestDefinition"));
const UniqueRegEx = new RegExp(/[a-zA-Z0-9]{2,}_[a-zA-Z0-9]{2,}:[\w]+/);
const GenericTermList = [
  "abilities",
  "actions",
  "adventure",
  "animals",
  "anims",
  "armor",
  "assets",
  "attachables",
  "banners",
  "base",
  "blocks",
  "book",
  "boss",
  "bosses",
  "bridge",
  "camping",
  "cars",
  "characters",
  "chests",
  "cinema",
  "cinematics",
  "cloud",
  "collectables",
  "collectibles",
  "custom_ui",
  "cut_scene",
  "cutscene",
  "deco",
  "decor",
  "dev",
  "dialogue",
  "dinos",
  "dinosaurs",
  "doors",
  "dragon",
  "dragons",
  "dummy",
  "effects",
  "enemies",
  "enemy",
  "entities",
  "entity",
  "env",
  "equipment",
  "events",
  "food",
  "furniture",
  "furnitures",
  "fx",
  "game_menu",
  "gameplay",
  "general",
  "glow",
  "guide",
  "guidebook",
  "inanimate",
  "interface",
  "internal",
  "intro",
  "item",
  "items",
  "launchers",
  "machines",
  "magic",
  "markers",
  "market",
  "marketing_banners",
  "mastermind",
  "mechanics",
  "menu",
  "menus",
  "minigames",
  "misc",
  "miscellaneous",
  "mob",
  "mobs",
  "monsters",
  "morphs",
  "mutant",
  "neutral",
  "npc",
  "npcs",
  "objectives",
  "particles",
  "passive",
  "passive_mobs",
  "pets",
  "platform",
  "player",
  "players",
  "portal",
  "portals",
  "preset",
  "projectile",
  "projectiles",
  "properties",
  "props",
  "recipe_models",
  "scanner",
  "script",
  "security",
  "sequences",
  "sfx",
  "shop",
  "skills",
  "sounds",
  "static_entity",
  "store",
  "structure",
  "structures",
  "systems",
  "tasks",
  "teams",
  "teleporter",
  "tnt",
  "tools",
  "towers",
  "trinkets",
  "tutorial",
  "ui",
  "user_interface",
  "util",
  "utilities",
  "vanilla",
  "vanilla_mob",
  "vehicle",
  "vehicles",
  "vr",
  "weapon",
  "weapons",
  "wrench",
  "opaque",
  "map",
  "on",
  "item",
  "player",
  "map",
  "charged",
  "beacon",
  "conduit",
  "moving",
  "banner",
  "bed",
  "cow",
  "chicken",
  "dragon",
  "ender",
  "enderman",
  "fang",
  "fireball",
  "mob",
  "warden",
  "village",
  "pattern",
  "wandering",
  "trial",
  "stray",
  "spider",
  "slime",
  "chest",
  "silverfish",
  "polar",
  "shield",
  "husk",
  "blaze",
  "axolotl"
];
var CooperativeAddOnRequirementsGeneratorTest = /* @__PURE__ */ ((CooperativeAddOnRequirementsGeneratorTest2) => {
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["noLooseFileInTypeFolder"] = 101] = "noLooseFileInTypeFolder";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["noCommonNamesInCreatorFolderName"] = 102] = "noCommonNamesInCreatorFolderName";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["noLooseFileInCreatorFolder"] = 104] = "noLooseFileInCreatorFolder";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["moreThanOneFolderInCreatorFolderBesidesMaybeCommon"] = 108] = "moreThanOneFolderInCreatorFolderBesidesMaybeCommon";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["noUnsupportedFolderNameInTypeFolder"] = 109] = "noUnsupportedFolderNameInTypeFolder";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["moreThanOneFolderInTypeFolder"] = 110] = "moreThanOneFolderInTypeFolder";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["noLooseFilesInTypeFolder"] = 111] = "noLooseFilesInTypeFolder";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["noDimensionElements"] = 131] = "noDimensionElements";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["noUiElements"] = 133] = "noUiElements";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["notOneBehaviorPackManifest"] = 160] = "notOneBehaviorPackManifest";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["notOneResourcePackManifest"] = 161] = "notOneResourcePackManifest";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["behaviorPackManifestNotValid"] = 163] = "behaviorPackManifestNotValid";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["resourcePackManifestNotValid"] = 164] = "resourcePackManifestNotValid";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["notOneDependencyFromBehaviorPackToResourcePack"] = 165] = "notOneDependencyFromBehaviorPackToResourcePack";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["dependencyFromBehaviorPackToResourcePackNotValid"] = 166] = "dependencyFromBehaviorPackToResourcePackNotValid";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["notOneDependencyFromResourcePackToBehaviorPack"] = 168] = "notOneDependencyFromResourcePackToBehaviorPack";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["dependencyFromResourcePackToBehaviorPackNotValid"] = 169] = "dependencyFromResourcePackToBehaviorPackNotValid";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["foundBehaviorPack"] = 200] = "foundBehaviorPack";
  CooperativeAddOnRequirementsGeneratorTest2[CooperativeAddOnRequirementsGeneratorTest2["noVibrantVisualsForNow"] = 210] = "noVibrantVisualsForNow";
  return CooperativeAddOnRequirementsGeneratorTest2;
})(CooperativeAddOnRequirementsGeneratorTest || {});
class CooperativeAddOnRequirementsGenerator {
  id = "CADDONREQ";
  title = "Cooperative Add-On Requirements";
  async generate(project, contentIndex) {
    const items = [];
    let behaviorPackManifest = void 0;
    let behaviorPackItem = void 0;
    let resourcePackManifest = void 0;
    let resourcePackItem = void 0;
    const itemsCopy = project.getItemsCopy();
    for (const projectItem of itemsCopy) {
      if (projectItem.primaryFile) {
        if (projectItem.itemType === import_IProjectItemData.ProjectItemType.behaviorPackManifestJson) {
          if (behaviorPackManifest) {
            items.push(
              new import_ProjectInfoItem.default(
                import_IInfoItemData.InfoItemType.error,
                this.id,
                160 /* notOneBehaviorPackManifest */,
                `Found more than one behavior pack manifest in a cooperative add-on, which is not a best practice`,
                projectItem
              )
            );
          } else {
            items.push(
              new import_ProjectInfoItem.default(
                import_IInfoItemData.InfoItemType.info,
                this.id,
                200 /* foundBehaviorPack */,
                `Behavior pack manifest found.`,
                projectItem
              )
            );
          }
          behaviorPackManifest = await import_BehaviorManifestDefinition.default.ensureOnFile(projectItem.primaryFile);
          behaviorPackItem = projectItem;
          await behaviorPackManifest?.load();
        } else if (projectItem.itemType === import_IProjectItemData.ProjectItemType.resourcePackManifestJson) {
          if (resourcePackManifest) {
            items.push(
              new import_ProjectInfoItem.default(
                import_IInfoItemData.InfoItemType.error,
                this.id,
                161 /* notOneResourcePackManifest */,
                `Found more than one resource pack manifest in a cooperative add-on, which is not a best practice`,
                projectItem
              )
            );
          }
          resourcePackManifest = await import_ResourceManifestDefinition.default.ensureOnFile(projectItem.primaryFile);
          resourcePackItem = projectItem;
          await resourcePackManifest?.load();
        }
      }
    }
    if (!behaviorPackManifest || !behaviorPackManifest.definition) {
      items.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          163 /* behaviorPackManifestNotValid */,
          `Did not find a valid behavior pack manifest.`,
          void 0
        )
      );
    }
    if (!resourcePackManifest || !resourcePackManifest.definition) {
      items.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          164 /* resourcePackManifestNotValid */,
          `Did not find a valid resource pack manifest.`,
          void 0
        )
      );
    }
    if (behaviorPackManifest && resourcePackManifest && behaviorPackManifest.definition && resourcePackManifest.definition) {
      const bpNonInternalDependency = behaviorPackManifest.getFirstNonScriptModuleDependency();
      if (!behaviorPackManifest.definition.dependencies || behaviorPackManifest.getNonScriptModuleDependencyCount() !== 1) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            165 /* notOneDependencyFromBehaviorPackToResourcePack */,
            `Did not find exactly one dependency on the corresponding resource pack in the behavior pack manifest.`,
            behaviorPackItem,
            behaviorPackManifest.getNonScriptModuleDependencyCount()
          )
        );
      } else if (!bpNonInternalDependency || !bpNonInternalDependency.uuid || !import_Utilities.default.uuidEqual(bpNonInternalDependency.uuid, resourcePackManifest.definition.header.uuid)) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            166 /* dependencyFromBehaviorPackToResourcePackNotValid */,
            `Behavior pack manifest does not have a proper dependency on the resource pack identifier.`,
            behaviorPackItem
          )
        );
      }
      if (!resourcePackManifest.definition.dependencies || resourcePackManifest.definition.dependencies.length !== 1) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            168 /* notOneDependencyFromResourcePackToBehaviorPack */,
            `Did not find exactly one dependency on the corresponding behavior pack in the resource pack manifest.`,
            resourcePackItem
          )
        );
      } else if (!resourcePackManifest.definition.dependencies[0].uuid || !import_Utilities.default.uuidEqual(
        resourcePackManifest.definition.dependencies[0].uuid,
        behaviorPackManifest.definition.header.uuid
      )) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            169 /* dependencyFromResourcePackToBehaviorPackNotValid */,
            `Resource pack manifest does not have a proper dependency on the behavior pack identifier.`,
            behaviorPackItem
          )
        );
      }
    }
    const bpFolder = await project.getDefaultBehaviorPackFolder();
    if (bpFolder) {
      await this.generateFromBehaviorPackFolder(project, bpFolder, items);
    }
    const rpFolder = await project.getDefaultResourcePackFolder();
    if (rpFolder) {
      await this.generateFromResourcePackFolder(project, rpFolder, items);
    }
    return items;
  }
  summarize(info, infoSet) {
  }
  async generateFromBehaviorPackFolder(project, folder, items) {
    if (!folder.isLoaded) {
      await folder.load();
    }
    for (const folderName in folder.folders) {
      const childFolder = folder.folders[folderName];
      const folderNameCanon = import_StorageUtilities.default.canonicalizeName(folderName);
      if (folderNameCanon === "dimensions") {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            131 /* noDimensionElements */,
            `Found dimensions in a cooperative add-on, which is not a best practice.`,
            void 0
          )
        );
      }
      if (childFolder && !folder.errorStatus) {
        if (folderNameCanon === "structures") {
          await this.generateFromFirstLevelFolderCreator_Game(project, childFolder, items);
        } else if (folderNameCanon !== "texts" && folderNameCanon !== "entities" && folderNameCanon !== "features" && folderNameCanon !== "feature_rules" && folderNameCanon !== "particles" && folderNameCanon !== "items" && folderNameCanon !== "scripts" && folderNameCanon !== "recipes" && folderNameCanon !== "spawn_rules" && folderNameCanon !== "animations" && folderNameCanon !== "animation_controllers" && folderNameCanon !== "render_controllers" && folderNameCanon !== "blocks") {
          await this.generateFromFirstLevelFolderCreatorNameGameName(project, childFolder, items);
        }
      }
    }
  }
  async generateFromResourcePackFolder(project, folder, items) {
    if (!folder.isLoaded) {
      await folder.load();
    }
    for (const folderName in folder.folders) {
      const childFolder = folder.folders[folderName];
      const folderNameCanon = import_StorageUtilities.default.canonicalizeName(folderName);
      if (folderNameCanon === "ui") {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            133 /* noUiElements */,
            `Found ui elements in a cooperative add-on, which is not supported`,
            void 0
          )
        );
      }
      if (childFolder && !folder.errorStatus) {
        if (folderNameCanon !== "texts" && folderNameCanon !== "entity" && folderNameCanon !== "items" && folderNameCanon !== "particles" && folderNameCanon !== "materials" && folderNameCanon !== "blocks" && folderNameCanon !== "models" && folderNameCanon !== "attachables" && folderNameCanon !== "render_controllers" && folderNameCanon !== "animation_controllers" && folderNameCanon !== "animations") {
          await this.generateFromFirstLevelFolderCreatorNameGameName(project, childFolder, items);
        }
      }
    }
  }
  static isNameGenericTerm(name) {
    name = import_StorageUtilities.default.canonicalizeName(name);
    return GenericTermList.includes(name);
  }
  static isNamespacedIdentifier(id) {
    let identifierSplit = id.split(":");
    if (identifierSplit.length !== 2) {
      return false;
    }
    if (identifierSplit[0].length < 2 || identifierSplit[1].length < 2) {
      return false;
    }
    return this.isNamespacedString(identifierSplit[0]);
  }
  static isNamespacedString(name) {
    let tokens = name.split("_");
    if (tokens.length < 2) {
      return false;
    }
    return tokens[0].length >= 2 && tokens[1].length >= 2;
  }
  static isCommonMaterialName(name) {
    let tokens = name.split("_");
    if (tokens.length < 3) {
      return true;
    }
    return GenericTermList.includes(tokens[0]);
  }
  static isUniqueNamespaceOrShortName(name) {
    name = import_StorageUtilities.default.canonicalizeName(name);
    if (name.length < 3) {
      return false;
    }
    return UniqueRegEx.test(name);
  }
  async generateFromFirstLevelFolderCreator_Game(project, folder, items) {
    if (!folder.isLoaded) {
      await folder.load();
    }
    for (const fileName in folder.files) {
      if (!this.isPerPackCatalogFile(folder.name, fileName)) {
        const file = folder.files[fileName];
        const projectItem = file?.extendedPath ? project.getItemByExtendedOrProjectPath(file?.extendedPath) : void 0;
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            111 /* noLooseFilesInTypeFolder */,
            `Found a loose file in the ${folder.name} folder. Should only see files in the folder ${folder.name}\\creatorshortname_gamename\\`,
            projectItem,
            fileName
          )
        );
      }
    }
    let folderCount = 0;
    for (const folderName in folder.folders) {
      const folderNameCanon = import_StorageUtilities.default.canonicalizeName(folderName);
      folderCount++;
      if (CooperativeAddOnRequirementsGenerator.isUniqueNamespaceOrShortName(folderNameCanon)) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            109 /* noUnsupportedFolderNameInTypeFolder */,
            `Found an add-on-blocked folder '${folderName}' in a parent folder pack\\${folder.name}. Should be named 'creatorshortname' and not a common term`,
            void 0,
            folderName
          )
        );
      }
    }
    if (folderCount > 1) {
      items.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          110 /* moreThanOneFolderInTypeFolder */,
          `Folder '${folder.name}' has more than one subfolder, which is not supported. There should only be one folder in pack\\${folder.name}\\<studioname>_<mygamename>`,
          void 0,
          folder.name
        )
      );
    }
  }
  isPerPackCatalogFile(folderName, fileName) {
    const fileNameCanon = import_StorageUtilities.default.canonicalizeName(fileName);
    if ((folderName !== "functions" || fileNameCanon !== "tick.json") && (folderName !== "textures" || fileNameCanon !== "flipbook_textures.json" && fileNameCanon !== "item_textures.json" && fileNameCanon !== "blocks.json" && fileNameCanon !== "block.json" && fileNameCanon !== "textures_list.json" && fileNameCanon !== "texture_list.json" && fileNameCanon !== "terrain_textures.json" && fileNameCanon !== "item_texture.json" && fileNameCanon !== "terrain_texture.json") && (folderName !== "item_catalog" || fileNameCanon !== "crafting_item_catalog.json") && (folderName !== "sounds" || fileNameCanon !== "sound_definitions.json" && fileNameCanon !== "sounds.json" && fileNameCanon !== "music_definitions.json")) {
      return false;
    }
    return true;
  }
  async generateFromFirstLevelFolderCreatorNameGameName(project, folder, items) {
    if (!folder.isLoaded) {
      await folder.load();
    }
    for (const fileName in folder.files) {
      if (!this.isPerPackCatalogFile(folder.name, fileName)) {
        const file = folder.files[fileName];
        const projectItem = file?.extendedPath ? project.getItemByExtendedOrProjectPath(file?.extendedPath) : void 0;
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            101 /* noLooseFileInTypeFolder */,
            `Found a loose file in the ${folder.name} folder. Should only see files in the folder ${folder.name}\\creatorshortname\\gamename\\`,
            projectItem,
            fileName
          )
        );
      }
    }
    for (const folderName in folder.folders) {
      const folderNameCanon = import_StorageUtilities.default.canonicalizeName(folderName);
      if (CooperativeAddOnRequirementsGenerator.isNameGenericTerm(folderNameCanon)) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            102 /* noCommonNamesInCreatorFolderName */,
            `Found an cooperative add-on common name folder '${folderName}' in a parent folder pack\\${folder.name}. Should be named 'creatorshortname' and not a common term`,
            void 0,
            folderName
          )
        );
      }
      const childFolder = folder.folders[folderName];
      if (childFolder) {
        await this.generateFromSecondLevelFolderGameName(project, folder.name, childFolder, items);
      }
    }
  }
  async generateFromSecondLevelFolderGameName(project, parentFolderName, folder, items) {
    if (!folder.isLoaded) {
      await folder.load();
    }
    for (const fileName in folder.files) {
      const file = folder.files[fileName];
      const projectItem = file?.extendedPath ? project.getItemByExtendedOrProjectPath(file.extendedPath) : void 0;
      items.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          104 /* noLooseFileInCreatorFolder */,
          `Found a loose file '${fileName}' in ${parentFolderName}\\${folder.name}. Files should only be in the folder ${parentFolderName}\\${folder.name}\\<mygamename>`,
          projectItem,
          fileName
        )
      );
    }
    let folderCount = 0;
    for (const childFolderName in folder.folders) {
      const folderNameCanon = import_StorageUtilities.default.canonicalizeName(childFolderName);
      if (folderNameCanon !== "common") {
        folderCount++;
      }
    }
    if (folderCount > 1) {
      items.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          108 /* moreThanOneFolderInCreatorFolderBesidesMaybeCommon */,
          `Secondary folder '${folder.name}' in ${parentFolderName} has more than one subfolder (besides 'common'), which is not supported. There should only be one folder (plus optionally 'common') in pack\\${parentFolderName}\\${folder.name}\\<mygamename>`,
          void 0,
          folder.name
        )
      );
    }
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CooperativeAddOnRequirementsGeneratorTest
});
//# sourceMappingURL=CooperativeAddOnRequirementsGenerator.js.map
