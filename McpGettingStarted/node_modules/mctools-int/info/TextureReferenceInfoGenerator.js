"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TextureReferenceInfoGenerator_exports = {};
__export(TextureReferenceInfoGenerator_exports, {
  TextureReferenceInfoGeneratorTest: () => TextureReferenceInfoGeneratorTest,
  default: () => TextureReferenceInfoGenerator
});
module.exports = __toCommonJS(TextureReferenceInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_IInfoItemData = require("./IInfoItemData");
var import_BlocksCatalogDefinition = __toESM(require("../minecraft/BlocksCatalogDefinition"));
var import_TerrainTextureCatalogDefinition = __toESM(require("../minecraft/TerrainTextureCatalogDefinition"));
var import_ItemTextureCatalogDefinition = __toESM(require("../minecraft/ItemTextureCatalogDefinition"));
var import_ContentIndex = require("../core/ContentIndex");
var TextureReferenceInfoGeneratorTest = /* @__PURE__ */ ((TextureReferenceInfoGeneratorTest2) => {
  TextureReferenceInfoGeneratorTest2[TextureReferenceInfoGeneratorTest2["textureReferences"] = 101] = "textureReferences";
  return TextureReferenceInfoGeneratorTest2;
})(TextureReferenceInfoGeneratorTest || {});
class TextureReferenceInfoGenerator {
  id = "TEXTUREREF";
  title = "Texture Reference Info Aggregation";
  performAddOnValidations = false;
  summarize(info, infoSet) {
    info.textureCount = infoSet.getSummedDataValue(this.id, 101 /* textureReferences */);
  }
  async generate(project, contentIndex) {
    const items = [];
    const textureRefCountPi = new import_ProjectInfoItem.default(
      import_IInfoItemData.InfoItemType.featureAggregate,
      this.id,
      101 /* textureReferences */,
      "Texture References"
    );
    items.push(textureRefCountPi);
    const itemsCopy = project.getItemsCopy();
    for (const projectItem of itemsCopy) {
      if (projectItem.itemType === import_IProjectItemData.ProjectItemType.blocksCatalogResourceJson) {
        if (!projectItem.isContentLoaded) {
          await projectItem.loadContent();
        }
        if (projectItem.primaryFile) {
          const blockCat = await import_BlocksCatalogDefinition.default.ensureOnFile(projectItem.primaryFile);
          if (blockCat && blockCat.blocksCatalog && projectItem.projectPath) {
            const textureRefs = blockCat.getTextureReferences();
            for (const textureRef of textureRefs) {
              contentIndex.insert(
                textureRef,
                projectItem.projectPath,
                import_ContentIndex.AnnotationCategory.blockTextureReferenceDependent
              );
            }
          }
        }
      } else if (projectItem.itemType === import_IProjectItemData.ProjectItemType.terrainTextureCatalogResourceJson) {
        if (!projectItem.isContentLoaded) {
          await projectItem.loadContent();
        }
        if (projectItem.primaryFile) {
          const blockCat = await import_TerrainTextureCatalogDefinition.default.ensureOnFile(projectItem.primaryFile);
          if (blockCat && projectItem.projectPath) {
            const textureRefs = blockCat.getTextureReferences();
            for (const textureRef of textureRefs) {
              contentIndex.insert(textureRef, projectItem.projectPath, import_ContentIndex.AnnotationCategory.blockTextureReferenceSource);
            }
          }
        }
      } else if (projectItem.itemType === import_IProjectItemData.ProjectItemType.itemTextureJson) {
        if (!projectItem.isContentLoaded) {
          await projectItem.loadContent();
        }
        if (projectItem.primaryFile) {
          const itemCat = await import_ItemTextureCatalogDefinition.default.ensureOnFile(projectItem.primaryFile);
          if (itemCat && projectItem.projectPath) {
            const textureRefs = itemCat.getTextureReferences();
            for (const textureRef of textureRefs) {
              contentIndex.insert(textureRef, projectItem.projectPath, import_ContentIndex.AnnotationCategory.itemTextureReferenceSource);
            }
          }
        }
      }
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  TextureReferenceInfoGeneratorTest
});
//# sourceMappingURL=TextureReferenceInfoGenerator.js.map
