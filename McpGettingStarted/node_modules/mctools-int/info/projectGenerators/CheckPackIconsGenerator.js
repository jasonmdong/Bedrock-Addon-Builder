"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CheckPackIconsGenerator_exports = {};
__export(CheckPackIconsGenerator_exports, {
  default: () => CheckPackIconsGenerator
});
module.exports = __toCommonJS(CheckPackIconsGenerator_exports);
var import_ProjectInfoItem = __toESM(require("../ProjectInfoItem"));
var import_Pack = require("../../minecraft/Pack");
var import_IInfoItemData = require("../IInfoItemData");
var import_ImageUtilites = require("../../storage/ImageUtilites");
var CheckPacksIconsGeneratorTest = /* @__PURE__ */ ((CheckPacksIconsGeneratorTest2) => {
  CheckPacksIconsGeneratorTest2[CheckPacksIconsGeneratorTest2["NoIconFound"] = 101] = "NoIconFound";
  CheckPacksIconsGeneratorTest2[CheckPacksIconsGeneratorTest2["MultipleIconsFound"] = 102] = "MultipleIconsFound";
  CheckPacksIconsGeneratorTest2[CheckPacksIconsGeneratorTest2["IconNotValidImage"] = 103] = "IconNotValidImage";
  CheckPacksIconsGeneratorTest2[CheckPacksIconsGeneratorTest2["IconNotValidSize"] = 104] = "IconNotValidSize";
  return CheckPacksIconsGeneratorTest2;
})(CheckPacksIconsGeneratorTest || {});
const IconMaxWidth = 256;
const IconMinWidth = 2;
class CheckPackIconsGenerator {
  id = "CPACKICON";
  title = "Check Pack Icon Generator";
  canAlwaysProcess = true;
  severity = import_IInfoItemData.InfoItemType.error;
  async generate(project) {
    const results = [];
    for (const pack of project.packs) {
      if (requiresPackIcon(pack)) {
        const result = await this.getResultForPack(pack);
        if (result) {
          results.push(result);
        }
      }
    }
    return results;
  }
  summarize() {
  }
  async getResultForPack(pack) {
    const icons = await pack.getFiles(import_ImageUtilites.isPackIcon);
    if (icons.length === 0) {
      const message = `pack_icon image file not found for ${pack.name}. It must use the .png extension.`;
      return this.createResult(101 /* NoIconFound */, message);
    } else if (icons.length > 1) {
      const message = `Found multiple pack icon files for ${pack.name}, there should be only one.`;
      return this.createResult(102 /* MultipleIconsFound */, message);
    }
    const file = icons[0];
    const image = await (0, import_ImageUtilites.parseImageMetadata)(file);
    if (!image?.ImageWidth || !image?.ImageHeight) {
      const message = `Pack Image (${file.name}) is not valid`;
      return this.createResult(103 /* IconNotValidImage */, message);
    }
    if (this.isValidSize(image.ImageWidth, image.ImageHeight)) {
      const message = `pack_icon must be square with size 2, 4, 8, 16, 32, 64, 128, or 256. Found [${image.ImageWidth} x ${image.ImageHeight}] on ${file.name}`;
      return this.createResult(104 /* IconNotValidSize */, message);
    }
    return null;
  }
  createResult(test, message) {
    return new import_ProjectInfoItem.default(this.severity, this.id, test, message);
  }
  isValidSize(width, height) {
    return width !== height || width < IconMinWidth || width > IconMaxWidth || (width & width - 1) !== 0;
  }
}
function requiresPackIcon(pack) {
  return pack.packType !== import_Pack.PackType.skin && pack.packType !== import_Pack.PackType.design;
}
//# sourceMappingURL=CheckPackIconsGenerator.js.map
