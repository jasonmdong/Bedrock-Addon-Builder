"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CheckResourcePackDependenciesGenerator_exports = {};
__export(CheckResourcePackDependenciesGenerator_exports, {
  CheckResourcePackDependenciesGeneratorTest: () => CheckResourcePackDependenciesGeneratorTest,
  default: () => CheckResourcePackDependenciesGenerator
});
module.exports = __toCommonJS(CheckResourcePackDependenciesGenerator_exports);
var import_ProjectInfoItem = __toESM(require("../ProjectInfoItem"));
var import_IInfoItemData = require("../IInfoItemData");
var import_ProjectInfoUtilities = __toESM(require("../ProjectInfoUtilities"));
var import_IProjectItemData = require("../../app/IProjectItemData");
var import_Utilities = __toESM(require("../../core/Utilities"));
var import_BehaviorManifestDefinition = __toESM(require("../../minecraft/BehaviorManifestDefinition"));
var import_ResourceManifestDefinition = __toESM(require("../../minecraft/ResourceManifestDefinition"));
var CheckResourcePackDependenciesGeneratorTest = /* @__PURE__ */ ((CheckResourcePackDependenciesGeneratorTest2) => {
  CheckResourcePackDependenciesGeneratorTest2[CheckResourcePackDependenciesGeneratorTest2["invalidManifestJson"] = 101] = "invalidManifestJson";
  CheckResourcePackDependenciesGeneratorTest2[CheckResourcePackDependenciesGeneratorTest2["missingResourcePackDependency"] = 102] = "missingResourcePackDependency";
  CheckResourcePackDependenciesGeneratorTest2[CheckResourcePackDependenciesGeneratorTest2["internalProcessingError"] = 103] = "internalProcessingError";
  return CheckResourcePackDependenciesGeneratorTest2;
})(CheckResourcePackDependenciesGeneratorTest || {});
class CheckResourcePackDependenciesGenerator {
  id = "RPDEPENDS";
  title = "Resource Pack Dependencies";
  canAlwaysProcess = true;
  summarize(info, infoSet) {
    info.invalidManifestJson = infoSet.getSummedDataValue(
      this.id,
      101 /* invalidManifestJson */
    );
    info.missingResourcePackDependencies = infoSet.getSummedDataValue(
      this.id,
      102 /* missingResourcePackDependency */
    );
    info.internalProcessingErrors = infoSet.getSummedDataValue(
      this.id,
      103 /* internalProcessingError */
    );
  }
  async generate(project) {
    const items = [];
    const projItems = project.getItemsCopy();
    const manifestDependencies = [];
    const availablePacks = [];
    for (const item of projItems) {
      if (item.itemType !== import_IProjectItemData.ProjectItemType.resourcePackManifestJson && item.itemType !== import_IProjectItemData.ProjectItemType.behaviorPackManifestJson) {
        continue;
      }
      if (!item.isContentLoaded) {
        await item.loadContent();
      }
      if (!item.primaryFile) {
        continue;
      }
      if (!item.primaryFile.isContentLoaded) {
        await item.primaryFile.loadContent();
      }
      try {
        if (item.itemType === import_IProjectItemData.ProjectItemType.resourcePackManifestJson) {
          const resourceManifest = await import_ResourceManifestDefinition.default.ensureOnFile(item.primaryFile);
          if (!resourceManifest || !resourceManifest.isLoaded) {
            items.push(
              new import_ProjectInfoItem.default(
                import_IInfoItemData.InfoItemType.error,
                this.id,
                101 /* invalidManifestJson */,
                import_ProjectInfoUtilities.default.getTitleFromEnum(
                  CheckResourcePackDependenciesGeneratorTest,
                  101 /* invalidManifestJson */
                ),
                item
              )
            );
            continue;
          }
          if (resourceManifest.id && import_Utilities.default.isValidUuid(resourceManifest.id)) {
            availablePacks.push({
              uuid: resourceManifest.id,
              manifestItem: item
            });
          }
          if (resourceManifest.dependencies) {
            for (const dependency of resourceManifest.dependencies) {
              if (dependency.uuid && typeof dependency.uuid === "string" && import_Utilities.default.isValidUuid(dependency.uuid)) {
                manifestDependencies.push({
                  uuid: dependency.uuid,
                  manifestItem: item
                });
              }
            }
          }
        } else if (item.itemType === import_IProjectItemData.ProjectItemType.behaviorPackManifestJson) {
          const behaviorManifest = await import_BehaviorManifestDefinition.default.ensureOnFile(item.primaryFile);
          if (!behaviorManifest || !behaviorManifest.isLoaded) {
            items.push(
              new import_ProjectInfoItem.default(
                import_IInfoItemData.InfoItemType.error,
                this.id,
                101 /* invalidManifestJson */,
                import_ProjectInfoUtilities.default.getTitleFromEnum(
                  CheckResourcePackDependenciesGeneratorTest,
                  101 /* invalidManifestJson */
                ),
                item
              )
            );
            continue;
          }
          if (behaviorManifest.id && import_Utilities.default.isValidUuid(behaviorManifest.id)) {
            availablePacks.push({
              uuid: behaviorManifest.id,
              manifestItem: item
            });
          }
        }
      } catch (error) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            103 /* internalProcessingError */,
            `Error processing manifest ${item.name}: ${error}`,
            item
          )
        );
      }
    }
    for (const dependency of manifestDependencies) {
      const foundPack = availablePacks.find((pack) => pack.uuid === dependency.uuid);
      if (!foundPack) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            102 /* missingResourcePackDependency */,
            `Dependency with uuid [${dependency.uuid}] specified but uuid is not located in any included resource or behavior packs.`,
            dependency.manifestItem
          )
        );
      }
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CheckResourcePackDependenciesGeneratorTest
});
//# sourceMappingURL=CheckResourcePackDependenciesGenerator.js.map
