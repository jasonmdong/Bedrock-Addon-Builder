"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CheckWorldPackReferencesGenerator_exports = {};
__export(CheckWorldPackReferencesGenerator_exports, {
  CheckWorldPackReferencesGeneratorTest: () => CheckWorldPackReferencesGeneratorTest,
  default: () => CheckWorldPackReferencesGenerator
});
module.exports = __toCommonJS(CheckWorldPackReferencesGenerator_exports);
var import_ProjectInfoItem = __toESM(require("../ProjectInfoItem"));
var import_IInfoItemData = require("../IInfoItemData");
var import_IProjectItemData = require("../../app/IProjectItemData");
var import_StorageUtilities = __toESM(require("../../storage/StorageUtilities"));
var import_Utilities = __toESM(require("../../core/Utilities"));
var import_ResourceManifestDefinition = __toESM(require("../../minecraft/ResourceManifestDefinition"));
var import_BehaviorManifestDefinition = __toESM(require("../../minecraft/BehaviorManifestDefinition"));
var CheckWorldPackReferencesGeneratorTest = /* @__PURE__ */ ((CheckWorldPackReferencesGeneratorTest2) => {
  CheckWorldPackReferencesGeneratorTest2[CheckWorldPackReferencesGeneratorTest2["invalidWorldPackReferencesJson"] = 201] = "invalidWorldPackReferencesJson";
  CheckWorldPackReferencesGeneratorTest2[CheckWorldPackReferencesGeneratorTest2["missingWorldPackReferencesFile"] = 202] = "missingWorldPackReferencesFile";
  CheckWorldPackReferencesGeneratorTest2[CheckWorldPackReferencesGeneratorTest2["invalidPackId"] = 203] = "invalidPackId";
  CheckWorldPackReferencesGeneratorTest2[CheckWorldPackReferencesGeneratorTest2["missingManifestVersion"] = 204] = "missingManifestVersion";
  CheckWorldPackReferencesGeneratorTest2[CheckWorldPackReferencesGeneratorTest2["invalidManifestVersion"] = 205] = "invalidManifestVersion";
  CheckWorldPackReferencesGeneratorTest2[CheckWorldPackReferencesGeneratorTest2["packReferenceNotFound"] = 206] = "packReferenceNotFound";
  CheckWorldPackReferencesGeneratorTest2[CheckWorldPackReferencesGeneratorTest2["internalProcessingError"] = 207] = "internalProcessingError";
  return CheckWorldPackReferencesGeneratorTest2;
})(CheckWorldPackReferencesGeneratorTest || {});
class CheckWorldPackReferencesGenerator {
  id = "WPACKREFS";
  title = "World Pack References";
  canAlwaysProcess = true;
  summarize(info, infoSet) {
    info.invalidWorldPackReferencesJson = infoSet.getSummedDataValue(
      this.id,
      201 /* invalidWorldPackReferencesJson */
    );
    info.missingWorldPackReferencesFile = infoSet.getSummedDataValue(
      this.id,
      202 /* missingWorldPackReferencesFile */
    );
    info.invalidPackIds = infoSet.getSummedDataValue(this.id, 203 /* invalidPackId */);
    info.missingManifestVersion = infoSet.getSummedDataValue(
      this.id,
      204 /* missingManifestVersion */
    );
    info.invalidManifestVersion = infoSet.getSummedDataValue(
      this.id,
      205 /* invalidManifestVersion */
    );
    info.packReferencesNotFound = infoSet.getSummedDataValue(
      this.id,
      206 /* packReferenceNotFound */
    );
    info.internalProcessingErrors = infoSet.getSummedDataValue(
      this.id,
      207 /* internalProcessingError */
    );
  }
  async generate(project) {
    const items = [];
    const projItems = project.getItemsCopy();
    const availablePacks = [];
    try {
      await this.collectAvailablePacks(projItems, availablePacks, items);
      for (const item of projItems) {
        if (item.itemType === import_IProjectItemData.ProjectItemType.resourcePackListJson) {
          await this.checkPackReferences(item, availablePacks, items);
        } else if (item.itemType === import_IProjectItemData.ProjectItemType.behaviorPackListJson) {
          await this.checkPackReferences(item, availablePacks, items);
        }
      }
    } catch (error) {
      items.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          207 /* internalProcessingError */,
          `Internal processing error: ${error}`,
          void 0
        )
      );
    }
    return items;
  }
  async collectAvailablePacks(projItems, availablePacks, items) {
    for (const item of projItems) {
      if (item.itemType !== import_IProjectItemData.ProjectItemType.resourcePackManifestJson && item.itemType !== import_IProjectItemData.ProjectItemType.behaviorPackManifestJson) {
        continue;
      }
      if (!item.isContentLoaded) {
        await item.loadContent();
      }
      if (!item.primaryFile) {
        continue;
      }
      if (!item.primaryFile.isContentLoaded) {
        await item.primaryFile.loadContent();
      }
      try {
        if (item.itemType === import_IProjectItemData.ProjectItemType.resourcePackManifestJson) {
          const resourceManifest = await import_ResourceManifestDefinition.default.ensureOnFile(item.primaryFile);
          if (resourceManifest && resourceManifest.id && import_Utilities.default.isValidUuid(resourceManifest.id)) {
            availablePacks.push({
              uuid: resourceManifest.id,
              manifestItem: item
            });
          }
        } else if (item.itemType === import_IProjectItemData.ProjectItemType.behaviorPackManifestJson) {
          const behaviorManifest = await import_BehaviorManifestDefinition.default.ensureOnFile(item.primaryFile);
          if (behaviorManifest && behaviorManifest.id && import_Utilities.default.isValidUuid(behaviorManifest.id)) {
            availablePacks.push({
              uuid: behaviorManifest.id,
              manifestItem: item
            });
          }
        }
      } catch (error) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            207 /* internalProcessingError */,
            `Error processing manifest ${item.name}: ${error}`,
            item
          )
        );
      }
    }
  }
  async checkPackReferences(packReferencesFile, availablePacks, items) {
    if (!packReferencesFile.isContentLoaded) {
      await packReferencesFile.loadContent();
    }
    if (!packReferencesFile.primaryFile) {
      return;
    }
    if (!packReferencesFile.primaryFile.isContentLoaded) {
      await packReferencesFile.primaryFile.loadContent();
    }
    const parsedContent = import_StorageUtilities.default.getJsonObject(packReferencesFile.primaryFile);
    if (!parsedContent || !Array.isArray(parsedContent)) {
      items.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          201 /* invalidWorldPackReferencesJson */,
          `Invalid JSON format in ${packReferencesFile.name}. Expected an array of pack references.`,
          packReferencesFile
        )
      );
      return;
    }
    const packRefsFoundInJson = [];
    for (let i = 0; i < parsedContent.length; i++) {
      const packRef = parsedContent[i];
      if (typeof packRef !== "object" || packRef === null) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            201 /* invalidWorldPackReferencesJson */,
            `Invalid pack reference object at index ${i} in ${packReferencesFile.name}`,
            packReferencesFile
          )
        );
        continue;
      }
      if (!packRef.pack_id || typeof packRef.pack_id !== "string") {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            203 /* invalidPackId */,
            `Missing or invalid pack_id at index ${i} in ${packReferencesFile.name}`,
            packReferencesFile
          )
        );
        continue;
      }
      if (!import_Utilities.default.isValidUuid(packRef.pack_id)) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            203 /* invalidPackId */,
            `Invalid UUID format for pack_id [${packRef.pack_id}] at index ${i} in ${packReferencesFile.name}`,
            packReferencesFile
          )
        );
        continue;
      }
      packRefsFoundInJson.push(packRef.pack_id);
      const versionValidation = import_ResourceManifestDefinition.default.validatePackReferenceVersion(packRef.version);
      if (!versionValidation.isValid) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            205 /* invalidManifestVersion */,
            `${versionValidation.errorMessage} for pack_id [${packRef.pack_id}] at index ${i} in ${packReferencesFile.name}`,
            packReferencesFile
          )
        );
        continue;
      }
    }
    for (const packRef of packRefsFoundInJson) {
      const foundPack = availablePacks.find((pack) => pack.uuid === packRef);
      if (!foundPack) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            206 /* packReferenceNotFound */,
            `Pack reference [${packRef}] not found in project.`,
            packReferencesFile
          )
        );
      }
    }
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CheckWorldPackReferencesGeneratorTest
});
//# sourceMappingURL=CheckWorldPackReferencesGenerator.js.map
