"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var SoundDefinitionsInfoGenerator_exports = {};
__export(SoundDefinitionsInfoGenerator_exports, {
  SoundsDefinitionInfoGeneratorTest: () => SoundsDefinitionInfoGeneratorTest,
  default: () => SoundsDefinitionInfoGenerator
});
module.exports = __toCommonJS(SoundDefinitionsInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("../ProjectInfoItem"));
var import_IInfoItemData = require("../IInfoItemData");
var import_ProjectInfoUtilities = __toESM(require("../ProjectInfoUtilities"));
var import_IProjectItemData = require("../../app/IProjectItemData");
var import_ISoundDefinitionCatalog = require("../../minecraft/ISoundDefinitionCatalog");
var import_zod = require("zod");
var import_StorageUtilities = __toESM(require("../../storage/StorageUtilities"));
var SoundsDefinitionInfoGeneratorTest = /* @__PURE__ */ ((SoundsDefinitionInfoGeneratorTest2) => {
  SoundsDefinitionInfoGeneratorTest2[SoundsDefinitionInfoGeneratorTest2["multipleSoundsDefinitionManifests"] = 101] = "multipleSoundsDefinitionManifests";
  SoundsDefinitionInfoGeneratorTest2[SoundsDefinitionInfoGeneratorTest2["invalidSoundsDefinitionManifest"] = 102] = "invalidSoundsDefinitionManifest";
  SoundsDefinitionInfoGeneratorTest2[SoundsDefinitionInfoGeneratorTest2["soundsDefinitionManifestInvalidJson"] = 103] = "soundsDefinitionManifestInvalidJson";
  SoundsDefinitionInfoGeneratorTest2[SoundsDefinitionInfoGeneratorTest2["foundALooseSoundDefinition"] = 104] = "foundALooseSoundDefinition";
  return SoundsDefinitionInfoGeneratorTest2;
})(SoundsDefinitionInfoGeneratorTest || {});
class SoundsDefinitionInfoGenerator {
  id = "SNDSDEF";
  title = "Sounds Definition Manifest Validation";
  summarize(info, infoSet) {
    info.multipleSoundsManifests = infoSet.getSummedDataValue(
      this.id,
      101 /* multipleSoundsDefinitionManifests */
    );
    info.soundsDefinitionManifestFormatIsValid = infoSet.getSummedDataValue(
      this.id,
      102 /* invalidSoundsDefinitionManifest */
    );
    info.soundsDefinitionManifestIsNotValidJson = infoSet.getSummedDataValue(
      this.id,
      103 /* soundsDefinitionManifestInvalidJson */
    );
  }
  async generate(project) {
    const items = [];
    const projItems = project.getItemsCopy();
    const packsWithSoundManifests = {};
    for (const item of projItems) {
      if (item.itemType !== import_IProjectItemData.ProjectItemType.soundDefinitionCatalog && item.itemType) {
        continue;
      }
      const pack = await item.getPack();
      if (!pack || !pack.projectItem.projectPath) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            104 /* foundALooseSoundDefinition */,
            import_ProjectInfoUtilities.default.getTitleFromEnum(
              SoundsDefinitionInfoGeneratorTest,
              104 /* foundALooseSoundDefinition */
            ),
            item
          )
        );
        continue;
      }
      if (packsWithSoundManifests[pack.projectItem.projectPath]) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            101 /* multipleSoundsDefinitionManifests */,
            import_ProjectInfoUtilities.default.getTitleFromEnum(
              SoundsDefinitionInfoGeneratorTest,
              101 /* multipleSoundsDefinitionManifests */
            ),
            item
          )
        );
        continue;
      } else {
        packsWithSoundManifests[pack.projectItem.projectPath] = true;
      }
      if (!item.isContentLoaded) {
        await item.loadContent();
      }
      if (!item.primaryFile) {
        continue;
      }
      if (!item.primaryFile.isContentLoaded) {
        await item.primaryFile.loadContent();
      }
      const content = item.primaryFile.content;
      if (!content || typeof content !== "string") {
        continue;
      }
      const parsedContent = import_StorageUtilities.default.getJsonObject(item.primaryFile);
      if (!parsedContent) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            103 /* soundsDefinitionManifestInvalidJson */,
            import_ProjectInfoUtilities.default.getTitleFromEnum(
              SoundsDefinitionInfoGeneratorTest,
              103 /* soundsDefinitionManifestInvalidJson */
            ),
            item
          )
        );
        continue;
      }
      items.push(...this.getIsManifestValidResult(parsedContent));
    }
    return items;
  }
  // Uses a union to check for both possible versions of a Sound Manifest (per comment in ISoundDefinitionCatalog)
  getIsManifestValidResult(fileContent) {
    const results = [];
    const parseResult = import_ISoundDefinitionCatalog.SoundDefinitionCatalogSchema.safeParse(fileContent);
    if (!parseResult.success) {
      const parsedErrors = [];
      for (const issue of parseResult.error.issues) {
        if (issue.code === import_zod.ZodIssueCode.invalid_union) {
          for (const e of issue.unionErrors) {
            for (const i of e.issues) {
              parsedErrors.push(`${i.message} at ${i.path.join("/")}`);
            }
          }
        }
      }
      results.push(
        new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          102 /* invalidSoundsDefinitionManifest */,
          `${import_ProjectInfoUtilities.default.getTitleFromEnum(
            SoundsDefinitionInfoGeneratorTest,
            102 /* invalidSoundsDefinitionManifest */
          )}: ${parsedErrors.join(", ")}`
        )
      );
    }
    return results;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  SoundsDefinitionInfoGeneratorTest
});
//# sourceMappingURL=SoundDefinitionsInfoGenerator.js.map
