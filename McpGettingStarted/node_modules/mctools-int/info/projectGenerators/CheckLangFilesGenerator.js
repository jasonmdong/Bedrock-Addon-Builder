"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CheckLangFilesGenerator_exports = {};
__export(CheckLangFilesGenerator_exports, {
  default: () => CheckLangFilesGenerator
});
module.exports = __toCommonJS(CheckLangFilesGenerator_exports);
var import_IProjectItemData = require("../../app/IProjectItemData");
var import_StorageUtilities = __toESM(require("../../storage/StorageUtilities"));
var import_TestDefinition = require("../tests/TestDefinition");
var import_LocalizationCatalog = require("../../app/localization/LocalizationCatalog");
const Tests = {
  MissingLanguagesJson: { id: 101, title: "languages.json Not Found" },
  PrimaryLangMissing: { id: 102, title: "en_US lang code is required." },
  FailedToParseFile: { id: 103, title: "Failed To Parse File" },
  LangFileMissing: {
    id: 104,
    title: "Lang File Missing",
    defaultMessage: "All entries in languages.json must have corresponding .lang file."
  },
  ExtraLangFile: {
    id: 105,
    title: "Lang File Without Catalog Entry",
    defaultMessage: ".lang file exists in pack but its lang code is not referenced in languages.json"
  }
};
class CheckLangFilesGenerator {
  id = "LANGFILES";
  title = "Check Lang File Generator";
  canAlwaysProcess = true;
  async generate(project) {
    const results = await Promise.all(project.packs.map((pack) => this.validatePack(pack)));
    return results.flat();
  }
  async validatePack(pack) {
    const items = pack.getPackItems();
    const catalogItem = items.find((item) => item.itemType === import_IProjectItemData.ProjectItemType.languagesCatalogJson);
    const langItems = items.filter((item) => item.itemType === import_IProjectItemData.ProjectItemType.lang);
    if (!catalogItem) {
      return [(0, import_TestDefinition.resultFromTest)(Tests.MissingLanguagesJson, { id: this.id })];
    }
    const [catalog, parseErrors] = await (0, import_LocalizationCatalog.parseLocalizationCatalogFromItem)(catalogItem);
    if (parseErrors) {
      return parseErrors.map((error) => ({ id: this.id, message: error.message, item: catalogItem })).map((data) => (0, import_TestDefinition.resultFromTest)(Tests.FailedToParseFile, data));
    }
    if (!catalog.langs.find((lang) => lang === "en_US")) {
      return [(0, import_TestDefinition.resultFromTest)(Tests.PrimaryLangMissing, { id: this.id, item: catalogItem })];
    }
    const allLangItems = langItems.map((item) => [item, import_StorageUtilities.default.getBaseFromName(item.name)]);
    const knownLangFileLangs = new Set(allLangItems.map(([, lang]) => lang));
    const knownCatalogLangs = new Set(catalog.langs);
    const langFileNotInCatalogResults = allLangItems.filter(([, lang]) => !knownCatalogLangs.has(lang)).map(([item]) => (0, import_TestDefinition.resultFromTest)(Tests.ExtraLangFile, { id: this.id, item }));
    const catalogLangNotInFile = catalog.langs.filter((lang) => !knownLangFileLangs.has(lang)).map((lang) => (0, import_TestDefinition.resultFromTest)(Tests.LangFileMissing, { id: this.id, data: lang, item: catalogItem }));
    return [...langFileNotInCatalogResults, ...catalogLangNotInFile];
  }
  summarize() {
  }
}
//# sourceMappingURL=CheckLangFilesGenerator.js.map
