"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CheckParticleIdentifierGenerator_exports = {};
__export(CheckParticleIdentifierGenerator_exports, {
  default: () => CheckParticleIdentifierGenerator
});
module.exports = __toCommonJS(CheckParticleIdentifierGenerator_exports);
var import_SemanticVersion = __toESM(require("../../core/versioning/SemanticVersion"));
var import_StorageUtilities = __toESM(require("../../storage/StorageUtilities"));
var import_ProjectInfoItem = __toESM(require("../ProjectInfoItem"));
var import_IInfoItemData = require("../IInfoItemData");
var import_IProjectItemData = require("../../app/IProjectItemData");
const MinVersionForCheck = new import_SemanticVersion.default(1, 20, 60);
const NamespaceRegexPattern = /^\\w{2,}:\\w+/;
var CheckParticleIdentifier = /* @__PURE__ */ ((CheckParticleIdentifier2) => {
  CheckParticleIdentifier2[CheckParticleIdentifier2["FailedToReadFile"] = 101] = "FailedToReadFile";
  CheckParticleIdentifier2[CheckParticleIdentifier2["FailedToReadVersion"] = 102] = "FailedToReadVersion";
  CheckParticleIdentifier2[CheckParticleIdentifier2["InvalidParticleIdentifier"] = 103] = "InvalidParticleIdentifier";
  return CheckParticleIdentifier2;
})(CheckParticleIdentifier || {});
class CheckParticleIdentifierGenerator {
  id = "CPARTI";
  title = "Check Particle Identifier Generator";
  canAlwaysProcess = true;
  severity = import_IInfoItemData.InfoItemType.error;
  async generate(project) {
    const results = [];
    const particleItems = project.getItemsByType(import_IProjectItemData.ProjectItemType.particleJson);
    for (const particleItem of particleItems) {
      if (!particleItem.isContentLoaded) {
        await particleItem.loadContent();
      }
      if (particleItem.primaryFile) {
        const packResults = await this.getResultsForFile(particleItem.primaryFile, particleItem);
        results.push(...packResults);
      }
    }
    return results;
  }
  async getResultsForFile(file, particleItem) {
    const json = await import_StorageUtilities.default.getJsonObject(file);
    if (!json) {
      const message = `Failed to read file: ${file.name}`;
      return [this.createResult(101 /* FailedToReadFile */, message, particleItem)];
    }
    const version = import_SemanticVersion.default.parse(json.format_version);
    if (!version) {
      const message = `'format_version' expected in json in file: ${file.name}`;
      return [this.createResult(102 /* FailedToReadVersion */, message, particleItem)];
    }
    const checkNeeded = version.compareTo(MinVersionForCheck) >= 0;
    if (!checkNeeded) {
      return [];
    }
    const indentifier = json.particle_effect?.description?.identifier;
    if (!isValidParticleIdentifier(indentifier)) {
      const message = "Invalid Particle Identifier. Particle identifier requires a namespace.";
      return [this.createResult(103 /* InvalidParticleIdentifier */, message, particleItem)];
    }
    return [];
  }
  createResult(test, message, particleItem) {
    return new import_ProjectInfoItem.default(this.severity, this.id, test, message, particleItem);
  }
  summarize() {
  }
}
function isValidParticleIdentifier(identifier) {
  return identifier && NamespaceRegexPattern.test(identifier);
}
//# sourceMappingURL=CheckParticleIdentifierGenerator.js.map
