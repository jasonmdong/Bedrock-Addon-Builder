"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CheckProjectIntegrityGenerator_exports = {};
__export(CheckProjectIntegrityGenerator_exports, {
  default: () => CheckProjectIntegrityGenerator
});
module.exports = __toCommonJS(CheckProjectIntegrityGenerator_exports);
var import_TestDefinition = require("../tests/TestDefinition");
const MaxOrphanFileResults = 5;
var CheckIntegrityTest = /* @__PURE__ */ ((CheckIntegrityTest2) => {
  CheckIntegrityTest2["OrphanedFile"] = "OrphanedFile";
  CheckIntegrityTest2["UnexpectedManifest"] = "UnexpectedManifest";
  return CheckIntegrityTest2;
})(CheckIntegrityTest || {});
const CheckIntegrityTests = {
  OrphanedFile: {
    id: 101,
    title: "Extraneous Files Or Folder",
    defaultMessage: "Project contains extraneous file or folder"
  },
  UnexpectedManifest: {
    id: 102,
    title: "Unexpected Manifest Structure",
    defaultMessage: "Pack has an unexpected structure, multiple manifests detected. Nested manifests are not allowed."
  }
};
class CheckProjectIntegrityGenerator {
  id = "PRJINT";
  title = "Check Project Integrity Generator";
  canAlwaysProcess = true;
  generate(project) {
    const orphanResults = this.checkOrphanedFiles(project);
    const manifestResults = this.checkNestedManifests(project);
    const results = [...orphanResults, ...manifestResults];
    return Promise.resolve(results);
  }
  checkOrphanedFiles(project) {
    const results = project.unknownFiles.slice(0, MaxOrphanFileResults).map((file) => (0, import_TestDefinition.resultFromTest)(CheckIntegrityTests.OrphanedFile, { id: this.id, data: file.extendedPath }));
    return results;
  }
  checkNestedManifests(project) {
    const results = project.packs.map((pack) => [pack, pack.getPackItems().filter((item) => item.name === "manifest.json")]).filter(([_pack, manifests]) => manifests.length > 1).map(([pack]) => (0, import_TestDefinition.resultFromTest)(CheckIntegrityTests.UnexpectedManifest, { id: this.id, data: pack.name }));
    return results;
  }
  summarize() {
  }
}
//# sourceMappingURL=CheckProjectIntegrityGenerator.js.map
