"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CheckGeometryFormatInfoGenerator_exports = {};
__export(CheckGeometryFormatInfoGenerator_exports, {
  CheckGeometryFormatInfoGeneratorTest: () => CheckGeometryFormatInfoGeneratorTest,
  default: () => CheckGeometryFormatInfoGenerator
});
module.exports = __toCommonJS(CheckGeometryFormatInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("../ProjectInfoItem"));
var import_IInfoItemData = require("../IInfoItemData");
var import_IProjectItemData = require("../../app/IProjectItemData");
var import_StorageUtilities = __toESM(require("../../storage/StorageUtilities"));
var CheckGeometryFormatInfoGeneratorTest = /* @__PURE__ */ ((CheckGeometryFormatInfoGeneratorTest2) => {
  CheckGeometryFormatInfoGeneratorTest2[CheckGeometryFormatInfoGeneratorTest2["restrictedPolyMeshFound"] = 101] = "restrictedPolyMeshFound";
  CheckGeometryFormatInfoGeneratorTest2[CheckGeometryFormatInfoGeneratorTest2["jsonParseError"] = 102] = "jsonParseError";
  return CheckGeometryFormatInfoGeneratorTest2;
})(CheckGeometryFormatInfoGeneratorTest || {});
class CheckGeometryFormatInfoGenerator {
  id = "GEOFMT";
  title = "Geometry Format";
  canAlwaysProcess = true;
  summarize(info, infoSet) {
    info.restrictedPolyMeshFound = infoSet.getSummedDataValue(
      this.id,
      101 /* restrictedPolyMeshFound */
    );
    info.jsonParseError = infoSet.getSummedDataValue(this.id, 102 /* jsonParseError */);
  }
  async generate(project) {
    const items = [];
    if (project.isMinecraftCreator === false) {
      const projItems = project.getItemsCopy();
      for (const item of projItems) {
        if (item.itemType === import_IProjectItemData.ProjectItemType.modelGeometryJson) {
          if (!item.loadContent) {
            await item.loadContent();
          }
          if (!item.primaryFile) {
            continue;
          }
          try {
            const jsonData = import_StorageUtilities.default.getJsonObject(item.primaryFile);
            if (jsonData && jsonData["minecraft:geometry"]) {
              const geometries = Array.isArray(jsonData["minecraft:geometry"]) ? jsonData["minecraft:geometry"] : [jsonData["minecraft:geometry"]];
              for (const geometry of geometries) {
                if (geometry && geometry.bones) {
                  for (const bone of geometry.bones) {
                    if (bone && bone.poly_mesh) {
                      items.push(
                        new import_ProjectInfoItem.default(
                          import_IInfoItemData.InfoItemType.error,
                          this.id,
                          101 /* restrictedPolyMeshFound */,
                          `Geometry bone "${bone.name || "unnamed"}" contains poly_mesh definition. This feature is not allowed!`,
                          item,
                          bone.name || "unnamed bone"
                        )
                      );
                    }
                  }
                }
              }
            }
          } catch (error) {
            items.push(
              new import_ProjectInfoItem.default(
                import_IInfoItemData.InfoItemType.error,
                this.id,
                102 /* jsonParseError */,
                `Failed to parse JSON for geometry file. Error: ${error}`,
                item
              )
            );
          }
        }
      }
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CheckGeometryFormatInfoGeneratorTest
});
//# sourceMappingURL=CheckGeometryFormatInfoGenerator.js.map
