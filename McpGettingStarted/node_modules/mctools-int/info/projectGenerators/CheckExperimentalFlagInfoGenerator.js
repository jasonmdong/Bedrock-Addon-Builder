"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CheckExperimentalFlagInfoGenerator_exports = {};
__export(CheckExperimentalFlagInfoGenerator_exports, {
  CheckExperimentalFlagInfoGeneratorTest: () => CheckExperimentalFlagInfoGeneratorTest,
  default: () => CheckExperimentalFlagInfoGenerator
});
module.exports = __toCommonJS(CheckExperimentalFlagInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("../ProjectInfoItem"));
var import_IInfoItemData = require("../IInfoItemData");
var import_IProjectItemData = require("../../app/IProjectItemData");
var import_MCWorld = __toESM(require("../../minecraft/MCWorld"));
var import_WorldLevelDat = __toESM(require("../../minecraft/WorldLevelDat"));
var CheckExperimentalFlagInfoGeneratorTest = /* @__PURE__ */ ((CheckExperimentalFlagInfoGeneratorTest2) => {
  CheckExperimentalFlagInfoGeneratorTest2[CheckExperimentalFlagInfoGeneratorTest2["flagIsOrWasTrue"] = 101] = "flagIsOrWasTrue";
  CheckExperimentalFlagInfoGeneratorTest2[CheckExperimentalFlagInfoGeneratorTest2["levelDatNotFound"] = 102] = "levelDatNotFound";
  CheckExperimentalFlagInfoGeneratorTest2[CheckExperimentalFlagInfoGeneratorTest2["worldNotFound"] = 103] = "worldNotFound";
  return CheckExperimentalFlagInfoGeneratorTest2;
})(CheckExperimentalFlagInfoGeneratorTest || {});
class CheckExperimentalFlagInfoGenerator {
  id = "EXPFLAG";
  title = "Check Experimental Flag";
  summarize(info, infoSet) {
    info.experimentalFlagIsOrWasTrue = infoSet.getSummedDataValue(
      this.id,
      101 /* flagIsOrWasTrue */
    );
    info.levelDatNotFound = infoSet.getSummedDataValue(
      this.id,
      102 /* levelDatNotFound */
    );
    info.worldNotFound = infoSet.getSummedDataValue(this.id, 103 /* worldNotFound */);
  }
  async generate(project) {
    const items = [];
    const projItems = project.getItemsCopy();
    for (const item of projItems) {
      if (item.itemType !== import_IProjectItemData.ProjectItemType.MCWorld && item.itemType !== import_IProjectItemData.ProjectItemType.MCTemplate && item.itemType !== import_IProjectItemData.ProjectItemType.worldFolder && item.itemType !== import_IProjectItemData.ProjectItemType.levelDat && item.itemType !== import_IProjectItemData.ProjectItemType.levelDatOld) {
        continue;
      }
      let levelDat = void 0;
      if (item.itemType === import_IProjectItemData.ProjectItemType.MCWorld || item.itemType === import_IProjectItemData.ProjectItemType.MCTemplate || item.itemType === import_IProjectItemData.ProjectItemType.worldFolder) {
        const mcworld = await import_MCWorld.default.ensureOnItem(item);
        if (!mcworld) {
          items.push(
            new import_ProjectInfoItem.default(
              import_IInfoItemData.InfoItemType.warning,
              this.id,
              103 /* worldNotFound */,
              "Could not load world.",
              item
            )
          );
          continue;
        }
        await mcworld.loadMetaFiles();
        if (!mcworld.levelData) {
          items.push(
            new import_ProjectInfoItem.default(
              import_IInfoItemData.InfoItemType.warning,
              this.id,
              102 /* levelDatNotFound */,
              "Level.dat not found in a broader world file.",
              item
            )
          );
          continue;
        }
        levelDat = mcworld.levelData;
      } else if (item.itemType === import_IProjectItemData.ProjectItemType.levelDat || item.itemType === import_IProjectItemData.ProjectItemType.levelDatOld) {
        if (!item.isContentLoaded) {
          await item.loadContent();
        }
        if (item.primaryFile && item.primaryFile.content && item.primaryFile.content instanceof Uint8Array) {
          levelDat = new import_WorldLevelDat.default();
          levelDat.loadFromNbtBytes(item.primaryFile.content);
        }
      }
      if (levelDat && (levelDat.experimentalGameplay === true || levelDat.experimentsEverUsed === true)) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.warning,
            this.id,
            101 /* flagIsOrWasTrue */,
            "Experimental gameplay is or was enabled in this world. Shareable content should not use experimental features.",
            item
          )
        );
      }
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CheckExperimentalFlagInfoGeneratorTest
});
//# sourceMappingURL=CheckExperimentalFlagInfoGenerator.js.map
