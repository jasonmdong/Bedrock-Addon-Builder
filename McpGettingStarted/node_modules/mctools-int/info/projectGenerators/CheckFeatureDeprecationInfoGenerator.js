"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CheckFeatureDeprecationInfoGenerator_exports = {};
__export(CheckFeatureDeprecationInfoGenerator_exports, {
  CheckFeatureDeprecationInfoGeneratorTest: () => CheckFeatureDeprecationInfoGeneratorTest,
  default: () => CheckFeatureDeprecationInfoGenerator
});
module.exports = __toCommonJS(CheckFeatureDeprecationInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("../ProjectInfoItem"));
var import_IInfoItemData = require("../IInfoItemData");
var import_StorageUtilities = __toESM(require("../../storage/StorageUtilities"));
var CheckFeatureDeprecationInfoGeneratorTest = /* @__PURE__ */ ((CheckFeatureDeprecationInfoGeneratorTest2) => {
  CheckFeatureDeprecationInfoGeneratorTest2[CheckFeatureDeprecationInfoGeneratorTest2["deprecatedBlockOverride"] = 101] = "deprecatedBlockOverride";
  CheckFeatureDeprecationInfoGeneratorTest2[CheckFeatureDeprecationInfoGeneratorTest2["deprecatedTerrainTexture"] = 102] = "deprecatedTerrainTexture";
  CheckFeatureDeprecationInfoGeneratorTest2[CheckFeatureDeprecationInfoGeneratorTest2["deprecatedTexture"] = 103] = "deprecatedTexture";
  CheckFeatureDeprecationInfoGeneratorTest2[CheckFeatureDeprecationInfoGeneratorTest2["jsonParseError"] = 104] = "jsonParseError";
  return CheckFeatureDeprecationInfoGeneratorTest2;
})(CheckFeatureDeprecationInfoGeneratorTest || {});
const DEPRECATED_BLOCKS = ["fletching_table", "smithing_table"];
const DEPRECATED_TEXTURES = [
  "smithing_table_top.png",
  "smithing_table_side1.png",
  "smithing_table_side2.png",
  "fletcher_table_top.png",
  "fletcher_table_side1.png",
  "fletcher_table_side2.png"
];
const DEPRECATED_TEXTURE_ENTRIES = [
  "smithing_table_top",
  "smithing_table_side_a",
  "smithing_table_side_b",
  "fletching_table_top",
  "fletching_table_side1",
  "fletching_table_side2"
];
class CheckFeatureDeprecationInfoGenerator {
  id = "CHECKFEATUREDEPRECATION";
  title = "Check Feature Deprecation";
  summarize(info, infoSet) {
    info.deprecatedBlockOverride = infoSet.getSummedDataValue(
      this.id,
      101 /* deprecatedBlockOverride */
    );
    info.deprecatedTerrainTexture = infoSet.getSummedDataValue(
      this.id,
      102 /* deprecatedTerrainTexture */
    );
    info.deprecatedTexture = infoSet.getSummedDataValue(
      this.id,
      103 /* deprecatedTexture */
    );
  }
  async generate(project) {
    const items = [];
    const projItems = project.getItemsCopy();
    for (const item of projItems) {
      if (item.name === "blocks.json") {
        if (!item.isContentLoaded) {
          await item.loadContent();
        }
        if (!item.primaryFile) {
          continue;
        }
        if (!item.primaryFile.isContentLoaded) {
          await item.primaryFile.loadContent();
        }
        const content = item.primaryFile.content;
        if (!content || typeof content !== "string") {
          continue;
        }
        try {
          const parsedContent = import_StorageUtilities.default.getJsonObject(item.primaryFile);
          if (parsedContent) {
            for (const deprecatedBlock of DEPRECATED_BLOCKS) {
              if (parsedContent[deprecatedBlock]) {
                items.push(
                  new import_ProjectInfoItem.default(
                    import_IInfoItemData.InfoItemType.warning,
                    this.id,
                    101 /* deprecatedBlockOverride */,
                    `Entity [${deprecatedBlock}] will be affected in an upcoming client update.`,
                    item,
                    deprecatedBlock
                  )
                );
              }
            }
          }
        } catch (error) {
          items.push(
            new import_ProjectInfoItem.default(
              import_IInfoItemData.InfoItemType.warning,
              this.id,
              104 /* jsonParseError */,
              `Failed to parse JSON for entity. Error: ${error}`,
              item
            )
          );
        }
      }
      if (item.name === "terrain_texture.json") {
        if (!item.isContentLoaded) {
          await item.loadContent();
        }
        if (!item.primaryFile) {
          continue;
        }
        if (!item.primaryFile.isContentLoaded) {
          await item.primaryFile.loadContent();
        }
        const content = item.primaryFile.content;
        if (!content || typeof content !== "string") {
          continue;
        }
        const parsedContent = import_StorageUtilities.default.getJsonObject(item.primaryFile);
        try {
          if (parsedContent) {
            for (const deprecatedTexture of DEPRECATED_TEXTURE_ENTRIES) {
              if (parsedContent.texture_data[deprecatedTexture]) {
                items.push(
                  new import_ProjectInfoItem.default(
                    import_IInfoItemData.InfoItemType.warning,
                    this.id,
                    102 /* deprecatedTerrainTexture */,
                    `Entity [${deprecatedTexture}] will be affected in an upcoming client update.`,
                    item,
                    deprecatedTexture
                  )
                );
              }
            }
          }
        } catch (error) {
          items.push(
            new import_ProjectInfoItem.default(
              import_IInfoItemData.InfoItemType.warning,
              this.id,
              104 /* jsonParseError */,
              `Failed to parse JSON for entity. Error: ${error}`,
              item
            )
          );
        }
      }
      if (item.getFolder()?.name === "blocks") {
        if (DEPRECATED_TEXTURES.includes(item.name)) {
          items.push(
            new import_ProjectInfoItem.default(
              import_IInfoItemData.InfoItemType.warning,
              this.id,
              103 /* deprecatedTexture */,
              `Texture [${item.name}] will be affected in an upcoming client update. Please resubmit with no modifications to this texture.`,
              item,
              item.name
            )
          );
        }
      }
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CheckFeatureDeprecationInfoGeneratorTest
});
//# sourceMappingURL=CheckFeatureDeprecationInfoGenerator.js.map
