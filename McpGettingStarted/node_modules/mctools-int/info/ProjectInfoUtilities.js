"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ProjectInfoUtilities_exports = {};
__export(ProjectInfoUtilities_exports, {
  default: () => ProjectInfoUtilities
});
module.exports = __toCommonJS(ProjectInfoUtilities_exports);
var import_ProjectUtilities = __toESM(require("../app/ProjectUtilities"));
var import_Utilities = __toESM(require("../core/Utilities"));
var import_IProjectInfoData = require("./IProjectInfoData");
var import_ProjectInfoSet = __toESM(require("./ProjectInfoSet"));
class ProjectInfoUtilities {
  static _generalTopicsById = {
    0: "Test Failure",
    1: "Test Success",
    2: "Test Not Applicable"
  };
  static getTitleFromEnum(categoryEnum, topicId) {
    if (categoryEnum[topicId]) {
      return import_Utilities.default.humanifyJsName(categoryEnum[topicId]);
    }
    const generalTitle = this._generalTopicsById[topicId];
    if (!!generalTitle) {
      return generalTitle;
    }
    return "General Item " + topicId;
  }
  static getGeneralTopicTitle(topicId) {
    return this._generalTopicsById[topicId];
  }
  static async getDerivedStates(project, pisData) {
    const isAddon = await import_ProjectUtilities.default.getIsAddon(project);
    const derivedStates = [];
    if (isAddon) {
      const pisAddon = new import_ProjectInfoSet.default(project, import_IProjectInfoData.ProjectInfoSuite.cooperativeAddOn);
      await pisAddon.generateForProject();
      const projectSet = {
        projectContainerName: project.containerName,
        projectPath: project.projectFolder?.storageRelativePath,
        projectName: project.name,
        projectTitle: project.title,
        infoSetData: pisAddon.getDataObject(),
        suite: import_IProjectInfoData.ProjectInfoSuite.cooperativeAddOn
      };
      derivedStates.push(projectSet);
    }
    const pisSharing = new import_ProjectInfoSet.default(project, import_IProjectInfoData.ProjectInfoSuite.sharing);
    await pisSharing.generateForProject();
    const sharing = {
      projectContainerName: project.containerName,
      projectPath: project.projectFolder?.storageRelativePath,
      projectName: project.name,
      projectTitle: project.title,
      infoSetData: pisSharing.getDataObject(),
      suite: import_IProjectInfoData.ProjectInfoSuite.cooperativeAddOn
    };
    derivedStates.push(sharing);
    const shouldRunPlatformVersion = pisData.info["CWave"] !== void 0;
    if (shouldRunPlatformVersion) {
      const pisPlatforMVersion = new import_ProjectInfoSet.default(project, import_IProjectInfoData.ProjectInfoSuite.currentPlatformVersions);
      await pisPlatforMVersion.generateForProject();
      const projectSet = {
        projectContainerName: project.containerName,
        projectPath: project.projectFolder?.storageRelativePath,
        projectName: project.name,
        projectTitle: project.title,
        infoSetData: pisPlatforMVersion.getDataObject(),
        suite: import_IProjectInfoData.ProjectInfoSuite.currentPlatformVersions
      };
      derivedStates.push(projectSet);
    }
    return derivedStates;
  }
}
//# sourceMappingURL=ProjectInfoUtilities.js.map
