"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var SharingInfoGenerator_exports = {};
__export(SharingInfoGenerator_exports, {
  SharingInfoGeneratorTest: () => SharingInfoGeneratorTest,
  default: () => SharingInfoGenerator
});
module.exports = __toCommonJS(SharingInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_IInfoItemData = require("./IInfoItemData");
var import_IProjectItemData2 = require("../app/IProjectItemData");
var import_ProjectItemUtilities = __toESM(require("../app/ProjectItemUtilities"));
var import_BehaviorManifestDefinition = __toESM(require("../minecraft/BehaviorManifestDefinition"));
var SharingInfoGeneratorTest = /* @__PURE__ */ ((SharingInfoGeneratorTest2) => {
  SharingInfoGeneratorTest2[SharingInfoGeneratorTest2["requiresCustomCapabilities"] = 100] = "requiresCustomCapabilities";
  SharingInfoGeneratorTest2[SharingInfoGeneratorTest2["hasStrongLanguageContent"] = 101] = "hasStrongLanguageContent";
  return SharingInfoGeneratorTest2;
})(SharingInfoGeneratorTest || {});
const TopicTestIdBase = 500;
const UnsupportedForSharingTypes = [
  import_IProjectItemData.ProjectItemType.jigsawProcessorList,
  import_IProjectItemData.ProjectItemType.jigsawStructure,
  import_IProjectItemData.ProjectItemType.jigsawStructureSet,
  import_IProjectItemData.ProjectItemType.jigsawTemplatePool,
  import_IProjectItemData.ProjectItemType.aimAssistPresetJson,
  import_IProjectItemData.ProjectItemType.aimAssistCategoryJson,
  import_IProjectItemData.ProjectItemType.behaviorTreeJson,
  import_IProjectItemData.ProjectItemType.spawnGroupJson
];
const UnsupportedForSharingTypesSet = new Set(UnsupportedForSharingTypes);
class SharingInfoGenerator {
  id = "SHARING";
  title = "Sharing Best Practices Information";
  canAlwaysProcess = true;
  getTopicData(topicId) {
    if (topicId >= TopicTestIdBase) {
      return {
        title: import_ProjectItemUtilities.default.getDescriptionForType(topicId - TopicTestIdBase)
      };
    }
    return {
      title: topicId.toString()
    };
  }
  summarize(info, infoSet) {
  }
  async generate(project, contentIndex) {
    const items = [];
    const itemsByType = {};
    const lineSizeCounts = [];
    for (let i = 0; i < import_IProjectItemData2.MaxItemTypes; i++) {
      lineSizeCounts[i] = 0;
    }
    const itemsCopy = project.getItemsCopy();
    for (let i = 0; i < itemsCopy.length; i++) {
      const pi = itemsCopy[i];
      let projInfoItem = void 0;
      if (pi.storageType === import_IProjectItemData.ProjectItemStorageType.singleFile) {
        if (!pi.isContentLoaded) {
          await pi.loadContent();
        }
        if (pi.primaryFile) {
        }
      }
      if (UnsupportedForSharingTypesSet.has(pi.itemType)) {
        projInfoItem = new import_ProjectInfoItem.default(
          import_IInfoItemData.InfoItemType.error,
          this.id,
          TopicTestIdBase + pi.itemType,
          import_ProjectItemUtilities.default.getDescriptionForType(pi.itemType) + " is not supported for sharing - probably because this item is still an experimental feature.",
          pi
        );
        itemsByType[pi.itemType] = projInfoItem;
        items.push(projInfoItem);
      } else if (pi.itemType === import_IProjectItemData.ProjectItemType.behaviorPackManifestJson) {
        if (!pi.isContentLoaded) {
          await pi.loadContent();
        }
        if (pi.primaryFile) {
          const bpManifest = await import_BehaviorManifestDefinition.default.ensureOnFile(pi.primaryFile);
          if (bpManifest) {
            await bpManifest.load();
            if (bpManifest && bpManifest.definition && bpManifest.definition.capabilities) {
              projInfoItem = new import_ProjectInfoItem.default(
                import_IInfoItemData.InfoItemType.error,
                this.id,
                100 /* requiresCustomCapabilities */,
                `Declares a custom capabilities requirement, which is not recommended for sharing.`,
                pi,
                JSON.stringify(bpManifest.definition.capabilities, null, 2)
              );
              itemsByType[pi.itemType] = projInfoItem;
              items.push(projInfoItem);
            }
          }
        }
      }
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  SharingInfoGeneratorTest
});
//# sourceMappingURL=SharingInfoGenerator.js.map
