"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ProjectInfoItem_exports = {};
__export(ProjectInfoItem_exports, {
  default: () => ProjectInfoItem
});
module.exports = __toCommonJS(ProjectInfoItem_exports);
var import_Log = __toESM(require("../core/Log"));
var import_Utilities = __toESM(require("../core/Utilities"));
var import_MinecraftUtilities = __toESM(require("../minecraft/MinecraftUtilities"));
var import_IInfoItemData2 = require("./IInfoItemData");
class ProjectInfoItem {
  #data;
  #projectItem;
  get dataObject() {
    return this.#data;
  }
  get itemType() {
    return this.#data.iTp;
  }
  get message() {
    return this.#data.m;
  }
  set message(newMessage) {
    this.#data.m = newMessage;
  }
  get generatorId() {
    return this.#data.gId;
  }
  get generatorIndex() {
    return this.#data.gIx;
  }
  get projectItem() {
    return this.#projectItem;
  }
  get data() {
    return this.#data.d;
  }
  set data(data) {
    this.#data.d = data;
  }
  get content() {
    return this.#data.c;
  }
  get featureSets() {
    return this.#data.fs;
  }
  set featureSets(value) {
    this.#data.fs = value;
  }
  get contentSummary() {
    let errorContent = this.#data.c;
    if (errorContent) {
      errorContent = errorContent.replace(/\n/gi, " ");
      errorContent = errorContent.replace(/\r/gi, " ");
      if (errorContent.length > 80) {
        errorContent = errorContent.substring(0, 77) + "...";
      }
    }
    return errorContent;
  }
  disconnect() {
    this.#projectItem = void 0;
  }
  get typeSummary() {
    switch (this.#data.iTp) {
      case import_IInfoItemData2.InfoItemType.info:
        return "Info";
      case import_IInfoItemData2.InfoItemType.warning:
        return "Warning";
      case import_IInfoItemData2.InfoItemType.recommendation:
        return "Recommendation";
      case import_IInfoItemData2.InfoItemType.featureAggregate:
        return "Feature aggregation";
      case import_IInfoItemData2.InfoItemType.testCompleteFail:
        return "Test fail";
      case import_IInfoItemData2.InfoItemType.testCompleteSuccess:
        return "Test success";
      case import_IInfoItemData2.InfoItemType.error:
        return "Error";
      default:
        return "Unknown";
    }
  }
  get projectItemPath() {
    if (this.projectItem) {
      return this.projectItem.projectPath;
    } else if (this.#data.p) {
      return this.#data.p;
    }
    return void 0;
  }
  get shortProjectItemPath() {
    if (this.projectItem && this.projectItem.projectPath) {
      return import_MinecraftUtilities.default.getAfterPackPath(this.projectItem.projectPath);
    } else if (this.#data.p) {
      return import_MinecraftUtilities.default.getAfterPackPath(this.#data.p);
    }
    return void 0;
  }
  get typeSummaryShort() {
    let short = this.typeSummary.toUpperCase();
    short = short.replace(/\s/g, "");
    return short;
  }
  toString() {
    let summaryString = this.typeSummaryShort + ": ";
    summaryString += "[" + this.generatorId + import_Utilities.default.frontPadToLength(this.generatorIndex, 3, "0") + "] ";
    if (this.projectItem) {
      summaryString += "(" + this.projectItem.projectPath + ") ";
    } else if (this.#data.p) {
      summaryString += "(" + this.#data.p + ") ";
    }
    if (this.message) {
      summaryString += this.message;
    }
    if (this.data) {
      summaryString += ": " + this.data;
    }
    const errorContent = this.contentSummary;
    if (errorContent) {
      summaryString += " [in " + errorContent + "]";
    }
    return summaryString;
  }
  minFeature(setName, measureName, newValue) {
    if (this.#data.fs === void 0) {
      this.#data.fs = {};
    }
    setName = import_Utilities.default.convertToJsonKey(setName);
    measureName = import_Utilities.default.convertToJsonKey(measureName);
    if (!import_Utilities.default.isUsableAsObjectKey(setName)) {
      import_Log.default.unsupportedToken(setName);
      throw new Error();
    }
    let setVal = this.#data.fs[setName];
    if (setVal === void 0) {
      setVal = {};
      this.#data.fs[setName] = setVal;
    }
    let curVal = setVal[measureName];
    if (curVal === void 0) {
      curVal = newValue;
    } else {
      curVal = Math.min(curVal, newValue);
    }
    setVal[measureName] = curVal;
  }
  maxFeature(setName, measureName, newValue) {
    if (this.#data.fs === void 0) {
      this.#data.fs = {};
    }
    setName = import_Utilities.default.convertToJsonKey(setName);
    measureName = import_Utilities.default.convertToJsonKey(measureName);
    if (!import_Utilities.default.isUsableAsObjectKey(setName)) {
      import_Log.default.unsupportedToken(setName);
      throw new Error();
    }
    let setVal = this.#data.fs[setName];
    if (setVal === void 0) {
      setVal = {};
      this.#data.fs[setName] = setVal;
    }
    let curVal = setVal[measureName];
    if (curVal === void 0) {
      curVal = newValue;
    } else {
      curVal = Math.max(curVal, newValue);
    }
    setVal[measureName] = curVal;
  }
  getFeatureContaining(token) {
    token = token.toLowerCase();
    for (const setName in this.#data.fs) {
      const featureSet = this.#data.fs[setName];
      if (featureSet) {
        for (const measureName in featureSet) {
          if (measureName.toLowerCase().indexOf(token) >= 0) {
            return featureSet[measureName];
          }
        }
      }
    }
    return void 0;
  }
  getFeatureMeasureNumber(setName, measure) {
    measure = measure.toLowerCase();
    if (!this.#data.fs) {
      return 0;
    }
    setName = import_Utilities.default.convertToJsonKey(setName);
    const featureSet = this.#data.fs[setName];
    if (featureSet) {
      for (const measureName in featureSet) {
        if (measureName.toLowerCase() === measure) {
          return featureSet[measureName];
        }
      }
    }
    return 0;
  }
  getNonZeroFeatureMeasures() {
    const results = [];
    if (!this.#data.fs) {
      return results;
    }
    for (const setName in this.#data.fs) {
      const featureSet = this.#data.fs[setName];
      if (featureSet) {
        for (const measureName in featureSet) {
          const val = featureSet[measureName];
          if (val !== void 0 && typeof val === "number" && val > 0 && (measureName === "count" || measureName === "instanceCount")) {
            results.push(setName);
          }
        }
      }
    }
    return results;
  }
  spectrumFeature(setName, newValue) {
    if (this.#data.fs === void 0) {
      this.#data.fs = {};
    }
    setName = import_Utilities.default.convertToJsonKey(setName);
    if (!import_Utilities.default.isUsableAsObjectKey(setName)) {
      import_Log.default.unsupportedToken(setName);
      throw new Error();
    }
    this.incrementFeature(setName, "instanceCount", 1);
    this.incrementFeature(setName, "total", newValue);
    this.maxFeature(setName, "max", newValue);
    this.minFeature(setName, "min", newValue);
    let setVal = this.#data.fs[setName];
    if (setVal === void 0) {
      setVal = {};
      this.#data.fs[setName] = setVal;
    }
    const curTotal = setVal["total"];
    const curCount = setVal["instanceCount"];
    if (curCount && curTotal !== void 0) {
      setVal["average"] = curTotal / curCount;
    }
  }
  spectrumIntFeature(setName, newValue) {
    if (this.#data.fs === void 0) {
      this.#data.fs = {};
    }
    setName = import_Utilities.default.convertToJsonKey(setName);
    let setVal = this.#data.fs[setName];
    if (setVal === void 0) {
      setVal = {};
      this.#data.fs[setName] = setVal;
    }
    this.incrementFeature(setName, "instanceCount", 1);
    this.incrementFeature(setName, "total", newValue);
    this.maxFeature(setName, "max", newValue);
    this.minFeature(setName, "min", newValue);
    const curTotal = setVal["total"];
    const curCount = setVal["instanceCount"];
    if (curCount && curTotal !== void 0) {
      setVal["average"] = Math.round(curTotal / curCount);
    }
  }
  incrementFeature(setName, measureName = "count", incrementalValue = 1) {
    if (this.#data.fs === void 0) {
      this.#data.fs = {};
    }
    setName = import_Utilities.default.convertToJsonKey(setName);
    measureName = import_Utilities.default.convertToJsonKey(measureName);
    if (!import_Utilities.default.isUsableAsObjectKey(setName)) {
      return;
    }
    let setVal = this.#data.fs[setName];
    if (setVal === void 0) {
      setVal = this.#data.fs[setName] = {};
    }
    setVal[measureName] = (setVal[measureName] ?? 0) + incrementalValue;
  }
  setFeature(setName, measureName, value) {
    if (this.#data.fs === void 0) {
      this.#data.fs = {};
    }
    setName = import_Utilities.default.convertToJsonKey(setName);
    measureName = import_Utilities.default.convertToJsonKey(measureName);
    if (!import_Utilities.default.isUsableAsObjectKey(setName)) {
      return;
    }
    let setVal = this.#data.fs[setName];
    if (setVal === void 0) {
      setVal = this.#data.fs[setName] = {};
    }
    setVal[measureName] = value;
  }
  constructor(itemType, generatorId, generatorIndex, message, projectItem, data, itemId, content, projectItemPath) {
    this.#data = {
      iTp: itemType,
      gId: generatorId,
      gIx: generatorIndex,
      m: message,
      p: projectItem ? projectItem.projectPath : projectItemPath,
      d: data,
      iId: itemId,
      c: content,
      fs: void 0
    };
    this.#projectItem = projectItem;
  }
}
//# sourceMappingURL=ProjectInfoItem.js.map
