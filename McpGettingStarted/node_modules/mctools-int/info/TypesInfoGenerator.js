"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TypesInfoGenerator_exports = {};
__export(TypesInfoGenerator_exports, {
  TypesInfoGeneratorTest: () => TypesInfoGeneratorTest,
  default: () => TypesInfoGenerator
});
module.exports = __toCommonJS(TypesInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_IInfoItemData = require("./IInfoItemData");
var import_ContentIndex = require("../core/ContentIndex");
var import_EntityTypeDefinition = __toESM(require("../minecraft/EntityTypeDefinition"));
var import_BlockTypeDefinition = __toESM(require("../minecraft/BlockTypeDefinition"));
var import_ItemTypeDefinition = __toESM(require("../minecraft/ItemTypeDefinition"));
var import_BlocksCatalogDefinition = __toESM(require("../minecraft/BlocksCatalogDefinition"));
var import_FeatureDefinition = __toESM(require("../minecraft/FeatureDefinition"));
var TypesInfoGeneratorTest = /* @__PURE__ */ ((TypesInfoGeneratorTest2) => {
  TypesInfoGeneratorTest2[TypesInfoGeneratorTest2["types"] = 101] = "types";
  return TypesInfoGeneratorTest2;
})(TypesInfoGeneratorTest || {});
class TypesInfoGenerator {
  id = "TYPES";
  title = "Types Info Aggregation";
  performAddOnValidations = false;
  summarize(info, infoSet) {
    info.textureCount = infoSet.getSummedDataValue(this.id, 101 /* types */);
  }
  async generate(project, contentIndex) {
    const items = [];
    const typesCountPi = new import_ProjectInfoItem.default(
      import_IInfoItemData.InfoItemType.featureAggregate,
      this.id,
      101 /* types */,
      "Types"
    );
    items.push(typesCountPi);
    const itemsCopy = project.getItemsCopy();
    for (const projectItem of itemsCopy) {
      if (projectItem.itemType === import_IProjectItemData.ProjectItemType.entityTypeBehavior) {
        if (!projectItem.isContentLoaded) {
          await projectItem.loadContent();
        }
        if (projectItem.primaryFile) {
          const etd = await import_EntityTypeDefinition.default.ensureOnFile(projectItem.primaryFile);
          if (etd && etd.id && projectItem.projectPath) {
            contentIndex.insert(etd.id, projectItem.projectPath, import_ContentIndex.AnnotationCategory.entityTypeSource);
          }
        }
      } else if (projectItem.itemType === import_IProjectItemData.ProjectItemType.blockTypeBehavior) {
        if (!projectItem.isContentLoaded) {
          await projectItem.loadContent();
        }
        if (projectItem.primaryFile) {
          const blockTypeDef = await import_BlockTypeDefinition.default.ensureOnFile(projectItem.primaryFile);
          if (blockTypeDef && blockTypeDef.id && projectItem.projectPath) {
            contentIndex.insert(blockTypeDef.id, projectItem.projectPath, import_ContentIndex.AnnotationCategory.blockTypeSource);
            let colon = blockTypeDef.id.indexOf(":");
            if (colon >= 0) {
              contentIndex.insert(
                blockTypeDef.id.substring(colon + 1),
                projectItem.projectPath,
                import_ContentIndex.AnnotationCategory.blockTypeSource
              );
            }
          }
        }
      } else if (projectItem.itemType === import_IProjectItemData.ProjectItemType.blocksCatalogResourceJson) {
        if (!projectItem.isContentLoaded) {
          await projectItem.loadContent();
        }
        if (projectItem.primaryFile) {
          const blockCatalog = await import_BlocksCatalogDefinition.default.ensureOnFile(projectItem.primaryFile);
          if (blockCatalog && projectItem.projectPath && blockCatalog.blocksCatalog) {
            for (const name in blockCatalog.blocksCatalog) {
              let adjustedName = name;
              let colon = adjustedName.indexOf(":");
              if (colon < 0 && project.isVanillaSourceProject) {
                adjustedName = "minecraft:" + adjustedName;
                contentIndex.insert(adjustedName, projectItem.projectPath, import_ContentIndex.AnnotationCategory.blockTypeSource);
              }
              contentIndex.insert(name, projectItem.projectPath, import_ContentIndex.AnnotationCategory.blockTypeSource);
            }
          }
        }
      } else if (projectItem.itemType === import_IProjectItemData.ProjectItemType.itemTypeBehavior) {
        if (!projectItem.isContentLoaded) {
          await projectItem.loadContent();
        }
        if (projectItem.primaryFile) {
          const itemTypeDef = await import_ItemTypeDefinition.default.ensureOnFile(projectItem.primaryFile);
          if (itemTypeDef && itemTypeDef.id && projectItem.projectPath) {
            contentIndex.insert(itemTypeDef.id, projectItem.projectPath, import_ContentIndex.AnnotationCategory.itemTypeSource);
          }
        }
      } else if (projectItem.itemType === import_IProjectItemData.ProjectItemType.featureBehavior) {
        if (!projectItem.isContentLoaded) {
          await projectItem.loadContent();
        }
        if (projectItem.primaryFile) {
          const featureDef = await import_FeatureDefinition.default.ensureOnFile(projectItem.primaryFile);
          if (featureDef && featureDef.id && projectItem.projectPath) {
            contentIndex.insert(featureDef.id, projectItem.projectPath, import_ContentIndex.AnnotationCategory.featureSource);
            let colon = featureDef.id.indexOf(":");
            if (colon >= 0) {
              contentIndex.insert(
                featureDef.id.substring(colon + 1),
                projectItem.projectPath,
                import_ContentIndex.AnnotationCategory.featureSource
              );
            }
          }
        }
      }
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  TypesInfoGeneratorTest
});
//# sourceMappingURL=TypesInfoGenerator.js.map
