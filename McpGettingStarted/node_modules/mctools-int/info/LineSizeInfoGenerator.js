"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var LineSizeInfoGenerator_exports = {};
__export(LineSizeInfoGenerator_exports, {
  default: () => LineSizeInfoGenerator
});
module.exports = __toCommonJS(LineSizeInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_IInfoItemData = require("./IInfoItemData");
var import_IProjectItemData2 = require("../app/IProjectItemData");
var import_Utilities = __toESM(require("../core/Utilities"));
var import_ProjectItemUtilities = __toESM(require("../app/ProjectItemUtilities"));
const TopicTestIdBase = 100;
class LineSizeInfoGenerator {
  id = "LINESIZE";
  title = "File Line/Size Information";
  canAlwaysProcess = true;
  getTopicData(topicId) {
    if (topicId >= TopicTestIdBase) {
      return {
        title: import_ProjectItemUtilities.default.getDescriptionForType(topicId - TopicTestIdBase)
      };
    }
    return {
      title: topicId.toString()
    };
  }
  summarize(info, infoSet) {
  }
  async generate(project, contentIndex) {
    const items = [];
    const itemsByType = {};
    const lineSizeCounts = [];
    for (let i = 0; i < import_IProjectItemData2.MaxItemTypes; i++) {
      lineSizeCounts[i] = 0;
    }
    const itemsCopy = project.getItemsCopy();
    for (let i = 0; i < itemsCopy.length; i++) {
      const pi = itemsCopy[i];
      let projInfoItem = void 0;
      if (itemsByType[pi.itemType] !== void 0) {
        projInfoItem = itemsByType[pi.itemType];
      } else {
        const name = import_ProjectItemUtilities.default.getDescriptionForType(pi.itemType) + " file " + (import_ProjectItemUtilities.default.isBinaryType(pi.itemType) ? "size" : "lines");
        projInfoItem = new import_ProjectInfoItem.default(import_IInfoItemData.InfoItemType.featureAggregate, this.id, TopicTestIdBase + pi.itemType, name);
        itemsByType[pi.itemType] = projInfoItem;
        items.push(projInfoItem);
      }
      if (pi.storageType === import_IProjectItemData.ProjectItemStorageType.singleFile) {
        if (!pi.isContentLoaded) {
          await pi.loadContent();
        }
        const file = pi.primaryFile;
        if (file) {
          if (!file.isContentLoaded) {
            await file.loadContent();
          }
          projInfoItem.spectrumIntFeature("size", file.coreContentLength);
          const content = file.content;
          if (content && typeof content === "string") {
            projInfoItem.spectrumIntFeature("lines", import_Utilities.default.countSignificantLines(content));
          }
        }
      }
    }
    return items;
  }
}
//# sourceMappingURL=LineSizeInfoGenerator.js.map
