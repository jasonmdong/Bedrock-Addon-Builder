"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var MCFunctionInfoGenerator_exports = {};
__export(MCFunctionInfoGenerator_exports, {
  MCFunctionInfoGeneratorTest: () => MCFunctionInfoGeneratorTest,
  default: () => MCFunctionInfoGenerator
});
module.exports = __toCommonJS(MCFunctionInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IInfoItemData = require("./IInfoItemData");
var import_ProjectInfoUtilities = __toESM(require("./ProjectInfoUtilities"));
var import_CommandStructure = __toESM(require("../app/CommandStructure"));
var import_CommandRegistry = __toESM(require("../app/CommandRegistry"));
var import_IProjectItemData = require("../app/IProjectItemData");
var MCFunctionInfoGeneratorTest = /* @__PURE__ */ ((MCFunctionInfoGeneratorTest2) => {
  MCFunctionInfoGeneratorTest2[MCFunctionInfoGeneratorTest2["invalidCommandSyntax"] = 102] = "invalidCommandSyntax";
  MCFunctionInfoGeneratorTest2[MCFunctionInfoGeneratorTest2["lineBeginsWithSlash"] = 103] = "lineBeginsWithSlash";
  return MCFunctionInfoGeneratorTest2;
})(MCFunctionInfoGeneratorTest || {});
class MCFunctionInfoGenerator {
  id = "MCFUNCTION";
  title = "MC Function Validation";
  summarize(info, infoSet) {
    info.invalidCommandSyntaxCount = infoSet.getSummedDataValue(
      this.id,
      102 /* invalidCommandSyntax */
    );
    info.linesWithSlashCount = infoSet.getSummedDataValue(this.id, 103 /* lineBeginsWithSlash */);
  }
  async generate(project) {
    const items = [];
    const projItems = project.getItemsCopy();
    for (const item of projItems) {
      if (item.itemType !== import_IProjectItemData.ProjectItemType.MCFunction) {
        continue;
      }
      if (!item.isContentLoaded) {
        await item.loadContent();
      }
      if (!item.primaryFile) {
        continue;
      }
      if (!item.primaryFile.isContentLoaded) {
        await item.primaryFile.loadContent();
      }
      const content = item.primaryFile.content;
      if (!content || typeof content !== "string") {
        continue;
      }
      const lines = content.trim().split(/\r?\n/).filter((line) => line.trim() !== "");
      const commands = lines.map(import_CommandStructure.default.parse);
      items.push(...this.getNewLineBeginsWithSlashesResult(lines, item));
      items.push(...this.getCommandSyntaxResult(commands, item));
    }
    return items;
  }
  getNewLineBeginsWithSlashesResult(lines, item) {
    const results = [];
    for (const line of lines) {
      if (line.startsWith("/")) {
        results.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.warning,
            this.id,
            103 /* lineBeginsWithSlash */,
            import_ProjectInfoUtilities.default.getTitleFromEnum(
              MCFunctionInfoGeneratorTest,
              103 /* lineBeginsWithSlash */
            ),
            item,
            `Relevant line: ${line}`
          )
        );
      }
    }
    return results;
  }
  getCommandSyntaxResult(commands, item) {
    const results = [];
    for (const command of commands) {
      if (!command.isNamespaced && !import_CommandRegistry.default.isMinecraftBuiltInCommand(command.fullName) && !import_CommandRegistry.default.isAddOnBlockedCommand(command.fullName)) {
        results.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            102 /* invalidCommandSyntax */,
            import_ProjectInfoUtilities.default.getTitleFromEnum(
              MCFunctionInfoGeneratorTest,
              102 /* invalidCommandSyntax */
            ),
            item,
            `${command.fullName} ${command.commandArguments.join(" ")}`
          )
        );
      }
    }
    return results;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  MCFunctionInfoGeneratorTest
});
//# sourceMappingURL=MCFunctionInfoGenerator.js.map
