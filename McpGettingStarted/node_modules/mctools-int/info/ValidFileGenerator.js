"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ValidFileGenerator_exports = {};
__export(ValidFileGenerator_exports, {
  ValidGeneratorTest: () => ValidGeneratorTest,
  default: () => ValidFileGenerator
});
module.exports = __toCommonJS(ValidFileGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IInfoItemData = require("./IInfoItemData");
var import_StorageUtilities = __toESM(require("../storage/StorageUtilities"));
var ValidGeneratorTest = /* @__PURE__ */ ((ValidGeneratorTest2) => {
  ValidGeneratorTest2[ValidGeneratorTest2["nonCompliantJson"] = 102] = "nonCompliantJson";
  ValidGeneratorTest2[ValidGeneratorTest2["emptyJson"] = 103] = "emptyJson";
  ValidGeneratorTest2[ValidGeneratorTest2["jsonNotString"] = 104] = "jsonNotString";
  return ValidGeneratorTest2;
})(ValidGeneratorTest || {});
class ValidFileGenerator {
  id = "VALFILE";
  title = "Valid files";
  summarize(info, infoSet) {
  }
  async generate(project, file, contentIndex) {
    const items = [];
    if (import_StorageUtilities.default.getMimeType(file) === "application/json") {
      if (!file.isString) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            104 /* jsonNotString */,
            `JSON file is not a string`,
            project.getItemByExtendedOrProjectPath(file.extendedPath),
            file.content ? file.content.length + " bytes" : "unloadable"
          )
        );
      } else if (!file.content || file.content?.length < 2) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.error,
            this.id,
            103 /* emptyJson */,
            `JSON file is empty`,
            project.getItemByExtendedOrProjectPath(file.extendedPath),
            file.content ? file.content.length + " characters" : "unloadable"
          )
        );
      } else {
        import_StorageUtilities.default.getJsonObject(file);
        if (file.isInErrorState) {
          items.push(
            new import_ProjectInfoItem.default(
              import_IInfoItemData.InfoItemType.error,
              this.id,
              102 /* nonCompliantJson */,
              `JSON file is not JSON compliant`,
              project.getItemByExtendedOrProjectPath(file.extendedPath),
              file.errorStateMessage ? file.errorStateMessage : "unparseable"
            )
          );
        }
      }
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ValidGeneratorTest
});
//# sourceMappingURL=ValidFileGenerator.js.map
