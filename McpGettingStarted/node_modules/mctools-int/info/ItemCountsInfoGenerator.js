"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ItemCountsInfoGenerator_exports = {};
__export(ItemCountsInfoGenerator_exports, {
  ItemCountsInfoGeneratorTest: () => ItemCountsInfoGeneratorTest,
  default: () => ItemCountsInfoGenerator
});
module.exports = __toCommonJS(ItemCountsInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_IInfoItemData = require("./IInfoItemData");
var import_IProjectItemData2 = require("../app/IProjectItemData");
var import_ProjectItemUtilities = __toESM(require("../app/ProjectItemUtilities"));
const TopicTestIdBase = 200;
var ItemCountsInfoGeneratorTest = /* @__PURE__ */ ((ItemCountsInfoGeneratorTest2) => {
  ItemCountsInfoGeneratorTest2[ItemCountsInfoGeneratorTest2["behaviorPackManifest"] = 102] = "behaviorPackManifest";
  ItemCountsInfoGeneratorTest2[ItemCountsInfoGeneratorTest2["resourcePackManifest"] = 103] = "resourcePackManifest";
  return ItemCountsInfoGeneratorTest2;
})(ItemCountsInfoGeneratorTest || {});
class ItemCountsInfoGenerator {
  id = "ITEMS";
  title = "Minimum Definition of a Pack";
  getTopicData(topicId) {
    if (topicId > TopicTestIdBase && topicId < TopicTestIdBase + import_IProjectItemData2.MaxItemTypes) {
      return {
        title: import_ProjectItemUtilities.default.getDescriptionForType(topicId - TopicTestIdBase) + " count"
      };
    }
    return {
      title: topicId.toString()
    };
  }
  summarize(info, infoSet) {
    info.behaviorPackManifestCount = infoSet.getFirstNumberDataValue(
      this.id,
      TopicTestIdBase + import_IProjectItemData.ProjectItemType.behaviorPackManifestJson
    );
    info.unknownJsonCount = infoSet.getFirstNumberDataValue(this.id, TopicTestIdBase + import_IProjectItemData.ProjectItemType.unknownJson);
    info.entityTypeManifestCount = infoSet.getFirstNumberDataValue(
      this.id,
      TopicTestIdBase + import_IProjectItemData.ProjectItemType.entityTypeBehavior
    );
    info.itemTypeManifestCount = infoSet.getFirstNumberDataValue(
      this.id,
      TopicTestIdBase + import_IProjectItemData.ProjectItemType.itemTypeBehavior
    );
    info.blockTypeManifestCount = infoSet.getFirstNumberDataValue(
      this.id,
      TopicTestIdBase + import_IProjectItemData.ProjectItemType.blockTypeBehavior
    );
    info.resourcePackManifestCount = infoSet.getFirstNumberDataValue(
      this.id,
      TopicTestIdBase + import_IProjectItemData.ProjectItemType.resourcePackManifestJson
    );
    info.worldCount = infoSet.getFirstNumberDataValue(this.id, TopicTestIdBase + import_IProjectItemData.ProjectItemType.MCWorld) + infoSet.getFirstNumberDataValue(this.id, TopicTestIdBase + import_IProjectItemData.ProjectItemType.worldFolder);
    info.entityTypeResourceCount = infoSet.getFirstNumberDataValue(
      this.id,
      TopicTestIdBase + import_IProjectItemData.ProjectItemType.entityTypeResource
    );
    info.behaviorPackAnimationCount = infoSet.getFirstNumberDataValue(
      this.id,
      TopicTestIdBase + import_IProjectItemData.ProjectItemType.animationBehaviorJson
    );
    info.behaviorPackAnimationControllerCount = infoSet.getFirstNumberDataValue(
      this.id,
      TopicTestIdBase + import_IProjectItemData.ProjectItemType.animationControllerBehaviorJson
    );
  }
  async generate(project, contentIndex) {
    const items = [];
    const typeCounts = [];
    for (let i = 0; i < import_IProjectItemData2.MaxItemTypes; i++) {
      typeCounts[i] = 0;
    }
    const itemsCopy = project.getItemsCopy();
    for (let i = 0; i < itemsCopy.length; i++) {
      const pi = itemsCopy[i];
      typeCounts[pi.itemType]++;
      if (pi.itemType === import_IProjectItemData.ProjectItemType.behaviorPackManifestJson) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.info,
            this.id,
            102 /* behaviorPackManifest */,
            "Behavior pack manifest found",
            pi
          )
        );
      } else if (pi.itemType === import_IProjectItemData.ProjectItemType.resourcePackManifestJson) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.info,
            this.id,
            103 /* resourcePackManifest */,
            "Resource pack manifest found",
            pi
          )
        );
      }
    }
    for (let i = 0; i < import_IProjectItemData2.MaxItemTypes; i++) {
      if (typeCounts[i] > 0) {
        items.push(
          new import_ProjectInfoItem.default(
            import_IInfoItemData.InfoItemType.info,
            this.id,
            TopicTestIdBase + i,
            import_ProjectItemUtilities.default.getDescriptionForType(i) + " item count",
            void 0,
            typeCounts[i]
          )
        );
      }
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ItemCountsInfoGeneratorTest
});
//# sourceMappingURL=ItemCountsInfoGenerator.js.map
