"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CheckNoBOMGenerator_exports = {};
__export(CheckNoBOMGenerator_exports, {
  default: () => CheckNoBOMGenerator
});
module.exports = __toCommonJS(CheckNoBOMGenerator_exports);
var import_StorageUtilities = __toESM(require("../../storage/StorageUtilities"));
var import_IInfoItemData = require("../IInfoItemData");
var import_ProjectInfoItem = __toESM(require("../ProjectInfoItem"));
var CheckNoBOMGeneratorTest = /* @__PURE__ */ ((CheckNoBOMGeneratorTest2) => {
  CheckNoBOMGeneratorTest2[CheckNoBOMGeneratorTest2["NoByteOrderMarkAllowedInJsonFile"] = 101] = "NoByteOrderMarkAllowedInJsonFile";
  return CheckNoBOMGeneratorTest2;
})(CheckNoBOMGeneratorTest || {});
class CheckNoBOMGenerator {
  id = "NOBOM";
  title = "No Byte Order Mark allowed in json file.";
  canAlwaysProcess = true;
  severity = import_IInfoItemData.InfoItemType.error;
  generate(_project, projectFile) {
    const results = [];
    if (import_StorageUtilities.default.isJsonFile(projectFile) && this.hasByteOrderMark(projectFile)) {
      results.push(this.getNewBOMResult(projectFile));
    }
    return Promise.resolve(results);
  }
  summarize() {
  }
  hasByteOrderMark(file) {
    const bytes = import_StorageUtilities.default.getContentsAsBinary(file);
    const hasBOM = import_StorageUtilities.default.hasUTF8ByteOrderMark(bytes);
    return hasBOM;
  }
  getNewBOMResult(file) {
    return new import_ProjectInfoItem.default(
      this.severity,
      this.id,
      101 /* NoByteOrderMarkAllowedInJsonFile */,
      `Byte Order Marks found in file: ${file.name}`
    );
  }
}
//# sourceMappingURL=CheckNoBOMGenerator.js.map
