"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var JsonSchemaItemInfoGenerator_exports = {};
__export(JsonSchemaItemInfoGenerator_exports, {
  JsonSchemaItemInfoGeneratorTest: () => JsonSchemaItemInfoGeneratorTest,
  default: () => JsonSchemaItemInfoGenerator
});
module.exports = __toCommonJS(JsonSchemaItemInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IInfoItemData = require("./IInfoItemData");
var import_Database = __toESM(require("../minecraft/Database"));
var import_CreatorToolsHost = __toESM(require("../app/CreatorToolsHost"));
var import_axios = __toESM(require("axios"));
var import_Utilities = __toESM(require("../core/Utilities"));
var import_MinecraftDefinitions = __toESM(require("../minecraft/MinecraftDefinitions"));
var import_ProjectItemUtilities = __toESM(require("../app/ProjectItemUtilities"));
var import_json_schema = require("json-schema");
const JsonSchemaErrorBase = 100;
const NotCurrentFormatVersionBase = 1100;
var JsonSchemaItemInfoGeneratorTest = /* @__PURE__ */ ((JsonSchemaItemInfoGeneratorTest2) => {
  JsonSchemaItemInfoGeneratorTest2[JsonSchemaItemInfoGeneratorTest2["couldNotParseJson"] = 1] = "couldNotParseJson";
  return JsonSchemaItemInfoGeneratorTest2;
})(JsonSchemaItemInfoGeneratorTest || {});
class JsonSchemaItemInfoGenerator {
  id = "JSON";
  title = "JSON Schema Validation";
  canAlwaysProcess = true;
  constructor() {
    this.loadSchema = this.loadSchema.bind(this);
  }
  summarize(info, infoSet) {
  }
  async loadSchema(uri) {
    const res = await import_axios.default.get(import_Utilities.default.ensureEndsWithSlash(import_CreatorToolsHost.default.contentWebRoot) + uri);
    return res.data;
  }
  async generate(projectItem, contentIndex) {
    const items = [];
    if (projectItem.primaryFile && projectItem.primaryFile.content && typeof projectItem.primaryFile.content === "string") {
      const schemaPath = projectItem.getOfficialSchemaPath();
      if (schemaPath) {
        let verIsCurrent = await import_MinecraftDefinitions.default.formatVersionIsCurrent(projectItem);
        if (verIsCurrent) {
          let schemaContents = await import_Database.default.getOfficialSchema(schemaPath);
          if (schemaContents) {
            let content = projectItem.primaryFile.content;
            let contentObj = void 0;
            content = import_Utilities.default.fixJsonContent(content);
            try {
              contentObj = JSON.parse(content);
              const results = (0, import_json_schema.validate)(contentObj, schemaContents);
              for (const err of results.errors) {
                items.push(
                  new import_ProjectInfoItem.default(
                    import_IInfoItemData.InfoItemType.warning,
                    this.id,
                    JsonSchemaErrorBase + projectItem.itemType,
                    `JSON structure error`,
                    projectItem,
                    `(${err.property}) ${err.message}`
                  )
                );
              }
            } catch (e) {
              let errorMess = e;
              if (e.message) {
                errorMess = e.message;
              }
              items.push(
                new import_ProjectInfoItem.default(
                  import_IInfoItemData.InfoItemType.error,
                  this.id,
                  1 /* couldNotParseJson */,
                  "Could not parse JSON - " + errorMess,
                  projectItem
                )
              );
            }
            if (contentObj) {
            }
          }
        } else {
          let fvStr = "";
          const fv = await import_MinecraftDefinitions.default.getFormatVersion(projectItem);
          if (fv) {
            fvStr = " (is at " + fv.join(".") + ")";
          }
          items.push(
            new import_ProjectInfoItem.default(
              import_IInfoItemData.InfoItemType.info,
              this.id,
              NotCurrentFormatVersionBase + projectItem.itemType,
              import_ProjectItemUtilities.default.getDescriptionForType(projectItem.itemType) + " is not at a current format version" + fvStr,
              projectItem
            )
          );
        }
      }
    }
    return items;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  JsonSchemaItemInfoGeneratorTest
});
//# sourceMappingURL=JsonSchemaItemInfoGenerator.js.map
