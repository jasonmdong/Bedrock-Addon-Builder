"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var AnimationResourceInfoGenerator_exports = {};
__export(AnimationResourceInfoGenerator_exports, {
  AnimationResourceInfoGeneratorTest: () => AnimationResourceInfoGeneratorTest,
  default: () => AnimationResourceInfoGenerator
});
module.exports = __toCommonJS(AnimationResourceInfoGenerator_exports);
var import_ProjectInfoItem = __toESM(require("./ProjectInfoItem"));
var import_IProjectItemData = require("../app/IProjectItemData");
var import_IInfoItemData = require("./IInfoItemData");
var import_AnimationResourceDefinition = __toESM(require("../minecraft/AnimationResourceDefinition"));
var import_ProjectInfoUtilities = __toESM(require("./ProjectInfoUtilities"));
var AnimationResourceInfoGeneratorTest = /* @__PURE__ */ ((AnimationResourceInfoGeneratorTest2) => {
  AnimationResourceInfoGeneratorTest2[AnimationResourceInfoGeneratorTest2["animations"] = 101] = "animations";
  AnimationResourceInfoGeneratorTest2[AnimationResourceInfoGeneratorTest2["bones"] = 102] = "bones";
  return AnimationResourceInfoGeneratorTest2;
})(AnimationResourceInfoGeneratorTest || {});
class AnimationResourceInfoGenerator {
  id = "RESOURCEANIMATION";
  title = "Resource Animation";
  performAddOnValidations = false;
  summarize(info, infoSet) {
    info.animationCount = infoSet.getSummedDataValue("RESOURCEANIMATION", 1);
  }
  async generate(project, contentIndex) {
    const items = [];
    const animationCountPi = new import_ProjectInfoItem.default(
      import_IInfoItemData.InfoItemType.featureAggregate,
      this.id,
      101 /* animations */,
      import_ProjectInfoUtilities.default.getTitleFromEnum(
        AnimationResourceInfoGeneratorTest,
        101 /* animations */
      )
    );
    items.push(animationCountPi);
    const boneCountPi = new import_ProjectInfoItem.default(
      import_IInfoItemData.InfoItemType.featureAggregate,
      this.id,
      102 /* bones */,
      import_ProjectInfoUtilities.default.getTitleFromEnum(
        AnimationResourceInfoGeneratorTest,
        102 /* bones */
      )
    );
    items.push(boneCountPi);
    const itemsCopy = project.getItemsCopy();
    for (const projectItem of itemsCopy) {
      if (projectItem.itemType === import_IProjectItemData.ProjectItemType.animationResourceJson) {
        if (!projectItem.isContentLoaded) {
          await projectItem.loadContent();
        }
        if (projectItem.primaryFile) {
          const ra = await import_AnimationResourceDefinition.default.ensureOnFile(projectItem.primaryFile);
          if (ra && ra.animations) {
            for (const animationName in ra.animations) {
              const animation = ra.animations[animationName];
              animationCountPi.incrementFeature("Resource", "Count", 1);
              if (animation && animation.bones) {
                for (const boneName in animation.bones) {
                  const bone = animation.bones[boneName];
                  if (bone) {
                    boneCountPi.incrementFeature("Resource Animation Bone", "Count", 1);
                    if (bone.position) {
                      this.processAnimationValue(boneCountPi, bone.position, "Position");
                    }
                    if (bone.rotation) {
                      this.processAnimationValue(boneCountPi, bone.rotation, "Rotation");
                    }
                    if (bone.scale) {
                      this.processAnimationValue(boneCountPi, bone.scale, "Scale");
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    return items;
  }
  processAnimationValue(boneCountPi, value, type) {
    let isKeyframedBoneAnimation = false;
    if (typeof value === "string") {
      boneCountPi.incrementFeature("Resource Animation Bone " + type + " Expression", "Count", 1);
      return;
    }
    for (const key in value) {
      const val = value[key];
      if (Array.isArray(val)) {
        isKeyframedBoneAnimation = true;
        break;
      }
    }
    if (isKeyframedBoneAnimation) {
      boneCountPi.incrementFeature("Resource Animation Bone " + type + " Keyframe", "Count", 1);
      for (const key in value) {
        const val = value[key];
        if (Array.isArray(val)) {
          for (const valAtom of val) {
            if (typeof valAtom === "string") {
              boneCountPi.incrementFeature("Resource Animation Bone " + type + " Keyframe with Molang", "Count", 1);
            } else {
              boneCountPi.incrementFeature("Resource Animation Bone " + type + " Keyframe", "Count", 1);
            }
          }
        }
      }
    } else {
      boneCountPi.incrementFeature("Resource Animation Bone " + type + " Continuous", "Count", 1);
      if (Array.isArray(value)) {
        for (const val of value) {
          if (typeof val.position === "string") {
            boneCountPi.incrementFeature("Resource Animation Bone " + type + " Continuous with Molang", "Count", 1);
          } else {
            boneCountPi.incrementFeature("Resource Animation Bone " + type + " Continuous", "Count", 1);
          }
        }
      }
    }
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  AnimationResourceInfoGeneratorTest
});
//# sourceMappingURL=AnimationResourceInfoGenerator.js.map
